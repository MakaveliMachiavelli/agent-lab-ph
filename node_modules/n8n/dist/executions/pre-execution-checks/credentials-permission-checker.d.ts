import { CredentialsRepository, SharedCredentialsRepository, UserRepository } from '@n8n/db';
import type { INode } from 'n8n-workflow';
import { CredentialsFinderService } from '../../credentials/credentials-finder.service';
import { NodeTypes } from '../../node-types';
import { OwnershipService } from '../../services/ownership.service';
import { ProjectService } from '../../services/project.service.ee';
export declare class CredentialsPermissionChecker {
    private readonly sharedCredentialsRepository;
    private readonly credentialsRepository;
    private readonly ownershipService;
    private readonly projectService;
    private readonly nodeTypes;
    private readonly userRepository;
    private readonly credentialsFinderService;
    constructor(sharedCredentialsRepository: SharedCredentialsRepository, credentialsRepository: CredentialsRepository, ownershipService: OwnershipService, projectService: ProjectService, nodeTypes: NodeTypes, userRepository: UserRepository, credentialsFinderService: CredentialsFinderService);
    checkForUser(userId: string, nodes: INode[]): Promise<void>;
    check(workflowId: string, nodes: INode[]): Promise<void>;
    private ensureOnlyWorkflowCredentials;
    private addGlobalCredentialsToAccessibleSet;
    private mapCredIdsToNodes;
    private getActiveCredentialTypes;
}
