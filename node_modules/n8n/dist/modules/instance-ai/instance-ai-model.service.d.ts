import { type InstanceAiCredits } from '@n8n/api-types';
import { OutboundHttp } from '@n8n/backend-network';
import type { User } from '@n8n/db';
import type { ModelConfig } from '@n8n/instance-ai';
import { AiService } from '../../services/ai.service';
import { ProxyTokenManager } from '../../services/proxy-token-manager';
import { InstanceAiSettingsService } from './instance-ai-settings.service';
export declare class InstanceAiModelService {
    private readonly settingsService;
    private readonly aiService;
    private readonly outboundHttp;
    constructor(settingsService: InstanceAiSettingsService, aiService: AiService, outboundHttp: OutboundHttp);
    isProxyEnabled(): boolean;
    resolveAgentModelConfig(user: User): Promise<ModelConfig>;
    resolveProxyModel(user: User, proxyBaseUrl: string, tokenManager: ProxyTokenManager): Promise<ModelConfig>;
    private resolveHttpProxyModel;
    getCredits(user: User): Promise<InstanceAiCredits>;
}
