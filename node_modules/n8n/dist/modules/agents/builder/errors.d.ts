import { UserError } from 'n8n-workflow';
export declare class BuilderNotConfiguredError extends UserError {
    readonly code: "BUILDER_NOT_CONFIGURED";
    constructor();
}
export declare class BuilderCheckpointUnavailableError extends UserError {
    readonly code: "BUILDER_CHECKPOINT_UNAVAILABLE";
    constructor(reason: 'expired' | 'not-found');
}
