import { WorkflowsConfig } from '@n8n/config';
import { type WorkflowEntity, WorkflowRepository } from '@n8n/db';
import { WorkflowPublishedDataService } from '../../../workflows/workflow-published-data.service';
export interface WorkflowToolWorkflowReference {
    workflowId?: string;
    workflowName: string;
}
export declare class WorkflowToolWorkflowLoader {
    private readonly workflowsConfig;
    private readonly workflowRepository;
    private readonly workflowPublishedDataService;
    constructor(workflowsConfig: WorkflowsConfig, workflowRepository: WorkflowRepository, workflowPublishedDataService: WorkflowPublishedDataService);
    loadPublishedWorkflow(projectId: string, reference: WorkflowToolWorkflowReference): Promise<WorkflowEntity | null>;
}
