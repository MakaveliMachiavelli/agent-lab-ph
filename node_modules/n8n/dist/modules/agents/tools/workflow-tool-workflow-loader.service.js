"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.WorkflowToolWorkflowLoader = void 0;
const config_1 = require("@n8n/config");
const db_1 = require("@n8n/db");
const di_1 = require("@n8n/di");
const workflow_published_data_service_1 = require("../../../workflows/workflow-published-data.service");
let WorkflowToolWorkflowLoader = class WorkflowToolWorkflowLoader {
    constructor(workflowsConfig, workflowRepository, workflowPublishedDataService) {
        this.workflowsConfig = workflowsConfig;
        this.workflowRepository = workflowRepository;
        this.workflowPublishedDataService = workflowPublishedDataService;
    }
    async loadPublishedWorkflow(projectId, reference) {
        const accessibleWorkflow = await this.workflowRepository.findOneByAgentToolReference(projectId, reference);
        if (!accessibleWorkflow || accessibleWorkflow.isArchived)
            return null;
        if (this.workflowsConfig.useWorkflowPublicationService) {
            const publishedWorkflow = await this.workflowPublishedDataService.getPublishedWorkflowDataForExecution(accessibleWorkflow.id);
            if (!publishedWorkflow || publishedWorkflow.isArchived)
                return null;
            return Object.assign(accessibleWorkflow, publishedWorkflow, { pinData: undefined });
        }
        const workflow = await this.workflowRepository.findById(accessibleWorkflow.id);
        if (!workflow || workflow.isArchived || !workflow.activeVersion)
            return null;
        return Object.assign(workflow, {
            versionId: workflow.activeVersion.versionId,
            nodes: workflow.activeVersion.nodes,
            connections: workflow.activeVersion.connections,
            nodeGroups: workflow.activeVersion.nodeGroups,
            pinData: undefined,
        });
    }
};
exports.WorkflowToolWorkflowLoader = WorkflowToolWorkflowLoader;
exports.WorkflowToolWorkflowLoader = WorkflowToolWorkflowLoader = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [config_1.WorkflowsConfig, db_1.WorkflowRepository, workflow_published_data_service_1.WorkflowPublishedDataService])
], WorkflowToolWorkflowLoader);
//# sourceMappingURL=workflow-tool-workflow-loader.service.js.map