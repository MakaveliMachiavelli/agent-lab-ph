import { DataSource, Repository, type EntityManager } from '@n8n/typeorm';
import { AgentTaskSnapshot } from '../entities/agent-task-snapshot.entity';
type AgentTaskSnapshotData = Pick<AgentTaskSnapshot, 'versionId' | 'taskId' | 'enabled' | 'name' | 'objective' | 'cronExpression'>;
export declare class AgentTaskSnapshotRepository extends Repository<AgentTaskSnapshot> {
    constructor(dataSource: DataSource);
    saveForVersion(snapshots: AgentTaskSnapshotData[], trx?: EntityManager): Promise<void>;
    findByVersionId(versionId: string, trx?: EntityManager): Promise<AgentTaskSnapshot[]>;
    findEnabledByVersionId(versionId: string): Promise<AgentTaskSnapshot[]>;
    findByVersionAndTaskId(versionId: string, taskId: string): Promise<AgentTaskSnapshot | null>;
}
export {};
