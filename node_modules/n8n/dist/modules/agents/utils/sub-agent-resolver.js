"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveUniqueSubAgents = resolveUniqueSubAgents;
async function resolveUniqueSubAgents({ refs, projectId, agentRepository, }) {
    const seen = new Set();
    const resolved = [];
    for (const ref of refs) {
        const { agentId } = ref;
        if (seen.has(agentId))
            continue;
        seen.add(agentId);
        const resolvedRef = {
            agentId,
            agent: await agentRepository.findByIdAndProjectId(agentId, projectId),
        };
        if (ref.useWhen !== undefined) {
            resolvedRef.useWhen = ref.useWhen;
        }
        resolved.push(resolvedRef);
    }
    return resolved;
}
//# sourceMappingURL=sub-agent-resolver.js.map