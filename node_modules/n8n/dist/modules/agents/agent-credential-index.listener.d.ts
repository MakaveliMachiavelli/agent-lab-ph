import { Logger } from '@n8n/backend-common';
import { EventService } from '../../events/event.service';
import { AgentCredentialIndexService } from './agent-credential-index.service';
export declare class AgentCredentialIndexListener {
    private readonly logger;
    private readonly eventService;
    private readonly indexService;
    constructor(logger: Logger, eventService: EventService, indexService: AgentCredentialIndexService);
    init(): void;
    private run;
}
