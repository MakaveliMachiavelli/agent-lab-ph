import { Logger } from '@n8n/backend-common';
import { WorkflowsConfig } from '@n8n/config';
import { AgentCredentialDependencyRepository } from './repositories/agent-credential-dependency.repository';
import { AgentRepository } from './repositories/agent.repository';
export declare class AgentCredentialIndexService {
    private readonly dependencyRepository;
    private readonly agentRepository;
    private readonly batchSize;
    private readonly logger;
    constructor(dependencyRepository: AgentCredentialDependencyRepository, agentRepository: AgentRepository, logger: Logger, workflowsConfig: WorkflowsConfig);
    remove(agentId: string): Promise<void>;
    refresh(agentId: string): Promise<void>;
    buildIndex(): Promise<void>;
}
