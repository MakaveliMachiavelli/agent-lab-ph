"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.WorkflowReviewAutoCloseService = void 0;
const backend_common_1 = require("@n8n/backend-common");
const db_1 = require("@n8n/db");
const di_1 = require("@n8n/di");
const collaboration_service_1 = require("../../collaboration/collaboration.service");
let WorkflowReviewAutoCloseService = class WorkflowReviewAutoCloseService {
    constructor(logger, workflowReviewRequestRepository, dbLockService, collaborationService) {
        this.logger = logger;
        this.workflowReviewRequestRepository = workflowReviewRequestRepository;
        this.dbLockService = dbLockService;
        this.collaborationService = collaborationService;
    }
    async afterWorkflowArchived(workflowId) {
        await this.closeOpenRequestsForWorkflows([workflowId], 'workflow-archived');
    }
    async afterWorkflowsTransferred(workflowIds) {
        await this.closeOpenRequestsForWorkflows(workflowIds, 'workflow-moved');
    }
    async beforeWorkflowDeleted(workflowId) {
        await this.closeOpenRequestsForWorkflows([workflowId], 'workflow-deleted', {
            rethrow: true,
        });
    }
    async afterWorkflowsDeleted(workflowIds) {
        try {
            const closedRequestIds = await this.workflowReviewRequestRepository.closeOrphanedOpenRequests({});
            if (closedRequestIds.length === 0)
                return;
            this.logger.info('Closed workflow review request(s) left without a workflow', {
                reason: 'workflow-deleted',
                workflowIds,
                workflowReviewRequestIds: closedRequestIds,
            });
        }
        catch (error) {
            this.logger.error('Failed to close workflow review request(s) left without a workflow', {
                workflowIds,
                error,
            });
        }
    }
    async closeOpenRequestsForWorkflows(workflowIds, reason, options = {}) {
        try {
            const affectedWorkflowIds = await this.dbLockService.withLockContext(1004, async (ctx) => {
                const openRequests = await this.workflowReviewRequestRepository.findOpenRequestsForWorkflows(workflowIds, ctx);
                const affected = new Set();
                for (const { request, workflowIds: linkedWorkflowIds } of openRequests) {
                    request.state = 'closed';
                    request.closedById = null;
                    await this.workflowReviewRequestRepository.saveRequest(request, ctx);
                    for (const linkedWorkflowId of linkedWorkflowIds)
                        affected.add(linkedWorkflowId);
                }
                return [...affected];
            });
            if (affectedWorkflowIds.length === 0)
                return;
            this.logger.info('Closed open workflow review request(s)', {
                reason,
                workflowIds: affectedWorkflowIds,
            });
            for (const workflowId of affectedWorkflowIds) {
                this.collaborationService
                    .broadcastWorkflowReviewStateChanged(workflowId)
                    .catch((error) => this.logger.warn('Failed to broadcast review state change', { workflowId, error }));
            }
        }
        catch (error) {
            this.logger.error('Failed to close open workflow review request(s)', {
                reason,
                workflowIds,
                error,
            });
            if (options.rethrow)
                throw error;
        }
    }
};
exports.WorkflowReviewAutoCloseService = WorkflowReviewAutoCloseService;
exports.WorkflowReviewAutoCloseService = WorkflowReviewAutoCloseService = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [backend_common_1.Logger, db_1.WorkflowReviewRequestRepository, db_1.DbLockService, collaboration_service_1.CollaborationService])
], WorkflowReviewAutoCloseService);
//# sourceMappingURL=workflow-review-auto-close.service.js.map