import { Logger } from '@n8n/backend-common';
import { DbLockService, WorkflowReviewRequestRepository } from '@n8n/db';
import { CollaborationService } from '../../collaboration/collaboration.service';
import type { WorkflowMutationHooks } from '../../workflows/workflow-mutation-hooks-proxy.service';
export declare class WorkflowReviewAutoCloseService implements WorkflowMutationHooks {
    private readonly logger;
    private readonly workflowReviewRequestRepository;
    private readonly dbLockService;
    private readonly collaborationService;
    constructor(logger: Logger, workflowReviewRequestRepository: WorkflowReviewRequestRepository, dbLockService: DbLockService, collaborationService: CollaborationService);
    afterWorkflowArchived(workflowId: string): Promise<void>;
    afterWorkflowsTransferred(workflowIds: string[]): Promise<void>;
    beforeWorkflowDeleted(workflowId: string): Promise<void>;
    afterWorkflowsDeleted(workflowIds: string[]): Promise<void>;
    private closeOpenRequestsForWorkflows;
}
