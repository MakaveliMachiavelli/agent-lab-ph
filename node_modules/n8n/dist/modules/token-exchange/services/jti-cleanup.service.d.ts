import { Logger } from '@n8n/backend-common';
import { InstanceSettings } from 'n8n-core';
import { TokenExchangeJtiRepository } from '../database/repositories/token-exchange-jti.repository';
import { TokenExchangeConfig } from '../token-exchange.config';
export declare class JtiCleanupService {
    private readonly config;
    private readonly jtiRepository;
    private readonly instanceSettings;
    private cleanupInterval;
    private isShuttingDown;
    private readonly logger;
    constructor(logger: Logger, config: TokenExchangeConfig, jtiRepository: TokenExchangeJtiRepository, instanceSettings: InstanceSettings);
    init(): void;
    startCleanup(): void;
    stopCleanup(): void;
    private cleanup;
    shutdown(): void;
}
