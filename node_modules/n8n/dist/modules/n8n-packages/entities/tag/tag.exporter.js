"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TagExporter = void 0;
const di_1 = require("@n8n/di");
const unique_filename_allocator_1 = require("../../io/unique-filename-allocator");
const tag_schema_1 = require("../../spec/serialized/tag.schema");
const tag_types_1 = require("./tag.types");
let TagExporter = class TagExporter {
    export(request) {
        const requirementsByTagId = new Map();
        for (const { workflowId, tag } of request.usages) {
            const requirement = requirementsByTagId.get(tag.id) ?? {
                id: tag.id,
                name: tag.name,
                usedByWorkflows: [],
            };
            if (!requirement.usedByWorkflows.includes(workflowId)) {
                requirement.usedByWorkflows.push(workflowId);
            }
            requirementsByTagId.set(tag.id, requirement);
        }
        const requirements = [...requirementsByTagId.values()].sort(tag_types_1.compareTagsByName);
        const allocator = new unique_filename_allocator_1.UniqueFilenameAllocator('tags', 'tag');
        const entries = [];
        for (const { id, name } of requirements) {
            const tagDirectory = allocator.allocate(name);
            const serializedTag = tag_schema_1.serializedTagSchema.parse({ id, name });
            request.writer.writeDirectory(tagDirectory);
            request.writer.writeFile(`${tagDirectory}/tag.json`, JSON.stringify(serializedTag, null, '\t'));
            entries.push({ id, name, target: tagDirectory });
        }
        return { entries, requirements };
    }
};
exports.TagExporter = TagExporter;
exports.TagExporter = TagExporter = __decorate([
    (0, di_1.Service)()
], TagExporter);
//# sourceMappingURL=tag.exporter.js.map