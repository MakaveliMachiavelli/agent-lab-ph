import { LicenseState } from '@n8n/backend-common';
import { EventService } from '../../../events/event.service';
import { FolderService } from '../../../services/folder.service';
import { ProjectService } from '../../../services/project.service.ee';
import { WorkflowPublisher } from '../entities/workflow/workflow-publisher';
import type { PackageReader } from '../io/package-reader';
import type { ImportPackageRequest, ImportResult } from '../n8n-packages.types';
import { ImportOrchestrator } from './import-orchestrator';
import { N8nPackageParser } from './n8n-package-parser';
import type { PackageManifest } from '../spec/manifest.schema';
export declare class WorkflowPackageImporter {
    private readonly packageParser;
    private readonly importOrchestrator;
    private readonly workflowPublisher;
    private readonly projectService;
    private readonly folderService;
    private readonly eventService;
    private readonly licenseState;
    constructor(packageParser: N8nPackageParser, importOrchestrator: ImportOrchestrator, workflowPublisher: WorkflowPublisher, projectService: ProjectService, folderService: FolderService, eventService: EventService, licenseState: LicenseState);
    import(request: ImportPackageRequest, reader: PackageReader, manifest: PackageManifest): Promise<ImportResult>;
    private assertFoldersLicensed;
    private findImportLocation;
    private resolveImportProject;
    private assertFolderExistsInProject;
}
