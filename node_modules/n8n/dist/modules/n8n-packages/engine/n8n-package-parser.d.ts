import type { PreparedFolder } from '../entities/folder/folder-import.types';
import type { PreparedProject } from '../entities/project/project-import.types';
import type { PreparedWorkflow } from '../entities/workflow/workflow-import.types';
import { WorkflowSerializer } from '../entities/workflow/workflow.serializer';
import type { PackageReader } from '../io/package-reader';
import type { PackageManifest } from '../spec/manifest.schema';
import type { SerializedDataTable } from '../spec/serialized/data-table.schema';
import { type SerializedVariable } from '../spec/serialized/variable.schema';
export declare class N8nPackageParser {
    private readonly workflowSerializer;
    constructor(workflowSerializer: WorkflowSerializer);
    getManifest(reader: PackageReader): Promise<PackageManifest>;
    getWorkflows(reader: PackageReader, basePrefix?: string): Promise<PreparedWorkflow[]>;
    getFolders(reader: PackageReader, basePrefix?: string): Promise<PreparedFolder[]>;
    getDataTables(reader: PackageReader): Promise<SerializedDataTable[]>;
    getProjects(reader: PackageReader): Promise<PreparedProject[]>;
    getVariables(reader: PackageReader): Promise<Map<string, SerializedVariable>>;
    private readWorkflow;
    private readFolder;
    private readDataTable;
    private readProject;
    private readVariable;
    private readJson;
}
