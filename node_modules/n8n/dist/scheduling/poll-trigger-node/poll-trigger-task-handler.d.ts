import { Logger } from '@n8n/backend-common';
import { WorkflowRepository } from '@n8n/db';
import type { ClaimedTask, DispatchDecision, DispatchReporter, TaskHandler } from '@n8n/scheduler';
import { TriggersAndPollers } from 'n8n-core';
import { TriggerExecutionContextFactory } from '../../workflows/triggers/trigger-execution-context.factory';
export declare class PollTriggerTaskHandler implements TaskHandler {
    private logger;
    private readonly triggerExecutionContextFactory;
    private readonly triggersAndPollers;
    private readonly workflowRepository;
    readonly taskType = "workflow:poll-trigger";
    constructor(logger: Logger, triggerExecutionContextFactory: TriggerExecutionContextFactory, triggersAndPollers: TriggersAndPollers, workflowRepository: WorkflowRepository);
    execute(task: ClaimedTask, report: DispatchReporter): Promise<DispatchDecision>;
    private parsePayload;
    private resolveTriggerNode;
}
