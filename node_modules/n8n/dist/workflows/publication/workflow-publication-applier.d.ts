import { Logger } from '@n8n/backend-common';
import { WorkflowHistoryRepository, WorkflowPublicationOutbox, WorkflowPublishedVersionRepository, WorkflowRepository } from '@n8n/db';
import type { PublicationResult } from '../../workflows/publication/publication-result';
import { WorkflowTriggerActivator, type TriggerOperationAbort } from '../../workflows/triggers/workflow-trigger-activator';
import { WorkflowPublishedDataService } from '../../workflows/workflow-published-data.service';
export declare class WorkflowPublicationApplier {
    private readonly logger;
    private readonly workflowRepository;
    private readonly workflowHistoryRepository;
    private readonly workflowPublishedVersionRepository;
    private readonly workflowTriggerActivator;
    private readonly workflowPublishedDataService;
    constructor(logger: Logger, workflowRepository: WorkflowRepository, workflowHistoryRepository: WorkflowHistoryRepository, workflowPublishedVersionRepository: WorkflowPublishedVersionRepository, workflowTriggerActivator: WorkflowTriggerActivator, workflowPublishedDataService: WorkflowPublishedDataService);
    apply(record: WorkflowPublicationOutbox, abort: TriggerOperationAbort): Promise<PublicationResult>;
    private publish;
    private unpublish;
    private classifyActivationOutcome;
    private buildTriggerStatuses;
    private toActivationError;
    private resolveVersions;
    private advancePublishedVersion;
}
