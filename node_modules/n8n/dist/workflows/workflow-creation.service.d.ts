import { LicenseState, Logger } from '@n8n/backend-common';
import { GlobalConfig } from '@n8n/config';
import type { User } from '@n8n/db';
import { ProjectRepository, SharedWorkflowRepository, TagRepository, WorkflowEntity } from '@n8n/db';
import { CredentialsService } from '../credentials/credentials.service';
import { EventService } from '../events/event.service';
import type { WorkflowActionSource } from '../events/maps/relay.event-map';
import { ExternalHooks } from '../external-hooks';
import { InstanceRedactionEnforcementService } from '../modules/redaction/instance-redaction-enforcement.service';
import { NodeTypes } from '../node-types';
import { FolderService } from '../services/folder.service';
import { ProjectService } from '../services/project.service.ee';
import { TagService } from '../services/tag.service';
import { WorkflowHookContextService } from '../workflow-hook-context.service';
import { WorkflowFinderService } from './workflow-finder.service';
import { WorkflowHistoryService } from './workflow-history/workflow-history.service';
import { WorkflowValidationService } from './workflow-validation.service';
import { EnterpriseWorkflowService } from './workflow.service.ee';
export declare class WorkflowCreationService {
    private readonly logger;
    private readonly sharedWorkflowRepository;
    private readonly tagService;
    private readonly workflowHistoryService;
    private readonly externalHooks;
    private readonly projectService;
    private readonly eventService;
    private readonly globalConfig;
    private readonly workflowFinderService;
    private readonly licenseState;
    private readonly projectRepository;
    private readonly tagRepository;
    private readonly credentialsService;
    private readonly folderService;
    private readonly enterpriseWorkflowService;
    private readonly nodeTypes;
    private readonly workflowValidationService;
    private readonly instanceRedactionEnforcementService;
    private readonly workflowHookContextService;
    constructor(logger: Logger, sharedWorkflowRepository: SharedWorkflowRepository, tagService: TagService, workflowHistoryService: WorkflowHistoryService, externalHooks: ExternalHooks, projectService: ProjectService, eventService: EventService, globalConfig: GlobalConfig, workflowFinderService: WorkflowFinderService, licenseState: LicenseState, projectRepository: ProjectRepository, tagRepository: TagRepository, credentialsService: CredentialsService, folderService: FolderService, enterpriseWorkflowService: EnterpriseWorkflowService, nodeTypes: NodeTypes, workflowValidationService: WorkflowValidationService, instanceRedactionEnforcementService: InstanceRedactionEnforcementService, workflowHookContextService: WorkflowHookContextService);
    createWorkflow(user: User, newWorkflow: WorkflowEntity, options?: {
        tagIds?: string[];
        parentFolderId?: string;
        projectId?: string;
        sourceWorkflowId?: string;
        autosaved?: boolean;
        uiContext?: string;
        publicApi?: boolean;
        source?: WorkflowActionSource;
        versionName?: string;
        versionDescription?: string;
    }): Promise<WorkflowEntity>;
    private findParentFolderInProjectOrFail;
    private readActiveRedactionFloor;
    private resolveRedactionPolicyOnCreate;
}
