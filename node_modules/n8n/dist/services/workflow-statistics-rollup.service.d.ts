import { Logger } from '@n8n/backend-common';
import { DatabaseConfig } from '@n8n/config';
import { DbConnection, DbLockService, WorkflowStatisticsRepository } from '@n8n/db';
import { ErrorReporter, InstanceSettings } from 'n8n-core';
import { WorkflowStatisticsService } from './workflow-statistics.service';
export declare class WorkflowStatisticsRollupService {
    private readonly logger;
    private readonly errorReporter;
    private readonly instanceSettings;
    private readonly dbConnection;
    private readonly databaseConfig;
    private readonly dbLockService;
    private readonly repository;
    private readonly statisticsService;
    private timeout;
    private inflightRollup;
    private isShuttingDown;
    private consecutiveLockSkips;
    private totalLockSkips;
    constructor(logger: Logger, errorReporter: ErrorReporter, instanceSettings: InstanceSettings, dbConnection: DbConnection, databaseConfig: DatabaseConfig, dbLockService: DbLockService, repository: WorkflowStatisticsRepository, statisticsService: WorkflowStatisticsService);
    get shouldRun(): boolean;
    init(): void;
    start(): void;
    stop(): void;
    shutdown(): Promise<void>;
    private scheduleNext;
    private rollup;
    private foldBatch;
    private registerLockSkip;
    private emitMilestones;
}
