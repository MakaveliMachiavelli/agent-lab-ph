import { UserRepository } from '@n8n/db';
import { CredentialsService } from '../../credentials/credentials.service';
import { FolderService } from '../../services/folder.service';
import { OwnershipService } from '../../services/ownership.service';
import { WorkflowService } from '../../workflows/workflow.service';
import { OwnershipTransferHandlerRegistry } from './ownership-transfer-handler.registry';
export declare class OwnershipTransferService {
    private readonly userRepository;
    private readonly workflowService;
    private readonly credentialsService;
    private readonly folderService;
    private readonly ownershipService;
    private readonly transferHandlers;
    constructor(userRepository: UserRepository, workflowService: WorkflowService, credentialsService: CredentialsService, folderService: FolderService, ownershipService: OwnershipService, transferHandlers: OwnershipTransferHandlerRegistry);
    transferAllResources(fromProjectIds: string[], toProjectId: string): Promise<void>;
    deleteModuleOwnedResources(projectIds: string[]): Promise<void>;
}
