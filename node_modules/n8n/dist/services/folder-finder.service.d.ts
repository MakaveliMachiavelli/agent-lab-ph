import type { Folder, User } from '@n8n/db';
import { FolderRepository } from '@n8n/db';
import { type Scope } from '@n8n/permissions';
import { RoleService } from '../services/role.service';
export declare class FolderFinderService {
    private readonly folderRepository;
    private readonly roleService;
    constructor(folderRepository: FolderRepository, roleService: RoleService);
    findExistingFolderIds(folderIds: string[]): Promise<Set<string>>;
    findFoldersByIdsForUser(folderIds: string[], user: User, scopes: Scope[]): Promise<Folder[]>;
    findFolderSubtreesForUser(folderIds: string[], user: User, scopes: Scope[]): Promise<Folder[]>;
    findFolderAncestorChainsForUser(folderIds: string[], user: User, scopes: Scope[]): Promise<Map<string, Folder[]>>;
    findFolderIdsInProject(projectId: string): Promise<string[]>;
    private buildFolderReadWhere;
}
