"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AuthStrategyRegistry = void 0;
const di_1 = require("@n8n/di");
let AuthStrategyRegistry = class AuthStrategyRegistry {
    constructor() {
        this.strategies = [];
    }
    register(strategy) {
        this.strategies.push(strategy);
    }
    async buildContextFromToken(token, options) {
        for (const strategy of this.strategies) {
            const result = await strategy.buildTokenGrant(token, options);
            if (result === false) {
                return null;
            }
            if (result !== null) {
                return result;
            }
        }
        return null;
    }
    async authenticate(req) {
        for (const strategy of this.strategies) {
            const result = await strategy.authenticate(req);
            if (result !== null) {
                return result;
            }
        }
        return false;
    }
};
exports.AuthStrategyRegistry = AuthStrategyRegistry;
exports.AuthStrategyRegistry = AuthStrategyRegistry = __decorate([
    (0, di_1.Service)()
], AuthStrategyRegistry);
//# sourceMappingURL=auth-strategy.registry.js.map