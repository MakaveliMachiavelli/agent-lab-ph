import { SettingsRepository } from '@n8n/db';
export declare const INSTANCE_ACTIVATED_SETTINGS_KEY = "instance.firstProductionSuccess";
export declare class InstanceActivationService {
    private readonly settingsRepository;
    private activatedAt;
    constructor(settingsRepository: SettingsRepository);
    isActivated(): Promise<boolean>;
    getActivatedAt(): Promise<number | undefined>;
    private parseTimestamp;
}
