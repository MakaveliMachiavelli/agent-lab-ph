import { PrometheusMetricsConfig } from '@n8n/config';
import { WorkflowRepository } from '@n8n/db';
import { InstanceSettings } from 'n8n-core';
import { CacheService } from '../../services/cache/cache.service';
import type { PrometheusMetricsCollector } from './base';
export declare class PrometheusWorkflowInfoMetricsService implements PrometheusMetricsCollector {
    private readonly config;
    private readonly workflowRepository;
    private readonly cacheService;
    private readonly instanceSettings;
    constructor(config: PrometheusMetricsConfig, workflowRepository: WorkflowRepository, cacheService: CacheService, instanceSettings: InstanceSettings);
    get enabled(): boolean;
    init(): void;
    private initGauge;
}
