import { PrometheusMetricsConfig } from '@n8n/config';
import { WorkflowRepository } from '@n8n/db';
import { CacheService } from '../../services/cache/cache.service';
import type { PrometheusMetricsCollector } from './base';
export declare class PrometheusActiveWorkflowMetricsService implements PrometheusMetricsCollector {
    private readonly config;
    private readonly workflowRepository;
    private readonly cacheService;
    constructor(config: PrometheusMetricsConfig, workflowRepository: WorkflowRepository, cacheService: CacheService);
    get enabled(): boolean;
    init(): void;
}
