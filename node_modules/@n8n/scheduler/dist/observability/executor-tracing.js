"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createExecutorTracing = createExecutorTracing;
exports.withHandoffTracing = withHandoffTracing;
const attributes_1 = require("./attributes");
const tracer_1 = require("./tracer");
function createExecutorTracing(tracer) {
    return {
        async fire(host, task, run) {
            return await tracer.startSpan({
                name: 'Scheduler fire',
                op: 'scheduler.fire',
                attributes: { ...(0, attributes_1.pickSchedulerTaskAttributes)(task), [attributes_1.SCHEDULER_ATTRIBUTES.host]: host },
                newTrace: true,
            }, async (span) => {
                const result = await run();
                span.setAttribute(attributes_1.SCHEDULER_ATTRIBUTES.outcome, fireOutcomeAttribute(result.outcome));
                if ('errorMessage' in result) {
                    span.setStatus({ code: tracer_1.SpanStatus.error, message: result.errorMessage });
                }
                else {
                    span.setStatus({ code: tracer_1.SpanStatus.ok });
                }
                return result;
            });
        },
    };
}
function withHandoffTracing(tracer, handler) {
    return {
        async execute(task, report) {
            return await tracer.startSpan({
                name: 'Scheduler handoff',
                op: 'scheduler.handoff',
                attributes: {
                    [attributes_1.SCHEDULER_ATTRIBUTES.taskId]: task.id,
                    [attributes_1.SCHEDULER_ATTRIBUTES.jobId]: task.jobId,
                    [attributes_1.SCHEDULER_ATTRIBUTES.taskType]: task.taskType,
                },
            }, async (span) => {
                const decision = await handler.execute(task, report);
                span.setStatus({ code: tracer_1.SpanStatus.ok });
                return decision;
            });
        },
    };
}
function fireOutcomeAttribute(outcome) {
    switch (outcome) {
        case 'completed':
            return attributes_1.SCHEDULER_FIRE_OUTCOME.completed;
        case 'rescheduled':
            return attributes_1.SCHEDULER_FIRE_OUTCOME.rescheduled;
        case 'dead-lettered':
            return attributes_1.SCHEDULER_FIRE_OUTCOME.deadLettered;
        case 'skipped-no-handler':
            return attributes_1.SCHEDULER_FIRE_OUTCOME.skippedNoHandler;
        case 'skipped-not-owned':
            return attributes_1.SCHEDULER_FIRE_OUTCOME.skippedNotOwned;
    }
}
//# sourceMappingURL=executor-tracing.js.map