import { type Tracer } from './tracer';
import type { ExecutorTracing, TaskHandler } from '../core/executor';
export declare function createExecutorTracing(tracer: Tracer): ExecutorTracing;
export declare function withHandoffTracing(tracer: Tracer, handler: TaskHandler): TaskHandler;
