"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.backoff = backoff;
const constants_1 = require("@n8n/constants");
const DEFAULT_BASE_MS = 5 * constants_1.Time.seconds.toMilliseconds;
const DEFAULT_FACTOR = 2;
const DEFAULT_MAX_MS = 5 * constants_1.Time.minutes.toMilliseconds;
function backoff(attempts, opts = {}) {
    const { baseMs = DEFAULT_BASE_MS, factor = DEFAULT_FACTOR, maxMs = DEFAULT_MAX_MS } = opts;
    if (attempts < 1)
        return 0;
    const raw = baseMs * factor ** (attempts - 1);
    return Math.min(raw, maxMs);
}
//# sourceMappingURL=backoff.js.map