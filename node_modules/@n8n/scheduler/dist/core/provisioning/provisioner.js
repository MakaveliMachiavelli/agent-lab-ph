"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createJobProvisioner = createJobProvisioner;
const provision_1 = require("./provision");
const provisioner_tracing_1 = require("../../observability/provisioner-tracing");
const tracer_1 = require("../../observability/tracer");
function createJobProvisioner(deps) {
    const tracing = (0, provisioner_tracing_1.createProvisionerTracing)(deps.tracer ?? tracer_1.noopTracer);
    return {
        async provision(scope, desired) {
            return await tracing.provision(async () => await (0, provision_1.provision)(deps.provisionTransaction(scope), desired));
        },
        async deprovision(scope) {
            return await tracing.deprovision(async () => await (0, provision_1.deprovision)(deps.deprovisionTransaction(scope)));
        },
    };
}
//# sourceMappingURL=provisioner.js.map