import type { RunInProvisionTransaction, RunInDeprovisionTransaction } from './transaction';
import type { DesiredJob, ProvisionSummary } from './types';
export declare function provision(runInTransaction: RunInProvisionTransaction, desired: DesiredJob[]): Promise<ProvisionSummary>;
export declare function deprovision(runInTransaction: RunInDeprovisionTransaction): Promise<{
    removed: number;
}>;
