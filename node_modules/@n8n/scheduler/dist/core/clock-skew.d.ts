export interface ClockSkew {
    offsetMs: number;
    roundTripMs: number;
}
export declare const CLOCK_SKEW_WARN_THRESHOLD_MS = 1000;
export interface ClockSkewOptions {
    warnThresholdMs: number;
}
export declare const DEFAULT_CLOCK_SKEW_OPTIONS: ClockSkewOptions;
export declare function measureClockSkew(sample: {
    before: number;
    referenceNow: number;
    after: number;
}): ClockSkew;
export declare function isClockSkewSignificant(skew: ClockSkew, thresholdMs?: number): boolean;
