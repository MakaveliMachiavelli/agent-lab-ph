"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateRecurringCron = validateRecurringCron;
exports.isOnCadence = isOnCadence;
exports.recurringCronNextRun = recurringCronNextRun;
exports.recurringCronFirstRun = recurringCronFirstRun;
exports.recurringCronOccurrences = recurringCronOccurrences;
exports.resolveRecurringCron = resolveRecurringCron;
const constants_1 = require("@n8n/constants");
const errors_1 = require("../../errors");
const field_1 = require("../field");
const periods_1 = require("../periods");
const cron_1 = require("./cron");
const MAX_RECURRENCE_CANDIDATES = 10_000;
function validateRecurringCron(schedule) {
    (0, cron_1.validateCron)(schedule);
    if (!constants_1.RecurringCronUnitList.includes(schedule.recurrenceUnit)) {
        throw new errors_1.InvalidScheduleError(`recurring_cron.recurrenceUnit must be one of ${constants_1.RecurringCronUnitList.join(', ')}, got ${JSON.stringify(schedule.recurrenceUnit)}`);
    }
    if (!Number.isInteger(schedule.recurrenceSize) || schedule.recurrenceSize < 2) {
        throw new errors_1.InvalidScheduleError(`recurring_cron.recurrenceSize must be an integer of at least 2 (a stride of 1 is a plain cron), got ${JSON.stringify(schedule.recurrenceSize)}`);
    }
}
function isOnCadence(previousFire, candidate, schedule, timezone) {
    const elapsed = (0, periods_1.periodsBetween)(previousFire, candidate, schedule.recurrenceUnit, timezone);
    return elapsed === 0 || elapsed >= schedule.recurrenceSize;
}
function advanceToOnCadence(cursor, schedule, previousFire, timezone) {
    for (let scanned = 0; scanned < MAX_RECURRENCE_CANDIDATES; scanned++) {
        const candidate = cursor.next().toDate();
        if (isOnCadence(previousFire, candidate, schedule, timezone)) {
            return candidate;
        }
    }
    throw new errors_1.InvalidScheduleError(`No fire of cron expression ${JSON.stringify(schedule.cronExpression)} lands on the every-${schedule.recurrenceSize}-${schedule.recurrenceUnit} cadence within ${MAX_RECURRENCE_CANDIDATES} candidates`);
}
function recurringCronNextRun(schedule, after) {
    const timezone = (0, cron_1.resolvedTimezone)(schedule);
    const cursor = (0, cron_1.parseCron)(schedule.cronExpression, after, timezone);
    return advanceToOnCadence(cursor, schedule, after, timezone);
}
function recurringCronFirstRun(schedule, from) {
    return (0, cron_1.cronNextRun)(schedule, from);
}
function* recurringCronOccurrences(schedule, first) {
    const timezone = (0, cron_1.resolvedTimezone)(schedule);
    const cursor = (0, cron_1.parseCron)(schedule.cronExpression, first, timezone);
    let previous = first;
    yield previous;
    for (;;) {
        previous = advanceToOnCadence(cursor, schedule, previous, timezone);
        yield previous;
    }
}
function resolveRecurringCron(job, defaultTimezone) {
    return {
        kind: 'recurring_cron',
        cronExpression: (0, field_1.required)(job, 'cronExpression'),
        timezone: job.timezone ?? defaultTimezone,
        recurrenceUnit: (0, field_1.required)(job, 'recurrenceUnit'),
        recurrenceSize: (0, field_1.required)(job, 'recurrenceSize'),
    };
}
//# sourceMappingURL=recurring-cron.js.map