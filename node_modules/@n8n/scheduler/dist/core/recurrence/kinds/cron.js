"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateCron = validateCron;
exports.resolvedTimezone = resolvedTimezone;
exports.parseCron = parseCron;
exports.cronNextRun = cronNextRun;
exports.cronOccurrences = cronOccurrences;
exports.resolveCron = resolveCron;
const cron_parser_1 = require("cron-parser");
const luxon_1 = require("luxon");
const errors_1 = require("../../errors");
const field_1 = require("../field");
const MIN_CRON_FIELD_COUNT = 5;
const MAX_CRON_FIELD_COUNT = 6;
function validateCron(schedule) {
    const expression = schedule.cronExpression;
    if (typeof expression !== 'string') {
        throw new errors_1.InvalidScheduleError(`${schedule.kind}.cronExpression must be a string, got ${JSON.stringify(expression)}`);
    }
    const fieldCount = expression.trim().split(/\s+/).length;
    if (fieldCount < MIN_CRON_FIELD_COUNT || fieldCount > MAX_CRON_FIELD_COUNT) {
        throw new errors_1.InvalidScheduleError(`Cron expression must have ${MIN_CRON_FIELD_COUNT} or ${MAX_CRON_FIELD_COUNT} fields (seconds optional), got ${fieldCount}: ${JSON.stringify(expression)}`);
    }
    if (schedule.timezone !== null && !luxon_1.IANAZone.isValidZone(schedule.timezone)) {
        throw new errors_1.InvalidScheduleError(`Unknown IANA timezone: ${JSON.stringify(schedule.timezone)}`);
    }
    try {
        cron_parser_1.CronExpressionParser.parse(expression, { tz: schedule.timezone ?? 'UTC' });
    }
    catch (error) {
        throw new errors_1.InvalidScheduleError(`Invalid cron expression ${JSON.stringify(expression)}: ${error.message}`);
    }
}
function resolvedTimezone(schedule) {
    if (schedule.timezone === null) {
        throw new errors_1.InvalidScheduleError('Cron timezone must be resolved to a concrete zone before computing the next run, got null');
    }
    return schedule.timezone;
}
function parseCron(cronExpression, after, timezone) {
    try {
        return cron_parser_1.CronExpressionParser.parse(cronExpression, { currentDate: after, tz: timezone });
    }
    catch (error) {
        throw new errors_1.InvalidScheduleError(`Failed to evaluate cron expression ${JSON.stringify(cronExpression)} in timezone ${JSON.stringify(timezone)}: ${error.message}`);
    }
}
function cronNextRun(schedule, after) {
    return parseCron(schedule.cronExpression, after, resolvedTimezone(schedule)).next().toDate();
}
function* cronOccurrences(schedule, first) {
    const cursor = parseCron(schedule.cronExpression, first, resolvedTimezone(schedule));
    yield first;
    for (;;) {
        yield cursor.next().toDate();
    }
}
function resolveCron(job, defaultTimezone) {
    return {
        kind: 'cron',
        cronExpression: (0, field_1.required)(job, 'cronExpression'),
        timezone: job.timezone ?? defaultTimezone,
    };
}
//# sourceMappingURL=cron.js.map