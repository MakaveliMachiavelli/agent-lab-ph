import { type RetentionOptions } from './options';
import type { RetentionStore } from './store';
export interface RetentionSummary {
    deleted: number;
    drained: boolean;
}
export declare function prune(store: RetentionStore, options?: RetentionOptions, signal?: AbortSignal): Promise<RetentionSummary>;
