"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.prune = prune;
const constants_1 = require("@n8n/constants");
const errors_1 = require("../errors");
const options_1 = require("./options");
function retentionWindows(options) {
    return [
        {
            statuses: [constants_1.ScheduledTaskStatus.Succeeded, constants_1.ScheduledTaskStatus.Cancelled],
            olderThanSeconds: options.retentionSeconds,
        },
        {
            statuses: [constants_1.ScheduledTaskStatus.Failed, constants_1.ScheduledTaskStatus.Missed],
            olderThanSeconds: options.failedRetentionSeconds,
        },
    ];
}
async function prune(store, options = options_1.DEFAULT_RETENTION_OPTIONS, signal) {
    if (!Number.isInteger(options.batchSize) || options.batchSize <= 0) {
        throw new errors_1.InvalidRetentionOptionsError(`batchSize must be a positive integer, got ${options.batchSize}`);
    }
    const { deleted, drained } = await pruneWindows(store, retentionWindows(options), options, {
        deleted: 0,
        budget: options.maxBatchesPerPass,
        drained: true,
    }, signal);
    return { deleted, drained };
}
async function pruneWindows(store, windows, options, state, signal) {
    const [window, ...rest] = windows;
    if (window === undefined) {
        return state;
    }
    const windowBudget = state.budget <= 0 ? 0 : Math.max(state.budget - rest.length, 1);
    const pruned = await pruneWindow(store, window, options.batchSize, windowBudget, signal);
    return await pruneWindows(store, rest, options, {
        deleted: state.deleted + pruned.deleted,
        budget: state.budget - pruned.batches,
        drained: state.drained && pruned.drained,
    }, signal);
}
async function pruneWindow(store, window, batchSize, budget, signal) {
    let deleted = 0;
    let batches = 0;
    while (batches < budget) {
        if (signal?.aborted === true) {
            return { deleted, batches, drained: false };
        }
        const affected = await store.deleteFinishedOlderThan({
            statuses: window.statuses,
            olderThanMs: window.olderThanSeconds * constants_1.Time.seconds.toMilliseconds,
            limit: batchSize,
        });
        batches += 1;
        deleted += affected;
        if (affected < batchSize) {
            return { deleted, batches, drained: true };
        }
    }
    return { deleted, batches, drained: false };
}
//# sourceMappingURL=prune.js.map