"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Loop = exports.PASS_TIMED_OUT = void 0;
const alarm_1 = require("./alarm");
const timeline_1 = require("./timeline");
const TIMED_OUT = Symbol('pass timed out');
exports.PASS_TIMED_OUT = Symbol('pass aborted after timeout');
function timeoutAfter(ms) {
    let timer;
    const timedOut = new Promise((resolve) => {
        timer = setTimeout(() => resolve(TIMED_OUT), Math.min(ms, alarm_1.MAX_DELAY_MS));
        timer.unref();
    });
    return { timedOut, cancel: () => clearTimeout(timer) };
}
class Loop {
    constructor(pass, options, hooks, random = Math.random, now = Date.now) {
        this.pass = pass;
        this.options = options;
        this.hooks = hooks;
        this.random = random;
        this.now = now;
        this.inFlight = new Set();
        this.inFlightControllers = new Set();
        this.stopped = false;
        this.limit = options.concurrency === 'sequential' ? 1 : options.maxConcurrent;
        this.alarm = new alarm_1.Alarm(now);
    }
    start() {
        if (this.timeline === undefined && !this.stopped) {
            this.timeline = new timeline_1.Timeline(this.options.intervalMs, this.options.jitterRatio, this.random, this.now());
            this.alarm.set(this.timeline.firstTickAt, () => this.tick());
        }
    }
    async stop() {
        this.stopped = true;
        this.alarm.cancel();
        for (const controller of this.inFlightControllers) {
            controller.abort();
        }
        await Promise.all(this.inFlight);
    }
    tick() {
        if (this.timeline) {
            this.alarm.set(this.timeline.nextTickAt(this.now()), () => this.tick());
            this.launch();
        }
    }
    launch() {
        if (this.inFlight.size >= this.limit) {
            this.hooks.onSkippedTick({ inFlight: this.inFlight.size, limit: this.limit });
        }
        else {
            const run = this.runOnce();
            this.inFlight.add(run);
            void run.finally(() => this.inFlight.delete(run));
        }
    }
    async runOnce() {
        const controller = new AbortController();
        this.inFlightControllers.add(controller);
        const timeout = timeoutAfter(this.options.timeoutMs);
        const pass = this.runPass(controller.signal);
        pass.catch(() => { });
        try {
            if ((await Promise.race([pass, timeout.timedOut])) === TIMED_OUT) {
                controller.abort(exports.PASS_TIMED_OUT);
                this.hooks.onTimeout({ timeoutMs: this.options.timeoutMs });
            }
        }
        catch (error) {
            if (!controller.signal.aborted) {
                this.hooks.onError(error);
            }
        }
        finally {
            this.inFlightControllers.delete(controller);
            timeout.cancel();
        }
    }
    async runPass(signal) {
        return await this.pass(signal);
    }
}
exports.Loop = Loop;
//# sourceMappingURL=loop.js.map