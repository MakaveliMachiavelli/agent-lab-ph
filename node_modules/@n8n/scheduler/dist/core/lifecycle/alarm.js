"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Alarm = exports.MAX_DELAY_MS = void 0;
const constants_1 = require("@n8n/constants");
exports.MAX_DELAY_MS = constants_1.MAX_INTEGER_32BITS_SIGNED;
class Alarm {
    constructor(now) {
        this.now = now;
    }
    set(targetAt, fire) {
        this.cancel();
        const delayMs = Math.min(Math.max(0, targetAt - this.now()), exports.MAX_DELAY_MS);
        this.timer = setTimeout(() => {
            this.timer = undefined;
            if (targetAt > this.now()) {
                this.set(targetAt, fire);
            }
            else {
                fire();
            }
        }, delayMs);
        this.timer.unref();
    }
    cancel() {
        if (this.timer !== undefined) {
            clearTimeout(this.timer);
            this.timer = undefined;
        }
    }
}
exports.Alarm = Alarm;
//# sourceMappingURL=alarm.js.map