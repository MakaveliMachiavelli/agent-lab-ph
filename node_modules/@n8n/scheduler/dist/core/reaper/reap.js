"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DEFAULT_REAPER_OPTIONS = void 0;
exports.reap = reap;
const backoff_1 = require("../executor/backoff");
const LEASE_EXPIRED_MESSAGE = 'Lease expired before completion';
exports.DEFAULT_REAPER_OPTIONS = {
    batchSize: 100,
};
async function retireStale(store, options, hooks, signal) {
    if (signal?.aborted === true)
        return 0;
    try {
        return await store.retireMissedPending(options.batchSize);
    }
    catch (error) {
        try {
            hooks.onRetireError?.(error);
        }
        catch {
        }
        return 0;
    }
}
async function reap(store, options = exports.DEFAULT_REAPER_OPTIONS, hooks = {}, signal) {
    const missed = await retireStale(store, options, hooks, signal);
    const expired = await store.findExpiredLeases(options.batchSize);
    if (expired.length === 0)
        return { reclaimed: 0, deadLettered: 0, missed };
    let reclaimed = 0;
    let deadLettered = 0;
    for (const task of expired) {
        if (signal?.aborted === true) {
            break;
        }
        try {
            const nextAttempts = task.attempts + 1;
            const ref = { id: task.id, claimedEpoch: task.leaseEpoch };
            if (task.dispatchedAt !== null) {
                const affected = await store.completeExpired(ref);
                if (affected > 0) {
                    try {
                        hooks.onCompletedAfterDispatch?.({ taskId: task.id });
                    }
                    catch {
                    }
                }
            }
            else if (nextAttempts >= task.maxAttempts) {
                const affected = await store.deadLetterExpired(ref, LEASE_EXPIRED_MESSAGE);
                deadLettered += affected;
                if (affected > 0) {
                    try {
                        hooks.onDeadLetter?.({
                            taskId: task.id,
                            attempts: nextAttempts,
                            maxAttempts: task.maxAttempts,
                        });
                    }
                    catch {
                    }
                }
            }
            else {
                reclaimed += await store.reclaimExpired(ref, (0, backoff_1.backoff)(nextAttempts), LEASE_EXPIRED_MESSAGE);
            }
        }
        catch (error) {
            try {
                hooks.onRowError?.(task.id, error);
            }
            catch {
            }
        }
    }
    return { reclaimed, deadLettered, missed };
}
//# sourceMappingURL=reap.js.map