"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.applyMisfirePolicy = applyMisfirePolicy;
exports.countMisfires = countMisfires;
const constants_1 = require("@n8n/constants");
const errors_1 = require("../errors");
function applyMisfirePolicy(due, now, job, truncated) {
    const graceDeadline = now.getTime() - job.misfireGraceSeconds * constants_1.Time.seconds.toMilliseconds;
    const behind = due.filter((occurrence) => occurrence.getTime() <= now.getTime());
    const ahead = due.slice(behind.length);
    if (behind.length === 0 || behind[0].getTime() > graceDeadline) {
        return { occurrences: due, catchUpAt: null };
    }
    switch (job.misfirePolicy) {
        case constants_1.ScheduledJobMisfirePolicy.Coalesce: {
            if (truncated && ahead.length === 0)
                return { occurrences: [], catchUpAt: null };
            const catchUpAt = behind[behind.length - 1];
            return { occurrences: [catchUpAt, ...ahead], catchUpAt };
        }
        case constants_1.ScheduledJobMisfirePolicy.Skip:
            return { occurrences: ahead, catchUpAt: null };
        default: {
            const exhaustive = job.misfirePolicy;
            throw new errors_1.CorruptStorageRowError(`scheduled_job ${job.id} has unknown misfire policy '${String(exhaustive)}'`);
        }
    }
}
function countMisfires(planned) {
    const grouped = planned
        .filter(({ plan }) => plan.skippedOccurrences > 0)
        .reduce((groups, { job, plan }) => {
        const key = `${job.taskType}:${job.misfirePolicy}`;
        return groups.set(key, {
            taskType: job.taskType,
            policy: job.misfirePolicy,
            discarded: (groups.get(key)?.discarded ?? 0) + plan.skippedOccurrences,
        });
    }, new Map());
    return [...grouped.values()];
}
//# sourceMappingURL=misfire.js.map