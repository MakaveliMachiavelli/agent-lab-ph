"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.planOccurrences = planOccurrences;
const constants_1 = require("@n8n/constants");
const next_run_1 = require("../recurrence/next-run");
const resolve_1 = require("../recurrence/resolve");
const misfire_1 = require("./misfire");
function planOccurrences(job, now, options) {
    const schedule = (0, resolve_1.resolveSchedule)(job, options.defaultTimezone);
    const windowEnd = now.getTime() + options.windowSeconds * constants_1.Time.seconds.toMilliseconds;
    if (job.nextRunAt === null) {
        return {
            occurrences: [],
            skippedOccurrences: 0,
            catchUpAt: null,
            nextRunAt: null,
            lastFiredAt: job.lastFiredAt,
        };
    }
    const due = [];
    const fires = (0, next_run_1.occurrencesFrom)(schedule, job.nextRunAt);
    let fire = fires.next();
    while (!fire.done && fire.value.getTime() <= windowEnd && due.length < options.maxPerJob) {
        due.push(fire.value);
        fire = fires.next();
    }
    const truncated = !fire.done && fire.value.getTime() <= windowEnd;
    const { occurrences, catchUpAt } = (0, misfire_1.applyMisfirePolicy)(due, now, job, truncated);
    return {
        occurrences,
        skippedOccurrences: due.length - occurrences.length,
        catchUpAt,
        nextRunAt: fire.done ? null : fire.value,
        lastFiredAt: due.at(-1) ?? job.lastFiredAt,
    };
}
//# sourceMappingURL=plan.js.map