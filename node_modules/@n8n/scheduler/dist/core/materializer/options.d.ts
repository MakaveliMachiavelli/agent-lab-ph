export interface MaterializerOptions {
    windowSeconds: number;
    lookaheadSeconds: number;
    maxPerJob: number;
    batchSize: number;
    planRetrySeconds: number;
    defaultTimezone: string;
}
export declare const DEFAULT_MATERIALIZER_OPTIONS: MaterializerOptions;
