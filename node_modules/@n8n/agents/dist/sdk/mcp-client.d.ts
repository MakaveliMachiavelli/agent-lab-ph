import type { McpConnectionFailedEvent, McpServerConfig, McpVerifyResult } from '../types/sdk/mcp';
import type { BuiltTool } from '../types/sdk/tool';
export declare class McpClient {
    private readonly configs;
    private connections;
    private listToolsPromise;
    private closePromise;
    private connectionFailures;
    constructor(configs: McpServerConfig[]);
    get serverNames(): string[];
    connect(): Promise<void>;
    listTools(): Promise<BuiltTool[]>;
    close(): Promise<void>;
    verify(): Promise<McpVerifyResult>;
    declaresApproval(): boolean;
    getConnectionFailures(): readonly McpConnectionFailedEvent[];
    private doListTools;
    private notifyConnectionFailed;
    private doClose;
}
