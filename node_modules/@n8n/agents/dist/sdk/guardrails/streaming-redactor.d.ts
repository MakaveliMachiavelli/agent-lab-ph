import type { RedactionOptions, RedactionResult } from './redactor';
export declare class StreamingRedactor {
    private readonly options;
    private buffer;
    private readonly holdback;
    constructor(options?: RedactionOptions, holdback?: number);
    push(delta: string): RedactionResult;
    flush(): RedactionResult;
}
