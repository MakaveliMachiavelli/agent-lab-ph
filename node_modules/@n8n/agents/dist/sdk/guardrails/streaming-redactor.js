"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.StreamingRedactor = void 0;
const redactor_1 = require("./redactor");
const DEFAULT_HOLDBACK = 32;
const WHITESPACE = /\s/;
class StreamingRedactor {
    constructor(options = {}, holdback = DEFAULT_HOLDBACK) {
        this.options = options;
        this.buffer = '';
        this.holdback = Math.max(0, holdback);
    }
    push(delta) {
        this.buffer += delta;
        if (this.buffer.length <= this.holdback) {
            return { text: '', matches: [] };
        }
        const target = this.buffer.length - this.holdback;
        let cut = target;
        while (cut > 0 && !WHITESPACE.test(this.buffer[cut - 1])) {
            cut -= 1;
        }
        for (const [start, end] of (0, redactor_1.findMatchRanges)(this.buffer, this.options)) {
            if (start < cut && end > cut)
                cut = start;
        }
        if (cut === 0) {
            return { text: '', matches: [] };
        }
        const segment = this.buffer.slice(0, cut);
        this.buffer = this.buffer.slice(cut);
        return (0, redactor_1.redactText)(segment, this.options);
    }
    flush() {
        const remainder = this.buffer;
        this.buffer = '';
        if (!remainder)
            return { text: '', matches: [] };
        return (0, redactor_1.redactText)(remainder, this.options);
    }
}
exports.StreamingRedactor = StreamingRedactor;
//# sourceMappingURL=streaming-redactor.js.map