"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.verify = verify;
function verify(source) {
    const errors = [];
    if (!source?.trim()) {
        return { ok: false, errors: ['No source code provided'] };
    }
    if (/\.build\s*\(/.test(source)) {
        errors.push('Do not call .build() — the engine handles this automatically. Remove all .build() calls.');
    }
    if (!/export\s+default\b/.test(source)) {
        errors.push('No default export found. Export an agent as the default: export default agent;');
    }
    if (/\bScorer\b/.test(source)) {
        errors.push("Scorer is deprecated. Use the Eval system instead: new Eval('name').check(...) or evals.correctness()");
    }
    if (/process\.env\b/.test(source)) {
        errors.push('process.env is not available. Use const variables for configuration.');
    }
    return { ok: errors.length === 0, errors };
}
//# sourceMappingURL=verify.js.map