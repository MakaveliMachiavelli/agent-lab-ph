import type { Agent } from './agent';
import type { Eval } from './eval';
import type { EvalResults } from '../types';
export interface DatasetRow {
    input: string;
    expected?: string;
    resumeData?: Record<string, 'deny' | Record<string, unknown>>;
}
export interface EvaluateConfig {
    dataset: DatasetRow[];
    evals: Eval[];
}
export declare function evaluate(agent: Agent, config: EvaluateConfig): Promise<EvalResults>;
