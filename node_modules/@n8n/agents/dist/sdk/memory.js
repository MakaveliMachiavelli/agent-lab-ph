"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Memory = exports.DEFAULT_OBSERVATION_LOG_RENDER_TOKEN_BUDGET = exports.DEFAULT_OBSERVATION_LOG_LOCK_TTL_MS = void 0;
exports.resolveObservationalMemoryConfig = resolveObservationalMemoryConfig;
exports.resolveEpisodicMemoryConfig = resolveEpisodicMemoryConfig;
exports.resolveMemoryConfigDefaults = resolveMemoryConfigDefaults;
exports.normalizeMemoryConfig = normalizeMemoryConfig;
const episodic_memory_1 = require("../runtime/memory/episodic-memory");
const episodic_memory_defaults_1 = require("../runtime/memory/episodic-memory-defaults");
const memory_store_1 = require("../runtime/memory/memory-store");
const observation_log_defaults_1 = require("../runtime/memory/observation-log-defaults");
Object.defineProperty(exports, "DEFAULT_OBSERVATION_LOG_LOCK_TTL_MS", { enumerable: true, get: function () { return observation_log_defaults_1.DEFAULT_OBSERVATION_LOG_LOCK_TTL_MS; } });
Object.defineProperty(exports, "DEFAULT_OBSERVATION_LOG_RENDER_TOKEN_BUDGET", { enumerable: true, get: function () { return observation_log_defaults_1.DEFAULT_OBSERVATION_LOG_RENDER_TOKEN_BUDGET; } });
const observation_log_store_1 = require("../runtime/memory/observation-log-store");
const model_factory_1 = require("../runtime/model/model-factory");
function resolveObservationalMemoryConfig(config, options) {
    const observerModel = options.defaultModel;
    const reflectorModel = options.defaultModel;
    return {
        observerThresholdTokens: config.observerThresholdTokens ?? observation_log_defaults_1.DEFAULT_OBSERVATION_LOG_OBSERVER_THRESHOLD_TOKENS,
        reflectorThresholdTokens: config.reflectorThresholdTokens ?? observation_log_defaults_1.DEFAULT_OBSERVATION_LOG_REFLECTOR_THRESHOLD_TOKENS,
        renderTokenBudget: config.renderTokenBudget ?? observation_log_defaults_1.DEFAULT_OBSERVATION_LOG_RENDER_TOKEN_BUDGET,
        observationLogTailLimit: config.observationLogTailLimit ?? observation_log_defaults_1.DEFAULT_OBSERVATION_LOG_TAIL_LIMIT,
        lockTtlMs: config.lockTtlMs ?? observation_log_defaults_1.DEFAULT_OBSERVATION_LOG_LOCK_TTL_MS,
        observe: config.observe ?? (0, observation_log_defaults_1.createObservationLogObserveFn)(observerModel),
        reflect: config.reflect ?? (0, observation_log_defaults_1.createObservationLogReflectFn)(reflectorModel),
    };
}
function resolveEpisodicMemoryConfig(config, options) {
    const embeddingModel = config.embeddingModel ?? episodic_memory_defaults_1.DEFAULT_EPISODIC_MEMORY_EMBEDDING_MODEL;
    const extractorModel = options.defaultModel;
    const reflectorModel = options.defaultModel;
    return {
        enabled: config.enabled,
        topK: config.topK ?? episodic_memory_defaults_1.DEFAULT_EPISODIC_MEMORY_TOP_K,
        maxEntriesPerRun: config.maxEntriesPerRun ?? episodic_memory_defaults_1.DEFAULT_EPISODIC_MEMORY_MAX_ENTRIES_PER_RUN,
        embedder: config.embedder ?? (0, model_factory_1.createEmbeddingModel)(embeddingModel, config.embeddingProviderOptions),
        embeddingModel,
        extract: config.extract ??
            (0, episodic_memory_defaults_1.createEpisodicMemoryExtractFn)(extractorModel, {
                extractionPrompt: config.prompts?.extraction,
            }),
        reflect: config.reflect ??
            (0, episodic_memory_defaults_1.createEpisodicMemoryReflectFn)(reflectorModel, {
                reflectionPrompt: config.prompts?.reflection,
            }),
        prompts: config.prompts,
    };
}
function resolveMemoryConfigDefaults(config, options) {
    const episodicMemory = (0, episodic_memory_1.isEpisodicMemoryEnabled)(config.episodicMemory)
        ? resolveEpisodicMemoryConfig(config.episodicMemory, options)
        : config.episodicMemory;
    if (!config.observationalMemory) {
        return normalizeMemoryConfig({
            ...config,
            episodicMemory,
        });
    }
    if (!(0, observation_log_store_1.hasObservationLogStore)(config.memory)) {
        throw new Error('Observational memory requires a storage backend that implements BuiltObservationLogStore.');
    }
    const observationalMemoryConfig = config.observationLog?.renderTokenBudget !== undefined &&
        config.observationalMemory.renderTokenBudget === undefined
        ? {
            ...config.observationalMemory,
            renderTokenBudget: config.observationLog.renderTokenBudget,
        }
        : config.observationalMemory;
    const observationalMemory = resolveObservationalMemoryConfig(observationalMemoryConfig, options);
    return normalizeMemoryConfig({
        ...config,
        memory: config.memory,
        observationalMemory,
        episodicMemory,
    });
}
function normalizeMemoryConfig(config) {
    if ((0, episodic_memory_1.isEpisodicMemoryEnabled)(config.episodicMemory) && !(0, episodic_memory_1.hasEpisodicMemoryStore)(config.memory)) {
        throw new Error('Episodic memory requires a storage backend that implements BuiltEpisodicMemoryStore.');
    }
    if (!config.observationalMemory) {
        return config;
    }
    if (!(0, observation_log_store_1.hasObservationLogStore)(config.memory)) {
        throw new Error('Observational memory requires a storage backend that implements BuiltObservationLogStore.');
    }
    return {
        ...config,
        observationLog: {
            ...config.observationLog,
            ...(config.observationalMemory.renderTokenBudget !== undefined && {
                renderTokenBudget: config.observationalMemory.renderTokenBudget,
            }),
        },
    };
}
class Memory {
    storage(backend) {
        if (backend === 'memory') {
            this.memoryBackend = undefined;
        }
        else {
            this.memoryBackend = backend;
        }
        return this;
    }
    episodicMemory(config = {}) {
        if (config.enabled === false) {
            this.episodicMemoryConfig = undefined;
        }
        else {
            this.episodicMemoryConfig = config;
        }
        return this;
    }
    titleGeneration(config) {
        if (config === true) {
            this.titleGenerationConfig = {};
        }
        else if (config === false) {
            this.titleGenerationConfig = undefined;
        }
        else {
            this.titleGenerationConfig = config;
        }
        return this;
    }
    observationalMemory(config = {}) {
        this.observationalMemoryConfig = config;
        return this;
    }
    build() {
        const memory = this.memoryBackend ?? new memory_store_1.InMemoryMemory();
        if ((0, episodic_memory_1.isEpisodicMemoryEnabled)(this.episodicMemoryConfig)) {
            if (!(0, episodic_memory_1.hasEpisodicMemoryStore)(memory)) {
                throw new Error('Episodic memory requires a storage backend that implements BuiltEpisodicMemoryStore.');
            }
        }
        const baseConfig = {
            memory,
            episodicMemory: this.episodicMemoryConfig,
            titleGeneration: this.titleGenerationConfig,
        };
        if (!this.observationalMemoryConfig) {
            return normalizeMemoryConfig(baseConfig);
        }
        if (!(0, observation_log_store_1.hasObservationLogStore)(memory)) {
            throw new Error('Observational memory requires a storage backend that implements BuiltObservationLogStore.');
        }
        return normalizeMemoryConfig({
            ...baseConfig,
            memory,
            observationalMemory: this.observationalMemoryConfig,
        });
    }
}
exports.Memory = Memory;
//# sourceMappingURL=memory.js.map