"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getCreatedAt = getCreatedAt;
exports.isLlmMessage = isLlmMessage;
exports.filterLlmMessages = filterLlmMessages;
function getCreatedAt(message) {
    if ('createdAt' in message) {
        if (message.createdAt instanceof Date) {
            return message.createdAt;
        }
        if (typeof message.createdAt === 'string' || typeof message.createdAt === 'number') {
            const date = new Date(message.createdAt);
            if (isNaN(date.getTime())) {
                return null;
            }
            return date;
        }
    }
    return null;
}
function isLlmMessage(message) {
    return message.type !== 'custom';
}
function filterLlmMessages(messages) {
    return messages.filter(isLlmMessage);
}
//# sourceMappingURL=message.js.map