import type { BuiltVectorStoreBackend, VectorFilter, VectorQueryResult, VectorRecord } from '../types/sdk/vector-store';
import type { JSONObject } from '../types/utils/json';
export declare abstract class BaseVectorStore<TConstructorOptions extends JSONObject = JSONObject> implements BuiltVectorStoreBackend {
    protected readonly name: string;
    protected readonly constructorOptions: TConstructorOptions;
    constructor(name: string, constructorOptions: TConstructorOptions);
    upsert(_records: VectorRecord[]): Promise<void>;
    query(_vector: number[], _opts: {
        topK: number;
        filter?: VectorFilter;
    }): Promise<VectorQueryResult[]>;
    delete(_opts: {
        ids: string[];
    }): Promise<void>;
    close?(): void | Promise<void>;
}
