"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createCopyFileTool = createCopyFileTool;
const zod_1 = require("zod");
const tool_1 = require("../../sdk/tool");
function createCopyFileTool(filesystem) {
    return new tool_1.Tool('workspace_copy_file')
        .description('Copy a file or directory in the workspace')
        .input(zod_1.z.object({
        src: zod_1.z.string().describe('Source path of the file or directory to copy'),
        dest: zod_1.z.string().describe('Destination path for the copy'),
        overwrite: zod_1.z
            .boolean()
            .optional()
            .describe('Overwrite the destination if it already exists'),
    }))
        .output(zod_1.z.object({
        success: zod_1.z.boolean().describe('Whether the copy was successful'),
    }))
        .handler(async (input, ctx) => {
        await filesystem.copyFile(input.src, input.dest, {
            overwrite: input.overwrite,
            abortSignal: ctx.abortSignal,
        });
        return { success: true };
    })
        .build();
}
//# sourceMappingURL=copy-file.js.map