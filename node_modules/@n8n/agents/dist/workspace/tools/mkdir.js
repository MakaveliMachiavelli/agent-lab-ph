"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createMkdirTool = createMkdirTool;
const zod_1 = require("zod");
const tool_1 = require("../../sdk/tool");
function createMkdirTool(filesystem) {
    return new tool_1.Tool('workspace_mkdir')
        .description('Create a directory in the workspace')
        .input(zod_1.z.object({
        path: zod_1.z.string().describe('Path of the directory to create'),
        recursive: zod_1.z.boolean().optional().describe('Create parent directories if needed'),
    }))
        .output(zod_1.z.object({
        success: zod_1.z.boolean().describe('Whether the directory was created'),
    }))
        .handler(async (input, ctx) => {
        await filesystem.mkdir(input.path, {
            recursive: input.recursive,
            abortSignal: ctx.abortSignal,
        });
        return { success: true };
    })
        .build();
}
//# sourceMappingURL=mkdir.js.map