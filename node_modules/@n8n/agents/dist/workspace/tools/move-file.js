"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createMoveFileTool = createMoveFileTool;
const zod_1 = require("zod");
const tool_1 = require("../../sdk/tool");
function createMoveFileTool(filesystem) {
    return new tool_1.Tool('workspace_move_file')
        .description('Move or rename a file or directory in the workspace')
        .input(zod_1.z.object({
        src: zod_1.z.string().describe('Source path of the file or directory to move'),
        dest: zod_1.z.string().describe('Destination path'),
        overwrite: zod_1.z
            .boolean()
            .optional()
            .describe('Overwrite the destination if it already exists'),
    }))
        .output(zod_1.z.object({
        success: zod_1.z.boolean().describe('Whether the move was successful'),
    }))
        .handler(async (input, ctx) => {
        await filesystem.moveFile(input.src, input.dest, {
            overwrite: input.overwrite,
            abortSignal: ctx.abortSignal,
        });
        return { success: true };
    })
        .build();
}
//# sourceMappingURL=move-file.js.map