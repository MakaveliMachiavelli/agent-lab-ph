"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createRmdirTool = createRmdirTool;
const zod_1 = require("zod");
const tool_1 = require("../../sdk/tool");
function createRmdirTool(filesystem) {
    return new tool_1.Tool('workspace_rmdir')
        .description('Remove a directory from the workspace')
        .input(zod_1.z.object({
        path: zod_1.z.string().describe('Path of the directory to remove'),
        recursive: zod_1.z
            .boolean()
            .optional()
            .describe('Remove the directory and its contents recursively'),
        force: zod_1.z.boolean().optional().describe('Ignore errors if the directory does not exist'),
    }))
        .output(zod_1.z.object({
        success: zod_1.z.boolean().describe('Whether the directory was removed'),
    }))
        .handler(async (input, ctx) => {
        await filesystem.rmdir(input.path, {
            recursive: input.recursive,
            force: input.force,
            abortSignal: ctx.abortSignal,
        });
        return { success: true };
    })
        .build();
}
//# sourceMappingURL=rmdir.js.map