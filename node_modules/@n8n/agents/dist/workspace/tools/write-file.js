"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createWriteFileTool = createWriteFileTool;
const zod_1 = require("zod");
const tool_1 = require("../../sdk/tool");
function createWriteFileTool(filesystem) {
    return new tool_1.Tool('workspace_write_file')
        .description('Write content to a file in the workspace')
        .input(zod_1.z.object({
        path: zod_1.z.string().describe('Path to the file to write'),
        content: zod_1.z.string().describe('Content to write to the file'),
        recursive: zod_1.z
            .boolean()
            .optional()
            .describe('Create parent directories if they do not exist'),
    }))
        .output(zod_1.z.object({
        success: zod_1.z.boolean().describe('Whether the write was successful'),
    }))
        .handler(async (input, ctx) => {
        await filesystem.writeFile(input.path, input.content, {
            recursive: input.recursive,
            abortSignal: ctx.abortSignal,
        });
        return { success: true };
    })
        .build();
}
//# sourceMappingURL=write-file.js.map