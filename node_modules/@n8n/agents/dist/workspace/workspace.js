"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Workspace = void 0;
const node_crypto_1 = require("node:crypto");
const lifecycle_1 = require("./lifecycle");
const workspace_tools_1 = require("./tools/workspace-tools");
class Workspace {
    constructor(config) {
        this._status = 'pending';
        this.id = config.id ?? `workspace-${(0, node_crypto_1.randomUUID)()}`;
        this.name = config.name ?? `workspace-${this.id}`;
        this.filesystemInstance = config.filesystem;
        this.sandboxInstance = config.sandbox;
    }
    get status() {
        return this._status;
    }
    get filesystem() {
        return this.filesystemInstance;
    }
    get sandbox() {
        return this.sandboxInstance;
    }
    async init() {
        if (this._status === 'ready')
            return;
        if (this._status === 'destroyed') {
            throw new Error(`Workspace "${this.id}" has been destroyed and cannot be re-initialized`);
        }
        if (this.initPromise)
            return await this.initPromise;
        this._status = 'initializing';
        this.initPromise = this.performInit();
        try {
            await this.initPromise;
        }
        finally {
            this.initPromise = undefined;
        }
    }
    async destroy() {
        if (this.destroyPromise)
            return await this.destroyPromise;
        if (this.initPromise) {
            try {
                await this.initPromise;
            }
            catch {
            }
        }
        this.destroyPromise = this.performDestroy();
        try {
            await this.destroyPromise;
        }
        finally {
            this.destroyPromise = undefined;
        }
    }
    getInstructions() {
        const parts = [];
        if (this.sandboxInstance?.getInstructions) {
            const instructions = this.sandboxInstance.getInstructions();
            if (instructions)
                parts.push(instructions);
        }
        if (this.filesystemInstance?.getInstructions) {
            const instructions = this.filesystemInstance.getInstructions();
            if (instructions)
                parts.push(instructions);
        }
        return parts.join('\n\n');
    }
    getTools() {
        return (0, workspace_tools_1.createWorkspaceTools)(this);
    }
    async performInit() {
        try {
            if (this.filesystemInstance) {
                await (0, lifecycle_1.callLifecycle)(this.filesystemInstance, 'init');
            }
            if (this.sandboxInstance) {
                await (0, lifecycle_1.callLifecycle)(this.sandboxInstance, 'start');
            }
        }
        catch (error) {
            if (this.filesystemInstance) {
                try {
                    await (0, lifecycle_1.callLifecycle)(this.filesystemInstance, 'destroy');
                }
                catch {
                }
            }
            if (this.sandboxInstance) {
                try {
                    await (0, lifecycle_1.callLifecycle)(this.sandboxInstance, 'destroy');
                }
                catch {
                }
            }
            this._status = 'error';
            throw error;
        }
        this._status = 'ready';
    }
    async performDestroy() {
        let firstError;
        if (this.sandboxInstance) {
            try {
                await (0, lifecycle_1.callLifecycle)(this.sandboxInstance, 'destroy');
            }
            catch (error) {
                firstError = error;
            }
        }
        if (this.filesystemInstance) {
            try {
                await (0, lifecycle_1.callLifecycle)(this.filesystemInstance, 'destroy');
            }
            catch (error) {
                firstError ??= error;
            }
        }
        this._status = 'destroyed';
        if (firstError) {
            this._status = 'error';
            throw firstError;
        }
    }
}
exports.Workspace = Workspace;
//# sourceMappingURL=workspace.js.map