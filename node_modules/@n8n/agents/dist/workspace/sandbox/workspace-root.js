"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.N8N_SANDBOX_WORKSPACE_ROOT = exports.N8N_SANDBOX_HOME = exports.DAYTONA_WORKSPACE_ROOT = exports.DAYTONA_HOME = exports.WORKSPACE_DIR = void 0;
exports.getPromptWorkspaceRoot = getPromptWorkspaceRoot;
exports.getWorkspaceRoot = getWorkspaceRoot;
const run_in_sandbox_1 = require("./run-in-sandbox");
exports.WORKSPACE_DIR = 'workspace';
exports.DAYTONA_HOME = '/home/daytona';
exports.DAYTONA_WORKSPACE_ROOT = `${exports.DAYTONA_HOME}/${exports.WORKSPACE_DIR}`;
exports.N8N_SANDBOX_HOME = '/home/user';
exports.N8N_SANDBOX_WORKSPACE_ROOT = `${exports.N8N_SANDBOX_HOME}/${exports.WORKSPACE_DIR}`;
function getPromptWorkspaceRoot(provider) {
    switch (provider) {
        case 'daytona':
            return exports.DAYTONA_WORKSPACE_ROOT;
        case 'n8n-sandbox':
            return exports.N8N_SANDBOX_WORKSPACE_ROOT;
    }
}
const workspaceRootCache = new WeakMap();
function getLocalFilesystemRoot(workspace) {
    const filesystem = workspace.filesystem;
    if (!filesystem)
        return null;
    const provider = filesystem.provider;
    if (provider !== 'local' && provider !== 'lazy')
        return null;
    const basePath = Reflect.get(filesystem, 'basePath');
    return typeof basePath === 'string' && basePath.length > 0 ? basePath : null;
}
async function initializeLazyFilesystem(workspace) {
    const filesystem = workspace.filesystem;
    if (filesystem?.provider !== 'lazy')
        return;
    await filesystem.init?.();
}
function getFallbackHome(workspace) {
    switch (workspace.sandbox?.provider) {
        case 'n8n-sandbox':
            return exports.N8N_SANDBOX_HOME;
        case 'daytona':
            return exports.DAYTONA_HOME;
        case undefined:
        default:
            return exports.DAYTONA_HOME;
    }
}
async function getWorkspaceRoot(workspace) {
    const cached = workspaceRootCache.get(workspace);
    if (cached)
        return cached;
    const localRoot = getLocalFilesystemRoot(workspace);
    if (localRoot) {
        workspaceRootCache.set(workspace, localRoot);
        return localRoot;
    }
    await initializeLazyFilesystem(workspace);
    const initializedLocalRoot = getLocalFilesystemRoot(workspace);
    if (initializedLocalRoot) {
        workspaceRootCache.set(workspace, initializedLocalRoot);
        return initializedLocalRoot;
    }
    const result = await (0, run_in_sandbox_1.runInSandbox)(workspace, 'echo $HOME');
    const home = result.stdout.trim() || getFallbackHome(workspace);
    const root = `${home}/${exports.WORKSPACE_DIR}`;
    workspaceRootCache.set(workspace, root);
    return root;
}
//# sourceMappingURL=workspace-root.js.map