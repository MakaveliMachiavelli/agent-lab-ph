import type { AbortableOptions, ProviderStatus, WorkspaceSandbox, BaseSandboxOptions, CommandResult, ExecuteCommandOptions, SandboxProcessManager } from '../types';
export declare function shellQuote(arg: string): string;
export declare abstract class BaseSandbox implements WorkspaceSandbox {
    abstract readonly id: string;
    abstract readonly name: string;
    abstract readonly provider: string;
    status: ProviderStatus;
    readonly processes?: SandboxProcessManager;
    private startPromise?;
    private stopPromise?;
    private destroyPromise?;
    private readonly onStartHook?;
    private readonly onStopHook?;
    private readonly onDestroyHook?;
    constructor(options?: BaseSandboxOptions);
    abstract start(): Promise<void>;
    abstract stop(): Promise<void>;
    abstract destroy(): Promise<void>;
    _start(): Promise<void>;
    _stop(): Promise<void>;
    _destroy(): Promise<void>;
    protected markNeedsStart(): void;
    ensureRunning(options?: AbortableOptions): Promise<void>;
    executeCommand(command: string, args?: string[], options?: ExecuteCommandOptions): Promise<CommandResult>;
    getInstructions(): string;
    private executeStart;
    private executeStop;
    private executeDestroy;
}
