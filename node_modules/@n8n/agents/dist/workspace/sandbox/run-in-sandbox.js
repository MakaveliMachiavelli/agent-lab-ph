"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.runInSandbox = runInSandbox;
const abort_1 = require("../../sdk/abort");
async function runInSandbox(workspace, command, cwdOrOptions) {
    const options = typeof cwdOrOptions === 'string' ? { cwd: cwdOrOptions } : (cwdOrOptions ?? {});
    const sandbox = workspace.sandbox;
    if (!sandbox)
        throw new Error('Workspace has no sandbox');
    if (sandbox.executeCommand) {
        const result = await sandbox.executeCommand(command, [], {
            cwd: options.cwd,
            abortSignal: options.abortSignal,
        });
        return { exitCode: result.exitCode, stdout: result.stdout, stderr: result.stderr };
    }
    if (sandbox.processes) {
        const handle = await sandbox.processes.spawn(command, {
            cwd: options.cwd,
            abortSignal: options.abortSignal,
        });
        const result = await (0, abort_1.raceWithAbort)(async () => await handle.wait(), options.abortSignal);
        return { exitCode: result.exitCode, stdout: result.stdout, stderr: result.stderr };
    }
    throw new Error('Sandbox has neither executeCommand nor processes available');
}
//# sourceMappingURL=run-in-sandbox.js.map