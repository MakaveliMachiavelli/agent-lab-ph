"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ProcessHandle = exports.SandboxProcessManager = void 0;
const node_stream_1 = require("node:stream");
class SandboxProcessManager {
}
exports.SandboxProcessManager = SandboxProcessManager;
class ProcessHandle {
    constructor(options) {
        this._stdout = '';
        this._stderr = '';
        this._stdoutListeners = new Set();
        this._stderrListeners = new Set();
        if (options?.onStdout)
            this._stdoutListeners.add(options.onStdout);
        if (options?.onStderr)
            this._stderrListeners.add(options.onStderr);
    }
    async wait(options) {
        if (options?.onStdout)
            this._stdoutListeners.add(options.onStdout);
        if (options?.onStderr)
            this._stderrListeners.add(options.onStderr);
        this._completion ??= this._wait();
        try {
            return await this._completion;
        }
        finally {
            if (options?.onStdout)
                this._stdoutListeners.delete(options.onStdout);
            if (options?.onStderr)
                this._stderrListeners.delete(options.onStderr);
        }
    }
    get stdout() {
        return this._stdout;
    }
    get stderr() {
        return this._stderr;
    }
    emitStdout(data) {
        this._stdout += data;
        for (const listener of this._stdoutListeners)
            listener(data);
        this._reader?.push(data);
    }
    emitStderr(data) {
        this._stderr += data;
        for (const listener of this._stderrListeners)
            listener(data);
    }
    addStdoutListener(listener) {
        this._stdoutListeners.add(listener);
    }
    addStderrListener(listener) {
        this._stderrListeners.add(listener);
    }
    get reader() {
        if (!this._reader) {
            this._reader = new node_stream_1.Readable({ read() { } });
            this._completion ??= this._wait();
            void this._completion.then(() => this._reader.push(null), () => this._reader.push(null));
        }
        return this._reader;
    }
    get writer() {
        this._writer ??= new node_stream_1.Writable({
            write: (chunk, _encoding, callbackFn) => {
                this.sendStdin(chunk.toString()).then(() => callbackFn(), callbackFn);
            },
        });
        return this._writer;
    }
}
exports.ProcessHandle = ProcessHandle;
//# sourceMappingURL=process.js.map