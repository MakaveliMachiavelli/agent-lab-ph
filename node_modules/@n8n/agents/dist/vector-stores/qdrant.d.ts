import { BaseVectorStore } from '../storage/base-vector-store';
import type { VectorFilter, VectorQueryResult, VectorRecord } from '../types/sdk/vector-store';
export type QdrantVectorStoreOptions = {
    url: string;
    apiKey?: string;
    collectionName: string;
};
export declare class QdrantVectorStore extends BaseVectorStore<QdrantVectorStoreOptions> {
    private readonly collectionName;
    private client?;
    constructor(name: string, options: QdrantVectorStoreOptions);
    upsert(records: VectorRecord[]): Promise<void>;
    query(vector: number[], opts: {
        topK: number;
        filter?: VectorFilter;
    }): Promise<VectorQueryResult[]>;
    delete({ ids }: {
        ids: string[];
    }): Promise<void>;
    close(): void;
    private getClient;
}
