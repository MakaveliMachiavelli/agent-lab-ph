import { BaseVectorStore } from '../storage/base-vector-store';
import type { VectorFilter, VectorQueryResult, VectorRecord } from '../types/sdk/vector-store';
export type SupabaseVectorStoreOptions = {
    url: string;
    apiKey: string;
    tableName: string;
    queryName?: string;
};
export declare class SupabaseVectorStore extends BaseVectorStore<SupabaseVectorStoreOptions> {
    private readonly tableName;
    private readonly queryName;
    private client?;
    constructor(name: string, options: SupabaseVectorStoreOptions);
    upsert(records: VectorRecord[]): Promise<void>;
    query(vector: number[], opts: {
        topK: number;
        filter?: VectorFilter;
    }): Promise<VectorQueryResult[]>;
    delete({ ids }: {
        ids: string[];
    }): Promise<void>;
    close(): void;
    private getClient;
}
