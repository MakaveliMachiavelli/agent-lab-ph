import { BaseVectorStore } from '../storage/base-vector-store';
import type { VectorFilter, VectorQueryResult, VectorRecord } from '../types/sdk/vector-store';
export type PineconeVectorStoreOptions = {
    apiKey: string;
    indexName: string;
    namespace?: string;
};
export declare class PineconeVectorStore extends BaseVectorStore<PineconeVectorStoreOptions> {
    private index?;
    upsert(records: VectorRecord[]): Promise<void>;
    query(vector: number[], opts: {
        topK: number;
        filter?: VectorFilter;
    }): Promise<VectorQueryResult[]>;
    delete({ ids }: {
        ids: string[];
    }): Promise<void>;
    close(): void;
    private getIndex;
}
