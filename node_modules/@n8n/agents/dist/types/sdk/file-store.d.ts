import type { ContentFileRef } from './message';
export interface BuiltFileStore {
    load(ref: ContentFileRef, scope?: {
        threadId?: string;
    }): Promise<Uint8Array | null>;
    isMediaTypeSupported?(mediaType: string | undefined): boolean;
}
