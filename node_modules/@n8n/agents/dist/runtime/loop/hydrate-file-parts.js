"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MAX_HYDRATED_FILE_BYTES = exports.MAX_HYDRATED_FILE_PARTS = void 0;
exports.hydrateFileParts = hydrateFileParts;
exports.MAX_HYDRATED_FILE_PARTS = 20;
exports.MAX_HYDRATED_FILE_BYTES = 50 * 1024 * 1024;
async function hydrateFileParts(messages, fileStore, scope) {
    if (!fileStore)
        return;
    const blocks = [];
    for (const message of messages) {
        if (!('content' in message) || !Array.isArray(message.content))
            continue;
        for (const block of message.content) {
            if (block.type !== 'file' || !block.fileRef || block.data !== undefined)
                continue;
            if (fileStore.isMediaTypeSupported && !fileStore.isMediaTypeSupported(block.mediaType)) {
                continue;
            }
            blocks.push(block);
        }
    }
    let budget = exports.MAX_HYDRATED_FILE_BYTES;
    const newestFirst = blocks.slice(-exports.MAX_HYDRATED_FILE_PARTS).reverse();
    for (const block of newestFirst) {
        if (budget <= 0)
            break;
        const declaredSize = block.fileRef.sizeBytes;
        if (declaredSize !== undefined && declaredSize > budget)
            continue;
        try {
            const data = await fileStore.load(block.fileRef, scope);
            if (!data || data.byteLength > budget)
                continue;
            block.data = data;
            budget -= data.byteLength;
        }
        catch {
        }
    }
}
//# sourceMappingURL=hydrate-file-parts.js.map