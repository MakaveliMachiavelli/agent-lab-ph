"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ModelStreamStallError = exports.MAX_MODEL_STREAM_STALL_RETRIES = exports.DEFAULT_MODEL_STREAM_FIRST_OUTPUT_TIMEOUT_MS = exports.DEFAULT_MODEL_STREAM_IDLE_TIMEOUT_MS = void 0;
exports.withChunkIdleTimeout = withChunkIdleTimeout;
exports.raceWithStallDeadline = raceWithStallDeadline;
exports.DEFAULT_MODEL_STREAM_IDLE_TIMEOUT_MS = 90_000;
exports.DEFAULT_MODEL_STREAM_FIRST_OUTPUT_TIMEOUT_MS = 3 * 60_000;
exports.MAX_MODEL_STREAM_STALL_RETRIES = 1;
class ModelStreamStallError extends Error {
    constructor(idleMs) {
        super(`The model stream stalled: no data received for ${Math.round(idleMs / 1000)} seconds. ` +
            'This is usually a transient connection issue — please try again.');
        this.name = 'ModelStreamStallError';
    }
}
exports.ModelStreamStallError = ModelStreamStallError;
function getChunkIterator(source) {
    if (Symbol.asyncIterator in source)
        return source[Symbol.asyncIterator]();
    return source[Symbol.iterator]();
}
async function* withChunkIdleTimeout(source, getIdleMs, onStall) {
    const iterator = getChunkIterator(source);
    try {
        while (true) {
            const next = Promise.resolve(iterator.next());
            next.catch(() => undefined);
            const result = await raceWithStallDeadline(next, getIdleMs(), onStall);
            if (result.done)
                return;
            yield result.value;
        }
    }
    finally {
        void Promise.resolve()
            .then(async () => await iterator.return?.())
            .catch(() => undefined);
    }
}
async function raceWithStallDeadline(promise, idleMs, onStall) {
    let timer;
    try {
        return await Promise.race([
            promise,
            new Promise((_, reject) => {
                timer = setTimeout(() => {
                    onStall?.();
                    reject(new ModelStreamStallError(idleMs));
                }, idleMs);
                timer.unref?.();
            }),
        ]);
    }
    finally {
        clearTimeout(timer);
    }
}
//# sourceMappingURL=stream-stall.js.map