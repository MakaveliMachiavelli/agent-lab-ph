import type { TextStreamPart, ToolSet } from 'ai';
import type { StreamChunk, TokenUsage } from '../../types';
export declare function toTokenUsage(usage: {
    inputTokens?: number;
    outputTokens?: number;
    totalTokens?: number;
    inputTokenDetails?: {
        noCacheTokens?: number;
        cacheReadTokens?: number;
        cacheWriteTokens?: number;
    };
    outputTokenDetails?: {
        reasoningTokens?: number;
    };
} | undefined, providerMetadata?: Record<string, unknown>): TokenUsage | undefined;
export declare function convertChunk(c: TextStreamPart<ToolSet>): StreamChunk | undefined;
