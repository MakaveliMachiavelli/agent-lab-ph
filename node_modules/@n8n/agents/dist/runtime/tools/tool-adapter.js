"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isSuspendedToolResult = isSuspendedToolResult;
exports.toAiSdkProviderTools = toAiSdkProviderTools;
exports.toAiSdkTools = toAiSdkTools;
exports.executeTool = executeTool;
exports.isInterruptible = isInterruptible;
exports.buildToolMap = buildToolMap;
const zod_1 = require("zod");
const cancellation_1 = require("../../sdk/cancellation");
const json_schema_1 = require("../../utils/json-schema");
const zod_2 = require("../../utils/zod");
const lazy_ai_1 = require("../model/lazy-ai");
const provider_quirks_1 = require("../model/provider-quirks");
const SUSPEND_BRAND = Symbol('SuspendBrand');
function isSuspendedToolResult(value) {
    return typeof value === 'object' && value !== null && SUSPEND_BRAND in value;
}
function toAiSdkProviderTools(tools) {
    if (!tools || tools.length === 0)
        return {};
    const result = {};
    for (const t of tools) {
        const providerTool = {
            type: 'provider',
            id: t.name,
            args: t.args,
            inputSchema: t.inputSchema ?? zod_1.z.any(),
            isProviderExecuted: true,
        };
        result[t.name] = providerTool;
    }
    return result;
}
function toAiSdkTools(tools) {
    if (!tools || tools.length === 0)
        return {};
    const result = {};
    for (const t of tools) {
        if (t.inputSchema) {
            const ai = (0, lazy_ai_1.loadAi)();
            const providerOptions = (0, provider_quirks_1.applyToolProviderOptionDefaults)(t.providerOptions);
            const strict = t.mcpTool ? false : undefined;
            if ((0, zod_2.isZodSchema)(t.inputSchema)) {
                result[t.name] = ai.tool({
                    description: t.description,
                    inputSchema: t.inputSchema,
                    providerOptions,
                    strict,
                });
            }
            else {
                result[t.name] = ai.tool({
                    description: t.description,
                    inputSchema: ai.jsonSchema((0, json_schema_1.fixSchema)(t.inputSchema)),
                    providerOptions,
                    strict,
                });
            }
        }
    }
    return result;
}
async function executeTool(args, builtTool, resumeData, parentTelemetry, toolCallId, executionContext = {}) {
    if (!builtTool.handler) {
        throw new Error(`No handler found for tool "${builtTool.name}"`);
    }
    if (builtTool.suspendSchema) {
        const isCancelled = (0, cancellation_1.isCancellation)(resumeData);
        const ctx = {
            suspend: async (payload, options) => {
                const resolvedOptions = {
                    continuation: executionContext.continuation,
                    ...options,
                    resumeSchema: options?.resumeSchema ?? executionContext.resumeSchema,
                };
                await executionContext.onSuspend?.(payload, resolvedOptions);
                return await Promise.resolve({
                    [SUSPEND_BRAND]: true,
                    payload,
                    ...resolvedOptions,
                });
            },
            resumeData: isCancelled ? undefined : resumeData,
            cancellation: isCancelled ? { message: resumeData.message } : undefined,
            parentTelemetry,
            toolCallId,
            toolName: builtTool.name,
            runId: executionContext.runId,
            persistence: executionContext.persistence,
            emitEvent: executionContext.emitEvent,
            abortSignal: executionContext.abortSignal,
            executionCounter: executionContext.executionCounter,
            suspendPayload: executionContext.suspendPayload,
            continuation: executionContext.continuation,
            resumeSchema: executionContext.resumeSchema,
        };
        return await builtTool.handler(args, ctx);
    }
    const ctx = {
        parentTelemetry,
        toolCallId,
        toolName: builtTool.name,
        runId: executionContext.runId,
        persistence: executionContext.persistence,
        emitEvent: executionContext.emitEvent,
        abortSignal: executionContext.abortSignal,
        executionCounter: executionContext.executionCounter,
    };
    return await builtTool.handler(args, ctx);
}
function isInterruptible(toolName, toolMap) {
    const builtTool = toolMap.get(toolName);
    return !!builtTool?.suspendSchema;
}
function buildToolMap(tools) {
    const map = new Map();
    if (tools) {
        for (const t of tools) {
            map.set(t.name, t);
        }
    }
    return map;
}
//# sourceMappingURL=tool-adapter.js.map