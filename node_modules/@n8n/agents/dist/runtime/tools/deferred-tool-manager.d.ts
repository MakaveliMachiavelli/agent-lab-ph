import type { AgentDbMessage } from '../../types/sdk/message';
import type { BuiltTool } from '../../types/sdk/tool';
export declare const SEARCH_TOOLS_TOOL_NAME = "search_tools";
export declare const LOAD_TOOL_TOOL_NAME = "load_tool";
export interface DeferredToolManagerOptions {
    topK?: number;
}
export declare class DeferredToolManager {
    private readonly toolsByName;
    private readonly loadedToolNames;
    private readonly topK;
    private readonly searchTool;
    private readonly loadTool;
    constructor(tools: BuiltTool[], options?: DeferredToolManagerOptions);
    get hasTools(): boolean;
    get totalToolCount(): number;
    get loadedToolCount(): number;
    getControllerTools(): BuiltTool[];
    getLoadedTools(): BuiltTool[];
    hydrateLoadedToolsFromMessages(messages: AgentDbMessage[]): void;
    private createSearchTool;
    private createLoadTool;
    private search;
    private load;
    private summarizeTool;
    private isSuccessfulLoadToolCall;
    private getLoadedToolNameFromOutput;
}
