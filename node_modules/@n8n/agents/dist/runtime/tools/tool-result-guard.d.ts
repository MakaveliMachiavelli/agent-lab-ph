import type { AgentMessage } from '../../types/sdk/message';
import type { JSONValue } from '../../types/utils/json';
import { type TokenCounter } from '../model/model-token-counter';
export declare const MAX_MODEL_TOOL_RESULT_TOKENS = 50000;
export interface GuardedToolResult {
    historyOutput: JSONValue;
    wireOutput: unknown;
    truncated: boolean;
}
export declare function guardToolResultForModel(output: unknown, tokenCounter?: TokenCounter): Promise<GuardedToolResult>;
export declare function guardToolErrorForModel(errorText: string, tokenCounter?: TokenCounter): Promise<string>;
export declare function guardToolMessageForModel(message: AgentMessage, tokenCounter?: TokenCounter): Promise<AgentMessage>;
