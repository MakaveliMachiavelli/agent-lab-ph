"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ROOT_SUB_AGENT_TASK_PATH = exports.DEFAULT_SUB_AGENT_MAX_CHILDREN = void 0;
exports.sanitizeSubAgentTaskName = sanitizeSubAgentTaskName;
exports.isSubAgentTaskPath = isSubAgentTaskPath;
exports.assertSubAgentTaskPath = assertSubAgentTaskPath;
exports.createChildSubAgentTaskPath = createChildSubAgentTaskPath;
exports.DEFAULT_SUB_AGENT_MAX_CHILDREN = 10;
exports.ROOT_SUB_AGENT_TASK_PATH = '/root';
const MAX_TASK_NAME_LENGTH = 64;
const SUB_AGENT_TASK_PATH_PATTERN = /^\/root(?:\/[a-z0-9_]+)?$/;
function sanitizeSubAgentTaskName(taskName) {
    const sanitized = taskName
        .trim()
        .toLowerCase()
        .replace(/[^a-z0-9]+/g, '_')
        .replace(/_+/g, '_')
        .replace(/^_+|_+$/g, '')
        .slice(0, MAX_TASK_NAME_LENGTH)
        .replace(/_+$/g, '');
    if (!sanitized) {
        throw new Error('Sub-agent task name must contain at least one alphanumeric character');
    }
    return sanitized;
}
function isSubAgentTaskPath(value) {
    return SUB_AGENT_TASK_PATH_PATTERN.test(value);
}
function assertSubAgentTaskPath(value) {
    if (!isSubAgentTaskPath(value)) {
        throw new Error(`Invalid sub-agent task path: ${value}`);
    }
}
function createChildSubAgentTaskPath(taskName, childCount) {
    const childPath = `${exports.ROOT_SUB_AGENT_TASK_PATH}/${sanitizeSubAgentTaskName(taskName)}_${childCount}`;
    assertSubAgentTaskPath(childPath);
    return childPath;
}
//# sourceMappingURL=sub-agent-task-path.js.map