"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.todoStatusSchema = void 0;
exports.buildTodosInputSchema = buildTodosInputSchema;
exports.buildTodosOutputSchema = buildTodosOutputSchema;
exports.todosEchoHandler = todosEchoHandler;
const zod_1 = require("zod");
exports.todoStatusSchema = zod_1.z.enum([
    'pending',
    'in_progress',
    'completed',
    'blocked',
    'cancelled',
]);
function buildTodosInputSchema(todoItemSchema) {
    return zod_1.z
        .object({
        todos: zod_1.z
            .array(todoItemSchema)
            .describe('Full task list for the current run. Replaces any previous list.'),
    })
        .superRefine((value, ctx) => {
        const seen = new Set();
        for (const [index, todo] of value.todos.entries()) {
            const id = todo.id;
            if (seen.has(id)) {
                ctx.addIssue({
                    code: 'custom',
                    message: `Duplicate todo id "${id}". Each task must have a unique id.`,
                    path: ['todos', index, 'id'],
                });
            }
            seen.add(id);
        }
    });
}
function buildTodosOutputSchema(todoItemSchema) {
    return zod_1.z.object({
        status: zod_1.z.literal('ok'),
        todoCount: zod_1.z.number(),
        todos: zod_1.z.array(todoItemSchema),
    });
}
async function todosEchoHandler(input) {
    const todos = [...input.todos];
    return await Promise.resolve({
        status: 'ok',
        todoCount: todos.length,
        todos,
    });
}
//# sourceMappingURL=todos-core.js.map