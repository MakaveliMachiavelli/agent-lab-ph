import type { TokenUsage } from '../../types';
export interface RawUsageReader {
    capture(rawValue: unknown): void;
    getUsage(): TokenUsage | undefined;
}
export declare class AnthropicRawUsageReader implements RawUsageReader {
    private raw;
    capture(rawValue: unknown): void;
    getUsage(): TokenUsage | undefined;
}
export declare function createRawUsageReader(modelId: string): RawUsageReader | undefined;
