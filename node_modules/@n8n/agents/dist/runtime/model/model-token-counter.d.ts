import type { ModelConfig } from '../../types/sdk/agent';
export type TokenCounter = (text: string) => number | Promise<number>;
export declare const estimateObservationTokens: TokenCounter;
export declare function createModelTokenCounter(model: ModelConfig): TokenCounter;
