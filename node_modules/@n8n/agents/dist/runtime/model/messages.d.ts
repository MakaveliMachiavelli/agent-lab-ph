import type { ModelMessage, FinishReason as AiFinishReason } from 'ai';
import type { FinishReason } from '../../types';
import type { AgentMessage, ContentFile, Message } from '../../types/sdk/message';
import type { JSONObject } from '../../types/utils/json';
type ToolInputNormalizationResult = {
    ok: true;
    input: JSONObject;
} | {
    ok: false;
    input: JSONObject;
    error: string;
};
export declare function normalizeToolInputForModel(value: unknown): ToolInputNormalizationResult;
export declare function fileMetadataText(block: ContentFile): string;
export declare function toAiMessages(messages: Message[]): ModelMessage[];
export declare function fromAiMessages(messages: ModelMessage[]): AgentMessage[];
export declare function fromAiFinishReason(reason: AiFinishReason): FinishReason;
export {};
