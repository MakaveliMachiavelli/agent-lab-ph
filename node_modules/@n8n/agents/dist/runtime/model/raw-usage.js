"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AnthropicRawUsageReader = void 0;
exports.createRawUsageReader = createRawUsageReader;
const is_record_1 = require("@n8n/utils/is-record");
const provider_quirks_1 = require("./provider-quirks");
class AnthropicRawUsageReader {
    capture(rawValue) {
        if (typeof rawValue !== 'object' || rawValue === null)
            return;
        const event = rawValue;
        if (event.type === 'message_start' && (0, is_record_1.isRecord)(event.message)) {
            const usage = event.message.usage;
            if ((0, is_record_1.isRecord)(usage))
                this.raw = usage;
        }
        else if (event.type === 'message_delta' && (0, is_record_1.isRecord)(event.usage)) {
            this.raw = { ...(this.raw ?? {}), ...event.usage };
        }
    }
    getUsage() {
        const raw = this.raw;
        if (!raw)
            return undefined;
        const num = (v) => (typeof v === 'number' && v > 0 ? v : 0);
        const noCache = num(raw.input_tokens);
        const cacheWrite = num(raw.cache_creation_input_tokens);
        const cacheRead = num(raw.cache_read_input_tokens);
        const output = num(raw.output_tokens);
        const promptTokens = noCache + cacheWrite + cacheRead;
        if (promptTokens + output <= 0)
            return undefined;
        const usage = {
            promptTokens,
            completionTokens: output,
            totalTokens: promptTokens + output,
        };
        if (noCache || cacheRead || cacheWrite) {
            usage.inputTokenDetails = {
                ...(noCache && { noCache }),
                ...(cacheRead && { cacheRead }),
                ...(cacheWrite && { cacheWrite }),
            };
        }
        return usage;
    }
}
exports.AnthropicRawUsageReader = AnthropicRawUsageReader;
const RAW_USAGE_READERS = {
    anthropic: () => new AnthropicRawUsageReader(),
};
function createRawUsageReader(modelId) {
    const provider = (0, provider_quirks_1.providerIdFromModelId)(modelId);
    return RAW_USAGE_READERS[provider]?.();
}
//# sourceMappingURL=raw-usage.js.map