"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildAiSdkTelemetry = buildAiSdkTelemetry;
function buildAiSdkTelemetry(telemetry, options = {}) {
    if (!telemetry?.enabled)
        return {};
    const baseFunctionId = telemetry.functionId ?? options.fallbackFunctionId ?? 'agent';
    const functionId = options.functionSuffix
        ? `${baseFunctionId}.${options.functionSuffix}`
        : baseFunctionId;
    const integrations = telemetry.resolveIntegrations?.(telemetry.metadata) ?? telemetry.integrations;
    return {
        telemetry: {
            isEnabled: true,
            functionId,
            recordInputs: telemetry.recordInputs,
            recordOutputs: telemetry.recordOutputs,
            integrations: integrations.length > 0 ? integrations : undefined,
        },
    };
}
//# sourceMappingURL=telemetry-options.js.map