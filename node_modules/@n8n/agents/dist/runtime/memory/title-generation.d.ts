import type { LanguageModel } from 'ai';
import type { BuiltMemory, BuiltTelemetry, TitleGenerationConfig } from '../../types';
import type { AgentExecutionCounter, ModelConfig } from '../../types/sdk/agent';
import type { AgentDbMessage } from '../../types/sdk/message';
import { type FetchFn } from '../model/model-factory';
interface GenerateTitleFromMessageOptions {
    instructions?: string;
    telemetry?: BuiltTelemetry;
    executionCounter?: AgentExecutionCounter;
}
export declare function generateTitleFromMessage(model: LanguageModel, userMessage: string, opts?: GenerateTitleFromMessageOptions): Promise<string | null>;
export declare function generateTitleAndEmojiFromMessage(model: LanguageModel, userMessage: string, opts?: {
    instructions?: string;
    executionCounter?: AgentExecutionCounter;
}): Promise<{
    title: string;
    emoji?: string;
} | null>;
export declare function generateThreadTitle(opts: {
    memory: BuiltMemory;
    threadId: string;
    resourceId: string;
    titleConfig: TitleGenerationConfig;
    agentModel: ModelConfig;
    modelFetch?: FetchFn;
    turnDelta: AgentDbMessage[];
    executionCounter?: AgentExecutionCounter;
}): Promise<void>;
export {};
