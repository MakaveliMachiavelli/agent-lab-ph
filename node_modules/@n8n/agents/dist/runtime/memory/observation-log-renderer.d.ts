import { type ObservationLogEntry } from '../../types/sdk/observation-log';
export interface RenderObservationLogOptions {
    renderTokenBudget?: number;
}
export declare function renderObservationLog(entries: ObservationLogEntry[], options?: RenderObservationLogOptions): string | null;
