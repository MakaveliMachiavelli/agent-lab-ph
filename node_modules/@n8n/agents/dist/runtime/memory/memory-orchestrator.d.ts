import type { BuiltTelemetry } from '../../types';
import type { AgentPersistenceOptions, ExecutionOptions, RunOptions } from '../../types/sdk/agent';
import type { AgentDbMessage } from '../../types/sdk/message';
import type { AgentRuntimeConfig } from '../loop/agent-runtime';
import type { AgentMessageList } from '../model/message-list';
import { type TokenCounter } from '../model/model-token-counter';
import type { BackgroundTaskTracker } from '../state/background-task-tracker';
import type { AgentEventBus } from '../state/event-bus';
import { type RuntimeTelemetry } from '../telemetry/runtime-telemetry';
export declare class MemoryOrchestrator {
    private readonly config;
    private readonly backgroundTasks;
    private readonly eventBus;
    private readonly runtimeTelemetry;
    private readonly tokenCounter;
    private memoryTasks;
    private episodicMemoryTasksByResource;
    constructor(config: AgentRuntimeConfig, backgroundTasks: BackgroundTaskTracker, eventBus: AgentEventBus, runtimeTelemetry: RuntimeTelemetry, tokenCounter?: TokenCounter);
    loadHistoryMessages(persistence: AgentPersistenceOptions, telemetry?: BuiltTelemetry): Promise<AgentDbMessage[]>;
    private queryResultAttributes;
    private hasActiveObservations;
    loadInto(list: AgentMessageList, options: (RunOptions & ExecutionOptions) | undefined): Promise<void>;
    private saveMessagesWithSpan;
    setListObservationLogMemory(list: AgentMessageList, options: AgentPersistenceOptions | undefined): Promise<void>;
    persistInputMessages(list: AgentMessageList, options: (RunOptions & ExecutionOptions) | undefined): Promise<void>;
    persistTurnDelta(list: AgentMessageList, options: (RunOptions & ExecutionOptions) | undefined): Promise<void>;
    saveToMemory(list: AgentMessageList, options: (RunOptions & ExecutionOptions) | undefined): Promise<void>;
    private scheduleObservationLogJobs;
    private scheduleEpisodicMemoryJob;
    private scheduleEpisodicMemoryTask;
    private runEpisodicMemoryTask;
    private releaseEpisodicMemoryTaskLock;
    private scheduleMemoryTask;
    private getMemoryTaskRunner;
    private getObservationLogScope;
}
