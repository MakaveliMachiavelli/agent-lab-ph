import { OperationalError } from 'n8n-workflow';
export declare const LockNamespace: {
    readonly KNOWN_LOCKS: 1;
    readonly CREDENTIALS: 2;
};
export type LockNamespace = (typeof LockNamespace)[keyof typeof LockNamespace];
export declare class LockAcquisitionTimeoutError extends OperationalError {
}
export interface ILockService {
    withLease<T>(ns: LockNamespace, key: string, fn: (signal: AbortSignal) => Promise<T>, options?: {
        waitTimeoutMs?: number;
        leaseTtlMs?: number;
    }): Promise<T>;
}
