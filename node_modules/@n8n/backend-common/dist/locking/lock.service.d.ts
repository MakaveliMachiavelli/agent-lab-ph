import { InProcessLockService } from './in-process-lock.service';
import { ILockService, LockNamespace } from './lock-service.interface';
export declare class LockService implements ILockService {
    private provider;
    constructor(inProcessLockService: InProcessLockService);
    setProvider(provider: ILockService): void;
    withLease<T>(ns: LockNamespace, key: string, fn: (signal: AbortSignal) => Promise<T>, options?: {
        waitTimeoutMs?: number;
        leaseTtlMs?: number;
    }): Promise<T>;
}
