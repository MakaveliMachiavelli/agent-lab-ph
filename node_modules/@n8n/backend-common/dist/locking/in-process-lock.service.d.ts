import { type ILockService, type LockNamespace } from './lock-service.interface';
export declare class InProcessLockService implements ILockService {
    private locks;
    private getOrCreateLockState;
    private createReleaseFn;
    private acquireLock;
    buildLockKey(ns: LockNamespace, key: string): string;
    withLease<T>(ns: LockNamespace, key: string, fn: (signal: AbortSignal) => Promise<T>, options?: {
        waitTimeoutMs?: number;
        leaseTtlMs?: number;
    }): Promise<T>;
}
