"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.isContainedWithin = isContainedWithin;
exports.safeJoinPath = safeJoinPath;
exports.pathComponents = pathComponents;
exports.pathSegmentsBetween = pathSegmentsBetween;
exports.containsSymlinkedComponent = containsSymlinkedComponent;
const n8n_workflow_1 = require("n8n-workflow");
const promises_1 = require("node:fs/promises");
const path = __importStar(require("node:path"));
function isContainedWithin(parentPath, childPath) {
    parentPath = path.resolve(parentPath);
    childPath = path.resolve(childPath);
    if (parentPath === childPath) {
        return true;
    }
    return childPath.startsWith(parentPath + path.sep);
}
function safeJoinPath(parentPath, ...paths) {
    const candidate = path.join(parentPath, ...paths);
    if (!isContainedWithin(parentPath, candidate)) {
        throw new n8n_workflow_1.UnexpectedError(`Path traversal detected, refusing to join paths: ${parentPath} and ${JSON.stringify(paths)}`);
    }
    return candidate;
}
function pathComponents(targetPath) {
    const root = path.parse(targetPath).root;
    const segments = targetPath.slice(root.length).split(path.sep).filter(Boolean);
    return segments.reduce((components, segment) => [...components, path.join(components.at(-1) ?? root, segment)], []);
}
function pathSegmentsBetween(ancestor, descendant) {
    if (!isContainedWithin(ancestor, descendant)) {
        return null;
    }
    const relative = path.relative(ancestor, descendant);
    return relative.split(path.sep).filter(Boolean);
}
async function containsSymlinkedComponent(resolvedPath) {
    for (const component of pathComponents(resolvedPath)) {
        let stats;
        try {
            stats = await (0, promises_1.lstat)(component);
        }
        catch (error) {
            if (error instanceof Error &&
                'code' in error &&
                (error.code === 'ENOENT' || error.code === 'ENOTDIR')) {
                continue;
            }
            throw error;
        }
        if (stats.isSymbolicLink()) {
            return true;
        }
    }
    return false;
}
//# sourceMappingURL=path-util.js.map