"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.prepareBinaryPropertyList = prepareBinaryPropertyList;
function prepareBinaryPropertyList(data) {
    if (Array.isArray(data))
        return data;
    if (typeof data === 'object')
        return [data];
    return data
        .split(',')
        .map((item) => item.trim())
        .filter((item) => item.length > 0);
}
//# sourceMappingURL=utils.js.map