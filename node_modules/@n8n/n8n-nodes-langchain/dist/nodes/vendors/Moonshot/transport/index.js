"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.apiRequest = apiRequest;
async function apiRequest(method, endpoint, parameters) {
    const { body, qs, option, headers } = parameters ?? {};
    const credentials = await this.getCredentials('moonshotApi');
    const baseUrl = credentials.url ?? 'https://api.moonshot.ai/v1';
    const url = `${baseUrl}${endpoint}`;
    const options = {
        headers: headers ?? {},
        method,
        body,
        qs,
        url,
        json: true,
    };
    if (option && Object.keys(option).length !== 0) {
        Object.assign(options, option);
    }
    return await this.helpers.httpRequestWithAuthentication.call(this, 'moonshotApi', options);
}
//# sourceMappingURL=index.js.map