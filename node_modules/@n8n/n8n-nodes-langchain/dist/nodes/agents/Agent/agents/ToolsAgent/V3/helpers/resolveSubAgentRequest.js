"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveSubAgentRequest = resolveSubAgentRequest;
const agent_execution_1 = require("../../../../../../../utils/agent-execution");
const n8n_core_1 = require("n8n-core");
const n8n_workflow_1 = require("n8n-workflow");
const common_1 = require("../../common");
async function resolveSubAgentRequest(ctx, request, deps) {
    let current = request;
    const node = ctx.getNode();
    const tools = (await (0, common_1.getTools)(ctx));
    while ((0, n8n_core_1.isEngineRequest)(current)) {
        assertNoHitlActions(node, current.actions);
        ctx.getExecutionCancelSignal?.()?.throwIfAborted?.();
        const actionResponses = await Promise.all(current.actions.map(async (action) => await (0, agent_execution_1.executeEngineAction)(node, action, tools)));
        current = await deps.runAgentBatch({ actionResponses, metadata: current.metadata });
    }
    return current;
}
function assertNoHitlActions(node, actions) {
    for (const action of actions) {
        if (action.metadata?.hitl) {
            throw new n8n_workflow_1.UserError(`Human-in-the-Loop nodes cannot be used inside a sub-agent. Move "${action.nodeName}" to the top-level workflow.`, {
                description: 'Approval flows need the top-level engine; the sub-agent step is aborted (sibling tool calls skipped).',
                extra: { node: node.name },
            });
        }
    }
}
//# sourceMappingURL=resolveSubAgentRequest.js.map