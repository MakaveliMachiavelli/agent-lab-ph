"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTools = getTools;
const runtime_1 = require("../shared/runtime");
async function getTools() {
    const authentication = this.getNodeParameter('authentication');
    const timeout = this.getNodeParameter('options.timeout', 60000);
    const node = this.getNode();
    let serverTransport;
    let endpointUrl;
    if (node.typeVersion === 1) {
        serverTransport = 'sse';
        endpointUrl = this.getNodeParameter('sseEndpoint');
    }
    else {
        serverTransport = this.getNodeParameter('serverTransport');
        endpointUrl = this.getNodeParameter('endpointUrl');
    }
    return await (0, runtime_1.loadMcpToolOptions)(this, {
        authentication,
        transport: serverTransport,
        endpointUrl,
        timeout,
    });
}
//# sourceMappingURL=loadOptions.js.map