import { UserError } from 'n8n-workflow';
export declare class ChatTriggerAuthorizationError extends UserError {
    readonly responseCode: number;
    constructor(responseCode: number, message?: string);
}
