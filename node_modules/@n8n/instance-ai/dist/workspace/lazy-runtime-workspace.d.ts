import { Workspace } from '@n8n/agents';
export type RuntimeWorkspaceResolver = () => Promise<Workspace | undefined>;
export declare const INSTANCE_AI_WORKSPACE_TOOL_ALLOWLIST: Set<string>;
export interface LazyRuntimeWorkspaceOptions {
    ensureWorkspace: RuntimeWorkspaceResolver;
    id?: string;
    name?: string;
    sandboxInstructions?: string;
    filesystemInstructions?: string;
}
export declare function createLazyRuntimeWorkspace({ ensureWorkspace, id, name, sandboxInstructions, filesystemInstructions, }: LazyRuntimeWorkspaceOptions): Workspace;
