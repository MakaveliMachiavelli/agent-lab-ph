import type { Daytona, Image } from '@daytona/sdk';
import type { RuntimeSkillSource } from '@n8n/agents';
import type { Logger } from '../logger';
import { BuilderTemplatesService } from './builder-templates-service';
export interface CreateSnapshotOptions {
    timeout?: number;
    onLogs?: (chunk: string) => void;
}
export declare class SnapshotManager {
    private readonly baseImage;
    private readonly logger;
    private readonly n8nVersion;
    private readonly runtimeSkillSource?;
    private readonly templatesService?;
    private cachedImage;
    private runtimeSkillBundlePromise;
    private knowledgeBaseBundlePromise;
    constructor(baseImage: string | undefined, logger: Logger, n8nVersion: string | undefined, runtimeSkillSource?: RuntimeSkillSource | undefined, templatesService?: BuilderTemplatesService | undefined);
    ensureImage(): Promise<Image>;
    private prepareImage;
    createSnapshot(daytona: Daytona, options?: CreateSnapshotOptions): Promise<string>;
    private verifySnapshot;
    snapshotName(): string | null;
    private runtimeSkillBundle;
    private knowledgeBaseBundle;
    private buildKnowledgeBaseBundle;
    invalidate(): void;
}
