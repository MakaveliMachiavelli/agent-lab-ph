import type { Logger } from '../logger';
export interface BuilderTemplatesServiceOptions {
    cdnBaseUrl?: string;
    sdkVersion?: string;
    cacheDir?: string;
    refreshIntervalMs?: number;
    fetchTimeoutMs?: number;
    maxAttempts?: number;
    retryBackoffBaseMs?: number;
    failureRetryIntervalMs?: number;
    disabled?: boolean;
    logger: Logger;
}
export interface BuilderTemplatesBundle {
    archive: Buffer | null;
    version: string | null;
}
export declare class BuilderTemplatesService {
    private readonly cdnBase;
    private readonly versionPrefix;
    private readonly sdkVersion;
    private readonly cacheDir;
    private readonly refreshIntervalMs;
    private readonly fetchTimeoutMs;
    private readonly maxAttempts;
    private readonly retryBackoffBaseMs;
    private readonly failureRetryIntervalMs;
    private readonly disabled;
    private readonly logger;
    private state;
    private hydratePromise;
    private backgroundRefresh;
    private lastHydrateFailureAt;
    constructor(opts: BuilderTemplatesServiceOptions);
    private channelPrefix;
    private archiveUrlFor;
    private sha256UrlFor;
    getBundle(): Promise<BuilderTemplatesBundle>;
    private isWithinHydrateFailureCooldown;
    getVersion(): string | null;
    private hydrate;
    private loadFromDisk;
    private readEtagFromDisk;
    private readSha256FromDisk;
    private readChannelFromDisk;
    private refresh;
    private fetchBundleWithRetries;
    private tryFetchBundleOnce;
    private fetchSha256Sidecar;
    private persist;
}
export declare function builderTemplatesOptionsFromEnv({ logger, }: {
    logger: Logger;
}): BuilderTemplatesServiceOptions;
