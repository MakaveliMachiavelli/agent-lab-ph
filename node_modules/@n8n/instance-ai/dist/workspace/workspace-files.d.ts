import type { Logger } from '../logger';
import { type SandboxWorkspace } from './sandbox-fs';
export interface WorkspaceFileTarget {
    filesystem?: {
        readFile?: (path: string, options?: {
            encoding?: 'utf-8';
            abortSignal?: AbortSignal;
        }) => Promise<string | Buffer>;
        writeFile: (path: string, content: string | Buffer, options?: {
            recursive?: boolean;
            abortSignal?: AbortSignal;
        }) => Promise<void>;
    };
    sandbox?: SandboxWorkspace['sandbox'];
}
export interface WorkspaceFileOptions {
    logger: Logger;
    resourceLabel?: string;
    retryBackoffBaseMs?: number;
    abortSignal?: AbortSignal;
}
export declare function readWorkspaceFile(workspace: WorkspaceFileTarget, filePath: string, options?: WorkspaceFileOptions): Promise<string | null>;
export declare function writeWorkspaceFile(workspace: WorkspaceFileTarget, filePath: string, content: string, options?: WorkspaceFileOptions): Promise<void>;
export declare function writeWorkspaceFileMap(workspace: WorkspaceFileTarget, files: Map<string, string>, options?: WorkspaceFileOptions): Promise<void>;
