"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SANDBOX_LINKED_WORKSPACE_PACKAGES = void 0;
exports.isLinkWorkspaceSdkEnabled = isLinkWorkspaceSdkEnabled;
exports.packWorkspacePackage = packWorkspacePackage;
exports.packSandboxLinkedWorkspacePackages = packSandboxLinkedWorkspacePackages;
exports.packWorkspaceSdk = packWorkspaceSdk;
const node_child_process_1 = require("node:child_process");
const promises_1 = require("node:fs/promises");
const node_module_1 = require("node:module");
const node_os_1 = require("node:os");
const node_path_1 = __importDefault(require("node:path"));
const node_util_1 = require("node:util");
const hostRequire = (0, node_module_1.createRequire)(__filename);
const execFileAsync = (0, node_util_1.promisify)(node_child_process_1.execFile);
const ENV_FLAG = 'N8N_INSTANCE_AI_SANDBOX_LINK_SDK';
exports.SANDBOX_LINKED_WORKSPACE_PACKAGES = ['n8n-workflow', '@n8n/workflow-sdk'];
function isLinkWorkspaceSdkEnabled() {
    const v = process.env[ENV_FLAG];
    return v === '1' || v === 'true';
}
async function packWorkspacePackage(logger, packageName) {
    const packagePath = resolvePackagePath(packageName);
    if (!packagePath) {
        logger.warn(`${ENV_FLAG} is set but ${packageName} could not be resolved on the host — skipping sandbox link`);
        return null;
    }
    const distPath = node_path_1.default.join(packagePath, 'dist');
    try {
        await (0, promises_1.stat)(distPath);
    }
    catch {
        logger.warn(`${ENV_FLAG} is set but ${packageName}/dist is missing — run \`pnpm build\` in ${packagePath} first. Skipping sandbox link.`);
        return null;
    }
    const tmpDir = await (0, promises_1.mkdtemp)(node_path_1.default.join((0, node_os_1.tmpdir)(), 'n8n-workspace-pack-'));
    try {
        const { stdout } = await execFileAsync('pnpm', ['pack', '--pack-destination', tmpDir], {
            cwd: packagePath,
            env: process.env,
            maxBuffer: 16 * 1024 * 1024,
        });
        const filename = parsePackFilename(stdout);
        if (!filename) {
            throw new Error(`pnpm pack produced no tarball — stdout:\n${stdout}`);
        }
        const tarballPath = node_path_1.default.join(tmpDir, filename);
        const tarball = await (0, promises_1.readFile)(tarballPath);
        const version = parseVersionFromFilename(filename);
        logger.info('Packed workspace package for sandbox link', {
            package: packageName,
            version,
            bytes: tarball.byteLength,
            packagePath,
        });
        return { tarball, packageName, version, filename, packagePath };
    }
    finally {
        await (0, promises_1.rm)(tmpDir, { recursive: true, force: true });
    }
}
async function packSandboxLinkedWorkspacePackages(logger) {
    if (!isLinkWorkspaceSdkEnabled())
        return null;
    const packed = [];
    for (const packageName of exports.SANDBOX_LINKED_WORKSPACE_PACKAGES) {
        const tarball = await packWorkspacePackage(logger, packageName);
        if (!tarball) {
            throw new Error(`${ENV_FLAG} is enabled, but ${packageName} could not be packed. Run \`pnpm build\` for packages/workflow and packages/@n8n/workflow-sdk, or unset ${ENV_FLAG}.`);
        }
        packed.push(tarball);
    }
    return packed;
}
async function packWorkspaceSdk(logger, packageName = '@n8n/workflow-sdk') {
    if (!isLinkWorkspaceSdkEnabled())
        return null;
    const packed = await packWorkspacePackage(logger, packageName);
    if (!packed)
        return null;
    return { ...packed, sdkPath: packed.packagePath };
}
function resolvePackagePath(name) {
    try {
        const pkgJsonPath = hostRequire.resolve(`${name}/package.json`);
        return node_path_1.default.dirname(pkgJsonPath);
    }
    catch {
        return null;
    }
}
function parsePackFilename(stdout) {
    const match = stdout.match(/([^\s/\\]+\.tgz)/g);
    return match ? match[match.length - 1] : null;
}
function parseVersionFromFilename(filename) {
    const match = filename.match(/-(\d[^/]*)\.tgz$/);
    return match ? match[1] : 'unknown';
}
//# sourceMappingURL=pack-workspace-sdk.js.map