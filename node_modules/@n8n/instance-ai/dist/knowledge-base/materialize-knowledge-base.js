"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.KNOWLEDGE_BASE_MANIFEST_SCHEMA_VERSION = exports.KNOWLEDGE_BASE_MANIFEST_FILE = exports.INSTANCE_AI_KNOWLEDGE_BASE_SOURCE_DIR = exports.KNOWLEDGE_BASE_INDEX_FILE = exports.KNOWLEDGE_BASE_REFERENCE_DIR = exports.KNOWLEDGE_BASE_BEST_PRACTICES_DIR = exports.SANDBOX_KNOWLEDGE_BASE_DIR = exports.KNOWLEDGE_BASE_TEMPLATES_DIR = void 0;
exports.buildKnowledgeBaseWorkspaceBundle = buildKnowledgeBaseWorkspaceBundle;
exports.loadPrebakedKnowledgeBaseBundle = loadPrebakedKnowledgeBaseBundle;
exports.materializeKnowledgeBaseIntoWorkspace = materializeKnowledgeBaseIntoWorkspace;
const best_practices_1 = require("@n8n/workflow-sdk/prompts/best-practices");
const sdk_reference_1 = require("@n8n/workflow-sdk/prompts/sdk-reference");
const promises_1 = require("node:fs/promises");
const node_path_1 = require("node:path");
const posix_1 = require("node:path/posix");
const build_templates_index_1 = require("./build-templates-index");
Object.defineProperty(exports, "KNOWLEDGE_BASE_TEMPLATES_DIR", { enumerable: true, get: function () { return build_templates_index_1.KNOWLEDGE_BASE_TEMPLATES_DIR; } });
const extract_builder_templates_archive_1 = require("./extract-builder-templates-archive");
const compute_workspace_content_hash_1 = require("../workspace/compute-workspace-content-hash");
const prebaked_workspace_bundle_1 = require("../workspace/prebaked-workspace-bundle");
const workspace_file_content_1 = require("../workspace/workspace-file-content");
const workspace_manifest_1 = require("../workspace/workspace-manifest");
exports.SANDBOX_KNOWLEDGE_BASE_DIR = 'knowledge-base';
exports.KNOWLEDGE_BASE_BEST_PRACTICES_DIR = 'best-practices';
exports.KNOWLEDGE_BASE_REFERENCE_DIR = 'reference';
exports.KNOWLEDGE_BASE_INDEX_FILE = 'index.json';
exports.INSTANCE_AI_KNOWLEDGE_BASE_SOURCE_DIR = (0, node_path_1.resolve)(__dirname, '..', '..', 'knowledge-base');
exports.KNOWLEDGE_BASE_MANIFEST_FILE = workspace_manifest_1.WORKSPACE_MANIFEST_FILE;
exports.KNOWLEDGE_BASE_MANIFEST_SCHEMA_VERSION = 4;
function addTemplatesToKnowledgeBaseFiles(files, rootDir, templatesArchive, logger) {
    const extracted = (0, extract_builder_templates_archive_1.extractBuilderTemplatesArchive)(templatesArchive);
    if (!extracted) {
        logger.warn('[knowledge-base] rejected templates archive during bundle build', {
            archiveBytes: templatesArchive.byteLength,
        });
        return;
    }
    const templatesIndex = (0, build_templates_index_1.buildTemplatesIndexFromArchive)(extracted);
    const templatesDir = (0, posix_1.join)(rootDir, build_templates_index_1.KNOWLEDGE_BASE_TEMPLATES_DIR);
    for (const [name, content] of extracted) {
        if (!name.endsWith('.ts'))
            continue;
        files.set((0, posix_1.join)(templatesDir, name), (0, workspace_file_content_1.withTrailingNewline)(content));
    }
    const templatesIndexPath = (0, posix_1.join)(templatesDir, exports.KNOWLEDGE_BASE_INDEX_FILE);
    files.set(templatesIndexPath, (0, workspace_file_content_1.stringifyWorkspaceJson)(templatesIndex));
    extracted.clear();
}
const KNOWLEDGE_BASE_REFERENCE_ENTRIES = [
    {
        id: 'trigger-input-data-shapes',
        description: 'Per-trigger inputData shapes for verify-built-workflow and executions(action="run")',
        fileName: 'trigger-input-data-shapes.md',
    },
    {
        id: 'open-ai-output-shape',
        description: 'OpenAI node (@n8n/n8n-nodes-langchain.openAi) output shape for downstream expressions',
        fileName: 'open-ai-output-shape.md',
    },
    {
        id: 'anthropic-output-shape',
        description: 'Anthropic node (@n8n/n8n-nodes-langchain.anthropic) output shape for downstream expressions and Code-node parsing',
        fileName: 'anthropic-output-shape.md',
    },
    {
        id: 'workflow-builder-guardrails',
        description: 'Workflow builder guardrails for source preservation, fan-out/fan-in, effects, and Code nodes',
        fileName: 'workflow-builder-guardrails.md',
    },
    {
        id: 'workflow-sdk-language',
        description: 'Allowed/forbidden constructs in workflow SDK builder code: methods, globals, language subset, node groups',
        fileName: 'workflow-sdk-language.md',
        content: sdk_reference_1.SDK_LANGUAGE_REFERENCE,
    },
    {
        id: 'node-groups',
        description: 'Node group rules for SDK builder code: .group(name, members, { description }), what makes a group valid, when to group',
        fileName: 'node-groups.md',
        content: `${sdk_reference_1.NODE_GROUPS_REFERENCE}\n\n${sdk_reference_1.GROUPING_GUIDANCE}`,
    },
];
async function addReferenceFilesToKnowledgeBase(files, rootDir) {
    const referenceSourceDir = (0, node_path_1.join)(exports.INSTANCE_AI_KNOWLEDGE_BASE_SOURCE_DIR, exports.KNOWLEDGE_BASE_REFERENCE_DIR);
    const referenceEntries = [];
    for (const entry of KNOWLEDGE_BASE_REFERENCE_ENTRIES) {
        const relativeFilePath = (0, posix_1.join)(exports.KNOWLEDGE_BASE_REFERENCE_DIR, entry.fileName);
        const content = entry.content ?? (await (0, promises_1.readFile)((0, node_path_1.join)(referenceSourceDir, entry.fileName), 'utf-8'));
        files.set((0, posix_1.join)(rootDir, relativeFilePath), (0, workspace_file_content_1.withTrailingNewline)(content));
        referenceEntries.push({
            id: entry.id,
            description: entry.description,
            file: relativeFilePath,
        });
    }
    const referenceIndexPath = (0, posix_1.join)(rootDir, exports.KNOWLEDGE_BASE_REFERENCE_DIR, exports.KNOWLEDGE_BASE_INDEX_FILE);
    const referenceIndex = { entries: referenceEntries };
    files.set(referenceIndexPath, (0, workspace_file_content_1.stringifyWorkspaceJson)(referenceIndex));
    const sourceFiles = (await (0, promises_1.readdir)(referenceSourceDir)).filter((name) => name.endsWith('.md'));
    const expectedFiles = new Set(KNOWLEDGE_BASE_REFERENCE_ENTRIES.map((entry) => entry.fileName));
    for (const sourceFile of sourceFiles) {
        if (!expectedFiles.has(sourceFile)) {
            throw new Error(`Unexpected knowledge-base reference file "${sourceFile}". Add it to KNOWLEDGE_BASE_REFERENCE_ENTRIES.`);
        }
    }
    return referenceEntries;
}
async function buildKnowledgeBaseWorkspaceBundle(options) {
    const { root, templatesArchive = null, logger } = options;
    const rootDir = (0, posix_1.join)(root, exports.SANDBOX_KNOWLEDGE_BASE_DIR);
    const files = new Map();
    const bestPracticeEntries = [];
    for (const guideId of Object.values(best_practices_1.WorkflowTechnique)) {
        const doc = best_practices_1.bestPracticesRegistry[guideId];
        const description = best_practices_1.TechniqueDescription[guideId];
        const entry = {
            id: guideId,
            description,
            hasDocumentation: Boolean(doc),
        };
        if (doc) {
            const relativeFilePath = (0, posix_1.join)(exports.KNOWLEDGE_BASE_BEST_PRACTICES_DIR, `${guideId}.md`);
            files.set((0, posix_1.join)(rootDir, relativeFilePath), (0, workspace_file_content_1.withTrailingNewline)(doc.getDocumentation()));
            entry.file = relativeFilePath;
            entry.version = doc.version;
        }
        bestPracticeEntries.push(entry);
    }
    const bestPracticesIndexPath = (0, posix_1.join)(rootDir, exports.KNOWLEDGE_BASE_BEST_PRACTICES_DIR, exports.KNOWLEDGE_BASE_INDEX_FILE);
    const bestPracticesIndex = { entries: bestPracticeEntries };
    files.set(bestPracticesIndexPath, (0, workspace_file_content_1.stringifyWorkspaceJson)(bestPracticesIndex));
    if (templatesArchive) {
        addTemplatesToKnowledgeBaseFiles(files, rootDir, templatesArchive, logger);
    }
    const referenceEntries = await addReferenceFilesToKnowledgeBase(files, rootDir);
    const rootIndexPath = (0, posix_1.join)(rootDir, exports.KNOWLEDGE_BASE_INDEX_FILE);
    const rootIndex = {
        bestPractices: {
            indexFile: (0, posix_1.join)(exports.KNOWLEDGE_BASE_BEST_PRACTICES_DIR, exports.KNOWLEDGE_BASE_INDEX_FILE),
            entries: bestPracticeEntries,
        },
        templates: {
            indexFile: (0, posix_1.join)(build_templates_index_1.KNOWLEDGE_BASE_TEMPLATES_DIR, exports.KNOWLEDGE_BASE_INDEX_FILE),
        },
        reference: {
            indexFile: (0, posix_1.join)(exports.KNOWLEDGE_BASE_REFERENCE_DIR, exports.KNOWLEDGE_BASE_INDEX_FILE),
            entries: referenceEntries,
        },
    };
    files.set(rootIndexPath, (0, workspace_file_content_1.stringifyWorkspaceJson)(rootIndex));
    const manifestPath = (0, posix_1.join)(rootDir, exports.KNOWLEDGE_BASE_MANIFEST_FILE);
    const contentHash = (0, compute_workspace_content_hash_1.computeWorkspaceContentHash)(files);
    const manifest = {
        schemaVersion: exports.KNOWLEDGE_BASE_MANIFEST_SCHEMA_VERSION,
        contentHash,
    };
    files.set(manifestPath, (0, workspace_file_content_1.stringifyWorkspaceJson)(manifest));
    return {
        rootDir,
        manifestPath,
        indexPath: rootIndexPath,
        files,
        contentHash,
    };
}
const KNOWLEDGE_BASE_FILE_LABEL = 'Knowledge base file';
async function loadPrebakedKnowledgeBaseBundle(options) {
    const bundle = await buildKnowledgeBaseWorkspaceBundle(options);
    return await (0, prebaked_workspace_bundle_1.loadPrebakedWorkspaceBundle)({
        workspace: options.workspace,
        manifestPath: bundle.manifestPath,
        expectedHash: bundle.contentHash,
        hashField: 'contentHash',
        schemaVersion: exports.KNOWLEDGE_BASE_MANIFEST_SCHEMA_VERSION,
        resourceLabel: KNOWLEDGE_BASE_FILE_LABEL,
        logger: options.logger,
        invalidManifestLogMessage: 'Ignoring invalid prebaked knowledge base manifest',
        staleManifestLogMessage: 'Ignoring stale prebaked knowledge base manifest',
        staleManifestLogKeys: {
            expected: 'expectedContentHash',
            actual: 'actualContentHash',
        },
        successLogMessage: 'Using prebaked knowledge base from workspace',
        successLogContext: (loadedBundle) => ({
            root: options.root,
            knowledgeBaseRoot: loadedBundle.rootDir,
            contentHash: loadedBundle.contentHash,
            fileCount: loadedBundle.files.size,
        }),
        buildBundle: () => bundle,
    });
}
async function materializeKnowledgeBaseIntoWorkspace(options) {
    return await (0, prebaked_workspace_bundle_1.materializeWorkspaceBundle)({
        workspace: options.workspace,
        resourceLabel: KNOWLEDGE_BASE_FILE_LABEL,
        logger: options.logger,
        loadPrebaked: async () => await loadPrebakedKnowledgeBaseBundle(options),
        buildBundle: async () => await buildKnowledgeBaseWorkspaceBundle(options),
        materializedLogMessage: 'Materialized knowledge base into workspace',
        materializedLogContext: (bundle) => ({
            root: options.root,
            knowledgeBaseRoot: bundle.rootDir,
            contentHash: bundle.contentHash,
            fileCount: bundle.files.size,
        }),
    });
}
//# sourceMappingURL=materialize-knowledge-base.js.map