"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.askUserResumeSchema = exports.askUserInputSchema = exports.ASK_USER_TOOL_ID = void 0;
exports.createAskUserTool = createAskUserTool;
const agents_1 = require("@n8n/agents");
const nanoid_1 = require("nanoid");
const zod_1 = require("zod");
const tool_ids_1 = require("../tool-ids");
Object.defineProperty(exports, "ASK_USER_TOOL_ID", { enumerable: true, get: function () { return tool_ids_1.ASK_USER_TOOL_ID; } });
const questionSchema = zod_1.z.object({
    id: zod_1.z.string().describe('Unique question identifier'),
    question: zod_1.z.string().describe('The question text to display to the user'),
    type: zod_1.z
        .enum(['single', 'multi', 'text'])
        .describe('single = pick one option, multi = pick many, text = free-form input'),
    options: zod_1.z
        .array(zod_1.z.string())
        .optional()
        .describe('Suggested answers (required for single/multi, ignored for text)'),
});
const answerSchema = zod_1.z.object({
    questionId: zod_1.z.string(),
    selectedOptions: zod_1.z.array(zod_1.z.string()),
    customText: zod_1.z.string().optional(),
    skipped: zod_1.z.boolean().optional(),
});
exports.askUserInputSchema = zod_1.z.object({
    questions: zod_1.z
        .array(questionSchema)
        .min(1)
        .describe('Questions to present to the user in a paginated wizard'),
    introMessage: zod_1.z.string().optional().describe('Brief intro text shown above the first question'),
});
exports.askUserResumeSchema = zod_1.z.object({
    approved: zod_1.z.boolean(),
    answers: zod_1.z.array(answerSchema).optional(),
});
function createAskUserTool() {
    return new agents_1.Tool(tool_ids_1.ASK_USER_TOOL_ID)
        .description('Ask the user when only a human can decide; the run suspends until they respond. ' +
        'Questions are single-select, multi-select, or free-text. ' +
        'Before the first build-workflow call, use only for choices that change workflow intent or topology ' +
        '(e.g. destination service) — setup values (recipients, accounts, resources, channels, credentials, ' +
        'timezone) use placeholders or unresolved newCredential() calls instead. ' +
        'The UI adds a built-in "Something else" free-text input to every select question: NEVER include ' +
        'catch-all options ("Something else", "Other", "None of the above") in the options array, and NEVER ' +
        'add a follow-up question elaborating a previous "other" answer. Keep questions concise and independent ' +
        "of each other's answers. A question is asked at most once — a skip or dismissal (answered: false, or " +
        'skipped: true) means "proceed without this": assume a sensible default or leave the detail for setup, ' +
        'and NEVER re-present an answered, deferred, or skipped question. ' +
        'NEVER ask the user to paste passwords, API keys, tokens, cookies, connection strings, or private keys here.')
        .input(exports.askUserInputSchema)
        .output(zod_1.z.object({
        answered: zod_1.z.boolean(),
        answers: zod_1.z
            .array(zod_1.z.object({
            questionId: zod_1.z.string(),
            question: zod_1.z.string(),
            selectedOptions: zod_1.z.array(zod_1.z.string()),
            customText: zod_1.z.string().optional(),
            skipped: zod_1.z.boolean().optional(),
        }))
            .optional(),
    }))
        .suspend(zod_1.z.object({
        requestId: zod_1.z.string(),
        message: zod_1.z.string(),
        severity: zod_1.z.literal('info'),
        inputType: zod_1.z.literal('questions'),
        questions: zod_1.z.array(questionSchema),
        introMessage: zod_1.z.string().optional(),
    }))
        .resume(exports.askUserResumeSchema)
        .handler(async (input, ctx) => {
        const resumeData = ctx.resumeData;
        if (resumeData === undefined || resumeData === null) {
            return await ctx.suspend({
                requestId: (0, nanoid_1.nanoid)(),
                message: input.introMessage ?? input.questions[0].question,
                severity: 'info',
                inputType: 'questions',
                questions: input.questions,
                introMessage: input.introMessage,
            });
        }
        if (!resumeData.approved || !resumeData.answers) {
            return { answered: false };
        }
        const enrichedAnswers = resumeData.answers.map((a) => {
            const q = input.questions.find((q2) => q2.id === a.questionId);
            return {
                ...a,
                question: q?.question ?? a.questionId,
            };
        });
        return { answered: true, answers: enrichedAnswers };
    })
        .build();
}
//# sourceMappingURL=ask-user.tool.js.map