import type { ResolvedVerifyInput, VerifyBuiltWorkflowOutput, VerifyToolInput, WorkflowTaskService } from './types';
import type { OrchestrationContext } from '../../../types';
import type { WorkflowBuildOutcome } from '../../../workflow-loop/workflow-loop-state';
export interface ResolvedVerificationTarget {
    input: ResolvedVerifyInput;
    buildOutcome: WorkflowBuildOutcome;
    workflowId: string;
    stateBefore: Awaited<ReturnType<WorkflowTaskService['getWorkflowLoopState']>> | undefined;
    workflowTaskService: WorkflowTaskService;
    domainContext: NonNullable<OrchestrationContext['domainContext']>;
}
export declare function resolveVerificationTarget(input: VerifyToolInput, context: OrchestrationContext): Promise<{
    kind: 'blocked';
    result: VerifyBuiltWorkflowOutput;
} | {
    kind: 'ready';
    target: ResolvedVerificationTarget;
}>;
