"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.handleMissingSimulationPlan = handleMissingSimulationPlan;
exports.persistVerificationOutcome = persistVerificationOutcome;
const analyze_result_1 = require("./analyze-result");
const remediation_1 = require("../../../workflow-loop/remediation");
async function handleMissingSimulationPlan(args) {
    const { input, context, workflowTaskService, workflowId } = args;
    const guidance = 'Verification was not run because the build outcome has no simulation plan. ' +
        'Rebuild or resubmit the workflow so destructive nodes can be classified before verification.';
    const remediation = (0, remediation_1.createRemediation)({
        category: 'blocked',
        shouldEdit: false,
        reason: 'missing_simulation_plan',
        guidance,
    });
    context.logger.warn('verify-built-workflow: build outcome has no simulation plan — refusing to run without simulation safeguards', { workItemId: input.workItemId, workflowId });
    try {
        await workflowTaskService.updateBuildOutcome(input.workItemId, {
            remediation,
            verification: {
                attempted: true,
                success: false,
                status: 'unknown',
                failureSignature: 'missing_simulation_plan',
                evidence: { errorMessage: guidance },
                verifiedAt: new Date().toISOString(),
            },
        });
    }
    catch {
    }
    try {
        await workflowTaskService.reportVerificationVerdict({
            workItemId: input.workItemId,
            runId: context.runId,
            workflowId,
            verdict: 'failed_terminal',
            failureSignature: 'missing_simulation_plan',
            diagnosis: guidance,
            remediation,
            summary: guidance,
        });
    }
    catch (error) {
        context.logger.warn('verify-built-workflow: failed to persist terminal verdict', {
            workItemId: input.workItemId,
            workflowId,
            error: error instanceof Error ? error.message : String(error),
        });
    }
    return {
        success: false,
        resolvedWorkItemId: input.workItemId,
        status: 'unknown',
        error: guidance,
        remediation,
        guidance,
    };
}
async function persistVerificationOutcome(args) {
    const { input, context, workflowTaskService, workflowId, result, analysis, verifyAttempts } = args;
    try {
        const executedForEvidence = (0, analyze_result_1.namesOrDataKeys)(analysis.reachedNames, result.data);
        await workflowTaskService.updateBuildOutcome(input.workItemId, {
            verifyAttempts,
            verification: {
                attempted: true,
                success: analysis.success,
                executionId: result.executionId || undefined,
                status: result.status,
                failureSignature: analysis.success ? undefined : analysis.errorMessage,
                evidence: {
                    nodesExecuted: executedForEvidence && executedForEvidence.length > 0 ? executedForEvidence : undefined,
                    nodesNotReached: analysis.nodesNotReached.length > 0 ? analysis.nodesNotReached : undefined,
                    producedOutputRows: (0, analyze_result_1.countProducedOutputRows)(result.data),
                    errorNodeName: analysis.success ? undefined : analysis.nodeErrors[0]?.nodeName,
                    errorMessage: analysis.success ? undefined : analysis.errorMessage,
                    nodeErrors: analysis.nodeErrors.length > 0 ? analysis.nodeErrors : undefined,
                },
                verifiedAt: new Date().toISOString(),
            },
        });
    }
    catch {
    }
    if (analysis.remediation && !analysis.remediation.shouldEdit) {
        await reportTerminalRemediation({
            input,
            context,
            workflowTaskService,
            workflowId,
            executionId: result.executionId || undefined,
            remediation: analysis.remediation,
        });
    }
}
async function reportTerminalRemediation(args) {
    const { input, context, workflowTaskService, workflowId, executionId, remediation } = args;
    try {
        await workflowTaskService.reportVerificationVerdict({
            workItemId: input.workItemId,
            runId: context.runId,
            workflowId,
            executionId,
            verdict: remediation.category === 'needs_setup' ? 'needs_user_input' : 'failed_terminal',
            failureSignature: remediation.reason,
            diagnosis: remediation.guidance,
            remediation,
            summary: remediation.guidance,
        });
    }
    catch (error) {
        context.logger.warn('verify-built-workflow: failed to persist terminal verdict', {
            workItemId: input.workItemId,
            workflowId,
            error: error instanceof Error ? error.message : String(error),
        });
    }
    try {
        context.trackTelemetry?.('Builder remediation guard fired', {
            thread_id: context.threadId,
            run_id: context.runId,
            work_item_id: input.workItemId,
            workflow_id: workflowId,
            category: remediation.category,
            attempt_count: remediation.attemptCount,
            reason: remediation.reason,
        });
    }
    catch (error) {
        context.logger.warn('verify-built-workflow: failed to emit remediation telemetry', {
            workItemId: input.workItemId,
            workflowId,
            error: error instanceof Error ? error.message : String(error),
        });
    }
}
//# sourceMappingURL=finalize-result.js.map