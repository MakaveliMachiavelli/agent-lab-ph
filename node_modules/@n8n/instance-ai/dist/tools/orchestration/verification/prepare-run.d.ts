import type { VerifyBuiltWorkflowOutput, VerifyToolInput } from './types';
import type { WaitGateScript, WorkflowBuildOutcome } from '../../../workflow-loop/workflow-loop-state';
export interface PreparedVerificationRun {
    verificationPinData: Record<string, unknown[]> | undefined;
    simulatedNodes: Array<{
        nodeName: string;
        reason: string;
    }>;
    haltedGateNames: string[];
    gateScript?: WaitGateScript;
}
export declare function prepareVerificationRun(buildOutcome: WorkflowBuildOutcome, fixtureOverrides: VerifyToolInput['fixtureOverrides']): {
    kind: 'ready';
    prepared: PreparedVerificationRun;
} | {
    kind: 'blocked';
    result: VerifyBuiltWorkflowOutput;
};
