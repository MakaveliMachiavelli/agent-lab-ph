"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.startSubAgentTrace = startSubAgentTrace;
exports.createDetachedSubAgentTracing = createDetachedSubAgentTracing;
exports.createDetachedSubAgentTraceFactory = createDetachedSubAgentTraceFactory;
exports.traceSubAgentTools = traceSubAgentTools;
exports.withTraceRun = withTraceRun;
exports.withTraceContextActor = withTraceContextActor;
exports.finishTraceRun = finishTraceRun;
exports.failTraceRun = failTraceRun;
const langsmith_tracing_1 = require("../../tracing/langsmith-tracing");
async function startSubAgentTrace(context, options) {
    if (!context.tracing)
        return undefined;
    return await context.tracing.startChildRun(context.tracing.actorRun, {
        name: `agent: ${options.role}`,
        canonicalName: `instance-ai.subagent.${options.role}.stream`,
        tags: ['sub-agent'],
        metadata: {
            agent_role: options.role,
            agent_id: options.agentId,
            task_kind: options.kind,
            ...(options.taskId ? { task_id: options.taskId } : {}),
            ...(options.plannedTaskId ? { planned_task_id: options.plannedTaskId } : {}),
            ...(options.workItemId ? { work_item_id: options.workItemId } : {}),
            ...options.metadata,
        },
        inputs: options.inputs,
    });
}
async function createDetachedSubAgentTracing(context, options) {
    return await createDetachedSubAgentTraceFactory(context, options)();
}
function createDetachedSubAgentTraceFactory(context, options) {
    if (!context.tracing)
        return async () => await Promise.resolve(undefined);
    const messageId = typeof context.tracing.actorRun.metadata?.message_id === 'string'
        ? context.tracing.actorRun.metadata.message_id
        : context.runId;
    const conversationId = typeof context.tracing.actorRun.metadata?.conversation_id === 'string'
        ? context.tracing.actorRun.metadata.conversation_id
        : context.threadId;
    const spawnedByAgentId = typeof context.tracing.actorRun.metadata?.agent_id === 'string'
        ? context.tracing.actorRun.metadata.agent_id
        : context.orchestratorAgentId;
    const spawnedByAgentRole = typeof context.tracing.actorRun.metadata?.agent_role === 'string'
        ? context.tracing.actorRun.metadata.agent_role
        : undefined;
    const parentN8nVersion = typeof context.tracing.actorRun.metadata?.n8n_version === 'string'
        ? context.tracing.actorRun.metadata.n8n_version
        : undefined;
    const parentWorkflowSdkVersion = typeof context.tracing.actorRun.metadata?.workflow_sdk_version === 'string'
        ? context.tracing.actorRun.metadata.workflow_sdk_version
        : undefined;
    const activeSpanContext = (0, langsmith_tracing_1.getCurrentOtelSpanContext)();
    const spawnedByToolCallId = (0, langsmith_tracing_1.getCurrentTraceToolCallId)();
    return async () => {
        if (!context.tracing)
            return undefined;
        const tracing = await (0, langsmith_tracing_1.createDetachedSubAgentTraceContext)({
            projectName: context.tracing.projectName,
            threadId: context.threadId,
            conversationId,
            messageGroupId: context.messageGroupId,
            messageId,
            runId: context.runId,
            userId: context.userId,
            modelId: context.modelId,
            input: options.inputs,
            metadata: options.metadata,
            n8nVersion: parentN8nVersion,
            workflowSdkVersion: parentWorkflowSdkVersion,
            agentId: options.agentId,
            role: options.role,
            kind: options.kind,
            taskId: options.taskId,
            plannedTaskId: options.plannedTaskId,
            workItemId: options.workItemId,
            spawnedByTraceId: activeSpanContext?.traceId ??
                context.tracing.rootRun.otelTraceId ??
                context.tracing.rootRun.traceId,
            spawnedBySpanId: activeSpanContext?.spanId ?? context.tracing.actorRun.otelSpanId,
            spawnedByRunId: context.tracing.actorRun.id,
            spawnedByAgentId,
            spawnedByAgentRole,
            spawnedByToolCallId,
            proxyConfig: context.tracingProxyConfig,
        });
        if (tracing) {
            (0, langsmith_tracing_1.mergeCurrentTraceMetadata)({
                detached_trace: true,
                spawned_role: options.role,
                ...(options.taskId ? { spawned_task_id: options.taskId } : {}),
                spawned_trace_id: tracing.rootRun.traceId,
                spawned_root_run_id: tracing.rootRun.id,
            });
        }
        return tracing;
    };
}
function traceSubAgentTools(context, tools, role) {
    return (context.tracing?.wrapTools(tools, {
        agentRole: role,
        tags: ['sub-agent'],
    }) ?? tools);
}
async function withTraceRun(context, traceRun, fn) {
    if (!traceRun || !context.tracing) {
        return await fn();
    }
    return await context.tracing.withActiveSpan(traceRun, fn);
}
async function withTraceContextActor(tracing, fn) {
    if (!tracing) {
        return await fn();
    }
    return await tracing.withActiveSpan(tracing.actorRun, fn);
}
async function finishTraceRun(context, traceRun, options) {
    if (!traceRun || !context.tracing)
        return;
    await context.tracing.finishRun(traceRun, options);
}
async function failTraceRun(context, traceRun, error, metadata) {
    if (!traceRun || !context.tracing)
        return;
    await context.tracing.failRun(traceRun, error, metadata);
}
//# sourceMappingURL=tracing-utils.js.map