"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.verifyBuiltWorkflowInputSchema = void 0;
exports.createVerifyBuiltWorkflowTool = createVerifyBuiltWorkflowTool;
const agents_1 = require("@n8n/agents");
const zod_1 = require("zod");
const analyze_result_1 = require("./verification/analyze-result");
const finalize_result_1 = require("./verification/finalize-result");
const prepare_run_1 = require("./verification/prepare-run");
const reconcile_plan_1 = require("./verification/reconcile-plan");
const resolve_target_1 = require("./verification/resolve-target");
const scripted_gate_run_1 = require("./verification/scripted-gate-run");
const workflow_loop_state_1 = require("../../workflow-loop/workflow-loop-state");
const DEFAULT_NODE_PREVIEW_CHARS = 600;
exports.verifyBuiltWorkflowInputSchema = zod_1.z.object({
    workItemId: zod_1.z
        .string()
        .optional()
        .describe('The work item ID from the build (wi_XXXXXXXX). Optional for follow-up verification; when omitted, the latest build outcome for workflowId in this thread is used.'),
    workflowId: zod_1.z.string().describe('The workflow ID to verify'),
    inputData: zod_1.z
        .record(zod_1.z.unknown())
        .optional()
        .describe("Input data for the workflow trigger. Shape MUST match the trigger's real-world output: " +
        'Form Trigger -> flat field map like {name: "Alice", email: "a@b.c"} (do NOT wrap in formFields); ' +
        'Webhook -> the body payload like {event: "signup", userId: "..."} (adapter wraps it under body); ' +
        'Chat Trigger -> {chatInput: "user message"}; ' +
        'Schedule Trigger -> omit inputData. ' +
        "If you wrap a form payload in {formFields: {...}} the adapter will reject the call; the builder's " +
        'downstream expressions reference $json.<field>, matching the flat production shape.'),
    timeout: zod_1.z
        .number()
        .int()
        .min(1000)
        .max(600_000)
        .optional()
        .describe('Max wait time in milliseconds (default 300000)'),
    includeData: zod_1.z
        .boolean()
        .optional()
        .describe('Set true only when you need the full execution data payload. Default false.'),
    maxDataChars: zod_1.z
        .number()
        .int()
        .min(0)
        .max(20_000)
        .optional()
        .describe('Max characters per node preview in the compact response (default 600).'),
    fixtureOverrides: zod_1.z
        .record(zod_1.z.array(zod_1.z.record(zod_1.z.unknown())))
        .optional()
        .describe('Optional per-run output fixtures keyed by node name. Only nodes already classified as simulated in the build outcome may be overridden. Use this for alternate deterministic scenarios, not raw trigger input.'),
});
const remediationOutputSchema = zod_1.z
    .object({
    category: zod_1.z.enum(['code_fixable', 'needs_setup', 'blocked']),
    shouldEdit: zod_1.z.boolean(),
    guidance: zod_1.z.string(),
    reason: zod_1.z.string().optional(),
    remainingSubmitFixes: zod_1.z.number().int().min(0).optional(),
    attemptCount: zod_1.z.number().int().min(0).optional(),
})
    .optional();
const verifyBuiltWorkflowOutputSchema = zod_1.z.object({
    resolvedWorkItemId: zod_1.z.string().optional(),
    executionId: zod_1.z.string().optional(),
    success: zod_1.z.boolean(),
    status: zod_1.z.enum(['running', 'success', 'error', 'waiting', 'unknown']).optional(),
    nodesExecuted: zod_1.z.array(zod_1.z.string()).optional(),
    nodePreviews: zod_1.z
        .array(zod_1.z.object({
        nodeName: zod_1.z.string(),
        itemCount: zod_1.z.number().optional(),
        preview: zod_1.z.string(),
        truncated: zod_1.z.boolean(),
        chars: zod_1.z.number(),
        simulated: zod_1.z.boolean().optional(),
    }))
        .optional(),
    simulatedNodes: zod_1.z.array(zod_1.z.object({ nodeName: zod_1.z.string(), reason: zod_1.z.string() })).optional(),
    simulationNote: zod_1.z.string().optional(),
    lastNodeExecuted: zod_1.z.string().optional(),
    nodeErrors: zod_1.z.array(workflow_loop_state_1.executionNodeErrorSchema).optional(),
    nodesNotReached: zod_1.z.array(zod_1.z.string()).optional(),
    coverageNote: zod_1.z.string().optional(),
    data: zod_1.z.record(zod_1.z.unknown()).optional(),
    error: zod_1.z.string().optional(),
    remediation: remediationOutputSchema,
    guidance: zod_1.z.string().optional(),
});
function createVerifyBuiltWorkflowTool(context) {
    return new agents_1.Tool('verify-built-workflow')
        .description('Standard post-build verifier: runs a built workflow with sidecar verification context from the build outcome ' +
        '(pin data, mocked credentials, trigger-shaped inputData; all trigger types supported). ' +
        'Use `executions(action="run")` only for ad hoc runs outside build verification. ' +
        'CRITICAL: `inputData` shape depends on the trigger type (see the field description) — a wrong shape produces ' +
        'null downstream values that look like an expression bug; re-run verify with the correct shape instead of patching the workflow.')
        .input(exports.verifyBuiltWorkflowInputSchema)
        .output(verifyBuiltWorkflowOutputSchema)
        .handler(async (input) => {
        const targetResult = await (0, resolve_target_1.resolveVerificationTarget)(input, context);
        if (targetResult.kind === 'blocked')
            return targetResult.result;
        const { target } = targetResult;
        const { input: resolvedInput, workflowId, workflowTaskService } = target;
        const buildOutcome = await (0, reconcile_plan_1.reconcileStaleCredentialPlan)({
            buildOutcome: target.buildOutcome,
            workflowId,
            domainContext: target.domainContext,
            workflowTaskService,
            logger: context.logger,
            fallbackModelConfig: context.modelId,
        });
        if (buildOutcome.nodeSimulationPlan === undefined) {
            return await (0, finalize_result_1.handleMissingSimulationPlan)({
                input: resolvedInput,
                context,
                workflowTaskService,
                workflowId,
            });
        }
        const preparedResult = (0, prepare_run_1.prepareVerificationRun)(buildOutcome, resolvedInput.fixtureOverrides);
        if (preparedResult.kind === 'blocked') {
            return {
                ...preparedResult.result,
                resolvedWorkItemId: resolvedInput.workItemId,
            };
        }
        const { prepared } = preparedResult;
        const { result, analysis } = prepared.gateScript
            ? await (0, scripted_gate_run_1.runScriptedGateVerification)({
                script: prepared.gateScript,
                prepared,
                executionService: target.domainContext.executionService,
                workflowId,
                inputData: resolvedInput.inputData,
                timeout: resolvedInput.timeout,
                abortSignal: context.abortSignal,
                buildOutcome,
                stateBefore: target.stateBefore,
                runId: context.runId,
            })
            : await (async () => {
                const runResult = await target.domainContext.executionService.run(workflowId, resolvedInput.inputData, {
                    timeout: resolvedInput.timeout,
                    verificationPinData: prepared.verificationPinData,
                    isVerificationRun: true,
                    abortSignal: context.abortSignal,
                });
                return {
                    result: runResult,
                    analysis: (0, analyze_result_1.analyzeVerificationResult)({
                        result: runResult,
                        buildOutcome,
                        simulatedNodes: prepared.simulatedNodes,
                        haltedGateNames: prepared.haltedGateNames,
                        stateBefore: target.stateBefore,
                        runId: context.runId,
                    }),
                };
            })();
        await (0, finalize_result_1.persistVerificationOutcome)({
            input: resolvedInput,
            context,
            workflowTaskService,
            workflowId,
            result,
            analysis,
            verifyAttempts: (buildOutcome.verifyAttempts ?? 0) + 1,
        });
        const maxDataChars = resolvedInput.maxDataChars ?? DEFAULT_NODE_PREVIEW_CHARS;
        const simulatedNames = new Set(analysis.reachedSimulatedNodes.map((n) => n.nodeName));
        return {
            resolvedWorkItemId: resolvedInput.workItemId,
            executionId: result.executionId || undefined,
            success: analysis.success,
            status: result.status,
            nodesExecuted: analysis.nodesExecuted,
            lastNodeExecuted: result.lastNodeExecuted,
            nodePreviews: (0, analyze_result_1.buildNodePreviews)(result.data, maxDataChars, simulatedNames),
            simulatedNodes: analysis.reachedSimulatedNodes.length > 0 ? analysis.reachedSimulatedNodes : undefined,
            simulationNote: analysis.simulationNote,
            nodeErrors: analysis.nodeErrors.length > 0 ? analysis.nodeErrors : undefined,
            nodesNotReached: analysis.nodesNotReached.length > 0 ? analysis.nodesNotReached : undefined,
            coverageNote: analysis.coverageNote,
            ...(resolvedInput.includeData ? { data: result.data } : {}),
            error: analysis.errorMessage,
            remediation: analysis.remediation,
            guidance: analysis.remediation?.guidance,
        };
    })
        .build();
}
//# sourceMappingURL=verify-built-workflow.tool.js.map