"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SUB_AGENT_RESOURCE_PREFIX = void 0;
exports.createSubAgentResourceIdPrefix = createSubAgentResourceIdPrefix;
exports.createSubAgentResourceId = createSubAgentResourceId;
exports.createSubAgentPersistence = createSubAgentPersistence;
const node_crypto_1 = require("node:crypto");
exports.SUB_AGENT_RESOURCE_PREFIX = 'instance-ai-subagent';
function createSubAgentResourceIdPrefix(parentThreadId) {
    return `${exports.SUB_AGENT_RESOURCE_PREFIX}:${parentThreadId}:`;
}
function normalizeSubAgentKind(agentKind) {
    const normalized = agentKind
        .trim()
        .toLowerCase()
        .replace(/[^a-z0-9._-]+/g, '-')
        .replace(/^-+|-+$/g, '');
    return (normalized || 'agent').slice(0, 64);
}
function createSubAgentResourceId(parentThreadId, agentKind) {
    return `${createSubAgentResourceIdPrefix(parentThreadId)}${normalizeSubAgentKind(agentKind)}`;
}
async function createSubAgentPersistence(context, options) {
    const threadId = options.threadId ?? (0, node_crypto_1.randomUUID)();
    const resourceId = options.resourceId ?? createSubAgentResourceId(context.threadId, options.agentKind);
    const metadata = {
        instanceAiHidden: true,
        instanceAiThreadKind: 'sub-agent',
        parentThreadId: context.threadId,
        agentKind: normalizeSubAgentKind(options.agentKind),
    };
    await context.memory?.saveThread({
        id: threadId,
        resourceId,
        title: '',
        metadata,
    });
    return {
        threadId,
        resourceId,
    };
}
//# sourceMappingURL=agent-persistence.js.map