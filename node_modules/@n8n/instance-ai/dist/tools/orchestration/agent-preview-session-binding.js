"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AGENT_PREVIEW_SESSION_METADATA_KEY = void 0;
exports.resolveAgentPreviewSession = resolveAgentPreviewSession;
exports.saveAgentPreviewSession = saveAgentPreviewSession;
const zod_1 = require("zod");
const thread_patch_1 = require("../../storage/thread-patch");
exports.AGENT_PREVIEW_SESSION_METADATA_KEY = 'instanceAiAgentPreviewSession';
const agentPreviewSessionSchema = zod_1.z.object({
    agentId: zod_1.z.string(),
    threadId: zod_1.z.string(),
    executionId: zod_1.z.string().optional(),
});
function parseSession(raw) {
    const parsed = agentPreviewSessionSchema.safeParse(raw);
    return parsed.success ? parsed.data : undefined;
}
async function readThreadSession(context) {
    if (!context.threadMemory || !context.threadId)
        return undefined;
    const thread = await (0, thread_patch_1.getThread)(context.threadMemory, context.threadId);
    return parseSession(thread?.metadata?.[exports.AGENT_PREVIEW_SESSION_METADATA_KEY]);
}
async function resolveAgentPreviewSession(context) {
    if (context.agentPreviewSession)
        return context.agentPreviewSession;
    const session = await readThreadSession(context);
    if (session)
        context.agentPreviewSession = session;
    return session;
}
async function saveAgentPreviewSession(context, session) {
    if (!context.threadMemory || !context.threadId) {
        context.logger?.warn('Cannot persist agent-preview session: no thread persistence available', {
            agentId: session.agentId,
            previewThreadId: session.threadId,
        });
        return;
    }
    await (0, thread_patch_1.patchThread)(context.threadMemory, {
        threadId: context.threadId,
        update: ({ metadata = {} }) => ({
            metadata: { ...metadata, [exports.AGENT_PREVIEW_SESSION_METADATA_KEY]: session },
        }),
    });
}
//# sourceMappingURL=agent-preview-session-binding.js.map