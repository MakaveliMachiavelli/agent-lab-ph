"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.applyPinData = applyPinData;
function applyPinData(workflow, generated) {
    const existing = workflow.pinData ?? {};
    const additions = {};
    for (const [nodeName, items] of Object.entries(generated)) {
        if (nodeName in existing)
            continue;
        additions[nodeName] = items;
    }
    if (Object.keys(additions).length === 0)
        return workflow;
    return {
        ...workflow,
        pinData: { ...existing, ...additions },
    };
}
//# sourceMappingURL=apply-pin-data.service.js.map