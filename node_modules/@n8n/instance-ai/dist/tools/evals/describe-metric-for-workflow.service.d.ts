import type { WorkflowJSON } from '@n8n/workflow-sdk';
import type { MetricId } from './metric-catalog';
type RecommendedMetricId = Exclude<MetricId, 'helpfulness'>;
export declare function describeMetricForWorkflow(workflow: WorkflowJSON, agentNodeName: string, metricId: string): string;
export declare function recommendedMetricId(workflow: WorkflowJSON, agentNodeName: string): RecommendedMetricId;
export {};
