import type { WorkflowJSON } from '@n8n/workflow-sdk';
import type { NodeSimulationVerdict, WaitGateScript } from '../../workflow-loop/workflow-loop-state';
type Edge = {
    source: string;
    target: string;
};
export declare function findCycleCutEdge(json: WorkflowJSON, gateName: string): Edge | undefined;
export declare function deriveWaitGateScripts(workflow: WorkflowJSON, plan: NodeSimulationVerdict[]): WaitGateScript[];
export {};
