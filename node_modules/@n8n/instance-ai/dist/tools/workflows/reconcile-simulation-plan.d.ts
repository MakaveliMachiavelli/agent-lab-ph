import type { WorkflowJSON } from '@n8n/workflow-sdk';
import type { CredentialMap } from './resolve-credentials';
import type { ModelConfig } from '../../types';
import type { WorkflowBuildOutcome } from '../../workflow-loop/workflow-loop-state';
export type SimulationPlanPatch = Pick<WorkflowBuildOutcome, 'mockedNodeNames' | 'mockedCredentialTypes' | 'mockedCredentialsByNode' | 'nodeSimulationPlan' | 'simulationFixtures' | 'setupRequirement'>;
export declare function reconcileSimulationPlan(args: {
    buildOutcome: WorkflowBuildOutcome;
    workflow: WorkflowJSON;
    availableCredentials: CredentialMap;
    fallbackModelConfig?: ModelConfig;
}): Promise<SimulationPlanPatch | undefined>;
