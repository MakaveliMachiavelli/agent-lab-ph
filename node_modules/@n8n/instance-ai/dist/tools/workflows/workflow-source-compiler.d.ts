import { type WorkflowJSON } from '@n8n/workflow-sdk';
import { type ValidationWarning } from './workflow-validation-warnings';
import type { InstanceAiContext } from '../../types';
export type WorkflowSourceCompiler = 'workflow-json' | 'sandbox-tsx';
export type WorkflowSourceCompileFailureReason = 'workflow_json_parse_failed' | 'workflow_json_invalid' | 'workflow_source_unsupported_extension' | 'workflow_source_sandbox_unavailable' | 'workflow_source_sandbox_failed' | 'workflow_source_build_failed';
export type WorkflowSourceCompileResult = {
    success: true;
    workflow: WorkflowJSON;
    declaredOutputFixtures?: NonNullable<WorkflowJSON['pinData']>;
    warnings: ValidationWarning[];
    compiler: WorkflowSourceCompiler;
} | {
    success: false;
    errors: string[];
    reason: WorkflowSourceCompileFailureReason;
    editable: boolean;
    summary: string;
};
export declare function isWorkflowJsonSourceFile(filePath: string): boolean;
export declare function compileWorkflowSource(context: InstanceAiContext, filePath: string, source: string, abortSignal?: AbortSignal): Promise<WorkflowSourceCompileResult>;
