"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AI_GATEWAY_CREDENTIAL = exports.N8N_CONNECT_DISPLAY_NAME = exports.GENERIC_AUTH_CREDENTIAL_TYPES = void 0;
exports.isAiGatewayManagedCredential = isAiGatewayManagedCredential;
exports.toSetupNodeCredential = toSetupNodeCredential;
exports.resolveCredentialForApply = resolveCredentialForApply;
exports.assignCredentialToNode = assignCredentialToNode;
exports.extractServiceHost = extractServiceHost;
exports.serviceHostsMatch = serviceHostsMatch;
const api_types_1 = require("@n8n/api-types");
exports.GENERIC_AUTH_CREDENTIAL_TYPES = new Set([
    api_types_1.TEMPLATED_CUSTOM_AUTH_CREDENTIAL_TYPE,
    'httpHeaderAuth',
    'httpBearerAuth',
    'httpQueryAuth',
    'httpBasicAuth',
    'httpDigestAuth',
    'httpCustomAuth',
    'oAuth1Api',
    'oAuth2Api',
]);
exports.N8N_CONNECT_DISPLAY_NAME = 'n8n credits';
exports.AI_GATEWAY_CREDENTIAL = {
    id: null,
    name: '',
    __aiGatewayManaged: true,
};
function isAiGatewayManagedCredential(credential) {
    return (typeof credential === 'object' &&
        credential !== null &&
        Reflect.get(credential, 'id') === null &&
        Reflect.get(credential, '__aiGatewayManaged') === true);
}
function toSetupNodeCredential(credential) {
    if (isAiGatewayManagedCredential(credential)) {
        return { ...exports.AI_GATEWAY_CREDENTIAL, name: credential.name };
    }
    if (typeof credential.id === 'string')
        return { id: credential.id, name: credential.name };
    return undefined;
}
async function resolveCredentialForApply(credType, credId, context) {
    if (credId === api_types_1.AI_GATEWAY_MANAGED_TAG) {
        if (context.credentialService.isAiGatewayCredentialType) {
            const supported = await context.credentialService.isAiGatewayCredentialType(credType);
            if (!supported) {
                return {
                    resolved: false,
                    error: `Credential type "${credType}" is not supported by n8n credits`,
                };
            }
        }
        return { resolved: true, credential: { ...exports.AI_GATEWAY_CREDENTIAL } };
    }
    try {
        const cred = await context.credentialService.get(credId);
        if (cred)
            return { resolved: true, credential: { id: cred.id, name: cred.name } };
        return {
            resolved: false,
            error: `Credential ${credId} (type: ${credType}) not found — it may have been deleted`,
        };
    }
    catch (error) {
        return {
            resolved: false,
            error: `Failed to resolve credential ${credId} (type: ${credType}): ${error instanceof Error ? error.message : 'Unknown error'}`,
        };
    }
}
function assignCredentialToNode(node, credType, credential) {
    node.credentials ??= {};
    node.credentials[credType] = credential;
}
function extractServiceHost(raw) {
    if (typeof raw !== 'string')
        return undefined;
    const plain = (raw.startsWith('=') ? raw.slice(1) : raw).split('{{')[0].trim();
    if (!/^https?:\/\//i.test(plain))
        return undefined;
    try {
        const host = new URL(plain).hostname.toLowerCase();
        return host || undefined;
    }
    catch {
        return undefined;
    }
}
function serviceHostsMatch(a, b) {
    return a === b || a.endsWith(`.${b}`) || b.endsWith(`.${a}`);
}
//# sourceMappingURL=credential-utils.js.map