import type { WorkflowJSON } from '@n8n/workflow-sdk';
import type { ResolvedCredential } from './resolved-credential.schema';
import type { InstanceAiContext } from '../../types';
export type { ResolvedCredential };
export interface CredentialEntry {
    id: string;
    name: string;
    type: string;
}
export type CredentialMap = Map<string, CredentialEntry[]>;
export declare function buildCredentialMap(credentialService: Pick<InstanceAiContext['credentialService'], 'list'>): Promise<CredentialMap>;
export interface CredentialResolutionResult {
    mockedNodeNames: string[];
    mockedCredentialTypes: string[];
    mockedCredentialsByNode: Record<string, string[]>;
    resolvedCredentialsByNode: Record<string, ResolvedCredential[]>;
}
export declare function buildCredentialResolutionNote(resolvedCredentialsByNode: Record<string, ResolvedCredential[]>): string | undefined;
export declare function resolveCredentials(json: WorkflowJSON, workflowId: string | undefined, ctx: InstanceAiContext, availableCredentials?: CredentialMap): Promise<CredentialResolutionResult>;
