"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PARAMETERS_INLINE_LIMIT_BYTES = exports.STRUCTURE_ONLY_NOTE = void 0;
exports.isSmallPayload = isSmallPayload;
exports.summarizeWorkflowStructure = summarizeWorkflowStructure;
const is_record_1 = require("@n8n/utils/is-record");
const n8n_workflow_1 = require("n8n-workflow");
exports.STRUCTURE_ONLY_NOTE = 'Node parameters omitted to keep context small. Pass full: true to include them in one call, or use get-json / get-as-code (optionally with versionId) for parameter-level detail.';
exports.PARAMETERS_INLINE_LIMIT_BYTES = 4096;
function isSmallPayload(detail) {
    return JSON.stringify(detail).length <= exports.PARAMETERS_INLINE_LIMIT_BYTES;
}
const CONNECTION_TYPES = new Set(Object.values(n8n_workflow_1.NodeConnectionTypes));
function isConnection(value) {
    return ((0, is_record_1.isRecord)(value) &&
        typeof value.node === 'string' &&
        typeof value.type === 'string' &&
        CONNECTION_TYPES.has(value.type) &&
        typeof value.index === 'number');
}
function toConnections(connections) {
    const result = {};
    for (const [from, byType] of Object.entries(connections)) {
        if (!(0, is_record_1.isRecord)(byType))
            continue;
        for (const [connectionType, outputs] of Object.entries(byType)) {
            if (!Array.isArray(outputs))
                continue;
            (result[from] ??= {})[connectionType] = outputs.map((targets) => Array.isArray(targets) ? targets.filter(isConnection) : null);
        }
    }
    return result;
}
function summarizeConnections(connections) {
    const edges = [];
    for (const [from, byType] of Object.entries(connections)) {
        if (!(0, is_record_1.isRecord)(byType))
            continue;
        for (const [connectionType, outputs] of Object.entries(byType)) {
            if (!Array.isArray(outputs))
                continue;
            outputs.forEach((targets, outputIndex) => {
                if (!Array.isArray(targets))
                    return;
                for (const target of targets) {
                    if (!(0, is_record_1.isRecord)(target) || typeof target.node !== 'string')
                        continue;
                    const typeLabel = connectionType === 'main' ? '' : `-(${connectionType})`;
                    const branchLabel = outputIndex > 0 ? `[${outputIndex}]` : '';
                    edges.push(`${from} ${typeLabel}${branchLabel}→ ${target.node}`);
                }
            });
        }
    }
    return edges;
}
async function summarizeWorkflowStructure(name, nodes, connections) {
    try {
        const { generateWorkflowCode } = await import('@n8n/workflow-sdk');
        return generateWorkflowCode({
            name,
            nodes: nodes.map((node) => ({
                id: node.name,
                name: node.name,
                type: node.type,
                typeVersion: node.typeVersion ?? 1,
                position: [node.position[0] ?? 0, node.position[1] ?? 0],
            })),
            connections: toConnections(connections),
        });
    }
    catch {
        return [
            'Nodes:',
            ...nodes.map((node) => `- ${node.name} (${node.type})`),
            'Connections:',
            ...summarizeConnections(connections).map((edge) => `- ${edge}`),
        ].join('\n');
    }
}
//# sourceMappingURL=summarize-workflow.js.map