import type { CredentialHostInfo } from '../../types';
export interface CredentialHostMeta {
    httpRequestNode?: unknown;
    test?: unknown;
    properties?: Array<{
        name: string;
        default?: unknown;
    }>;
}
export type CredentialUrlResolution = {
    status: 'match';
    credentialType: string;
} | {
    status: 'ambiguous';
    candidates: string[];
} | {
    status: 'none';
};
export declare function deriveCredentialHosts(meta: CredentialHostMeta): string[];
export declare function buildCredentialHostIndex(infos: CredentialHostInfo[]): Map<string, string[]>;
export declare function resolveCredentialByUrl(url: string, index: Map<string, string[]>): CredentialUrlResolution;
