import type { WorkflowJSON } from '@n8n/workflow-sdk';
import type { INodeIssues } from 'n8n-workflow';
import type { InstanceAiContext } from '../../types';
export interface ValidateWorkflowResult {
    workflowId?: string;
    issues: Record<string, INodeIssues>;
    summary: string[];
    valid: boolean;
}
export interface ValidateWorkflowInput {
    workflowId?: string;
    workflow?: WorkflowJSON;
    ignoreIssues?: string[];
}
export declare function validateWorkflowConfig(context: InstanceAiContext, input: ValidateWorkflowInput): Promise<ValidateWorkflowResult>;
