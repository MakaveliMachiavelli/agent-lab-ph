import type { WorkflowNode } from '../../types';
export declare const STRUCTURE_ONLY_NOTE = "Node parameters omitted to keep context small. Pass full: true to include them in one call, or use get-json / get-as-code (optionally with versionId) for parameter-level detail.";
export declare const PARAMETERS_INLINE_LIMIT_BYTES = 4096;
export declare function isSmallPayload(detail: unknown): boolean;
export declare function summarizeWorkflowStructure(name: string, nodes: WorkflowNode[], connections: Record<string, unknown>): Promise<string>;
