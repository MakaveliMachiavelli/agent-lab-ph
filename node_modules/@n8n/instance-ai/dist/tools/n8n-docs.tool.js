"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toPublicDocsUrl = exports.resetN8nDocsRegistryCacheForTests = exports.rankN8nDocsEntries = exports.parseN8nDocsRegistry = exports.normalizeDocsUrl = exports.getDocsUrlCandidates = exports.N8N_DOCS_REGISTRY_URL = exports.N8N_DOCS_TOOL_ID = void 0;
exports.createN8nDocsTool = createN8nDocsTool;
const agents_1 = require("@n8n/agents");
const ranking_1 = require("./n8n-docs/ranking");
Object.defineProperty(exports, "rankN8nDocsEntries", { enumerable: true, get: function () { return ranking_1.rankN8nDocsEntries; } });
const registry_1 = require("./n8n-docs/registry");
Object.defineProperty(exports, "getDocsUrlCandidates", { enumerable: true, get: function () { return registry_1.getDocsUrlCandidates; } });
Object.defineProperty(exports, "normalizeDocsUrl", { enumerable: true, get: function () { return registry_1.normalizeDocsUrl; } });
Object.defineProperty(exports, "N8N_DOCS_REGISTRY_URL", { enumerable: true, get: function () { return registry_1.N8N_DOCS_REGISTRY_URL; } });
Object.defineProperty(exports, "resetN8nDocsRegistryCacheForTests", { enumerable: true, get: function () { return registry_1.resetN8nDocsRegistryCacheForTests; } });
Object.defineProperty(exports, "toPublicDocsUrl", { enumerable: true, get: function () { return registry_1.toPublicDocsUrl; } });
Object.defineProperty(exports, "parseN8nDocsRegistry", { enumerable: true, get: function () { return registry_1.parseN8nDocsRegistry; } });
const schemas_1 = require("./n8n-docs/schemas");
const tool_ids_1 = require("./tool-ids");
Object.defineProperty(exports, "N8N_DOCS_TOOL_ID", { enumerable: true, get: function () { return tool_ids_1.N8N_DOCS_TOOL_ID; } });
const SOURCE_ATTRIBUTION_INSTRUCTION = 'When answering from returned docs, end with `Source: [Page title](page URL)` for one page or `Sources:` for multiple pages, using only returned page titles and URLs.';
function clamp(value, fallback, max) {
    if (value === undefined)
        return fallback;
    return Math.min(value, max);
}
function toPublicMatch(match) {
    const publicUrl = (0, registry_1.toPublicDocsUrl)(match.url);
    return {
        ...match,
        url: publicUrl,
        path: new URL(publicUrl).pathname,
    };
}
function emptyLookupResult(input, registry) {
    return {
        query: (0, ranking_1.getLookupQuery)(input),
        intent: input.intent ?? 'general',
        registryUrl: registry_1.N8N_DOCS_REGISTRY_URL,
        registryFetchedAt: registry.registry?.fetchedAt ?? '',
        matches: [],
        documents: [],
        ...(registry.hint ? { hint: registry.hint } : {}),
        ...(registry.error ? { error: registry.error } : {}),
    };
}
async function handleSearch(context, input, abortSignal) {
    const registry = await (0, registry_1.getN8nDocsRegistry)(context, abortSignal);
    if (!registry.registry)
        return emptyLookupResult(input, registry);
    const maxResults = clamp(input.maxResults, schemas_1.DEFAULT_MAX_RESULTS, schemas_1.MAX_RESULTS);
    const matches = (0, ranking_1.rankN8nDocsEntries)(registry.registry.entries, input).slice(0, maxResults);
    return {
        query: (0, ranking_1.getLookupQuery)(input),
        intent: input.intent ?? 'general',
        registryUrl: registry_1.N8N_DOCS_REGISTRY_URL,
        registryFetchedAt: registry.registry.fetchedAt,
        matches: matches.map(toPublicMatch),
        ...(registry.hint ? { hint: registry.hint } : {}),
    };
}
async function handleLookup(context, input, abortSignal) {
    const registry = await (0, registry_1.getN8nDocsRegistry)(context, abortSignal);
    if (!registry.registry)
        return emptyLookupResult(input, registry);
    const maxPages = clamp(input.maxPages, schemas_1.DEFAULT_MAX_PAGES, schemas_1.MAX_PAGES);
    const maxContentLength = clamp(input.maxContentLength, schemas_1.DEFAULT_MAX_CONTENT_LENGTH, schemas_1.MAX_CONTENT_LENGTH);
    const matches = (0, ranking_1.rankN8nDocsEntries)(registry.registry.entries, input);
    const pagesToRead = (0, ranking_1.pickLookupMatches)(matches, maxPages);
    const documents = [];
    const readErrors = [];
    const readResults = await Promise.allSettled(pagesToRead.map(async (match) => await (0, registry_1.readN8nDocsEntry)(match, maxContentLength, abortSignal)));
    for (const [index, result] of readResults.entries()) {
        const match = pagesToRead[index];
        if (!match)
            continue;
        if (result.status === 'fulfilled') {
            documents.push(result.value);
        }
        else if ((0, agents_1.isAbortError)(result.reason) || abortSignal?.aborted) {
            throw result.reason instanceof Error
                ? result.reason
                : new Error('This operation was aborted');
        }
        else {
            readErrors.push(`${match.title}: ${(0, registry_1.getFetchErrorMessage)(result.reason)}`);
        }
    }
    const hints = [
        registry.hint,
        readErrors.length ? `Some docs pages could not be read: ${readErrors.join('; ')}` : undefined,
    ].filter((hint) => typeof hint === 'string' && hint.length > 0);
    return {
        query: (0, ranking_1.getLookupQuery)(input),
        intent: input.intent ?? 'general',
        registryUrl: registry_1.N8N_DOCS_REGISTRY_URL,
        registryFetchedAt: registry.registry.fetchedAt,
        matches: pagesToRead.map(toPublicMatch),
        documents,
        ...(hints.length ? { hint: hints.join(' ') } : {}),
    };
}
async function handleRead(context, input, abortSignal) {
    const registry = await (0, registry_1.getN8nDocsRegistry)(context, abortSignal);
    if (!registry.registry) {
        return {
            url: input.url,
            registryUrl: registry_1.N8N_DOCS_REGISTRY_URL,
            registryFetchedAt: '',
            documents: [],
            ...(registry.hint ? { hint: registry.hint } : {}),
            ...(registry.error ? { error: registry.error } : {}),
        };
    }
    const docsRegistry = registry.registry;
    const entry = (0, registry_1.getDocsUrlCandidates)(input.url)
        .map((candidate) => docsRegistry.byUrl.get(candidate))
        .find((candidate) => candidate !== undefined);
    if (!entry) {
        return {
            url: input.url,
            registryUrl: registry_1.N8N_DOCS_REGISTRY_URL,
            registryFetchedAt: docsRegistry.fetchedAt,
            documents: [],
            error: 'URL is not an n8n docs registry entry.',
            ...(registry.hint ? { hint: registry.hint } : {}),
        };
    }
    const maxContentLength = clamp(input.maxContentLength, schemas_1.DEFAULT_MAX_CONTENT_LENGTH, schemas_1.MAX_CONTENT_LENGTH);
    return {
        url: (0, registry_1.toPublicDocsUrl)(entry.url),
        registryUrl: registry_1.N8N_DOCS_REGISTRY_URL,
        registryFetchedAt: docsRegistry.fetchedAt,
        documents: [await (0, registry_1.readN8nDocsEntry)(entry, maxContentLength, abortSignal)],
        ...(registry.hint ? { hint: registry.hint } : {}),
    };
}
function createN8nDocsTool(context) {
    return new agents_1.Tool(tool_ids_1.N8N_DOCS_TOOL_ID)
        .description(`Search and read current n8n documentation from docs.n8n.io. Load via \`load_tool\` before calling (search "n8n docs" if not visible). Use for n8n product, setup, credential, node, hosting, API, and troubleshooting questions. ${SOURCE_ATTRIBUTION_INSTRUCTION}`)
        .input(schemas_1.n8nDocsToolInputSchema)
        .handler(async (input, ctx) => {
        const parsedInput = schemas_1.n8nDocsRuntimeInputSchema.parse(input);
        const abortSignal = ctx.abortSignal;
        switch (parsedInput.action) {
            case 'lookup':
                return await handleLookup(context, parsedInput, abortSignal);
            case 'search':
                return await handleSearch(context, parsedInput, abortSignal);
            case 'read':
                return await handleRead(context, parsedInput, abortSignal);
        }
    })
        .build();
}
//# sourceMappingURL=n8n-docs.tool.js.map