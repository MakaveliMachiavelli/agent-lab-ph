"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.WorkSummaryAccumulator = exports.workSummarySchema = exports.toolCallSummarySchema = void 0;
const zod_1 = require("zod");
exports.toolCallSummarySchema = zod_1.z.object({
    toolCallId: zod_1.z.string(),
    toolName: zod_1.z.string(),
    succeeded: zod_1.z.boolean(),
    errorSummary: zod_1.z.string().optional(),
});
exports.workSummarySchema = zod_1.z.object({
    toolCalls: zod_1.z.array(exports.toolCallSummarySchema),
    totalToolCalls: zod_1.z.number().int().min(0),
    totalToolErrors: zod_1.z.number().int().min(0),
});
class WorkSummaryAccumulator {
    calls = new Map();
    observe(event) {
        switch (event.type) {
            case 'tool-call': {
                const { toolCallId, toolName } = event.payload;
                if (!toolCallId)
                    break;
                this.calls.set(toolCallId, {
                    toolCallId,
                    toolName,
                    succeeded: true,
                });
                break;
            }
            case 'tool-result': {
                const { toolCallId } = event.payload;
                if (!toolCallId)
                    break;
                const existing = this.calls.get(toolCallId);
                if (existing) {
                    existing.succeeded = true;
                }
                break;
            }
            case 'tool-error': {
                const { toolCallId, error } = event.payload;
                if (!toolCallId)
                    break;
                const existing = this.calls.get(toolCallId);
                if (existing) {
                    existing.succeeded = false;
                    existing.errorSummary = typeof error === 'string' ? error.slice(0, 500) : undefined;
                }
                break;
            }
            default:
                break;
        }
    }
    toSummary() {
        const toolCalls = [...this.calls.values()].map((c) => ({ ...c }));
        return {
            toolCalls,
            totalToolCalls: toolCalls.length,
            totalToolErrors: toolCalls.filter((c) => !c.succeeded).length,
        };
    }
}
exports.WorkSummaryAccumulator = WorkSummaryAccumulator;
//# sourceMappingURL=work-summary-accumulator.js.map