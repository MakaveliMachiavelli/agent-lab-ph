"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.UsageAccumulator = exports.runTokenUsageSchema = exports.builderUsageItemSchema = void 0;
exports.tokenUsageToBuilderUsageItems = tokenUsageToBuilderUsageItems;
const zod_1 = require("zod");
exports.builderUsageItemSchema = zod_1.z.object({
    type: zod_1.z.literal('llmTokens'),
    model: zod_1.z.string(),
    uncachedInput: zod_1.z.number().min(0),
    cacheRead: zod_1.z.number().min(0),
    cacheWrite: zod_1.z.number().min(0),
    output: zod_1.z.number().min(0),
});
exports.runTokenUsageSchema = zod_1.z.object({
    promptTokens: zod_1.z.number().int().min(0),
    completionTokens: zod_1.z.number().int().min(0),
    totalTokens: zod_1.z.number().int().min(0),
    costUsd: zod_1.z.number().min(0),
    usage: zod_1.z.array(exports.builderUsageItemSchema).optional(),
});
const finishChunkSchema = zod_1.z.object({
    type: zod_1.z.literal('finish'),
    model: zod_1.z.string().optional(),
    usage: zod_1.z
        .object({
        promptTokens: zod_1.z.number().optional(),
        completionTokens: zod_1.z.number().optional(),
        totalTokens: zod_1.z.number().optional(),
        cost: zod_1.z.number().optional(),
        inputTokenDetails: zod_1.z
            .object({
            noCache: zod_1.z.number().optional(),
            cacheRead: zod_1.z.number().optional(),
            cacheWrite: zod_1.z.number().optional(),
        })
            .optional(),
    })
        .optional(),
});
class UsageAccumulator {
    promptTokens = 0;
    completionTokens = 0;
    totalTokens = 0;
    costUsd = 0;
    seen = false;
    perModel = new Map();
    observe(chunk) {
        const parsed = finishChunkSchema.safeParse(chunk);
        if (!parsed.success || !parsed.data.usage)
            return;
        const { promptTokens, completionTokens, totalTokens, cost, inputTokenDetails } = parsed.data.usage;
        this.promptTokens += promptTokens ?? 0;
        this.completionTokens += completionTokens ?? 0;
        this.totalTokens += totalTokens ?? 0;
        this.costUsd += cost ?? 0;
        this.seen = true;
        const model = parsed.data.model ?? 'unknown';
        const entry = this.perModel.get(model) ?? {
            uncachedInput: 0,
            cacheRead: 0,
            cacheWrite: 0,
            output: 0,
        };
        const noCache = Math.max(0, inputTokenDetails?.noCache ?? 0);
        const cacheRead = Math.max(0, inputTokenDetails?.cacheRead ?? 0);
        const cacheWrite = Math.max(0, inputTokenDetails?.cacheWrite ?? 0);
        const hasUsableBreakdown = noCache + cacheRead + cacheWrite > 0;
        entry.uncachedInput += hasUsableBreakdown ? noCache : Math.max(0, promptTokens ?? 0);
        entry.cacheRead += cacheRead;
        entry.cacheWrite += cacheWrite;
        entry.output += Math.max(0, completionTokens ?? 0);
        this.perModel.set(model, entry);
    }
    hasUsage() {
        return this.seen;
    }
    toUsage() {
        const usage = [];
        for (const [model, e] of this.perModel) {
            if (e.uncachedInput + e.cacheRead + e.cacheWrite + e.output > 0) {
                usage.push({ type: 'llmTokens', model, ...e });
            }
        }
        return {
            promptTokens: this.promptTokens,
            completionTokens: this.completionTokens,
            totalTokens: this.totalTokens,
            costUsd: this.costUsd,
            usage,
        };
    }
}
exports.UsageAccumulator = UsageAccumulator;
function tokenUsageToBuilderUsageItems(model, usage) {
    const accumulator = new UsageAccumulator();
    accumulator.observe({ type: 'finish', model, usage });
    return accumulator.toUsage().usage ?? [];
}
//# sourceMappingURL=usage-accumulator.js.map