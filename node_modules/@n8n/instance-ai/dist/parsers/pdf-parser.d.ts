import { type AttachmentInfo } from './structured-file-parser';
export interface PdfExtractionResult {
    text: string;
    pages: number;
    truncated: boolean;
}
export declare function extractPdfText(attachment: AttachmentInfo): Promise<PdfExtractionResult>;
