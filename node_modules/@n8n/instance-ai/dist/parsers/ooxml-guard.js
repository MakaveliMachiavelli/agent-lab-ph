"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MAX_OOXML_ENTRIES = exports.MAX_OOXML_UNCOMPRESSED_BYTES = void 0;
exports.assertOoxmlWithinBounds = assertOoxmlWithinBounds;
const EOCD_SIGNATURE = 0x06054b50;
const CENTRAL_FILE_HEADER_SIGNATURE = 0x02014b50;
const EOCD_MIN_SIZE = 22;
const CENTRAL_FILE_HEADER_MIN_SIZE = 46;
const MAX_EOCD_COMMENT = 0xffff;
const ZIP64_SIZE_MARKER = 0xffffffff;
exports.MAX_OOXML_UNCOMPRESSED_BYTES = 200 * 1024 * 1024;
exports.MAX_OOXML_ENTRIES = 1024;
function formatMB(bytes) {
    return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
}
function assertOoxmlWithinBounds(buffer, fileName) {
    const eocd = findEndOfCentralDirectory(buffer);
    if (eocd === undefined)
        return;
    const entryCount = buffer.readUInt16LE(eocd + 10);
    if (entryCount > exports.MAX_OOXML_ENTRIES) {
        throw new Error(`"${fileName}" has too many archive entries (${entryCount} > ${exports.MAX_OOXML_ENTRIES}); refusing to parse a potential decompression bomb.`);
    }
    let offset = buffer.readUInt32LE(eocd + 16);
    let totalUncompressed = 0;
    for (let i = 0; i < entryCount; i++) {
        if (offset + CENTRAL_FILE_HEADER_MIN_SIZE > buffer.length)
            break;
        if (buffer.readUInt32LE(offset) !== CENTRAL_FILE_HEADER_SIGNATURE)
            break;
        const uncompressed = buffer.readUInt32LE(offset + 24);
        if (uncompressed === ZIP64_SIZE_MARKER) {
            throw new Error(`"${fileName}" declares a ZIP64-sized entry; refusing to parse a potential decompression bomb.`);
        }
        totalUncompressed += uncompressed;
        if (totalUncompressed > exports.MAX_OOXML_UNCOMPRESSED_BYTES) {
            throw new Error(`"${fileName}" decompresses to more than ${formatMB(exports.MAX_OOXML_UNCOMPRESSED_BYTES)}; refusing to parse a potential decompression bomb.`);
        }
        const nameLen = buffer.readUInt16LE(offset + 28);
        const extraLen = buffer.readUInt16LE(offset + 30);
        const commentLen = buffer.readUInt16LE(offset + 32);
        offset += CENTRAL_FILE_HEADER_MIN_SIZE + nameLen + extraLen + commentLen;
    }
}
function findEndOfCentralDirectory(buffer) {
    if (buffer.length < EOCD_MIN_SIZE)
        return undefined;
    const scanLimit = Math.max(0, buffer.length - EOCD_MIN_SIZE - MAX_EOCD_COMMENT);
    for (let i = buffer.length - EOCD_MIN_SIZE; i >= scanLimit; i--) {
        if (buffer.readUInt32LE(i) === EOCD_SIGNATURE)
            return i;
    }
    return undefined;
}
//# sourceMappingURL=ooxml-guard.js.map