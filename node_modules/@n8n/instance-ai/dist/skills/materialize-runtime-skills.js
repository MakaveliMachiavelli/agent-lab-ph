"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.N8N_WORKSPACE_DIR_ENV = exports.N8N_SKILL_DIR_ENV = exports.N8N_SKILLS_DIR_ENV = exports.RUNTIME_SKILL_MANIFEST_SCHEMA_VERSION = exports.RUNTIME_SKILL_MANIFEST_FILE = exports.SANDBOX_RUNTIME_SKILL_REGISTRY_FILE = exports.SANDBOX_RUNTIME_SKILLS_DIR = void 0;
exports.loadPrebakedRuntimeSkillsBundle = loadPrebakedRuntimeSkillsBundle;
exports.buildRuntimeSkillWorkspaceBundle = buildRuntimeSkillWorkspaceBundle;
exports.materializeRuntimeSkillsIntoWorkspace = materializeRuntimeSkillsIntoWorkspace;
exports.createLazyWorkspaceRuntimeSkillSource = createLazyWorkspaceRuntimeSkillSource;
const agents_1 = require("@n8n/agents");
const sandbox_1 = require("@n8n/agents/sandbox");
const posix_1 = require("node:path/posix");
const prebaked_workspace_bundle_1 = require("../workspace/prebaked-workspace-bundle");
const workspace_file_content_1 = require("../workspace/workspace-file-content");
const workspace_manifest_1 = require("../workspace/workspace-manifest");
exports.SANDBOX_RUNTIME_SKILLS_DIR = 'skills';
exports.SANDBOX_RUNTIME_SKILL_REGISTRY_FILE = 'registry.json';
exports.RUNTIME_SKILL_MANIFEST_FILE = workspace_manifest_1.WORKSPACE_MANIFEST_FILE;
exports.RUNTIME_SKILL_MANIFEST_SCHEMA_VERSION = 1;
exports.N8N_SKILLS_DIR_ENV = 'N8N_SKILLS_DIR';
exports.N8N_SKILL_DIR_ENV = 'N8N_SKILL_DIR';
exports.N8N_WORKSPACE_DIR_ENV = 'N8N_WORKSPACE_DIR';
const LINKED_FILE_GROUPS = [
    'references',
    'templates',
    'scripts',
    'assets',
    'examples',
    'other',
];
const N8N_SKILL_DIR_TEMPLATE = '$' + '{N8N_SKILL_DIR}';
const N8N_SKILLS_DIR_TEMPLATE = '$' + '{N8N_SKILLS_DIR}';
const N8N_WORKSPACE_DIR_TEMPLATE = '$' + '{N8N_WORKSPACE_DIR}';
const LOAD_SKILL_OUTPUT_LIMIT_BYTES = 64 * 1024;
function isNonEmptyRecord(value) {
    return Object.keys(value).length > 0;
}
function toFrontmatterSection(value, build) {
    if (!value)
        return undefined;
    const output = build(value);
    return isNonEmptyRecord(output) ? output : undefined;
}
function materializedSkillById(materialized) {
    return new Map(materialized.map((skill) => [skill.id, skill]));
}
function safeSkillDirectory(entry) {
    const raw = entry.sourceDirectory ?? entry.name;
    if (!raw || raw.includes('\0') || raw.includes('\\') || raw.startsWith('/')) {
        throw new Error(`Invalid runtime skill directory for "${entry.name}"`);
    }
    const normalized = (0, posix_1.normalize)(raw);
    if (normalized === '.' ||
        normalized === '..' ||
        normalized.startsWith('../') ||
        normalized.split('/').includes('..')) {
        throw new Error(`Runtime skill directory escapes skills root for "${entry.name}"`);
    }
    return normalized;
}
function materializedSkillDirectory(skillsRoot, entry) {
    return (0, posix_1.join)(skillsRoot, safeSkillDirectory(entry));
}
function safeLinkedFilePath(directory, entry, linkedFile) {
    const raw = linkedFile.path;
    if (!raw ||
        raw.trim() === '' ||
        raw.includes('\0') ||
        raw.includes('\\') ||
        raw.startsWith('/')) {
        throw new Error(`Invalid runtime skill linked file for "${entry.name}": ${raw}`);
    }
    const relativePath = (0, posix_1.normalize)(raw);
    const materializedPath = (0, posix_1.normalize)((0, posix_1.join)(directory, relativePath));
    const directoryBoundary = directory.endsWith('/') ? directory : `${directory}/`;
    if (relativePath === '.' ||
        relativePath.startsWith('../') ||
        materializedPath === directory ||
        !materializedPath.startsWith(directoryBoundary)) {
        throw new Error(`Runtime skill linked file escapes skill directory for "${entry.name}": ${raw}`);
    }
    return { relativePath, materializedPath };
}
function substituteRuntimeSkillVars(content, skillDir, workspaceRoot, skillsRoot) {
    return content
        .replaceAll(N8N_SKILL_DIR_TEMPLATE, skillDir)
        .replaceAll(N8N_SKILLS_DIR_TEMPLATE, skillsRoot)
        .replaceAll(N8N_WORKSPACE_DIR_TEMPLATE, workspaceRoot);
}
function toFrontmatterInterface(value) {
    return toFrontmatterSection(value, (section) => ({
        ...(section.displayName ? { display_name: section.displayName } : {}),
        ...(section.shortDescription ? { short_description: section.shortDescription } : {}),
        ...(section.defaultPrompt ? { default_prompt: section.defaultPrompt } : {}),
        ...(section.icon ? { icon: section.icon } : {}),
        ...(section.brandColor ? { brand_color: section.brandColor } : {}),
    }));
}
function toFrontmatterPolicy(value) {
    return toFrontmatterSection(value, (section) => ({
        ...(section.allowImplicitInvocation !== undefined
            ? { allow_implicit_invocation: section.allowImplicitInvocation }
            : {}),
        ...(section.product ? { product: section.product } : {}),
    }));
}
function toFrontmatterDependencies(value) {
    return toFrontmatterSection(value, (section) => ({
        ...(section.tools?.length ? { tools: section.tools } : {}),
        ...(section.secrets?.length ? { secrets: section.secrets } : {}),
        ...(section.mcpServers?.length
            ? {
                mcp_servers: section.mcpServers.map((server) => ({
                    name: server.name,
                    ...(server.description ? { description: server.description } : {}),
                    ...(server.transport ? { transport: server.transport } : {}),
                    ...(server.url ? { url: server.url } : {}),
                    ...(server.command ? { command: server.command } : {}),
                })),
            }
            : {}),
    }));
}
function addFrontmatterField(lines, key, value) {
    if (value === undefined)
        return;
    if (Array.isArray(value) && value.length === 0)
        return;
    if (typeof value === 'object' &&
        value !== null &&
        !Array.isArray(value) &&
        Object.keys(value).length === 0) {
        return;
    }
    const serialized = JSON.stringify(value);
    if (serialized) {
        lines.push(`${key}: ${serialized}`);
    }
}
function renderRuntimeSkillMarkdown(skill, entry, skillDir, workspaceRoot, skillsRoot) {
    const lines = ['---'];
    addFrontmatterField(lines, 'name', skill.name);
    addFrontmatterField(lines, 'description', skill.description);
    addFrontmatterField(lines, 'recommended_tools', skill.recommendedTools);
    addFrontmatterField(lines, 'allowed_tools', skill.allowedTools);
    addFrontmatterField(lines, 'interface', toFrontmatterInterface(skill.interface));
    addFrontmatterField(lines, 'policy', toFrontmatterPolicy(skill.policy));
    addFrontmatterField(lines, 'dependencies', toFrontmatterDependencies(skill.dependencies));
    addFrontmatterField(lines, 'version', skill.version);
    addFrontmatterField(lines, 'license', skill.license);
    addFrontmatterField(lines, 'compatibility', skill.compatibility);
    addFrontmatterField(lines, 'platforms', skill.platforms);
    addFrontmatterField(lines, 'metadata', skill.metadata);
    lines.push('---', '');
    const instructions = substituteRuntimeSkillVars(skill.instructions, skillDir, workspaceRoot, skillsRoot);
    const sourceNote = entry.sourceDirectory && entry.sourceDirectory !== entry.name
        ? `<!-- materialized from ${entry.sourceDirectory} -->\n\n`
        : '';
    return (0, workspace_file_content_1.withTrailingNewline)(`${lines.join('\n')}\n${sourceNote}${instructions.trim()}`);
}
function materializedRegistry(registry, materialized) {
    const materializedById = materializedSkillById(materialized);
    return {
        ...registry,
        skills: registry.skills.map((entry) => {
            const skill = materializedById.get(entry.id);
            if (!skill)
                return entry;
            const materializedEntry = { ...entry };
            delete materializedEntry.sourcePath;
            return {
                ...materializedEntry,
                path: skill.path,
                directory: skill.directory,
            };
        }),
    };
}
function createMaterializedRuntimeSkillSource(source, registry, materialized, workspaceRoot, skillsRoot) {
    const materializedById = materializedSkillById(materialized);
    const loadFile = source.loadFile;
    return {
        registry,
        loadSkill: async (skillId) => {
            const skill = await source.loadSkill(skillId);
            const materializedSkill = materializedById.get(skillId);
            if (!skill || !materializedSkill)
                return skill;
            return {
                ...skill,
                path: materializedSkill.path,
                directory: materializedSkill.directory,
                instructions: substituteRuntimeSkillVars(skill.instructions, materializedSkill.directory, workspaceRoot, skillsRoot),
            };
        },
        ...(loadFile
            ? {
                loadFile: async (skillId, filePath) => {
                    const file = await loadFile(skillId, filePath);
                    const materializedSkill = materializedById.get(skillId);
                    if (!file || !materializedSkill)
                        return file;
                    return {
                        ...file,
                        content: substituteRuntimeSkillVars(file.content, materializedSkill.directory, workspaceRoot, skillsRoot),
                    };
                },
            }
            : {}),
    };
}
const RUNTIME_SKILL_FILE_LABEL = 'Runtime skill file';
async function loadPrebakedRuntimeSkillsBundle({ source, workspace, root, workspaceRoot = root, logger, }) {
    if (source.registry.skills.length === 0)
        return undefined;
    const skillsRoot = (0, posix_1.join)(root, exports.SANDBOX_RUNTIME_SKILLS_DIR);
    const manifestPath = (0, posix_1.join)(skillsRoot, exports.RUNTIME_SKILL_MANIFEST_FILE);
    return await (0, prebaked_workspace_bundle_1.loadPrebakedWorkspaceBundle)({
        workspace,
        manifestPath,
        expectedHash: source.registry.skillsHash,
        hashField: 'skillsHash',
        schemaVersion: exports.RUNTIME_SKILL_MANIFEST_SCHEMA_VERSION,
        resourceLabel: RUNTIME_SKILL_FILE_LABEL,
        logger,
        invalidManifestLogMessage: 'Ignoring invalid prebaked runtime skills manifest',
        staleManifestLogMessage: 'Ignoring stale prebaked runtime skills manifest',
        staleManifestLogKeys: {
            expected: 'expectedSkillsHash',
            actual: 'actualSkillsHash',
        },
        successLogMessage: 'Using prebaked runtime skills from workspace',
        successLogContext: (bundle) => ({
            root,
            workspaceRoot,
            skillsRoot: bundle.rootDir,
            registryPath: bundle.registryPath,
            skillsHash: bundle.skillsHash,
            count: bundle.skills.length,
        }),
        buildBundle: async () => await buildRuntimeSkillWorkspaceBundle({
            source,
            root,
            workspaceRoot,
            skillsRoot,
            logger,
        }),
    });
}
function linkedFilesFor(entry) {
    return LINKED_FILE_GROUPS.flatMap((group) => entry.linkedFiles[group]);
}
function warnIfExceedsLoadSkillLimit(logger, entry, filePath, content) {
    const bytes = Buffer.byteLength(content, 'utf8');
    if (bytes <= LOAD_SKILL_OUTPUT_LIMIT_BYTES)
        return;
    logger.warn('Runtime skill file exceeds load_skill output limit', {
        skill: entry.name,
        path: filePath,
        bytes,
        maxBytes: LOAD_SKILL_OUTPUT_LIMIT_BYTES,
    });
}
async function buildRuntimeSkillWorkspaceBundle({ source, root, workspaceRoot = root, skillsRoot = (0, posix_1.join)(root, exports.SANDBOX_RUNTIME_SKILLS_DIR), logger, }) {
    if (source.registry.skills.length === 0)
        return undefined;
    const files = new Map();
    const materialized = await Promise.all(source.registry.skills.map(async (entry) => {
        const skill = await source.loadSkill(entry.id);
        if (!skill) {
            throw new Error(`Runtime skill "${entry.name}" is registered but cannot be loaded`);
        }
        const directory = materializedSkillDirectory(skillsRoot, entry);
        const path = (0, posix_1.join)(directory, agents_1.RUNTIME_SKILL_FILE_NAME);
        const skillMarkdown = renderRuntimeSkillMarkdown(skill, entry, directory, workspaceRoot, skillsRoot);
        warnIfExceedsLoadSkillLimit(logger, entry, path, skillMarkdown);
        files.set(path, skillMarkdown);
        const linkedFiles = linkedFilesFor(entry);
        if (linkedFiles.length > 0 && !source.loadFile) {
            throw new Error(`Runtime skill "${entry.name}" has linked files but no file loader`);
        }
        await Promise.all(linkedFiles.map(async (linkedFile) => {
            const { relativePath, materializedPath } = safeLinkedFilePath(directory, entry, linkedFile);
            const content = await source.loadFile?.(entry.id, relativePath);
            if (!content) {
                throw new Error(`Runtime skill "${entry.name}" linked file is registered but cannot be loaded: ${linkedFile.path}`);
            }
            const materializedContent = substituteRuntimeSkillVars(content.content, directory, workspaceRoot, skillsRoot);
            warnIfExceedsLoadSkillLimit(logger, entry, materializedPath, materializedContent);
            files.set(materializedPath, materializedContent);
        }));
        return { id: entry.id, name: entry.name, path, directory };
    }));
    const registry = materializedRegistry(source.registry, materialized);
    const registryPath = (0, posix_1.join)(skillsRoot, exports.SANDBOX_RUNTIME_SKILL_REGISTRY_FILE);
    files.set(registryPath, (0, workspace_file_content_1.stringifyWorkspaceJson)(registry));
    const manifest = {
        schemaVersion: exports.RUNTIME_SKILL_MANIFEST_SCHEMA_VERSION,
        skillsHash: source.registry.skillsHash,
    };
    const manifestPath = (0, posix_1.join)(skillsRoot, exports.RUNTIME_SKILL_MANIFEST_FILE);
    files.set(manifestPath, (0, workspace_file_content_1.stringifyWorkspaceJson)(manifest));
    const env = {
        [exports.N8N_WORKSPACE_DIR_ENV]: workspaceRoot,
        [exports.N8N_SKILLS_DIR_ENV]: skillsRoot,
    };
    return {
        rootDir: skillsRoot,
        registryPath,
        skills: materialized,
        env,
        source: createMaterializedRuntimeSkillSource(source, registry, materialized, workspaceRoot, skillsRoot),
        files,
        manifest,
        manifestPath,
        skillsHash: source.registry.skillsHash,
    };
}
async function materializeRuntimeSkillsIntoWorkspace({ source, workspace, root, logger, }) {
    if (source.registry.skills.length === 0)
        return undefined;
    return await (0, prebaked_workspace_bundle_1.materializeWorkspaceBundle)({
        workspace,
        resourceLabel: RUNTIME_SKILL_FILE_LABEL,
        logger,
        loadPrebaked: async () => await loadPrebakedRuntimeSkillsBundle({ source, workspace, root, logger }),
        buildBundle: async () => {
            const bundle = await buildRuntimeSkillWorkspaceBundle({ source, root, logger });
            if (!bundle) {
                throw new Error('Expected runtime skill bundle after registry validation');
            }
            return bundle;
        },
        materializedLogMessage: 'Materialized runtime skills into workspace',
        materializedLogContext: (bundle) => ({
            root,
            skillsRoot: bundle.rootDir,
            registryPath: bundle.registryPath,
            skillsHash: bundle.skillsHash,
            count: bundle.skills.length,
        }),
    });
}
function createLazyWorkspaceRuntimeSkillSource({ source, workspace, logger, }) {
    if (!workspace || source.registry.skills.length === 0)
        return source;
    const runtimeWorkspace = workspace;
    let materialized;
    let materializePromise;
    const workspaceSource = {
        registry: source.registry,
        prepare: async () => {
            await ensureMaterialized();
        },
        loadSkill: async (skillId) => await (await ensureSource()).loadSkill(skillId),
        ...(source.loadFile
            ? {
                loadFile: async (skillId, filePath) => {
                    const preparedSource = await ensureSource();
                    return (await preparedSource.loadFile?.(skillId, filePath)) ?? null;
                },
            }
            : {}),
    };
    async function ensureMaterialized() {
        if (materialized)
            return materialized;
        materializePromise ??= (async () => {
            const root = await (0, sandbox_1.getWorkspaceRoot)(runtimeWorkspace);
            const options = { source, workspace: runtimeWorkspace, root, logger };
            const result = (await loadPrebakedRuntimeSkillsBundle(options)) ??
                (await materializeRuntimeSkillsIntoWorkspace(options));
            if (result) {
                materialized = result;
                workspaceSource.registry = result.source.registry;
            }
            return result;
        })().catch((error) => {
            materializePromise = undefined;
            throw error;
        });
        return await materializePromise;
    }
    async function ensureSource() {
        return (await ensureMaterialized())?.source ?? source;
    }
    return workspaceSource;
}
//# sourceMappingURL=materialize-runtime-skills.js.map