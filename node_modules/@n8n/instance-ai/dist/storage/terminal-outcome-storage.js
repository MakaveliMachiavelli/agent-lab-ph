"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TerminalOutcomeStorage = void 0;
const zod_1 = require("zod");
const thread_patch_1 = require("./thread-patch");
const METADATA_KEY = 'instanceAiTerminalOutcomes';
const terminalOutcomeStatusSchema = zod_1.z.enum(['completed', 'failed', 'cancelled']);
const terminalOutcomeSchema = zod_1.z.object({
    id: zod_1.z.string(),
    threadId: zod_1.z.string(),
    runId: zod_1.z.string(),
    messageGroupId: zod_1.z.string().optional(),
    correlationId: zod_1.z.string().optional(),
    taskId: zod_1.z.string(),
    agentId: zod_1.z.string(),
    status: terminalOutcomeStatusSchema,
    userFacingMessage: zod_1.z.string(),
    createdAt: zod_1.z.string(),
    deliveredAt: zod_1.z.string().optional(),
});
class TerminalOutcomeStorage {
    memory;
    constructor(memory) {
        this.memory = memory;
    }
    async upsert(threadId, outcome) {
        await (0, thread_patch_1.patchThread)(this.memory, {
            threadId,
            update: ({ metadata = {} }) => {
                const current = parseOutcomes(metadata[METADATA_KEY]);
                return {
                    metadata: {
                        ...metadata,
                        [METADATA_KEY]: {
                            ...current,
                            [outcome.id]: outcome,
                        },
                    },
                };
            },
        });
    }
    async markDelivered(threadId, outcomeId, deliveredAt) {
        await (0, thread_patch_1.patchThread)(this.memory, {
            threadId,
            update: ({ metadata = {} }) => {
                const current = parseOutcomes(metadata[METADATA_KEY]);
                const outcome = current[outcomeId];
                if (!outcome)
                    return null;
                return {
                    metadata: {
                        ...metadata,
                        [METADATA_KEY]: {
                            ...current,
                            [outcomeId]: {
                                ...outcome,
                                deliveredAt,
                            },
                        },
                    },
                };
            },
        });
    }
    async getUndelivered(threadId) {
        const thread = await (0, thread_patch_1.getThread)(this.memory, threadId);
        const outcomes = parseOutcomes(thread?.metadata?.[METADATA_KEY]);
        return Object.values(outcomes).filter((outcome) => !outcome.deliveredAt);
    }
}
exports.TerminalOutcomeStorage = TerminalOutcomeStorage;
function parseOutcomes(raw) {
    if (!raw || typeof raw !== 'object')
        return {};
    const outcomes = {};
    for (const [key, value] of Object.entries(raw)) {
        const parsed = terminalOutcomeSchema.safeParse(value);
        if (parsed.success)
            outcomes[key] = parsed.data;
    }
    return outcomes;
}
//# sourceMappingURL=terminal-outcome-storage.js.map