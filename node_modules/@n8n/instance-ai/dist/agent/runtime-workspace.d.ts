import type { Agent, RuntimeSkillSource, Workspace } from '@n8n/agents';
export interface RuntimeWorkspaceCapabilities {
    workspace?: Workspace;
    runtimeSkills?: RuntimeSkillSource;
}
export declare function attachRuntimeWorkspaceCapabilities(agent: Agent, { workspace, runtimeSkills }: RuntimeWorkspaceCapabilities): Agent;
