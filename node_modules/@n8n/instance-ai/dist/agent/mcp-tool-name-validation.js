"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.McpToolNameValidationError = void 0;
exports.isSafeMcpIdentifierName = isSafeMcpIdentifierName;
exports.normalizeMcpToolName = normalizeMcpToolName;
exports.validateMcpToolName = validateMcpToolName;
exports.createClaimedToolNames = createClaimedToolNames;
exports.addSafeMcpTools = addSafeMcpTools;
const api_types_1 = require("@n8n/api-types");
class McpToolNameValidationError extends Error {
    toolName;
    source;
    constructor(message, toolName, source) {
        super(message);
        this.toolName = toolName;
        this.source = source;
        this.name = 'McpToolNameValidationError';
    }
}
exports.McpToolNameValidationError = McpToolNameValidationError;
const MCP_TOOL_NAME_PATTERN = /^[A-Za-z][A-Za-z0-9_-]{0,63}$/;
function isSafeMcpIdentifierName(name) {
    const normalizedName = name.normalize('NFKC');
    return (normalizedName === name &&
        MCP_TOOL_NAME_PATTERN.test(name) &&
        (0, api_types_1.isSafeObjectKey)(normalizedName.toLowerCase()));
}
function normalizeMcpToolName(name) {
    return name
        .normalize('NFKC')
        .toLowerCase()
        .replace(/[^a-z0-9]/g, '');
}
function validateMcpToolName(name, source) {
    if (!isSafeMcpIdentifierName(name)) {
        throw new McpToolNameValidationError(`MCP tool "${name}" from ${source} has an invalid name`, name, source);
    }
    return normalizeMcpToolName(name);
}
function createClaimedToolNames(names) {
    const claimed = new Map();
    for (const name of names) {
        claimed.set(normalizeMcpToolName(name), name);
    }
    return claimed;
}
function addSafeMcpTools(target, sourceTools, options) {
    for (const [name, tool] of sourceTools) {
        try {
            const normalizedName = validateMcpToolName(name, options.source);
            const claimedBy = options.claimedToolNames.get(normalizedName);
            if (claimedBy) {
                throw new McpToolNameValidationError(`MCP tool "${name}" from ${options.source} conflicts with "${claimedBy}"`, name, options.source);
            }
            options.claimedToolNames.set(normalizedName, name);
            target.set(name, tool);
        }
        catch (error) {
            if (error instanceof McpToolNameValidationError) {
                options.warn?.(error);
                continue;
            }
            throw error;
        }
    }
}
//# sourceMappingURL=mcp-tool-name-validation.js.map