"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EPHEMERAL_CACHE = exports.HAIKU_MODEL = exports.SONNET_MODEL = exports.Tool = void 0;
exports.resolveEvalModelConfig = resolveEvalModelConfig;
exports.createEvalAgent = createEvalAgent;
exports.extractText = extractText;
const agents_1 = require("@n8n/agents");
Object.defineProperty(exports, "Tool", { enumerable: true, get: function () { return agents_1.Tool; } });
const apply_agent_thinking_1 = require("../agent/apply-agent-thinking");
exports.SONNET_MODEL = 'anthropic/claude-sonnet-4-6';
exports.HAIKU_MODEL = 'anthropic/claude-haiku-4-5-20251001';
const PROVIDER_API_KEY_ENV = {
    anthropic: 'ANTHROPIC_API_KEY',
    google: 'GOOGLE_GENERATIVE_AI_API_KEY',
    openai: 'OPENAI_API_KEY',
    xai: 'XAI_API_KEY',
};
function getModelId(model) {
    const modelId = model ??
        process.env.N8N_INSTANCE_AI_EVAL_MODEL ??
        process.env.N8N_INSTANCE_AI_MODEL ??
        exports.SONNET_MODEL;
    return modelId;
}
function getApiKey(modelId) {
    const [provider] = modelId.split('/');
    const providerKeyEnv = PROVIDER_API_KEY_ENV[provider];
    const providerKey = providerKeyEnv ? process.env[providerKeyEnv] : undefined;
    const key = process.env.N8N_INSTANCE_AI_MODEL_API_KEY ??
        (provider === 'anthropic' ? process.env.N8N_AI_ANTHROPIC_KEY : undefined) ??
        providerKey;
    if (!key) {
        throw new Error(`Missing API key for eval model "${modelId}". Set N8N_INSTANCE_AI_MODEL_API_KEY${provider === 'anthropic'
            ? ' or N8N_AI_ANTHROPIC_KEY or ANTHROPIC_API_KEY'
            : providerKeyEnv
                ? ` or ${providerKeyEnv}`
                : ''} in your environment.`);
    }
    return key;
}
function getModelUrl() {
    const url = process.env.N8N_INSTANCE_AI_MODEL_URL?.trim();
    if (!url)
        return undefined;
    return url;
}
function resolveEvalModelConfig(model) {
    const modelId = getModelId(model);
    const [provider, ...rest] = modelId.split('/');
    const joinedProviderModelId = rest.join('/');
    let providerModelId = modelId;
    if (joinedProviderModelId.length > 0) {
        providerModelId = joinedProviderModelId;
    }
    return {
        modelId,
        provider,
        providerModelId,
        apiKey: getApiKey(modelId),
        url: getModelUrl(),
    };
}
exports.EPHEMERAL_CACHE = {
    anthropic: { cacheControl: { type: 'ephemeral' } },
};
const CACHE_PROVIDER_OPTS = {
    providerOptions: exports.EPHEMERAL_CACHE,
};
function resolveAgentModel(model, fallbackModelConfig) {
    try {
        const { modelId, apiKey, url } = resolveEvalModelConfig(model);
        return { id: modelId, apiKey, url };
    }
    catch (error) {
        if (fallbackModelConfig)
            return fallbackModelConfig;
        throw error;
    }
}
function createEvalAgent(name, options) {
    const model = resolveAgentModel(options.model, options.fallbackModelConfig);
    const agent = new agents_1.Agent(name).model(model);
    if (options.cache) {
        agent.instructions(options.instructions, CACHE_PROVIDER_OPTS);
    }
    else {
        agent.instructions(options.instructions);
    }
    (0, apply_agent_thinking_1.applyAgentThinking)(agent, model);
    return agent;
}
function extractText(result) {
    const texts = [];
    for (const msg of result.messages) {
        if (!('role' in msg) || msg.role !== 'assistant')
            continue;
        if (!('content' in msg) || !Array.isArray(msg.content))
            continue;
        for (const part of msg.content) {
            if (typeof part === 'object' &&
                part !== null &&
                'type' in part &&
                part.type === 'text' &&
                'text' in part) {
                texts.push(String(part.text));
            }
        }
    }
    return texts.join('');
}
//# sourceMappingURL=eval-agents.js.map