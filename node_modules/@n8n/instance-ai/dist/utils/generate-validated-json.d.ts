import type { ModelConfig } from '@n8n/agents';
import type { z } from 'zod';
export type ValidatedJsonResult<T> = {
    ok: true;
    data: T;
} | {
    ok: false;
    reason: 'generation_failed' | 'invalid_json' | 'schema_mismatch';
    issues?: z.ZodIssue[];
};
export interface GenerateValidatedJsonOptions<T> {
    model?: string;
    instructions: string;
    userText: string;
    schema: z.ZodType<T>;
    fallbackModelConfig?: ModelConfig;
}
export declare function generateValidatedJson<T>(agentName: string, options: GenerateValidatedJsonOptions<T>): Promise<ValidatedJsonResult<T>>;
