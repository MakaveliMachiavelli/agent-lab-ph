"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateValidatedJson = generateValidatedJson;
const eval_agents_1 = require("./eval-agents");
function stripMarkdownFences(text) {
    const trimmed = text.trim();
    const fencedMatch = /^```(?:json)?\s*([\s\S]*?)\s*```$/i.exec(trimmed);
    return fencedMatch ? fencedMatch[1].trim() : trimmed;
}
async function generateValidatedJson(agentName, options) {
    let text;
    try {
        const llm = (0, eval_agents_1.createEvalAgent)(agentName, {
            model: options.model,
            instructions: options.instructions,
            fallbackModelConfig: options.fallbackModelConfig,
        });
        const result = await llm.generate([
            { role: 'user', content: [{ type: 'text', text: options.userText }] },
        ]);
        text = (0, eval_agents_1.extractText)(result);
    }
    catch {
        return { ok: false, reason: 'generation_failed' };
    }
    let parsed;
    try {
        parsed = JSON.parse(stripMarkdownFences(text));
    }
    catch {
        return { ok: false, reason: 'invalid_json' };
    }
    const validated = options.schema.safeParse(parsed);
    if (!validated.success) {
        return { ok: false, reason: 'schema_mismatch', issues: validated.error.issues };
    }
    return { ok: true, data: validated.data };
}
//# sourceMappingURL=generate-validated-json.js.map