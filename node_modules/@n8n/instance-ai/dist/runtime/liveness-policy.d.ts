export type InstanceAiLivenessSurface = 'active-run' | 'suspended-run' | 'pending-confirmation' | 'background-task';
export type InstanceAiLivenessTimeoutReason = 'idle_timeout' | 'max_lifetime';
export interface InstanceAiLivenessPolicyConfig {
    confirmationTimeoutMs: number;
    backgroundTaskIdleTimeoutMs: number;
    backgroundTaskMaxLifetimeMs: number;
    activeRunIdleTimeoutMs: number;
    activeRunMaxLifetimeMs: number;
}
export declare const INSTANCE_AI_DEFAULT_LIVENESS_POLICY_CONFIG: {
    confirmationTimeoutMs: number;
    backgroundTaskIdleTimeoutMs: number;
    backgroundTaskMaxLifetimeMs: number;
    activeRunIdleTimeoutMs: number;
    activeRunMaxLifetimeMs: number;
};
export declare function createInstanceAiLivenessPolicyConfig(overrides?: Partial<Pick<InstanceAiLivenessPolicyConfig, 'confirmationTimeoutMs'>>): InstanceAiLivenessPolicyConfig;
export interface InstanceAiLivenessInput {
    surface: InstanceAiLivenessSurface;
    startedAt: number;
    lastActivityAt: number;
    now?: number;
}
export type InstanceAiLivenessDecision = {
    action: 'keep-alive';
} | {
    action: 'timeout';
    reason: InstanceAiLivenessTimeoutReason;
    surface: InstanceAiLivenessSurface;
    timeoutMs: number;
    elapsedMs: number;
    idleMs: number;
};
export declare class InstanceAiLivenessPolicy {
    private readonly config;
    constructor(config: InstanceAiLivenessPolicyConfig);
    hasEnabledTimeouts(): boolean;
    evaluate(input: InstanceAiLivenessInput): InstanceAiLivenessDecision;
    private getLimits;
}
