"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createOrchestratorRunControl = createOrchestratorRunControl;
exports.createOrchestratorRunControlForState = createOrchestratorRunControlForState;
function createOrchestratorRunControl(context, state = {}) {
    const control = {
        state,
        requestHandoff(reason) {
            state.handoffReason ??= reason;
        },
        getStopSignal() {
            return state.handoffReason ? { reason: state.handoffReason } : undefined;
        },
        shouldEmitTerminalOutcome(stopReason) {
            return stopReason === undefined;
        },
    };
    if (context) {
        context.requestRunHandoff = (reason) => control.requestHandoff(reason);
    }
    return control;
}
function createOrchestratorRunControlForState(state) {
    return createOrchestratorRunControl(undefined, state);
}
//# sourceMappingURL=orchestrator-run-control.js.map