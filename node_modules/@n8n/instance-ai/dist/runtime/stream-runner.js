"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.streamAgentRun = streamAgentRun;
exports.resumeAgentRun = resumeAgentRun;
const resumable_stream_executor_1 = require("./resumable-stream-executor");
const stream_helpers_1 = require("../utils/stream-helpers");
async function streamAgentRun(agent, input, streamOptions, options) {
    const result = await agent.stream(input, streamOptions);
    const stream = (0, resumable_stream_executor_1.normalizeStreamSource)(result);
    const agentRunId = typeof stream.runId === 'string' ? stream.runId : '';
    return await consumeStream(agent, stream, { ...options, agentRunId });
}
async function resumeAgentRun(agent, resumeData, resumeOptions, options) {
    const resumed = await (0, stream_helpers_1.resumeAgentStream)(agent, resumeData, resumeOptions);
    const stream = (0, resumable_stream_executor_1.normalizeStreamSource)(resumed);
    const agentRunId = (typeof stream.runId === 'string' && stream.runId) || options.agentRunId;
    return await consumeStream(agent, stream, { ...options, agentRunId });
}
async function consumeStream(agent, stream, options) {
    const result = await (0, resumable_stream_executor_1.executeResumableStream)({
        agent,
        stream,
        context: {
            threadId: options.threadId,
            runId: options.runId,
            agentId: options.agentId,
            eventBus: options.eventBus,
            signal: options.signal,
            logger: options.logger,
            onActivity: options.onActivity,
            stopSignal: options.stopSignal,
            outputRedaction: options.outputRedaction,
        },
        control: { mode: 'manual' },
        initialAgentRunId: options.agentRunId,
    });
    if (result.status === 'suspended' && result.suspension) {
        return {
            status: 'suspended',
            agentRunId: result.agentRunId,
            text: result.text,
            workSummary: result.workSummary,
            usage: result.usage,
            suspension: result.suspension,
            ...(result.confirmationEvent ? { confirmationEvent: result.confirmationEvent } : {}),
        };
    }
    return {
        status: result.status === 'cancelled'
            ? 'cancelled'
            : result.status === 'errored'
                ? 'errored'
                : 'completed',
        agentRunId: result.agentRunId,
        text: result.text,
        ...(result.error !== undefined ? { error: result.error } : {}),
        workSummary: result.workSummary,
        usage: result.usage,
        ...(result.stopReason ? { stopReason: result.stopReason } : {}),
    };
}
//# sourceMappingURL=stream-runner.js.map