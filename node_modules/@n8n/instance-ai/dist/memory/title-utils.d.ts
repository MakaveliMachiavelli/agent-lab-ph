import { type BuiltTelemetry, type Telemetry } from '@n8n/agents';
import type { ModelConfig } from '../types';
export declare function truncateToTitle(message: string): string;
export declare function generateTitleForRun(modelId: ModelConfig, userMessage: string, options?: {
    telemetry?: BuiltTelemetry | Telemetry;
}): Promise<string | null>;
