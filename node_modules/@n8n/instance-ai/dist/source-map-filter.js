"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const EMPTY_SOURCE_MAP = '{"version":3,"sources":[],"names":[],"mappings":""}';
const EMPTY_SOURCE_FILE = '\n';
function loadSourceMapSupport() {
    try {
        const mod = require('source-map-support');
        return mod;
    }
    catch {
        return undefined;
    }
}
const FILTERED_PATH_FRAGMENTS = [
    '/node_modules/@ai-sdk/',
    '/node_modules/langsmith/',
    '/node_modules/zod/',
    '/node_modules/ai/',
];
function isFilteredPath(source) {
    if (typeof source !== 'string')
        return false;
    const normalizedSource = source.replace(/\\/g, '/');
    for (const frag of FILTERED_PATH_FRAGMENTS) {
        if (normalizedSource.includes(frag))
            return true;
    }
    return false;
}
const sms = loadSourceMapSupport();
sms?.install({
    retrieveSourceMap(source) {
        return isFilteredPath(source) ? { url: source, map: EMPTY_SOURCE_MAP } : null;
    },
    retrieveFile(path) {
        return isFilteredPath(path) ? EMPTY_SOURCE_FILE : null;
    },
});
//# sourceMappingURL=source-map-filter.js.map