"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sanitizeDebugSnapshotValue = sanitizeDebugSnapshotValue;
exports.sanitizeDebugSnapshotRecord = sanitizeDebugSnapshotRecord;
const is_record_1 = require("@n8n/utils/is-record");
const scrub_secrets_1 = require("@n8n/utils/scrub-secrets");
const OMIT_KEYS = new Set(['abortSignal']);
const SENSITIVE_KEY_PATTERN = /(api[_-]?key|authorization|bearer|cookie|credentials?|password|secret|access[_-]?token|refresh[_-]?token|id[_-]?token|session[_-]?token|auth[_-]?token|(?:^|[._-])token$)/i;
function shouldOmitKey(key, parentKey) {
    if (OMIT_KEYS.has(key)) {
        return true;
    }
    if (key === 'body' && parentKey === 'response') {
        return true;
    }
    return false;
}
function redactSensitiveKey(key, value, seen) {
    if (SENSITIVE_KEY_PATTERN.test(key) && typeof value === 'string') {
        return '[redacted]';
    }
    return sanitizeDebugSnapshotValue(value, key, seen);
}
function sanitizeDebugSnapshotValue(value, keyHint, seen) {
    const seenObjects = seen ?? new WeakSet();
    if (value === undefined || value === null) {
        return value;
    }
    if (typeof value === 'string') {
        if (keyHint && SENSITIVE_KEY_PATTERN.test(keyHint)) {
            return '[redacted]';
        }
        return (0, scrub_secrets_1.scrubSecretsInText)(value);
    }
    if (typeof value === 'number' || typeof value === 'boolean') {
        return value;
    }
    if (typeof value === 'bigint') {
        return value.toString();
    }
    if (typeof value === 'function') {
        return `[function ${value.name || 'anonymous'}]`;
    }
    if (value instanceof AbortSignal) {
        return '[AbortSignal]';
    }
    if (value instanceof Date) {
        return value.toISOString();
    }
    if (value instanceof Error) {
        return {
            name: value.name,
            message: (0, scrub_secrets_1.scrubSecretsInText)(value.message),
        };
    }
    if (value instanceof Uint8Array) {
        return `[binary ${value.byteLength} bytes]`;
    }
    if (Array.isArray(value)) {
        if (seenObjects.has(value)) {
            return '[Circular]';
        }
        seenObjects.add(value);
        return value.map((entry) => sanitizeDebugSnapshotValue(entry, keyHint, seenObjects));
    }
    if ((0, is_record_1.isRecord)(value)) {
        if (seenObjects.has(value)) {
            return '[Circular]';
        }
        seenObjects.add(value);
        const sanitized = {};
        for (const [key, entryValue] of Object.entries(value)) {
            if (shouldOmitKey(key, keyHint)) {
                continue;
            }
            sanitized[key] = redactSensitiveKey(key, entryValue, seenObjects);
        }
        return sanitized;
    }
    if (typeof value === 'symbol') {
        return value.toString();
    }
    return '[unsupported value]';
}
function sanitizeDebugSnapshotRecord(value) {
    const sanitized = sanitizeDebugSnapshotValue(value);
    return (0, is_record_1.isRecord)(sanitized) ? sanitized : { value: sanitized };
}
//# sourceMappingURL=sanitize-debug-snapshot.js.map