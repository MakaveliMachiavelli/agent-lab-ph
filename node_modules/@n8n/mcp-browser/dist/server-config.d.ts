import type { Config } from './types';
export interface ServerOptions {
    config: Partial<Config>;
    transport: 'stdio' | 'http';
    host: string;
    port: number;
    authToken: string;
    authTokenGenerated: boolean;
}
export declare function parseServerOptions(argv?: string[]): ServerOptions;
