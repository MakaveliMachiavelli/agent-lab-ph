import type { SecretHit } from '../redaction/redact';
import type { HtmlProbeResult } from '../types';
export interface SensitivityOk {
    ok: true;
    sensitive: boolean;
    hits: SecretHit[];
}
export interface SensitivityErr {
    ok: false;
    error: string;
}
export type SensitivityResult = SensitivityOk | SensitivityErr;
export declare function analyzeHtmlSensitivity(probe: HtmlProbeResult): SensitivityResult;
