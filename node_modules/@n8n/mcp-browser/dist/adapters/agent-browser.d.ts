import { type ConnectionLostReason } from '../errors';
import type { Adapter, ClickOptions, ConnectConfig, ConsoleEntry, Cookie, ElementTarget, ModalState, NavigateResult, NetworkEntry, PageInfo, HtmlProbeResult, ResolvedConfig, ScreenshotOptions, ScrollOptions, SnapshotResult, TypeOptions, WaitOptions } from '../types';
export declare class AgentBrowserAdapter implements Adapter {
    readonly name = "agent-browser";
    private resolvedConfig;
    private relay?;
    private relayPort?;
    private cdpEndpoint?;
    private urlCache;
    private tabCache;
    onDisconnect?: (reason: ConnectionLostReason) => void;
    constructor(config: ResolvedConfig);
    private run;
    private refreshTabs;
    private switchToTab;
    private resolveTarget;
    private static assertSafeArg;
    private runAction;
    private navResult;
    private killSession;
    launch(config: ConnectConfig): Promise<void>;
    close(): Promise<void>;
    listTabs(): Promise<PageInfo[]>;
    listTabSessionIds(): Promise<string[]>;
    listTabIds(): Promise<string[]>;
    newPage(url?: string): Promise<PageInfo>;
    closePage(pageId: string): Promise<void>;
    focusPage(pageId: string): Promise<void>;
    navigate(pageId: string, url: string, _waitUntil?: 'load' | 'domcontentloaded' | 'networkidle'): Promise<NavigateResult>;
    back(pageId: string): Promise<NavigateResult>;
    forward(pageId: string): Promise<NavigateResult>;
    reload(pageId: string, _waitUntil?: 'load' | 'domcontentloaded' | 'networkidle'): Promise<NavigateResult>;
    click(pageId: string, target: ElementTarget, options?: ClickOptions): Promise<void>;
    type(pageId: string, target: ElementTarget, text: string, options?: TypeOptions): Promise<void>;
    select(pageId: string, target: ElementTarget, values: string[]): Promise<string[]>;
    hover(pageId: string, target: ElementTarget): Promise<void>;
    press(pageId: string, keys: string): Promise<void>;
    drag(_pageId: string, _from: ElementTarget, _to: ElementTarget): Promise<void>;
    scroll(pageId: string, target?: ElementTarget, options?: ScrollOptions): Promise<void>;
    upload(_pageId: string, _target: ElementTarget | undefined, _files: string[]): Promise<void>;
    dialog(_pageId: string, _action: 'accept' | 'dismiss', _text?: string): Promise<string>;
    snapshot(pageId: string, _target?: ElementTarget, interactive?: boolean): Promise<SnapshotResult>;
    probePageHtml(pageId: string): Promise<HtmlProbeResult>;
    screenshot(pageId: string, _target?: ElementTarget, _options?: ScreenshotOptions): Promise<string>;
    getText(pageId: string, target?: ElementTarget): Promise<string>;
    getContent(pageId: string, selector?: string): Promise<{
        html: string;
        url: string;
    }>;
    evaluate(pageId: string, script: string): Promise<unknown>;
    getConsole(_pageId: string, _level?: string, _clear?: boolean): Promise<ConsoleEntry[]>;
    getConsoleSummary(_pageId: string): {
        errors: number;
        warnings: number;
    };
    getModalStates(_pageId: string): ModalState[];
    getNetwork(_pageId: string, _filter?: string, _clear?: boolean): Promise<NetworkEntry[]>;
    pdf(_pageId: string, _options?: {
        format?: string;
        landscape?: boolean;
    }): Promise<{
        data: string;
        pages: number;
    }>;
    wait(pageId: string, options: WaitOptions): Promise<number>;
    waitForCompletion<T>(_pageId: string, action: () => Promise<T>): Promise<T>;
    getCookies(pageId: string, _url?: string): Promise<Cookie[]>;
    setCookies(pageId: string, cookies: Cookie[]): Promise<void>;
    clearCookies(pageId: string): Promise<void>;
    getStorage(pageId: string, kind: 'local' | 'session'): Promise<Record<string, string>>;
    setStorage(pageId: string, kind: 'local' | 'session', data: Record<string, string>): Promise<void>;
    clearStorage(pageId: string, kind: 'local' | 'session'): Promise<void>;
    getPageUrl(pageId: string): string | undefined;
    getElementValue(pageId: string, target: ElementTarget): Promise<string>;
}
