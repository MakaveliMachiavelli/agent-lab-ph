#!/usr/bin/env node
import { createServer, type IncomingMessage } from 'node:http';
import type { BrowserConnection } from './connection';
import type { Config } from './types';
export declare function buildAllowedHosts(host: string, port: number): string[] | undefined;
export declare function isAuthorized(req: IncomingMessage, expectedToken: string): boolean;
export declare function startHttpTransport(opts: {
    config: Partial<Config>;
    host: string;
    port: number;
    authToken: string;
}): Promise<{
    httpServer: ReturnType<typeof createServer>;
    activeConnections: Set<BrowserConnection>;
}>;
