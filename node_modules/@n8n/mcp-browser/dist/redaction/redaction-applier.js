"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.applyRedactions = applyRedactions;
const redact_1 = require("./redact");
function redactText(input, result, marker) {
    let output = input;
    for (const hit of result.hits) {
        output = (0, redact_1.replaceLiteral)(output, hit.value, marker(hit));
    }
    return output;
}
function isPlainObject(value) {
    if (value === null || typeof value !== 'object')
        return false;
    return Object.getPrototypeOf(value) === Object.prototype;
}
function redactValue(value, result, marker) {
    if (typeof value === 'string')
        return redactText(value, result, marker);
    if (Array.isArray(value))
        return value.map((entry) => redactValue(entry, result, marker));
    if (isPlainObject(value)) {
        const out = {};
        for (const [key, entry] of Object.entries(value)) {
            out[key] = redactValue(entry, result, marker);
        }
        return out;
    }
    return value;
}
function applyRedactions(result, sensitivity) {
    const marker = (0, redact_1.createRedactionMarkerFormatter)(sensitivity.hits);
    if (result.structuredContent !== undefined) {
        const redacted = redactValue(result.structuredContent, sensitivity, marker);
        if (isPlainObject(redacted))
            result.structuredContent = redacted;
    }
    for (const item of result.content) {
        if (item.type === 'text' && typeof item.text === 'string') {
            item.text = redactText(item.text, sensitivity, marker);
        }
    }
    return result;
}
//# sourceMappingURL=redaction-applier.js.map