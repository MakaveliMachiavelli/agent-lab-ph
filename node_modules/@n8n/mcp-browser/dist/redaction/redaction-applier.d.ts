import type { CallToolResult } from '../types';
import type { SensitivityOk } from '../sensitivity/analyze-html';
export declare function applyRedactions(result: CallToolResult, sensitivity: SensitivityOk): CallToolResult;
