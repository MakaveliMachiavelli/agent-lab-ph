import type { CDPRelayServer } from './cdp-relay';
import type { BrowserName, Config, ConnectResult, ConnectionState, ResolvedConfig } from './types';
export interface BrowserConnectionOptions {
    relay?: CDPRelayServer;
    cdpEndpoint?: string;
    cdpConnectHeaders?: Record<string, string>;
}
export declare class BrowserConnection {
    private state;
    private disconnectReason;
    private readonly config;
    private readonly externalRelay?;
    private readonly externalCdpEndpoint?;
    private readonly cdpConnectHeaders?;
    private pendingAdapter;
    constructor(userConfig?: Partial<Config>, options?: BrowserConnectionOptions);
    connect(overrideBrowser?: BrowserName): Promise<ConnectResult>;
    disconnect(): Promise<void>;
    getConnection(): ConnectionState;
    get isConnected(): boolean;
    shutdown(): Promise<void>;
    getResolvedConfig(): ResolvedConfig;
    getAvailableBrowsers(): BrowserName[];
    private requireBrowserAvailable;
    private createAdapter;
}
