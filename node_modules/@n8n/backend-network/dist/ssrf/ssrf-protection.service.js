"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SsrfProtectionService = void 0;
const backend_common_1 = require("@n8n/backend-common");
const config_1 = require("@n8n/config");
const di_1 = require("@n8n/di");
const ensure_error_1 = require("@n8n/utils/errors/ensure-error");
const result_1 = require("@n8n/utils/result");
const node_assert_1 = __importDefault(require("node:assert"));
const node_net_1 = require("node:net");
const dns_1 = require("../dns");
const hostname_matcher_1 = require("./hostname-matcher");
const ip_range_builder_1 = require("./ip-range-builder");
const ssrf_blocked_hostname_error_1 = require("./ssrf-blocked-hostname.error");
const ssrf_blocked_ip_error_1 = require("./ssrf-blocked-ip.error");
let SsrfProtectionService = class SsrfProtectionService {
    constructor(ssrfConfig, dnsResolver, logger) {
        this.ssrfConfig = ssrfConfig;
        this.dnsResolver = dnsResolver;
        this.events = new backend_common_1.TypedEmitter();
        this.logger = logger.scoped('ssrf-protection');
        const blocked = (0, ip_range_builder_1.buildIpRangeList)(this.ssrfConfig.blockedIpRanges);
        for (const issue of blocked.issues) {
            this.logger.warn(`Invalid value '${issue.entry}' in N8N_SSRF_BLOCKED_IP_RANGES: ${issue.error}`);
        }
        this.blockedIps = blocked.list;
        const allowed = (0, ip_range_builder_1.buildIpRangeList)(this.ssrfConfig.allowedIpRanges);
        for (const issue of allowed.issues) {
            this.logger.warn(`Invalid value '${issue.entry}' in N8N_SSRF_ALLOWED_IP_RANGES: ${issue.error}`);
        }
        this.allowedIps = allowed.list;
        this.allowedHostnameMatcher = new hostname_matcher_1.HostnameMatcher(this.ssrfConfig.allowedHostnames);
        this.blockedHostnameMatcher = new hostname_matcher_1.HostnameMatcher(this.ssrfConfig.blockedHostnames);
    }
    async validateUrl(url) {
        const parsed = this.tryParseUrl(url);
        if (!parsed) {
            this.events.emit('ssrf.blocked', {
                phase: 'pre_flight',
                reason: 'invalid_url',
                durationMs: 0,
            });
            return (0, result_1.createResultError)(new Error(`Invalid URL: ${String(url)}`));
        }
        const result = await this.withEvents('pre_flight', async () => await this.lookupAndValidate(parsed.hostname, { all: true }));
        return result.ok ? (0, result_1.createResultOk)(undefined) : result;
    }
    validateIp(ip) {
        const family = this.getIpFamily(ip);
        (0, node_assert_1.default)(family !== null, `Invalid IP address: ${ip}`);
        if (this.allowedIps.check(ip, family)) {
            return (0, result_1.createResultOk)(undefined);
        }
        if (this.blockedIps.check(ip, family)) {
            return (0, result_1.createResultError)(new ssrf_blocked_ip_error_1.SsrfBlockedIpError(ip));
        }
        return (0, result_1.createResultOk)(undefined);
    }
    validateConnectionHost(host) {
        const ip = this.normalizeIpInHostname(host);
        if (!(0, node_net_1.isIP)(ip)) {
            return (0, result_1.createResultOk)(undefined);
        }
        return this.withEvents('connect_time', () => this.validateIp(ip));
    }
    createSecureLookup() {
        return async (hostname, options, onResult) => {
            try {
                const resolved = await this.secureLookupAsync(hostname, options);
                if (options.all) {
                    onResult(null, resolved);
                    return;
                }
                const first = resolved[0];
                onResult(null, first.address, first.family);
            }
            catch (error) {
                onResult((0, ensure_error_1.ensureError)(error), options.all ? [] : '', undefined);
            }
        };
    }
    validateRedirectSync(url) {
        const parsed = this.tryParseUrl(url);
        if (!parsed)
            return;
        const { hostname } = parsed;
        if (this.allowedHostnameMatcher.matches(hostname))
            return;
        if (this.blockedHostnameMatcher.matches(hostname)) {
            const error = new ssrf_blocked_hostname_error_1.SsrfBlockedHostnameError(hostname);
            this.withEvents('redirect', () => (0, result_1.createResultError)(error));
            throw error;
        }
        const cleanIp = this.normalizeIpInHostname(hostname);
        if ((0, node_net_1.isIP)(cleanIp)) {
            const result = this.withEvents('redirect', () => this.validateIp(cleanIp));
            if (!result.ok) {
                throw result.error;
            }
        }
    }
    normalizeIpInHostname(hostname) {
        return hostname.startsWith('[') && hostname.endsWith(']') ? hostname.slice(1, -1) : hostname;
    }
    async secureLookupAsync(hostname, options) {
        const result = await this.withEvents('connect_time', async () => await this.lookupAndValidate(hostname, options));
        if (!result.ok) {
            throw result.error;
        }
        return result.result;
    }
    async lookupAndValidate(hostname, options) {
        const cleanIp = this.normalizeIpInHostname(hostname);
        const ipFamily = (0, node_net_1.isIP)(cleanIp);
        if (ipFamily) {
            const result = this.validateIp(cleanIp);
            if (!result.ok) {
                return result;
            }
            return (0, result_1.createResultOk)([
                {
                    address: cleanIp,
                    family: ipFamily,
                },
            ]);
        }
        const allowedByName = this.allowedHostnameMatcher.matches(hostname);
        if (!allowedByName && this.blockedHostnameMatcher.matches(hostname)) {
            return (0, result_1.createResultError)(new ssrf_blocked_hostname_error_1.SsrfBlockedHostnameError(hostname));
        }
        const resolved = await this.dnsResolver.lookup(hostname, options);
        (0, node_assert_1.default)(resolved.length > 0, `DNS lookup for ${hostname} returned no results`);
        if (allowedByName) {
            return (0, result_1.createResultOk)(resolved);
        }
        for (const ip of resolved) {
            const result = this.validateIp(ip.address);
            if (!result.ok) {
                return result;
            }
        }
        return (0, result_1.createResultOk)(resolved);
    }
    withEvents(phase, fn) {
        const start = performance.now();
        const result = fn();
        const emitAndReturn = (result) => {
            const durationMs = performance.now() - start;
            if (result.ok) {
                this.events.emit('ssrf.allowed', { phase, durationMs });
            }
            else {
                this.events.emit('ssrf.blocked', {
                    phase,
                    reason: this.toReason(result.error),
                    durationMs,
                });
            }
            return result;
        };
        const emitAndRethrow = (error) => {
            emitAndReturn((0, result_1.createResultError)((0, ensure_error_1.ensureError)(error)));
            throw error;
        };
        return result instanceof Promise
            ? result.then(emitAndReturn, emitAndRethrow)
            : emitAndReturn(result);
    }
    toReason(error) {
        if (error instanceof ssrf_blocked_ip_error_1.SsrfBlockedIpError) {
            return 'blocked_ip';
        }
        if (error instanceof ssrf_blocked_hostname_error_1.SsrfBlockedHostnameError) {
            return 'blocked_hostname';
        }
        return 'dns_error';
    }
    tryParseUrl(url) {
        try {
            return url instanceof URL ? url : new URL(url);
        }
        catch {
            return null;
        }
    }
    getIpFamily(ip) {
        const version = (0, node_net_1.isIP)(ip);
        if (version === 4)
            return 'ipv4';
        if (version === 6)
            return 'ipv6';
        return null;
    }
};
exports.SsrfProtectionService = SsrfProtectionService;
exports.SsrfProtectionService = SsrfProtectionService = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [config_1.SsrfProtectionConfig, dns_1.DnsResolver, backend_common_1.Logger])
], SsrfProtectionService);
//# sourceMappingURL=ssrf-protection.service.js.map