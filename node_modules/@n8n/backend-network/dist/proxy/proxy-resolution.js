"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveProxyUrl = resolveProxyUrl;
exports.createHttpProxyAgent = createHttpProxyAgent;
exports.createHttpsProxyAgent = createHttpsProxyAgent;
exports.hasProxyEnvironmentVariables = hasProxyEnvironmentVariables;
const http_1 = __importDefault(require("http"));
const https_1 = __importDefault(require("https"));
const proxy_from_env_1 = require("proxy-from-env");
const proxied_agents_1 = require("./proxied-agents");
function resolveProxyUrl(targetUrl, fallbackUrl) {
    return (0, proxy_from_env_1.getProxyForUrl)(targetUrl ?? fallbackUrl ?? '') || undefined;
}
function createHttpProxyAgent(targetUrl, customProxyUrl, options) {
    const proxyUrl = customProxyUrl ?? (0, proxy_from_env_1.getProxyForUrl)(targetUrl);
    if (proxyUrl) {
        return (0, proxied_agents_1.createProxiedHttpAgent)(proxyUrl, options);
    }
    return new http_1.default.Agent(options);
}
function createHttpsProxyAgent(targetUrl, customProxyUrl, options) {
    const proxyUrl = customProxyUrl ?? (0, proxy_from_env_1.getProxyForUrl)(targetUrl);
    if (proxyUrl) {
        return (0, proxied_agents_1.createProxiedHttpsAgent)(proxyUrl, options);
    }
    return new https_1.default.Agent(options);
}
function hasProxyEnvironmentVariables() {
    return [
        process.env.HTTP_PROXY,
        process.env.http_proxy,
        process.env.HTTPS_PROXY,
        process.env.https_proxy,
        process.env.ALL_PROXY,
        process.env.all_proxy,
    ].some((value) => Boolean(value));
}
//# sourceMappingURL=proxy-resolution.js.map