"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.isSupportedProxyUrl = isSupportedProxyUrl;
exports.buildNodeAgents = buildNodeAgents;
const n8n_workflow_1 = require("n8n-workflow");
const node_http_1 = __importDefault(require("node:http"));
const node_https_1 = __importDefault(require("node:https"));
const connection_guards_1 = require("./connection-guards");
const env_proxy_http_agent_1 = require("./env-proxy-http-agent");
const env_proxy_https_agent_1 = require("./env-proxy-https-agent");
const proxied_agents_1 = require("../proxy/proxied-agents");
function isSupportedProxyUrl(value) {
    return (value !== null &&
        value !== undefined &&
        (value.startsWith('http://') || value.startsWith('https://')));
}
function buildNodeAgents(proxy, ssrf, agentOptions) {
    if (agentOptions?.lookup) {
        throw new n8n_workflow_1.UnexpectedError('`agentOptions.lookup` is not supported: DNS resolution is managed by the SSRF policy. Remove it from `agentOptions`.');
    }
    const lookup = ssrf !== 'disabled' ? ssrf.createSecureLookup() : undefined;
    if (proxy === false) {
        return applyConnectionGuard({
            httpAgent: new node_http_1.default.Agent({ ...agentOptions, lookup }),
            httpsAgent: new node_https_1.default.Agent({ ...agentOptions, lookup }),
        }, ssrf);
    }
    if (proxy === 'env') {
        return applyConnectionGuard({
            httpAgent: new env_proxy_http_agent_1.EnvProxyHttpAgent(lookup, agentOptions),
            httpsAgent: new env_proxy_https_agent_1.EnvProxyHttpsAgent(lookup, agentOptions),
        }, ssrf);
    }
    const agents = {
        httpAgent: (0, proxied_agents_1.createProxiedHttpAgent)(proxy, { ...agentOptions, lookup }),
        httpsAgent: (0, proxied_agents_1.createProxiedHttpsAgent)(proxy, { ...agentOptions, lookup }),
    };
    if (ssrf !== 'disabled') {
        (0, connection_guards_1.installProxyConnectionGuard)(agents.httpAgent, ssrf);
        (0, connection_guards_1.installProxyConnectionGuard)(agents.httpsAgent, ssrf);
    }
    return agents;
}
function applyConnectionGuard(agents, ssrf) {
    if (ssrf !== 'disabled') {
        (0, connection_guards_1.installConnectionGuard)(agents.httpAgent, ssrf);
        (0, connection_guards_1.installConnectionGuard)(agents.httpsAgent, ssrf);
    }
    return agents;
}
//# sourceMappingURL=node-agents.js.map