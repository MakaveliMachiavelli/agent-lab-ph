"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.UnknownConnectionHostError = void 0;
exports.installProxyConnectionGuard = installProxyConnectionGuard;
exports.installConnectionGuard = installConnectionGuard;
const n8n_workflow_1 = require("n8n-workflow");
class UnknownConnectionHostError extends n8n_workflow_1.UnexpectedError {
    constructor() {
        super('Cannot determine the host for this connection');
    }
}
exports.UnknownConnectionHostError = UnknownConnectionHostError;
function connectionHostError(ssrf, options) {
    const host = options.host ?? options.hostname ?? undefined;
    if (typeof host !== 'string' || host.length === 0) {
        return new UnknownConnectionHostError();
    }
    const result = ssrf.validateConnectionHost(host);
    return result.ok ? undefined : result.error;
}
function installProxyConnectionGuard(agent, ssrf) {
    const connect = agent.connect.bind(agent);
    agent.connect = async (req, opts) => {
        const error = connectionHostError(ssrf, agent.connectOpts);
        if (error) {
            throw error;
        }
        return await connect(req, opts);
    };
}
function installConnectionGuard(target, ssrf) {
    const createConnection = target.createConnection.bind(target);
    target.createConnection = (options, onConnect) => {
        const error = connectionHostError(ssrf, options);
        if (error) {
            if (onConnect) {
                onConnect(error);
                return undefined;
            }
            throw error;
        }
        return createConnection(options, onConnect);
    };
}
//# sourceMappingURL=connection-guards.js.map