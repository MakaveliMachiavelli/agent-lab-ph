"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createFakeOutboundHttp = createFakeOutboundHttp;
function resolvesStatus(status, ignore) {
    if (ignore === true)
        return true;
    if (typeof ignore === 'object')
        return !ignore.except.includes(status);
    return status >= 200 && status < 300;
}
const isAbsoluteUrl = (url) => /^[a-z][a-z\d+\-.]*:\/\//i.test(url);
function combineUrls(baseURL, url) {
    return isAbsoluteUrl(url) ? url : `${baseURL.replace(/\/+$/, '')}/${url.replace(/^\/+/, '')}`;
}
function applyClientDefaults(options, request) {
    if (!options)
        return request;
    const resolved = { ...request };
    if (options.baseURL !== undefined && request.baseURL === undefined) {
        resolved.url = combineUrls(options.baseURL, request.url);
    }
    if (options.headers !== undefined) {
        const defaults = typeof options.headers === 'function' ? options.headers() : options.headers;
        const merged = { ...defaults, ...request.headers };
        for (const key of Object.keys(merged)) {
            if (merged[key] === undefined)
                delete merged[key];
        }
        resolved.headers = merged;
    }
    return resolved;
}
function respondWith(routes) {
    const consumed = new Set();
    return async (options) => {
        const { pathname } = new URL(options.url);
        const matching = routes.filter((r) => r.pathname === pathname && (r.method ?? 'GET') === (options.method ?? 'GET'));
        const route = matching.find((r) => !consumed.has(r)) ?? matching[matching.length - 1];
        if (!route) {
            throw new Error(`Unexpected request: ${options.method ?? 'GET'} ${options.url}`);
        }
        consumed.add(route);
        if (route.networkError) {
            const error = new Error('connect ECONNREFUSED');
            error.code = route.networkError;
            throw error;
        }
        const status = route.status ?? 200;
        if (!resolvesStatus(status, options.ignoreHttpStatusErrors)) {
            const error = new Error(`Request failed with status ${status}`);
            error.response = { status };
            throw error;
        }
        if (options.returnFullResponse) {
            return { statusCode: status, body: route.body, headers: {} };
        }
        return route.body;
    };
}
function createFakeOutboundHttp(routes, fn) {
    const stub = respondWith(routes);
    const httpRequest = fn(stub);
    const requests = fn((options) => ({
        request: (async (request) => await httpRequest(applyClientDefaults(options, request))),
        requestLegacy: () => {
            throw new Error('requestLegacy is not supported by the fake OutboundHttp');
        },
    }));
    const outboundHttp = {
        requests,
        transport: () => {
            throw new Error('transport is not supported by the fake OutboundHttp');
        },
    };
    return { outboundHttp, httpRequest, requests };
}
//# sourceMappingURL=fake-outbound-http.js.map