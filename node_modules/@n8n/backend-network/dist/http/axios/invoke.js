"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.invokeAxios = invokeAxios;
const axios_1 = __importDefault(require("axios"));
const utils_1 = require("./utils");
async function invokeAxios(axiosConfig, authSendImmediately) {
    const challengeAuth = authSendImmediately === false ? axiosConfig.auth : undefined;
    if (challengeAuth) {
        delete axiosConfig.auth;
    }
    try {
        return await (0, axios_1.default)(axiosConfig);
    }
    catch (error) {
        if (authSendImmediately !== false || !(error instanceof axios_1.default.AxiosError)) {
            throw error;
        }
        const { response } = error;
        const wwwAuthenticate = response?.headers['www-authenticate'];
        if (response?.status !== 401 ||
            typeof wwwAuthenticate !== 'string' ||
            !wwwAuthenticate.includes('nonce')) {
            throw error;
        }
        axiosConfig = (0, utils_1.digestAuthAxiosConfig)(axiosConfig, response, challengeAuth);
        return await (0, axios_1.default)(axiosConfig);
    }
}
//# sourceMappingURL=invoke.js.map