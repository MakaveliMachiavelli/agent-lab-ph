"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EnvProxyRouter = void 0;
const backend_common_1 = require("@n8n/backend-common");
const di_1 = require("@n8n/di");
const proxy_from_env_1 = require("proxy-from-env");
const MAX_CACHED_AGENTS = 64;
class EnvProxyRouter {
    constructor(scheme, defaultPort, createProxyAgent) {
        this.scheme = scheme;
        this.defaultPort = defaultPort;
        this.createProxyAgent = createProxyAgent;
        this.proxyCache = new Map();
        this.cacheLimitWarned = false;
    }
    resolve(options) {
        const hostname = String(options.hostname ?? options.host ?? 'localhost');
        const port = this.resolvePort(options.port);
        const portSuffix = port === this.defaultPort ? '' : `:${port}`;
        const proxyUrl = (0, proxy_from_env_1.getProxyForUrl)(`${this.scheme}://${hostname}${portSuffix}`);
        if (!proxyUrl) {
            return undefined;
        }
        const cached = this.proxyCache.get(proxyUrl);
        if (cached) {
            return cached;
        }
        const agent = this.createProxyAgent(proxyUrl);
        this.cacheAgent(proxyUrl, agent);
        return agent;
    }
    cacheAgent(proxyUrl, agent) {
        if (this.proxyCache.size >= MAX_CACHED_AGENTS) {
            if (!this.cacheLimitWarned) {
                this.cacheLimitWarned = true;
                di_1.Container.get(backend_common_1.Logger).warn(`${this.scheme} proxy agent cache reached its limit of ${MAX_CACHED_AGENTS} entries; not caching further proxy agents. This is unexpected and likely indicates a misconfiguration.`);
            }
        }
        else {
            this.proxyCache.set(proxyUrl, agent);
        }
    }
    resolvePort(rawPort) {
        const parsed = typeof rawPort === 'string' ? parseInt(rawPort, 10) : rawPort;
        if (Number.isInteger(parsed)) {
            return parsed;
        }
        if (rawPort !== undefined && rawPort !== null) {
            di_1.Container.get(backend_common_1.Logger).warn(`Unparseable port "${String(rawPort)}" for ${this.scheme} proxy routing, falling back to default port ${this.defaultPort}`);
        }
        return this.defaultPort;
    }
}
exports.EnvProxyRouter = EnvProxyRouter;
//# sourceMappingURL=env-proxy-router.js.map