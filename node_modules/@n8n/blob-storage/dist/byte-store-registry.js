"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ByteStoreRegistry = void 0;
const n8n_workflow_1 = require("n8n-workflow");
class ByteStoreRegistry {
    constructor(byteStores) {
        this.byteStores = new Map();
        for (const [loc, store] of Object.entries(byteStores)) {
            if (store)
                this.byteStores.set(loc, store);
        }
    }
    register(loc, store) {
        this.byteStores.set(loc, store);
    }
    has(loc) {
        return this.byteStores.has(loc);
    }
    find(loc) {
        return this.byteStores.get(loc);
    }
    get(loc) {
        const store = this.byteStores.get(loc);
        if (!store) {
            throw new n8n_workflow_1.UnexpectedError(`Byte store for location "${loc}" is not configured.`);
        }
        return store;
    }
}
exports.ByteStoreRegistry = ByteStoreRegistry;
//# sourceMappingURL=byte-store-registry.js.map