import { Logger } from '@n8n/backend-common';
import { Readable } from 'node:stream';
import { AzureBlobConfig } from './azure-blob.config';
import type { BlobMetadata, PreWriteBlobMetadata } from '../types';
export declare class AzureBlobService {
    private readonly logger;
    private readonly config;
    private readonly containerClient;
    private isReady;
    constructor(logger: Logger, config: AzureBlobConfig);
    private buildContainerClient;
    init(): Promise<void>;
    checkConnection(): Promise<void>;
    put(blobName: string, body: Buffer, metadata?: PreWriteBlobMetadata): Promise<void>;
    get(blobName: string, opts: {
        mode: 'buffer';
    }): Promise<Buffer>;
    get(blobName: string, opts: {
        mode: 'stream';
        chunkSize?: number;
    }): Promise<Readable>;
    getMetadata(blobName: string): Promise<BlobMetadata>;
    delete(blobName: string): Promise<void>;
    private handleError;
}
