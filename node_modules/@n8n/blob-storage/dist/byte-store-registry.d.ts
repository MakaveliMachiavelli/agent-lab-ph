import type { ByteStore, StorageLocation } from './types';
export declare class ByteStoreRegistry {
    private readonly byteStores;
    constructor(byteStores: Partial<Record<StorageLocation, ByteStore>>);
    register(loc: StorageLocation, store: ByteStore): void;
    has(loc: StorageLocation): boolean;
    find(loc: StorageLocation): ByteStore | undefined;
    get(loc: StorageLocation): ByteStore;
}
