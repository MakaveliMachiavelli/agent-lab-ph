"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.readPoolStats = readPoolStats;
function readPoolStats(dataSource) {
    switch (dataSource.options.type) {
        case 'postgres': {
            const pool = dataSource.driver.master;
            if (!pool)
                return undefined;
            return {
                active: pool.totalCount - pool.idleCount,
                idle: pool.idleCount,
                waiting: pool.waitingCount,
            };
        }
        case 'sqlite-pooled':
            return dataSource.driver.getPoolStats();
        default:
            return undefined;
    }
}
//# sourceMappingURL=db-pool-stats.js.map