"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.OLDEST_COMPATIBILITY_POSTGRES_MAJOR = exports.OLDEST_SUPPORTED_POSTGRES_MAJOR = void 0;
exports.getPostgresVersionWarning = getPostgresVersionWarning;
exports.OLDEST_SUPPORTED_POSTGRES_MAJOR = 17;
exports.OLDEST_COMPATIBILITY_POSTGRES_MAJOR = 16;
function parsePostgresMajor(version) {
    const major = Number.parseInt(version.split('.')[0], 10);
    return Number.isInteger(major) ? major : null;
}
function getPostgresVersionWarning(version) {
    const major = parsePostgresMajor(version);
    if (major === null)
        return null;
    if (major < exports.OLDEST_COMPATIBILITY_POSTGRES_MAJOR) {
        return `Postgres ${major} is not supported. n8n supports Postgres ${exports.OLDEST_SUPPORTED_POSTGRES_MAJOR} and newer, with ${exports.OLDEST_COMPATIBILITY_POSTGRES_MAJOR} on compatibility support. Upgrade to Postgres ${exports.OLDEST_SUPPORTED_POSTGRES_MAJOR} or newer.`;
    }
    if (major < exports.OLDEST_SUPPORTED_POSTGRES_MAJOR) {
        return `Postgres ${major} is outside the supported range and receives compatibility support only. Upgrade to Postgres ${exports.OLDEST_SUPPORTED_POSTGRES_MAJOR} or newer.`;
    }
    return null;
}
//# sourceMappingURL=postgres-version-policy.js.map