import { type Logger } from '@n8n/backend-common';
import type { DatabaseConfig } from '@n8n/config';
import type { DataSource } from '@n8n/typeorm';
import type { ErrorReporter } from 'n8n-core';
import type { DbConnectionMetrics } from './db-connection-metrics';
export declare class DbConnectionMonitor {
    private readonly dataSource;
    private readonly onConnectedChange;
    private readonly databaseConfig;
    private readonly logger;
    private readonly errorReporter;
    private readonly dbConnectionMetrics;
    private pingTimer;
    private consecutiveFailures;
    private recovering;
    private connected;
    private stopped;
    private stopAbortController;
    private pendingRecovery;
    private resolvePendingRecovery;
    private recoveryPromise;
    private liveObtainMasterConnection;
    constructor(dataSource: DataSource, onConnectedChange: (connected: boolean) => void, databaseConfig: DatabaseConfig, logger: Logger, errorReporter: ErrorReporter, dbConnectionMetrics: DbConnectionMetrics, initialConnected?: boolean);
    start(): void;
    stop(): Promise<void>;
    private scheduleNextPing;
    private ping;
    private runPing;
    private raceTimeout;
    private safeDestroyClient;
    private recoverDataSource;
    private destroyDataSource;
    private forceClosePostgresPool;
    private get isPostgres();
    private get recoveryEnabled();
    private get postgresDriver();
    private attachPoolErrorHandler;
    private wrapConnectionAcquisition;
    private acquireConnection;
    private timeAcquisition;
    private awaitRecovery;
    private startRecovery;
    private markRecoveryPending;
    private stopRecovery;
    private clearPendingRecovery;
    private isRecoverableConnectionError;
    private setConnected;
}
