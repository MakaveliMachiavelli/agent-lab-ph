"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.EvaluationConfigRepository = void 0;
const di_1 = require("@n8n/di");
const typeorm_1 = require("@n8n/typeorm");
const evaluation_config_ee_1 = require("../entities/evaluation-config.ee");
let EvaluationConfigRepository = class EvaluationConfigRepository extends typeorm_1.Repository {
    constructor(dataSource) {
        super(evaluation_config_ee_1.EvaluationConfig, dataSource.manager);
    }
    async listByWorkflowId(workflowId) {
        return await this.find({ where: { workflowId }, order: { createdAt: 'ASC' } });
    }
    async findByIdAndWorkflowId(id, workflowId) {
        return await this.findOne({ where: { id, workflowId } });
    }
    async createForWorkflow(id, workflowId, payload) {
        const entity = this.create({
            id,
            workflowId,
            status: 'valid',
            invalidReason: null,
            ...payload,
        });
        return await this.save(entity);
    }
    async updateForWorkflow(id, workflowId, payload) {
        const existing = await this.findByIdAndWorkflowId(id, workflowId);
        if (!existing)
            return null;
        Object.assign(existing, payload, { status: 'valid', invalidReason: null });
        return await this.save(existing);
    }
    async deleteByIdAndWorkflowId(id, workflowId) {
        const result = await this.delete({ id, workflowId });
        return result.affected ?? 0;
    }
    async markInvalid(id, invalidReason) {
        await this.update({ id }, { status: 'invalid', invalidReason });
    }
    async countDistinctWorkflowsWithConfigs() {
        return await this.createQueryBuilder('evaluation_config')
            .select('evaluation_config.workflowId')
            .distinct(true)
            .getCount();
    }
};
exports.EvaluationConfigRepository = EvaluationConfigRepository;
exports.EvaluationConfigRepository = EvaluationConfigRepository = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [typeorm_1.DataSource])
], EvaluationConfigRepository);
//# sourceMappingURL=evaluation-config.repository.js.map