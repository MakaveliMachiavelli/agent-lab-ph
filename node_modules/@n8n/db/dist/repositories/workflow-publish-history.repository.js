"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.WorkflowPublishHistoryRepository = void 0;
const di_1 = require("@n8n/di");
const typeorm_1 = require("@n8n/typeorm");
const entities_1 = require("../entities");
let WorkflowPublishHistoryRepository = class WorkflowPublishHistoryRepository extends typeorm_1.Repository {
    constructor(dataSource) {
        super(entities_1.WorkflowPublishHistory, dataSource.manager);
    }
    async addRecord({ workflowId, versionId, event, userId, }, trx) {
        const repository = trx ? trx.getRepository(entities_1.WorkflowPublishHistory) : this;
        await repository.insert({
            workflowId,
            versionId,
            event,
            userId,
        });
    }
    async getPublishedVersions(workflowId) {
        return await this.manager
            .createQueryBuilder(entities_1.WorkflowPublishHistory, 'wph')
            .select('wph.versionId')
            .distinct(true)
            .where('wph.workflowId = :workflowId', { workflowId })
            .getMany();
    }
    async getVersionPublicationStates(workflowId, versionIds) {
        const states = new Map();
        const requested = [...new Set(versionIds)];
        if (requested.length === 0) {
            return states;
        }
        const [versions, activatedVersions, newestActivated] = await Promise.all([
            this.manager.find(entities_1.WorkflowHistory, {
                where: { workflowId, versionId: (0, typeorm_1.In)(requested) },
                select: ['versionId', 'createdAt'],
            }),
            this.manager.find(entities_1.WorkflowPublishHistory, {
                where: { workflowId, versionId: (0, typeorm_1.In)(requested), event: 'activated' },
                select: ['versionId'],
            }),
            this.manager
                .createQueryBuilder(entities_1.WorkflowHistory, 'history')
                .innerJoin(entities_1.WorkflowPublishHistory, 'publishHistory', 'publishHistory.workflowId = history.workflowId AND publishHistory.versionId = history.versionId')
                .where('history.workflowId = :workflowId', { workflowId })
                .andWhere('publishHistory.event = :event', { event: 'activated' })
                .select(['history.versionId', 'history.createdAt'])
                .orderBy('history.createdAt', 'DESC')
                .take(1)
                .getOne(),
        ]);
        const createdAtByVersionId = new Map(versions.map((version) => [version.versionId, version.createdAt]));
        const activatedVersionIds = activatedVersions
            .map((record) => record.versionId)
            .filter((id) => id !== null);
        const activatedVersionIdSet = new Set(activatedVersionIds);
        for (const versionId of requested) {
            const createdAt = createdAtByVersionId.get(versionId);
            if (!createdAt) {
                states.set(versionId, 'unknown');
            }
            else if (activatedVersionIdSet.has(versionId)) {
                states.set(versionId, 'published');
            }
            else if (newestActivated && newestActivated.createdAt > createdAt) {
                states.set(versionId, 'superseded');
            }
            else {
                states.set(versionId, 'not_published');
            }
        }
        return states;
    }
    async findActivatedByUserId(workflowId) {
        const record = await this.findOne({
            select: ['userId'],
            where: { workflowId, event: 'activated' },
            order: { createdAt: 'DESC' },
        });
        return record?.userId ?? undefined;
    }
};
exports.WorkflowPublishHistoryRepository = WorkflowPublishHistoryRepository;
exports.WorkflowPublishHistoryRepository = WorkflowPublishHistoryRepository = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [typeorm_1.DataSource])
], WorkflowPublishHistoryRepository);
//# sourceMappingURL=workflow-publish-history.repository.js.map