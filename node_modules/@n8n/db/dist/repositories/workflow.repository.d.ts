import { GlobalConfig } from '@n8n/config';
import type { Scope } from '@n8n/permissions';
import { DataSource } from '@n8n/typeorm';
import type { SelectQueryBuilder, UpdateResult, FindOptionsWhere, FindOptionsRelations, EntityManager } from '@n8n/typeorm';
import { BaseRepository } from './base-repository';
import { FolderRepository } from './folder.repository';
import { SharedWorkflowRepository } from './shared-workflow.repository';
import { WorkflowHistoryRepository } from './workflow-history.repository';
import { WorkflowEntity, User } from '../entities';
import type { ListQueryDb, FolderWithWorkflowAndSubFolderCount, ListQuery } from '../entities/types-db';
import type { OperationContext } from '../services/transaction';
type ResourceType = 'folder' | 'workflow';
export type WorkflowFolderUnionFull = (ListQueryDb.Workflow.Plain | ListQueryDb.Workflow.WithSharing | FolderWithWorkflowAndSubFolderCount) & {
    resource: ResourceType;
};
type WorkflowListResult = {
    workflows: ListQueryDb.Workflow.Plain[] | ListQueryDb.Workflow.WithSharing[];
    count: number;
};
export declare class WorkflowRepository extends BaseRepository<WorkflowEntity> {
    private readonly globalConfig;
    private readonly folderRepository;
    private readonly sharedWorkflowRepository;
    private readonly workflowHistoryRepository;
    constructor(dataSource: DataSource, globalConfig: GlobalConfig, folderRepository: FolderRepository, sharedWorkflowRepository: SharedWorkflowRepository, workflowHistoryRepository: WorkflowHistoryRepository);
    get(where: FindOptionsWhere<WorkflowEntity>, options?: {
        relations: string[] | FindOptionsRelations<WorkflowEntity>;
    }): Promise<WorkflowEntity | null>;
    findArchivedState(workflowId: string, ctx?: OperationContext): Promise<{
        isArchived: boolean;
    } | null>;
    getAllActiveIds(): Promise<string[]>;
    getActiveIds({ maxResults }?: {
        maxResults?: number;
    }): Promise<string[]>;
    getActiveCount(): Promise<number>;
    getWorkflowInfo({ activeOnly, }: {
        activeOnly: boolean;
    }): Promise<Array<{
        id: string;
        name: string;
    }>>;
    getPublishedCount(): Promise<number>;
    getPublishedPersonalWorkflowsCount(): Promise<number>;
    hasAnyWorkflowsWithErrorWorkflow(): Promise<boolean>;
    findByCredentialResolverId(resolverId: string): Promise<Array<Pick<WorkflowEntity, 'id' | 'name'>>>;
    findActiveByCredentialResolverId(resolverId: string): Promise<string[]>;
    private addCredentialResolverFilter;
    clearCredentialResolverId(resolverId: string, trx?: EntityManager): Promise<void>;
    findById(workflowId: string): Promise<WorkflowEntity | null>;
    findByIds(workflowIds: string[], { fields }?: {
        fields?: string[];
    }): Promise<WorkflowEntity[]>;
    findManyByAgentToolReferences(projectId: string, workflowIds: string[], legacyWorkflowNames: string[]): Promise<WorkflowEntity[]>;
    findOneByAgentToolReference(projectId: string, reference: {
        workflowId?: string;
        workflowName: string;
    }): Promise<WorkflowEntity | null>;
    findPreExistingWorkflows(workflowIds: string[]): Promise<WorkflowEntity[]>;
    getActiveTriggerCount(): Promise<number>;
    updateWorkflowTriggerCount(id: string, triggerCount: number): Promise<UpdateResult>;
    getWorkflowsWithEvaluationCount(): Promise<number>;
    private buildBaseUnionQuery;
    getWorkflowsAndFoldersUnion(workflowIds: string[], options?: ListQuery.Options, accessibleProjectIds?: string[]): Promise<{
        id: string;
        name: string;
        resource: ResourceType;
        createdAt: Date;
        updatedAt: Date;
    }[]>;
    private buildUnionQuery;
    private applySortingToUnionQuery;
    private applyPaginationToUnionQuery;
    private removeNameLowerFromResults;
    getWorkflowsAndFoldersCount(workflowIds: string[], options?: ListQuery.Options, accessibleProjectIds?: string[]): Promise<number>;
    getWorkflowsAndFoldersWithCount(workflowIds: string[], options?: ListQuery.Options, accessibleProjectIds?: string[]): Promise<readonly [WorkflowFolderUnionFull[], number]>;
    getWorkflowsAndFoldersWithCountWithSharingSubquery(user: User, sharingOptions: {
        scopes?: Scope[];
        projectRoles?: string[];
        workflowRoles?: string[];
        isPersonalProject?: boolean;
        personalProjectOwnerId?: string;
        onlySharedWithMe?: boolean;
    }, options?: ListQuery.Options, callableForParentWorkflowId?: string): Promise<readonly [WorkflowFolderUnionFull[], number]>;
    private getWorkflowsAndFoldersUnionWithSharingSubquery;
    private getWorkflowsAndFoldersCountWithSharingSubquery;
    private buildBaseUnionQueryWithSharingSubquery;
    getAllWorkflowIdsInHierarchy(folderId: string, projectId: string): Promise<string[]>;
    private getFolderIds;
    private getWorkflowsIds;
    private fetchExtraData;
    private enrichDataWithExtras;
    getMany(workflowIds: string[], options?: ListQuery.Options): Promise<WorkflowListResult['workflows']>;
    getManyAndCount(sharedWorkflowIds: string[], options?: ListQuery.Options): Promise<WorkflowListResult>;
    getManyAndCountWithSharingSubquery(user: User, sharingOptions: {
        scopes?: Scope[];
        projectRoles?: string[];
        workflowRoles?: string[];
        isPersonalProject?: boolean;
        personalProjectOwnerId?: string;
        onlySharedWithMe?: boolean;
    }, options?: ListQuery.Options, callableForParentWorkflowId?: string): Promise<WorkflowListResult>;
    private getManyQueryWithSharingSubquery;
    private buildCallablePolicySubquery;
    private buildCallablePolicyConditions;
    private settingsTextValue;
    private escapeLike;
    private buildSharedWorkflowIdsSubquery;
    getManyQuery(workflowIds: string[], options?: ListQuery.Options): SelectQueryBuilder<WorkflowEntity>;
    private createBaseQuery;
    private applyFilters;
    private applyAvailableInMCPFilter;
    private applyTriggerNodeTypesFilter;
    private parseSearchWords;
    private getFieldConcatExpression;
    private buildSearchConditions;
    private applyNameFilter;
    private applyParentFolderFilter;
    private applyActiveFilter;
    private applyIsArchivedFilter;
    private applyTagsFilter;
    private applyProjectFilter;
    private applyNodeTypesFilter;
    private buildWorkflowIdsByNodeTypesSubQuery;
    private applyOwnedByRelation;
    private applySelect;
    private applyRelations;
    private applyActiveVersionRelation;
    private applyTagsRelation;
    private applySorting;
    private parseSortingParams;
    private applySortingByColumn;
    private applyPagination;
    findStartingWith(workflowName: string): Promise<Array<{
        name: string;
    }>>;
    findIn(workflowIds: string[]): Promise<WorkflowEntity[]>;
    findWebhookBasedActiveWorkflows(): Promise<{
        id: string;
        name: string;
    }[]>;
    updateActiveState(workflowId: string, newState: boolean): Promise<UpdateResult>;
    publishVersion(workflowId: string, versionId?: string): Promise<UpdateResult>;
    unpublishAll(): Promise<UpdateResult>;
    findByActiveState(activeState: boolean): Promise<WorkflowEntity[]>;
    moveAllToFolder(fromFolderId: string, toFolderId: string, tx: EntityManager): Promise<void>;
    moveToFolder(workflowIds: string[], toFolderId: string): Promise<void>;
    findWorkflowsWithNodeType(nodeTypes: string[], includeNodes?: boolean): Promise<(Pick<WorkflowEntity, "active" | "activeVersionId" | "id" | "name"> & Partial<Pick<WorkflowEntity, "nodes">>)[]>;
    findWorkflowsNeedingIndexing(batchSize?: number): Promise<WorkflowEntity[]>;
    findWorkflowsNeedingPublishedVersionIndexing(batchSize?: number): Promise<WorkflowEntity[]>;
    isActive(workflowId: string): Promise<boolean>;
}
export {};
