import { DataSource } from '@n8n/typeorm';
import { CredentialsEntity, InstanceCredentialAssignment } from '../entities';
import { BaseRepository } from './base-repository';
import type { OperationContext } from '../services/transaction';
export declare class InstanceCredentialAssignmentRepository extends BaseRepository<InstanceCredentialAssignment> {
    constructor(dataSource: DataSource);
    findAvailableCredentials(credentialTypes: readonly string[], ctx?: OperationContext): Promise<CredentialsEntity[]>;
    assignCredential(credentialUseId: string, credentialId: string, credentialTypes: readonly string[], ctx?: OperationContext): Promise<CredentialsEntity | null>;
    clearCredential(credentialUseId: string, ctx?: OperationContext): Promise<void>;
    findAssignedCredentialId(credentialUseId: string, ctx?: OperationContext): Promise<string | null>;
    findCredentialUseIds(credentialId: string, ctx?: OperationContext): Promise<string[]>;
    findAssignedCredential(credentialUseId: string, credentialTypes: readonly string[], ctx?: OperationContext): Promise<{
        credentialId: string;
        credential: CredentialsEntity | null;
    } | null>;
    private findCredential;
}
