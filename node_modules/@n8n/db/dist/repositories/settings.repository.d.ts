import type { EntityManager } from '@n8n/typeorm';
import { DataSource } from '@n8n/typeorm';
import { Settings } from '../entities';
import { BaseRepository } from './base-repository';
import type { OperationContext } from '../services/transaction';
export declare class SettingsRepository extends BaseRepository<Settings> {
    constructor(dataSource: DataSource);
    findByKey(key: string, em?: EntityManager): Promise<Settings | null>;
    findByKeyInContext(key: string, ctx: OperationContext): Promise<Settings | null>;
    upsertByKey(key: string, value: string, loadOnStartup: boolean, ctx: OperationContext): Promise<void>;
    claimKey(key: string, value: string): Promise<boolean>;
    findByKeys(keys: string[]): Promise<Settings[]>;
    findByKeyPrefix(prefix: string): Promise<Settings[]>;
}
