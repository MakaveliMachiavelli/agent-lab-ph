"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AgentEvalResultRepository = void 0;
const di_1 = require("@n8n/di");
const typeorm_1 = require("@n8n/typeorm");
const entities_1 = require("../entities");
const SEED_CHUNK_SIZE = 100;
let AgentEvalResultRepository = class AgentEvalResultRepository extends typeorm_1.Repository {
    constructor(dataSource) {
        super(entities_1.AgentEvalResult, dataSource.manager);
    }
    async seedResults(cases) {
        const results = cases.map((c, index) => this.create({
            status: 'new',
            runId: c.runId,
            sourceRowId: c.sourceRowId ?? null,
            runIndex: c.runIndex ?? index,
            input: c.input ?? null,
        }));
        return await this.save(results, { chunk: SEED_CHUNK_SIZE });
    }
    async countByStatus(runId) {
        const rows = await this.createQueryBuilder('result')
            .select('result.status', 'status')
            .addSelect('COUNT(*)', 'count')
            .where('result.runId = :runId', { runId })
            .groupBy('result.status')
            .getRawMany();
        const counts = {
            new: 0,
            running: 0,
            success: 0,
            error: 0,
            cancelled: 0,
        };
        for (const row of rows)
            counts[row.status] = Number(row.count);
        return counts;
    }
    async markAsRunning(id) {
        return await this.update(id, { status: 'running', runAt: new Date() });
    }
    async markAsCancelled(id) {
        return await this.update(id, { status: 'cancelled', completedAt: new Date() });
    }
    async markAsCompleted(id, attrs) {
        return await this.update(id, {
            status: 'success',
            completedAt: new Date(),
            output: attrs.output,
            toolCalls: attrs.toolCalls ?? null,
            metrics: attrs.metrics ?? null,
        });
    }
    async markAsError(id, errorCode, errorDetails) {
        return await this.update(id, {
            status: 'error',
            completedAt: new Date(),
            errorCode,
            errorDetails: errorDetails ?? null,
        });
    }
    async findAndCountByRunId(runId, options = {}) {
        return await this.findAndCount({
            where: { runId },
            order: { runIndex: 'ASC' },
            skip: options.skip,
            take: options.take,
        });
    }
    async findById(id) {
        return await this.findOneBy({ id });
    }
};
exports.AgentEvalResultRepository = AgentEvalResultRepository;
exports.AgentEvalResultRepository = AgentEvalResultRepository = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [typeorm_1.DataSource])
], AgentEvalResultRepository);
//# sourceMappingURL=agent-eval-result.repository.ee.js.map