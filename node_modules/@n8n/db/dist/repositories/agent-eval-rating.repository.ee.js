"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AgentEvalRatingRepository = void 0;
const di_1 = require("@n8n/di");
const typeorm_1 = require("@n8n/typeorm");
const entities_1 = require("../entities");
const ID_LOOKUP_CHUNK_SIZE = 100;
let AgentEvalRatingRepository = class AgentEvalRatingRepository extends typeorm_1.Repository {
    constructor(dataSource) {
        super(entities_1.AgentEvalRating, dataSource.manager);
    }
    async createRating(attrs) {
        const rating = this.create({
            resultId: attrs.resultId,
            vote: attrs.vote,
            comment: attrs.comment ?? null,
            correction: attrs.correction ?? null,
            ratedById: attrs.ratedById ?? null,
        });
        return await this.save(rating);
    }
    async findByResultId(resultId) {
        return await this.find({ where: { resultId }, order: { createdAt: 'DESC' } });
    }
    async findLatestByRunId(runId) {
        const rows = await this.createQueryBuilder('rating')
            .select('rating.id', 'id')
            .addSelect('rating.resultId', 'resultId')
            .innerJoin(entities_1.AgentEvalResult, 'result', 'result.id = rating.resultId')
            .where('result.runId = :runId', { runId })
            .orderBy('rating.resultId', 'ASC')
            .addOrderBy('rating.createdAt', 'DESC')
            .addOrderBy('rating.id', 'DESC')
            .getRawMany();
        const seen = new Set();
        const latestIds = rows
            .filter((row) => {
            if (seen.has(row.resultId))
                return false;
            seen.add(row.resultId);
            return true;
        })
            .map((row) => row.id);
        if (latestIds.length === 0)
            return [];
        const found = [];
        for (let i = 0; i < latestIds.length; i += ID_LOOKUP_CHUNK_SIZE) {
            const chunk = latestIds.slice(i, i + ID_LOOKUP_CHUNK_SIZE);
            found.push(...(await this.find({ where: { id: (0, typeorm_1.In)(chunk) } })));
        }
        const byId = new Map(found.map((rating) => [rating.id, rating]));
        return latestIds
            .map((id) => byId.get(id))
            .filter((rating) => rating !== undefined);
    }
};
exports.AgentEvalRatingRepository = AgentEvalRatingRepository;
exports.AgentEvalRatingRepository = AgentEvalRatingRepository = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [typeorm_1.DataSource])
], AgentEvalRatingRepository);
//# sourceMappingURL=agent-eval-rating.repository.ee.js.map