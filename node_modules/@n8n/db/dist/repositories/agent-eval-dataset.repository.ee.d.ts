import type { DatasetRef } from '@n8n/api-types';
import { DataSource, Repository } from '@n8n/typeorm';
import { AgentEvalDataset } from '../entities';
import type { AgentEvalColumnMapping } from '../entities/agent-eval-dataset.ee';
type CreateAgentEvalDatasetAttrs = {
    name: string;
    agentId: string;
    datasetSource: DatasetRef['datasetSource'];
    datasetRef: DatasetRef['datasetRef'];
    description?: string | null;
    columnMapping?: AgentEvalColumnMapping | null;
    createdById?: string | null;
};
type UpdateAgentEvalDatasetAttrs = {
    name?: string;
    description?: string | null;
    columnMapping?: AgentEvalColumnMapping | null;
};
export declare class AgentEvalDatasetRepository extends Repository<AgentEvalDataset> {
    constructor(dataSource: DataSource);
    createDataset(attrs: CreateAgentEvalDatasetAttrs): Promise<AgentEvalDataset>;
    findByAgentId(agentId: string): Promise<AgentEvalDataset[]>;
    findById(id: string): Promise<AgentEvalDataset | null>;
    findByIdAndAgentId(id: string, agentId: string): Promise<AgentEvalDataset | null>;
    updateDataset(id: string, agentId: string, attrs: UpdateAgentEvalDatasetAttrs): Promise<AgentEvalDataset | null>;
    deleteDataset(id: string, agentId: string): Promise<boolean>;
}
export {};
