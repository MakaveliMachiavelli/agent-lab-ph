"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.WorkflowReviewRequestReviewerRepository = void 0;
const di_1 = require("@n8n/di");
const typeorm_1 = require("@n8n/typeorm");
const base_repository_1 = require("./base-repository");
const workflow_review_request_reviewer_ee_1 = require("../entities/workflow-review-request-reviewer.ee");
let WorkflowReviewRequestReviewerRepository = class WorkflowReviewRequestReviewerRepository extends base_repository_1.BaseRepository {
    constructor(dataSource) {
        super(workflow_review_request_reviewer_ee_1.WorkflowReviewRequestReviewer, dataSource.manager);
    }
    async addReviewers(input, ctx) {
        const uniqueUserIds = [...new Set(input.userIds)];
        if (uniqueUserIds.length === 0) {
            return [];
        }
        const entities = uniqueUserIds.map((userId) => this.create({
            workflowReviewRequestId: input.workflowReviewRequestId,
            userId,
        }));
        return await this.managerFor(ctx).save(workflow_review_request_reviewer_ee_1.WorkflowReviewRequestReviewer, entities);
    }
    async setReviewers(requestId, userIds) {
        const uniqueUserIds = [...new Set(userIds)];
        return await this.manager.transaction(async (tx) => {
            await tx.delete(workflow_review_request_reviewer_ee_1.WorkflowReviewRequestReviewer, {
                workflowReviewRequestId: requestId,
            });
            if (uniqueUserIds.length === 0) {
                return [];
            }
            const entities = uniqueUserIds.map((userId) => this.create({
                workflowReviewRequestId: requestId,
                userId,
            }));
            return await tx.save(workflow_review_request_reviewer_ee_1.WorkflowReviewRequestReviewer, entities);
        });
    }
    async findByRequestId(requestId) {
        return await this.find({
            where: { workflowReviewRequestId: requestId },
            order: { userId: 'ASC' },
        });
    }
    async findByRequestIds(requestIds) {
        if (requestIds.length === 0) {
            return [];
        }
        return await this.find({
            where: { workflowReviewRequestId: (0, typeorm_1.In)(requestIds) },
            order: { userId: 'ASC' },
        });
    }
};
exports.WorkflowReviewRequestReviewerRepository = WorkflowReviewRequestReviewerRepository;
exports.WorkflowReviewRequestReviewerRepository = WorkflowReviewRequestReviewerRepository = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [typeorm_1.DataSource])
], WorkflowReviewRequestReviewerRepository);
//# sourceMappingURL=workflow-review-request-reviewer.repository.js.map