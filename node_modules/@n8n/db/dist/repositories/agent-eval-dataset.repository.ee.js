"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AgentEvalDatasetRepository = void 0;
const di_1 = require("@n8n/di");
const typeorm_1 = require("@n8n/typeorm");
const entities_1 = require("../entities");
let AgentEvalDatasetRepository = class AgentEvalDatasetRepository extends typeorm_1.Repository {
    constructor(dataSource) {
        super(entities_1.AgentEvalDataset, dataSource.manager);
    }
    async createDataset(attrs) {
        const dataset = this.create({
            name: attrs.name,
            agentId: attrs.agentId,
            datasetSource: attrs.datasetSource,
            datasetRef: attrs.datasetRef,
            description: attrs.description ?? null,
            columnMapping: attrs.columnMapping ?? null,
            createdById: attrs.createdById ?? null,
        });
        return await this.save(dataset);
    }
    async findByAgentId(agentId) {
        return await this.find({ where: { agentId }, order: { createdAt: 'DESC' } });
    }
    async findById(id) {
        return await this.findOneBy({ id });
    }
    async findByIdAndAgentId(id, agentId) {
        return await this.findOneBy({ id, agentId });
    }
    async updateDataset(id, agentId, attrs) {
        const patch = {
            ...(attrs.name !== undefined && { name: attrs.name }),
            ...(attrs.description !== undefined && { description: attrs.description }),
            ...(attrs.columnMapping !== undefined && { columnMapping: attrs.columnMapping }),
        };
        if (Object.keys(patch).length > 0) {
            await this.update({ id, agentId }, patch);
        }
        return await this.findByIdAndAgentId(id, agentId);
    }
    async deleteDataset(id, agentId) {
        const result = await this.delete({ id, agentId });
        return (result.affected ?? 0) > 0;
    }
};
exports.AgentEvalDatasetRepository = AgentEvalDatasetRepository;
exports.AgentEvalDatasetRepository = AgentEvalDatasetRepository = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [typeorm_1.DataSource])
], AgentEvalDatasetRepository);
//# sourceMappingURL=agent-eval-dataset.repository.ee.js.map