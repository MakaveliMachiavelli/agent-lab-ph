"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.EvaluationCollectionRepository = void 0;
const di_1 = require("@n8n/di");
const typeorm_1 = require("@n8n/typeorm");
const evaluation_collection_ee_1 = require("../entities/evaluation-collection.ee");
const test_run_ee_1 = require("../entities/test-run.ee");
let EvaluationCollectionRepository = class EvaluationCollectionRepository extends typeorm_1.Repository {
    constructor(dataSource) {
        super(evaluation_collection_ee_1.EvaluationCollection, dataSource.manager);
    }
    async createCollection(input) {
        const entity = this.create({
            id: input.id,
            name: input.name,
            description: input.description,
            workflowId: input.workflowId,
            evaluationConfigId: input.evaluationConfigId,
            createdById: input.createdById,
            insightsCache: null,
        });
        return await this.save(entity);
    }
    async findByIdAndWorkflowId(id, workflowId) {
        return await this.findOne({ where: { id, workflowId } });
    }
    async listByWorkflowId(workflowId) {
        const collections = await this.find({
            where: { workflowId },
            order: { createdAt: 'DESC' },
        });
        if (collections.length === 0)
            return [];
        const runCounts = await this.manager
            .createQueryBuilder(test_run_ee_1.TestRun, 'tr')
            .select('tr.collectionId', 'collectionId')
            .addSelect('COUNT(tr.id)', 'count')
            .where('tr.collectionId IN (:...ids)', { ids: collections.map((c) => c.id) })
            .groupBy('tr.collectionId')
            .getRawMany();
        const countByCollectionId = new Map(runCounts.map(({ collectionId, count }) => [collectionId, Number(count)]));
        return collections.map((collection) => ({
            ...collection,
            runCount: countByCollectionId.get(collection.id) ?? 0,
        }));
    }
    async getDetailByIdAndWorkflowId(id, workflowId) {
        const collection = await this.findOne({ where: { id, workflowId } });
        if (!collection)
            return null;
        const runs = await this.manager.find(test_run_ee_1.TestRun, {
            where: { collectionId: id },
            order: { createdAt: 'ASC' },
        });
        return { collection, runs };
    }
    async addRunsToCollection(collectionId, testRunIds) {
        if (testRunIds.length === 0)
            return;
        await this.manager.update(test_run_ee_1.TestRun, { id: (0, typeorm_1.In)(testRunIds) }, { collectionId });
    }
    async removeRunFromCollection(collectionId, testRunId) {
        const result = await this.manager.update(test_run_ee_1.TestRun, { id: testRunId, collectionId }, { collectionId: null });
        return result.affected ?? 0;
    }
    async removeRunsFromCollection(collectionId, testRunIds) {
        if (testRunIds.length === 0)
            return;
        await this.manager.update(test_run_ee_1.TestRun, { id: (0, typeorm_1.In)(testRunIds), collectionId }, { collectionId: null });
    }
    async deleteByIdAndWorkflowId(id, workflowId) {
        const collection = await this.findOne({ where: { id, workflowId } });
        if (!collection)
            return { deleted: false, runsUnlinked: 0 };
        const runsUnlinked = await this.manager.count(test_run_ee_1.TestRun, { where: { collectionId: id } });
        await this.delete({ id });
        return { deleted: true, runsUnlinked };
    }
    async updateInsightsCache(id, insights) {
        await this.update(id, { insightsCache: insights });
    }
    async updateMeta(id, workflowId, payload) {
        const existing = await this.findByIdAndWorkflowId(id, workflowId);
        if (!existing)
            return null;
        if (payload.name !== undefined)
            existing.name = payload.name;
        if (payload.description !== undefined)
            existing.description = payload.description;
        return await this.save(existing);
    }
};
exports.EvaluationCollectionRepository = EvaluationCollectionRepository;
exports.EvaluationCollectionRepository = EvaluationCollectionRepository = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [typeorm_1.DataSource])
], EvaluationCollectionRepository);
//# sourceMappingURL=evaluation-collection.repository.js.map