"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.WorkflowPublicationTriggerStatusRepository = void 0;
const di_1 = require("@n8n/di");
const typeorm_1 = require("@n8n/typeorm");
const entities_1 = require("../entities");
const workflow_publication_outbox_1 = require("../entities/workflow-publication-outbox");
let WorkflowPublicationTriggerStatusRepository = class WorkflowPublicationTriggerStatusRepository extends typeorm_1.Repository {
    constructor(dataSource) {
        super(entities_1.WorkflowPublicationTriggerStatus, dataSource.manager);
    }
    async replaceForWorkflow(workflowId, rows, trx) {
        const run = async (em) => {
            await em.delete(entities_1.WorkflowPublicationTriggerStatus, { workflowId });
            if (rows.length === 0)
                return;
            await em.insert(entities_1.WorkflowPublicationTriggerStatus, rows.map((row) => ({ workflowId, ...row })));
        };
        await (trx ? run(trx) : this.manager.transaction(run));
    }
    async findByWorkflowId(workflowId) {
        return await this.findBy({ workflowId });
    }
    async findActivatedInMemoryTriggers() {
        return await this.createQueryBuilder('ts')
            .select(['ts.workflowId AS "workflowId"', 'ts.nodeId AS "nodeId"'])
            .where('ts.status = :status', { status: 'activated' })
            .andWhere('ts.triggerKind = :kind', { kind: 'in-memory' })
            .andWhere((qb) => {
            const inFlight = qb
                .subQuery()
                .select('1')
                .from(workflow_publication_outbox_1.WorkflowPublicationOutbox, 'outbox')
                .where('outbox.workflowId = ts.workflowId')
                .andWhere('outbox.status IN (:...inFlightStatuses)', {
                inFlightStatuses: [
                    workflow_publication_outbox_1.WorkflowPublicationOutboxStatus.Pending,
                    workflow_publication_outbox_1.WorkflowPublicationOutboxStatus.InProgress,
                ],
            })
                .getQuery();
            return `NOT EXISTS ${inFlight}`;
        })
            .getRawMany();
    }
};
exports.WorkflowPublicationTriggerStatusRepository = WorkflowPublicationTriggerStatusRepository;
exports.WorkflowPublicationTriggerStatusRepository = WorkflowPublicationTriggerStatusRepository = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [typeorm_1.DataSource])
], WorkflowPublicationTriggerStatusRepository);
//# sourceMappingURL=workflow-publication-trigger-status.repository.js.map