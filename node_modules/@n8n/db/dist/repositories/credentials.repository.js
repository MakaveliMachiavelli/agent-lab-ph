"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CredentialsRepository = void 0;
const di_1 = require("@n8n/di");
const typeorm_1 = require("@n8n/typeorm");
const entities_1 = require("../entities");
const base_repository_1 = require("./base-repository");
const credential_dependency_repository_1 = require("./credential-dependency.repository");
const instance_credential_assignment_repository_1 = require("./instance-credential-assignment.repository");
const shared_credentials_repository_1 = require("./shared-credentials.repository");
let CredentialsRepository = class CredentialsRepository extends base_repository_1.BaseRepository {
    constructor(dataSource, instanceCredentialAssignmentRepository) {
        super(entities_1.CredentialsEntity, dataSource.manager);
        this.instanceCredentialAssignmentRepository = instanceCredentialAssignmentRepository;
    }
    async findStartingWith(credentialName) {
        return await this.find({
            select: ['name'],
            where: { name: (0, typeorm_1.Like)(`${credentialName}%`), usageScope: 'project' },
        });
    }
    async findNonProjectCredentialsByIds(ids) {
        return await this.find({
            where: { id: (0, typeorm_1.In)(ids), usageScope: (0, typeorm_1.Not)('project') },
            select: ['id'],
        });
    }
    async findDanglingProjectCredentials() {
        return await this.createQueryBuilder('credentials')
            .leftJoinAndSelect('credentials.shared', 'shared')
            .where('shared.credentialsId is null')
            .andWhere('credentials.usageScope = :usageScope', { usageScope: 'project' })
            .getMany();
    }
    async findInstanceCredentialById(credentialId, ctx) {
        return await this.managerFor(ctx).findOneBy(entities_1.CredentialsEntity, {
            id: credentialId,
            usageScope: 'instance',
        });
    }
    async saveInstanceCredential(credential, ctx) {
        return await this.managerFor(ctx).save(entities_1.CredentialsEntity, credential);
    }
    async updateInstanceCredential(credentialId, data, ctx) {
        const manager = this.managerFor(ctx);
        await manager.update(entities_1.CredentialsEntity, { id: credentialId, usageScope: 'instance' }, data);
        return await manager.findOneBy(entities_1.CredentialsEntity, {
            id: credentialId,
            usageScope: 'instance',
        });
    }
    async deleteInstanceCredentialIfUnassigned(credentialId, ctx = {}) {
        const manager = this.managerFor(ctx);
        const credential = await manager.findOneBy(entities_1.CredentialsEntity, {
            id: credentialId,
            usageScope: 'instance',
        });
        if (!credential)
            return { status: 'notFound' };
        const credentialUseIds = await this.instanceCredentialAssignmentRepository.findCredentialUseIds(credentialId, ctx);
        if (credentialUseIds.length > 0)
            return { status: 'assigned', credentialUseIds };
        try {
            const result = await manager.delete(entities_1.CredentialsEntity, {
                id: credentialId,
                usageScope: 'instance',
            });
            return result.affected === 0 ? { status: 'notFound' } : { status: 'deleted' };
        }
        catch (error) {
            if (error instanceof typeorm_1.QueryFailedError && !ctx.trx) {
                const concurrentCredentialUseIds = await this.instanceCredentialAssignmentRepository.findCredentialUseIds(credentialId, ctx);
                if (concurrentCredentialUseIds.length > 0) {
                    return { status: 'assigned', credentialUseIds: concurrentCredentialUseIds };
                }
            }
            throw error;
        }
    }
    async findMany(listQueryOptions, credentialIds) {
        const findManyOptions = this.toFindManyOptions(listQueryOptions);
        if (credentialIds) {
            findManyOptions.where = { ...findManyOptions.where, id: (0, typeorm_1.In)(credentialIds) };
        }
        return await this.find(this.onlyProjectCredentials(findManyOptions));
    }
    async findManyAndCount(listQueryOptions, credentialIds) {
        const findManyOptions = this.toFindManyOptions(listQueryOptions);
        if (credentialIds) {
            findManyOptions.where = { ...findManyOptions.where, id: (0, typeorm_1.In)(credentialIds) };
        }
        return await this.findAndCount(this.onlyProjectCredentials(findManyOptions));
    }
    onlyProjectCredentials(findManyOptions) {
        findManyOptions.where = { ...findManyOptions.where, usageScope: 'project' };
        return findManyOptions;
    }
    toFindManyOptions(listQueryOptions) {
        const findManyOptions = {};
        const defaultRelations = ['shared', 'shared.project', 'shared.project.projectRelations'];
        const defaultSelect = [
            'id',
            'name',
            'type',
            'isManaged',
            'createdAt',
            'updatedAt',
            'isGlobal',
            'isResolvable',
            'resolverId',
        ];
        if (!listQueryOptions) {
            return {
                select: defaultSelect,
                relations: defaultRelations,
            };
        }
        const { filter, select, take, skip, order } = listQueryOptions;
        if (typeof filter?.name === 'string' && filter?.name !== '') {
            filter.name = (0, typeorm_1.Like)(`%${filter.name}%`);
        }
        if (typeof filter?.type === 'string' && filter?.type !== '') {
            filter.type = (0, typeorm_1.Like)(`%${filter.type}%`);
        }
        this.handleSharedFilters(listQueryOptions);
        if (filter)
            findManyOptions.where = filter;
        if (select)
            findManyOptions.select = select;
        if (take)
            findManyOptions.take = take;
        if (skip)
            findManyOptions.skip = skip;
        if (take && select && !select?.id) {
            findManyOptions.select = { ...findManyOptions.select, id: true };
        }
        if (!findManyOptions.select) {
            findManyOptions.select = defaultSelect;
            findManyOptions.relations = defaultRelations;
        }
        if (order !== undefined) {
            findManyOptions.order = order;
        }
        if (listQueryOptions.includeData) {
            if (Array.isArray(findManyOptions.select)) {
                findManyOptions.select.push('data');
            }
            else {
                findManyOptions.select.data = true;
            }
        }
        return findManyOptions;
    }
    handleSharedFilters(listQueryOptions) {
        if (!listQueryOptions?.filter)
            return;
        const { filter } = listQueryOptions;
        if (typeof filter.projectId === 'string' && filter.projectId !== '') {
            filter.shared = {
                projectId: filter.projectId,
            };
            delete filter.projectId;
        }
        if (typeof filter.withRole === 'string' && filter.withRole !== '') {
            filter.shared = {
                ...(filter?.shared ? filter.shared : {}),
                role: filter.withRole,
            };
            delete filter.withRole;
        }
        if (filter.user &&
            typeof filter.user === 'object' &&
            'id' in filter.user &&
            typeof filter.user.id === 'string') {
            filter.shared = {
                ...(filter?.shared ? filter.shared : {}),
                project: {
                    projectRelations: {
                        userId: filter.user.id,
                    },
                },
            };
            delete filter.user;
        }
    }
    async getManyByIds(ids, { withSharings } = { withSharings: false }) {
        const findManyOptions = {
            where: { id: (0, typeorm_1.In)(ids), usageScope: 'project' },
        };
        if (withSharings) {
            findManyOptions.relations = {
                shared: {
                    project: true,
                },
            };
        }
        return await this.find(findManyOptions);
    }
    async findAllGlobalCredentials(options = {}) {
        const { includeData = false, type, filters } = options;
        const dependencyFilter = filters?.dependency;
        if (dependencyFilter) {
            return await this.findAllGlobalCredentialsByDependencyFilter({
                dependencyFilter,
                includeData,
                type,
            });
        }
        const findManyOptions = this.toFindManyOptions({ includeData });
        findManyOptions.where = {
            ...findManyOptions.where,
            isGlobal: true,
            usageScope: 'project',
            ...(type ? { type: (0, typeorm_1.Like)(`%${type}%`) } : {}),
        };
        return await this.find(findManyOptions);
    }
    async findAllGlobalCredentialsByDependencyFilter(options) {
        const { includeData, dependencyFilter, type } = options;
        const qb = this.createQueryBuilder('credential');
        qb.where('credential.isGlobal = :isGlobal', { isGlobal: true });
        qb.andWhere('credential.usageScope = :usageScope', { usageScope: 'project' });
        if (type) {
            qb.andWhere('credential.type LIKE :type', { type: `%${type}%` });
        }
        (0, credential_dependency_repository_1.addCredentialDependencyExistsFilter)(qb, dependencyFilter);
        const defaultSelect = [
            'id',
            'name',
            'type',
            'isManaged',
            'createdAt',
            'updatedAt',
            'isGlobal',
            'isResolvable',
            'resolverId',
        ];
        const selectColumns = defaultSelect.map((k) => `credential.${k}`);
        if (includeData) {
            selectColumns.push('credential.data');
        }
        qb.select(selectColumns);
        qb.leftJoinAndSelect('credential.shared', 'shared');
        qb.leftJoinAndSelect('shared.project', 'project');
        qb.leftJoinAndSelect('project.projectRelations', 'projectRelations');
        return await qb.getMany();
    }
    async findAllPersonalCredentials() {
        return await this.findBy({
            usageScope: 'project',
            shared: { project: { type: 'personal' } },
        });
    }
    async findAllCredentialsForWorkflow(workflowId) {
        return await this.findBy({
            usageScope: 'project',
            shared: { project: { sharedWorkflows: { workflowId } } },
        });
    }
    async findAllCredentialsForProject(projectId) {
        return await this.findBy({ usageScope: 'project', shared: { projectId } });
    }
    async findByNameAndTypeInProject(name, type, projectId) {
        return await this.findBy({ name, type, usageScope: 'project', shared: { projectId } });
    }
    async getManyAndCountWithSharingSubquery(user, sharingOptions, options = {}) {
        const query = this.getManyQueryWithSharingSubquery(user, sharingOptions, options);
        const credentials = await query.getMany();
        const countQuery = this.getManyQueryWithSharingSubquery(user, sharingOptions, {
            ...options,
            take: undefined,
            skip: undefined,
            select: undefined,
        });
        const count = await countQuery.select('credential.id').getCount();
        return { credentials, count };
    }
    getManyQueryWithSharingSubquery(user, sharingOptions, options = {}) {
        const qb = this.createQueryBuilder('credential');
        qb.andWhere('credential.usageScope = :usageScope', { usageScope: 'project' });
        if (options.filters?.dependency) {
            (0, credential_dependency_repository_1.addCredentialDependencyExistsFilter)(qb, options.filters.dependency);
        }
        const projectId = typeof options.filter?.projectId === 'string' ? options.filter.projectId : undefined;
        const sharingOptionsWithProjectId = {
            ...sharingOptions,
            projectId,
        };
        const sharedCredentialsRepository = di_1.Container.get(shared_credentials_repository_1.SharedCredentialsRepository);
        const sharedCredentialSubquery = sharedCredentialsRepository.buildSharedCredentialIdsSubquery(user, sharingOptionsWithProjectId);
        qb.andWhere(`credential.id IN (${sharedCredentialSubquery.getQuery()})`);
        qb.setParameters(sharedCredentialSubquery.getParameters());
        const filtersToApply = options.filter && typeof options.filter.projectId !== 'undefined'
            ? { ...options.filter, projectId: undefined }
            : options.filter;
        if (typeof filtersToApply?.name === 'string' && filtersToApply.name !== '') {
            qb.andWhere('credential.name LIKE :name', { name: `%${filtersToApply.name}%` });
        }
        if (typeof filtersToApply?.type === 'string' && filtersToApply.type !== '') {
            qb.andWhere('credential.type LIKE :type', { type: `%${filtersToApply.type}%` });
        }
        const defaultSelect = [
            'id',
            'name',
            'type',
            'isManaged',
            'createdAt',
            'updatedAt',
            'isGlobal',
            'isResolvable',
            'resolverId',
        ];
        if (options.select) {
            const selectFields = options.select;
            if (options.take && !selectFields.id) {
                qb.select([...Object.keys(selectFields).map((k) => `credential.${k}`), 'credential.id']);
            }
            else {
                qb.select(Object.keys(selectFields).map((k) => `credential.${k}`));
            }
        }
        else {
            qb.select(defaultSelect.map((k) => `credential.${k}`));
        }
        if (options.includeData) {
            qb.addSelect('credential.data');
        }
        if (!options.select) {
            qb.leftJoinAndSelect('credential.shared', 'shared')
                .leftJoinAndSelect('shared.project', 'project')
                .leftJoinAndSelect('project.projectRelations', 'projectRelations');
        }
        if (options.order) {
            Object.entries(options.order).forEach(([key, direction]) => {
                qb.addOrderBy(`credential.${key}`, direction);
            });
        }
        if (options.take) {
            qb.take(options.take);
        }
        if (options.skip) {
            qb.skip(options.skip);
        }
        return qb;
    }
};
exports.CredentialsRepository = CredentialsRepository;
exports.CredentialsRepository = CredentialsRepository = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [typeorm_1.DataSource, instance_credential_assignment_repository_1.InstanceCredentialAssignmentRepository])
], CredentialsRepository);
//# sourceMappingURL=credentials.repository.js.map