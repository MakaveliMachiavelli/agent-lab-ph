import type { WorkflowReviewApprovedPublicationState } from '@n8n/api-types';
import { DataSource, Repository } from '@n8n/typeorm';
import type { EntityManager } from '@n8n/typeorm';
import { WorkflowPublishHistory } from '../entities';
export declare class WorkflowPublishHistoryRepository extends Repository<WorkflowPublishHistory> {
    constructor(dataSource: DataSource);
    addRecord({ workflowId, versionId, event, userId, }: Pick<WorkflowPublishHistory, 'event' | 'workflowId' | 'versionId' | 'userId'>, trx?: EntityManager): Promise<void>;
    getPublishedVersions(workflowId: string): Promise<Array<Pick<WorkflowPublishHistory, 'versionId'>>>;
    getVersionPublicationStates(workflowId: string, versionIds: string[]): Promise<Map<string, WorkflowReviewApprovedPublicationState>>;
    findActivatedByUserId(workflowId: string): Promise<string | undefined>;
}
