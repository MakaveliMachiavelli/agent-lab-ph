import { DataSource, Repository } from '@n8n/typeorm';
import type { JsonObject } from 'n8n-workflow';
import { AgentEvalRating } from '../entities';
import type { AgentEvalVote } from '../entities/agent-eval-rating.ee';
type CreateAgentEvalRatingAttrs = {
    resultId: string;
    vote: AgentEvalVote;
    comment?: string | null;
    correction?: JsonObject | null;
    ratedById?: string | null;
};
export declare class AgentEvalRatingRepository extends Repository<AgentEvalRating> {
    constructor(dataSource: DataSource);
    createRating(attrs: CreateAgentEvalRatingAttrs): Promise<AgentEvalRating>;
    findByResultId(resultId: string): Promise<AgentEvalRating[]>;
    findLatestByRunId(runId: string): Promise<AgentEvalRating[]>;
}
export {};
