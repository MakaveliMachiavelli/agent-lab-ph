"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.WorkflowStatisticsRepository = void 0;
const config_1 = require("@n8n/config");
const di_1 = require("@n8n/di");
const permissions_1 = require("@n8n/permissions");
const typeorm_1 = require("@n8n/typeorm");
const n8n_workflow_1 = require("n8n-workflow");
const entities_1 = require("../entities");
let WorkflowStatisticsRepository = class WorkflowStatisticsRepository extends typeorm_1.Repository {
    constructor(dataSource, globalConfig) {
        super(entities_1.WorkflowStatistics, dataSource.manager);
        this.globalConfig = globalConfig;
    }
    async insertWorkflowStatistics(eventName, workflowId, workflowName) {
        try {
            const exists = await this.findOne({
                where: {
                    workflowId,
                    name: eventName,
                },
            });
            if (exists)
                return 'alreadyExists';
            await this.insert({
                workflowId,
                name: eventName,
                count: 1,
                rootCount: 1,
                latestEvent: new Date(),
                workflowName: workflowName ?? null,
            });
            return 'insert';
        }
        catch (error) {
            if (!(error instanceof typeorm_1.QueryFailedError)) {
                throw error;
            }
            return 'failed';
        }
    }
    async upsertWorkflowStatistics(eventName, workflowId, isRootExecution, workflowName) {
        if (this.globalConfig.database.type !== 'sqlite') {
            throw new n8n_workflow_1.UnexpectedError('upsertWorkflowStatistics is SQLite-only; use appendIncrement + rollup on Postgres');
        }
        const escapedTableName = this.manager.connection.driver.escape(this.metadata.tableName);
        try {
            const rootCountIncrement = isRootExecution ? 1 : 0;
            await this.query(`INSERT INTO ${escapedTableName} ("count", "rootCount", "name", "workflowId", "workflowName", "latestEvent")
				VALUES (1, ?, ?, ?, ?, CURRENT_TIMESTAMP)
				ON CONFLICT (workflowId, name)
				DO UPDATE SET
					count = count + 1,
					rootCount = rootCount + ?,
					workflowName = excluded.workflowName,
					latestEvent = CURRENT_TIMESTAMP`, [rootCountIncrement, eventName, workflowId, workflowName ?? null, rootCountIncrement]);
            const counter = await this.findOne({
                select: ['count'],
                where: { name: eventName, workflowId },
            });
            return (counter?.count ?? 0) > 1 ? 'update' : counter?.count === 1 ? 'insert' : 'failed';
        }
        catch (error) {
            if (error instanceof typeorm_1.QueryFailedError)
                return 'failed';
            throw error;
        }
    }
    deltaTableName() {
        const { tablePrefix } = this.globalConfig.database;
        return this.manager.connection.driver.escape(`${tablePrefix}workflow_statistics_delta`);
    }
    async appendIncrement(eventName, workflowId, isRootExecution, workflowName) {
        const table = this.deltaTableName();
        await this.query(`INSERT INTO ${table} ("workflowId", "name", "rootCountDelta", "workflowName") VALUES ($1, $2, $3, $4)`, [workflowId, eventName, isRootExecution ? 1 : 0, workflowName ?? null]);
    }
    async rollupIncrements(manager, batchSize) {
        const stats = this.manager.connection.driver.escape(this.metadata.tableName);
        const delta = this.deltaTableName();
        const rows = await manager.query(`WITH batch AS (
				DELETE FROM ${delta}
				WHERE id IN (SELECT id FROM ${delta} ORDER BY id LIMIT $1)
				RETURNING "workflowId", "name", "rootCountDelta", "createdAt", "workflowName"
			),
			agg AS (
				SELECT "workflowId", "name",
					COUNT(*) AS "countIncrement",
					SUM("rootCountDelta") AS "rootCountIncrement",
					MAX("createdAt") AS "latestEvent",
					MIN("createdAt") AS "firstEvent",
					(ARRAY_AGG("workflowName" ORDER BY "createdAt" DESC NULLS LAST))[1] AS "workflowName"
				FROM batch GROUP BY "workflowId", "name"
			),
			upsert AS (
				INSERT INTO ${stats} ("workflowId", "name", "count", "rootCount", "latestEvent", "workflowName")
				SELECT "workflowId", "name", "countIncrement", "rootCountIncrement", "latestEvent", "workflowName" FROM agg
				ON CONFLICT ("name", "workflowId") DO UPDATE SET
					"count" = ${stats}."count" + EXCLUDED."count",
					"rootCount" = ${stats}."rootCount" + EXCLUDED."rootCount",
					"latestEvent" = GREATEST(${stats}."latestEvent", EXCLUDED."latestEvent"),
					"workflowName" = EXCLUDED."workflowName"
				RETURNING "workflowId", "name", ("xmax" = 0) AS inserted
			)
			SELECT (SELECT COUNT(*) FROM batch)::int AS delta_rows,
				u."workflowId", u."name", u.inserted, a."workflowName", a."firstEvent"
			FROM upsert u JOIN agg a ON a."workflowId" = u."workflowId" AND a."name" = u."name"`, [batchSize]);
        if (rows.length === 0)
            return { increments: 0, firstOccurrences: [] };
        const firstOccurrences = rows
            .filter((r) => r.inserted &&
            (r.name === "production_success" ||
                r.name === "production_error"))
            .map((r) => ({
            name: r.name,
            workflowId: r.workflowId,
            workflowName: r.workflowName,
            firstEventMs: new Date(r.firstEvent).getTime(),
        }));
        return { increments: rows[0].delta_rows, firstOccurrences };
    }
    async queryNumWorkflowsUserHasWithFiveOrMoreProdExecs(userId) {
        const result = await this.createQueryBuilder('ws')
            .select('COUNT(DISTINCT ws.workflowId)', 'count')
            .innerJoin(entities_1.WorkflowEntity, 'we', 'ws.workflowId = we.id')
            .innerJoin(entities_1.SharedWorkflow, 'sw', 'we.id = sw.workflowId')
            .innerJoin(entities_1.ProjectRelation, 'pr', 'sw.projectId = pr.projectId')
            .innerJoin(entities_1.Role, 'r', 'pr.role = r.slug')
            .where('sw.role = :role', { role: 'workflow:owner' })
            .andWhere('pr.userId = :userId', { userId })
            .andWhere('r.slug = :slug', { slug: permissions_1.PROJECT_OWNER_ROLE_SLUG })
            .andWhere('we.activeVersionId IS NOT NULL')
            .andWhere('ws.name = :name', { name: "production_success" })
            .andWhere('ws.count >= :minCount', { minCount: 5 })
            .getRawOne();
        return Number(result?.count ?? 0);
    }
};
exports.WorkflowStatisticsRepository = WorkflowStatisticsRepository;
exports.WorkflowStatisticsRepository = WorkflowStatisticsRepository = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [typeorm_1.DataSource, config_1.GlobalConfig])
], WorkflowStatisticsRepository);
//# sourceMappingURL=workflow-statistics.repository.js.map