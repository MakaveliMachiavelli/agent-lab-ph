import { DataSource, EntityManager, Repository } from '@n8n/typeorm';
import { WorkflowPublicationTriggerStatus } from '../entities';
export type TriggerStatusRow = {
    nodeId: string;
    versionId: string;
    status: WorkflowPublicationTriggerStatus['status'];
    triggerKind: WorkflowPublicationTriggerStatus['triggerKind'];
    errorMessage: string | null;
};
export type InMemoryTriggerRef = {
    workflowId: string;
    nodeId: string;
};
export declare class WorkflowPublicationTriggerStatusRepository extends Repository<WorkflowPublicationTriggerStatus> {
    constructor(dataSource: DataSource);
    replaceForWorkflow(workflowId: string, rows: TriggerStatusRow[], trx?: EntityManager): Promise<void>;
    findByWorkflowId(workflowId: string): Promise<WorkflowPublicationTriggerStatus[]>;
    findActivatedInMemoryTriggers(): Promise<InMemoryTriggerRef[]>;
}
