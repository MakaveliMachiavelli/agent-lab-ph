"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.InstanceCredentialAssignmentRepository = void 0;
const di_1 = require("@n8n/di");
const typeorm_1 = require("@n8n/typeorm");
const entities_1 = require("../entities");
const base_repository_1 = require("./base-repository");
let InstanceCredentialAssignmentRepository = class InstanceCredentialAssignmentRepository extends base_repository_1.BaseRepository {
    constructor(dataSource) {
        super(entities_1.InstanceCredentialAssignment, dataSource.manager);
    }
    async findAvailableCredentials(credentialTypes, ctx = {}) {
        return await this.managerFor(ctx).find(entities_1.CredentialsEntity, {
            select: ['id', 'name', 'type'],
            where: {
                usageScope: 'instance',
                type: (0, typeorm_1.In)([...credentialTypes]),
            },
            order: { name: 'ASC' },
        });
    }
    async assignCredential(credentialUseId, credentialId, credentialTypes, ctx = {}) {
        const manager = this.managerFor(ctx);
        const credential = await this.findCredential(credentialId, credentialTypes, manager, ctx.trx !== undefined);
        if (!credential)
            return null;
        try {
            await manager.upsert(entities_1.InstanceCredentialAssignment, { credentialUseId, credentialId, updatedAt: new Date() }, ['credentialUseId']);
        }
        catch (error) {
            if (error instanceof typeorm_1.QueryFailedError) {
                const stillExists = await this.findCredential(credentialId, credentialTypes, manager)
                    .then((found) => Boolean(found))
                    .catch(() => true);
                if (!stillExists)
                    return null;
            }
            throw error;
        }
        return credential;
    }
    async clearCredential(credentialUseId, ctx = {}) {
        const manager = this.managerFor(ctx);
        await manager.delete(entities_1.InstanceCredentialAssignment, { credentialUseId });
    }
    async findAssignedCredentialId(credentialUseId, ctx = {}) {
        const manager = this.managerFor(ctx);
        const assignment = await manager.findOneBy(entities_1.InstanceCredentialAssignment, { credentialUseId });
        return assignment?.credentialId ?? null;
    }
    async findCredentialUseIds(credentialId, ctx = {}) {
        const assignments = await this.managerFor(ctx).find(entities_1.InstanceCredentialAssignment, {
            select: ['credentialUseId'],
            where: { credentialId },
            order: { credentialUseId: 'ASC' },
        });
        return assignments.map(({ credentialUseId }) => credentialUseId);
    }
    async findAssignedCredential(credentialUseId, credentialTypes, ctx = {}) {
        const manager = this.managerFor(ctx);
        const assignment = await manager.findOneBy(entities_1.InstanceCredentialAssignment, { credentialUseId });
        if (!assignment)
            return null;
        return {
            credentialId: assignment.credentialId,
            credential: await this.findCredential(assignment.credentialId, credentialTypes, manager),
        };
    }
    async findCredential(credentialId, credentialTypes, manager, lock = false) {
        return await manager.findOne(entities_1.CredentialsEntity, {
            where: {
                id: credentialId,
                usageScope: 'instance',
                type: (0, typeorm_1.In)([...credentialTypes]),
            },
            ...(lock && manager.connection.options.type === 'postgres'
                ? { lock: { mode: 'pessimistic_write' } }
                : {}),
        });
    }
};
exports.InstanceCredentialAssignmentRepository = InstanceCredentialAssignmentRepository;
exports.InstanceCredentialAssignmentRepository = InstanceCredentialAssignmentRepository = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [typeorm_1.DataSource])
], InstanceCredentialAssignmentRepository);
//# sourceMappingURL=instance-credential-assignment.repository.js.map