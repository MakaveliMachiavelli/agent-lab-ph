"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CreateWorkflowReviewRequestTables1784000000052 = void 0;
const REQUEST_TABLE = 'workflow_review_request';
const WORKFLOW_TABLE = 'workflow_review_request_workflow';
const REVIEWER_TABLE = 'workflow_review_request_reviewers';
const AUTHOR_TABLE = 'workflow_review_request_authors';
const PROJECT_TABLE = 'project';
const USER_TABLE = 'user';
const WORKFLOW_ENTITY_TABLE = 'workflow_entity';
const WORKFLOW_HISTORY_TABLE = 'workflow_history';
const IDX_REQUEST_PROJECT_STATE = 'workflow_review_request_project_state';
const UQ_WORKFLOW_REQUEST_WORKFLOW = 'workflow_review_request_workflow_request_workflow';
const IDX_WORKFLOW_REQUEST_WORKFLOW_WORKFLOW_REQUEST = 'workflow_review_request_workflow_workflow_request';
const IDX_WORKFLOW_REQUEST_REVIEWERS_USER = 'workflow_review_request_reviewers_user';
class CreateWorkflowReviewRequestTables1784000000052 {
    async up(context) {
        await this.createRequestTable(context);
        await this.createWorkflowChildTable(context);
        await this.createReviewerJunctionTable(context);
        await this.createAuthorJunctionTable(context);
        await this.createChildWorkflowAndJunctionIndexes(context);
    }
    async down({ schemaBuilder: { dropTable }, runQuery, escape, tablePrefix }) {
        await runQuery(`DROP INDEX IF EXISTS ${escape.indexName(IDX_WORKFLOW_REQUEST_REVIEWERS_USER)}`);
        await runQuery(`DROP INDEX IF EXISTS ${escape.indexName(IDX_WORKFLOW_REQUEST_WORKFLOW_WORKFLOW_REQUEST)}`);
        await runQuery(`DROP INDEX IF EXISTS ${this.uniqueIndexName(tablePrefix, UQ_WORKFLOW_REQUEST_WORKFLOW)}`);
        await runQuery(`DROP INDEX IF EXISTS ${escape.indexName(IDX_REQUEST_PROJECT_STATE)}`);
        await dropTable(AUTHOR_TABLE);
        await dropTable(REVIEWER_TABLE);
        await dropTable(WORKFLOW_TABLE);
        await dropTable(REQUEST_TABLE);
    }
    uniqueIndexName(tablePrefix, name) {
        return `"UQ_${tablePrefix}${name}"`;
    }
    async createRequestTable({ schemaBuilder: { createTable, column }, runQuery, escape, }) {
        await createTable(REQUEST_TABLE)
            .withColumns(column('id').varchar(36).primary, column('projectId').varchar(36).notNull, column('state')
            .varchar(16)
            .notNull.default("'open'")
            .withEnumCheck(['open', 'closed'])
            .comment('Review lifecycle: open reviews accept actions; closed reviews are done'), column('decision')
            .varchar(50)
            .notNull.default("'pending'")
            .withEnumCheck(['pending', 'changes_requested', 'approved'])
            .comment('Latest review outcome while the request is open'), column('title').varchar(255).notNull, column('description').text, column('createdById').uuid, column('updatedById').uuid, column('closedById').uuid, column('approvedAt').timestampTimezone())
            .withTimestamps.withForeignKey('projectId', {
            tableName: PROJECT_TABLE,
            columnName: 'id',
            onDelete: 'CASCADE',
        })
            .withForeignKey('createdById', {
            tableName: USER_TABLE,
            columnName: 'id',
            onDelete: 'SET NULL',
        })
            .withForeignKey('updatedById', {
            tableName: USER_TABLE,
            columnName: 'id',
            onDelete: 'SET NULL',
        })
            .withForeignKey('closedById', {
            tableName: USER_TABLE,
            columnName: 'id',
            onDelete: 'SET NULL',
        });
        const requestTable = escape.tableName(REQUEST_TABLE);
        const projectIdColumn = escape.columnName('projectId');
        const stateColumn = escape.columnName('state');
        await runQuery(`CREATE INDEX IF NOT EXISTS ${escape.indexName(IDX_REQUEST_PROJECT_STATE)}
			ON ${requestTable}(${projectIdColumn}, ${stateColumn})`);
    }
    async createWorkflowChildTable({ schemaBuilder: { createTable, column }, }) {
        await createTable(WORKFLOW_TABLE)
            .withColumns(column('id').varchar(36).primary, column('workflowReviewRequestId').varchar(36).notNull, column('workflowId').varchar(36).notNull, column('workflowVersionId')
            .varchar(36)
            .comment('Pinned workflow_history version for this review item'))
            .withForeignKey('workflowReviewRequestId', {
            tableName: REQUEST_TABLE,
            columnName: 'id',
            onDelete: 'CASCADE',
        })
            .withForeignKey('workflowId', {
            tableName: WORKFLOW_ENTITY_TABLE,
            columnName: 'id',
            onDelete: 'CASCADE',
        })
            .withForeignKey('workflowVersionId', {
            tableName: WORKFLOW_HISTORY_TABLE,
            columnName: 'versionId',
            onDelete: 'SET NULL',
        });
    }
    async createReviewerJunctionTable({ schemaBuilder: { createTable, column }, }) {
        await createTable(REVIEWER_TABLE)
            .withColumns(column('workflowReviewRequestId').varchar(36).notNull.primary, column('userId').uuid.notNull.primary)
            .withForeignKey('workflowReviewRequestId', {
            tableName: REQUEST_TABLE,
            columnName: 'id',
            onDelete: 'CASCADE',
        })
            .withForeignKey('userId', {
            tableName: USER_TABLE,
            columnName: 'id',
            onDelete: 'CASCADE',
        });
    }
    async createAuthorJunctionTable({ schemaBuilder: { createTable, column }, }) {
        await createTable(AUTHOR_TABLE)
            .withColumns(column('workflowReviewRequestId').varchar(36).notNull.primary, column('userId').uuid.notNull.primary)
            .withForeignKey('workflowReviewRequestId', {
            tableName: REQUEST_TABLE,
            columnName: 'id',
            onDelete: 'CASCADE',
        })
            .withForeignKey('userId', {
            tableName: USER_TABLE,
            columnName: 'id',
            onDelete: 'CASCADE',
        });
    }
    async createChildWorkflowAndJunctionIndexes({ runQuery, escape, tablePrefix, }) {
        const workflowTable = escape.tableName(WORKFLOW_TABLE);
        const reviewerTable = escape.tableName(REVIEWER_TABLE);
        const requestIdColumn = escape.columnName('workflowReviewRequestId');
        const workflowIdColumn = escape.columnName('workflowId');
        const userIdColumn = escape.columnName('userId');
        await runQuery(`CREATE UNIQUE INDEX IF NOT EXISTS ${this.uniqueIndexName(tablePrefix, UQ_WORKFLOW_REQUEST_WORKFLOW)}
			ON ${workflowTable}(${requestIdColumn}, ${workflowIdColumn})`);
        await runQuery(`CREATE INDEX IF NOT EXISTS ${escape.indexName(IDX_WORKFLOW_REQUEST_WORKFLOW_WORKFLOW_REQUEST)}
			ON ${workflowTable}(${workflowIdColumn}, ${requestIdColumn})`);
        await runQuery(`CREATE INDEX IF NOT EXISTS ${escape.indexName(IDX_WORKFLOW_REQUEST_REVIEWERS_USER)}
			ON ${reviewerTable}(${userIdColumn}, ${requestIdColumn})`);
    }
}
exports.CreateWorkflowReviewRequestTables1784000000052 = CreateWorkflowReviewRequestTables1784000000052;
//# sourceMappingURL=1784000000052-CreateWorkflowReviewRequestTables.js.map