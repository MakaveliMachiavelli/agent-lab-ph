"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MigrateRedactionEnforcementToFloor1784000000025 = void 0;
const n8n_workflow_1 = require("n8n-workflow");
const REDACTION_ENFORCEMENT_KEY = 'redaction.enforcement';
const FLOORS = ['off', 'production', 'all'];
class MigrateRedactionEnforcementToFloor1784000000025 {
    async up({ escape, runQuery, logger, migrationName }) {
        const settingsTable = escape.tableName('settings');
        const keyCol = escape.columnName('key');
        const valueCol = escape.columnName('value');
        const rows = await runQuery(`SELECT ${valueCol} AS value FROM ${settingsTable} WHERE ${keyCol} = :key;`, { key: REDACTION_ENFORCEMENT_KEY });
        if (rows.length === 0) {
            logger.info(`[${migrationName}] No stored redaction enforcement setting found, nothing to migrate`);
            return;
        }
        const floor = this.toFloor(rows[0].value);
        const serialized = JSON.stringify(floor);
        await runQuery(`UPDATE ${settingsTable} SET ${valueCol} = :value WHERE ${keyCol} = :key;`, {
            value: serialized,
            key: REDACTION_ENFORCEMENT_KEY,
        });
        logger.info(`[${migrationName}] Migrated redaction enforcement setting to floor='${floor}'`);
    }
    toFloor(raw) {
        let parsed;
        try {
            parsed = (0, n8n_workflow_1.jsonParse)(raw);
        }
        catch {
            return 'all';
        }
        if (typeof parsed === 'string' && FLOORS.includes(parsed)) {
            return parsed;
        }
        if (!this.isBooleanSettings(parsed))
            return 'all';
        if (!parsed.enforced)
            return 'off';
        if (!parsed.manual && parsed.production)
            return 'production';
        return 'all';
    }
    isBooleanSettings(value) {
        if (value === null || typeof value !== 'object')
            return false;
        const candidate = value;
        return (typeof candidate.enforced === 'boolean' &&
            typeof candidate.manual === 'boolean' &&
            typeof candidate.production === 'boolean');
    }
}
exports.MigrateRedactionEnforcementToFloor1784000000025 = MigrateRedactionEnforcementToFloor1784000000025;
//# sourceMappingURL=1784000000025-MigrateRedactionEnforcementToFloor.js.map