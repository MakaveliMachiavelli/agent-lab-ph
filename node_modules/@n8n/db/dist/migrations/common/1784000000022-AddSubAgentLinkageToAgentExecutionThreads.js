"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AddSubAgentLinkageToAgentExecutionThreads1784000000022 = void 0;
const COLUMNS_TO_WIDEN = [
    { table: 'agents_threads', column: 'id' },
    { table: 'agent_execution_threads', column: 'id' },
    { table: 'agent_execution', column: 'threadId' },
];
const SQLITE_DECLARED_TYPE_REPLACEMENTS = [
    { table: 'agents_threads', from: '"id" varchar(36)', to: '"id" varchar(128)' },
    {
        table: 'agent_execution_threads',
        from: '"id" varchar(36)',
        to: '"id" varchar(128)',
    },
    {
        table: 'agent_execution',
        from: '"threadId" varchar(36)',
        to: '"threadId" varchar(128)',
    },
];
class AddSubAgentLinkageToAgentExecutionThreads1784000000022 {
    async up({ schemaBuilder: { addColumns, column }, isPostgres, isSqlite, runQuery, escape, tablePrefix, }) {
        await addColumns('agent_execution_threads', [
            column('parentThreadId')
                .varchar(128)
                .comment('Parent session thread id that delegated this subagent run.'),
            column('parentAgentId')
                .varchar(36)
                .comment('Saved agent id of the parent that delegated this subagent run.'),
        ], { recreatesOnSqlite: true });
        if (isPostgres) {
            for (const { table, column: columnName } of COLUMNS_TO_WIDEN) {
                await runQuery(`ALTER TABLE ${escape.tableName(table)} ALTER COLUMN ${escape.columnName(columnName)} TYPE VARCHAR(128);`);
            }
        }
        else if (isSqlite) {
            await this.widenSqliteDeclaredColumnTypes({ runQuery, tablePrefix });
        }
    }
    async widenSqliteDeclaredColumnTypes({ runQuery, tablePrefix, }) {
        await runQuery('PRAGMA writable_schema = 1;');
        try {
            for (const { table, from, to } of SQLITE_DECLARED_TYPE_REPLACEMENTS) {
                await runQuery("UPDATE sqlite_master SET sql = replace(sql, :from, :to) WHERE type = 'table' AND name = :tableName", { from, to, tableName: `${tablePrefix}${table}` });
            }
        }
        finally {
            await runQuery('PRAGMA writable_schema = 0;');
        }
    }
}
exports.AddSubAgentLinkageToAgentExecutionThreads1784000000022 = AddSubAgentLinkageToAgentExecutionThreads1784000000022;
//# sourceMappingURL=1784000000022-AddSubAgentLinkageToAgentExecutionThreads.js.map