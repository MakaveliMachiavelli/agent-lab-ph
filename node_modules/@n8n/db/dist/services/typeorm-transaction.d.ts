import { Logger } from '@n8n/backend-common';
import { DataSource, EntityManager } from '@n8n/typeorm';
import type { IsolationLevel, OperationContext, RunOptions } from './transaction';
import { Transaction, TransactionRunner } from './transaction';
export declare class TypeOrmTransaction extends Transaction {
    #private;
    constructor(manager: EntityManager, isolationLevel?: IsolationLevel);
    getEntityManager(): EntityManager;
}
export declare class TypeOrmTransactionRunner extends TransactionRunner {
    private readonly dataSource;
    private readonly logger;
    constructor(dataSource: DataSource, logger: Logger);
    run<T>(context: OperationContext, fn: (ctx: OperationContext) => Promise<T>, options?: RunOptions): Promise<T>;
}
