"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _TypeOrmTransaction_manager;
Object.defineProperty(exports, "__esModule", { value: true });
exports.TypeOrmTransactionRunner = exports.TypeOrmTransaction = void 0;
const backend_common_1 = require("@n8n/backend-common");
const di_1 = require("@n8n/di");
const typeorm_1 = require("@n8n/typeorm");
const transaction_1 = require("./transaction");
class TypeOrmTransaction extends transaction_1.Transaction {
    constructor(manager, isolationLevel) {
        super(isolationLevel);
        _TypeOrmTransaction_manager.set(this, void 0);
        __classPrivateFieldSet(this, _TypeOrmTransaction_manager, manager, "f");
    }
    getEntityManager() {
        return __classPrivateFieldGet(this, _TypeOrmTransaction_manager, "f");
    }
}
exports.TypeOrmTransaction = TypeOrmTransaction;
_TypeOrmTransaction_manager = new WeakMap();
let TypeOrmTransactionRunner = class TypeOrmTransactionRunner extends transaction_1.TransactionRunner {
    constructor(dataSource, logger) {
        super();
        this.dataSource = dataSource;
        this.logger = logger;
    }
    async run(context, fn, options) {
        if (context.trx) {
            const requested = options?.isolationLevel;
            const active = context.trx.getIsolationLevel();
            if (requested && requested !== active) {
                this.logger.warn(`Transaction requested isolation level "${requested}" but is joining an active ` +
                    `transaction (${active ? `at "${active}"` : 'at the database default'}); ` +
                    'the requested level is ignored.');
            }
            return await fn(context);
        }
        const isolationLevel = options?.isolationLevel;
        const runInTransaction = async (manager) => await fn({ ...context, trx: new TypeOrmTransaction(manager, isolationLevel) });
        return isolationLevel
            ? await this.dataSource.transaction(isolationLevel, runInTransaction)
            : await this.dataSource.transaction(runInTransaction);
    }
};
exports.TypeOrmTransactionRunner = TypeOrmTransactionRunner;
exports.TypeOrmTransactionRunner = TypeOrmTransactionRunner = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [typeorm_1.DataSource, backend_common_1.Logger])
], TypeOrmTransactionRunner);
//# sourceMappingURL=typeorm-transaction.js.map