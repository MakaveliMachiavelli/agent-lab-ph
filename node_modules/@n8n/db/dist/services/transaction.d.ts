export type IsolationLevel = 'READ UNCOMMITTED' | 'READ COMMITTED' | 'REPEATABLE READ' | 'SERIALIZABLE';
export declare abstract class Transaction {
    private readonly isolationLevel?;
    constructor(isolationLevel?: IsolationLevel | undefined);
    getIsolationLevel(): IsolationLevel | undefined;
}
export interface OperationContext {
    readonly trx?: Transaction;
}
export interface RunOptions {
    isolationLevel?: IsolationLevel;
}
export declare abstract class TransactionRunner {
    abstract run<T>(context: OperationContext, fn: (ctx: OperationContext) => Promise<T>, options?: RunOptions): Promise<T>;
}
