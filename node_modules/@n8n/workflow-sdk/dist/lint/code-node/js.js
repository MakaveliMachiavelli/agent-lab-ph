"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.lintJsCode = lintJsCode;
exports.hasNestedTemplateLiterals = hasNestedTemplateLiterals;
const acorn = __importStar(require("acorn"));
const ast_walk_1 = require("../ast-walk");
const types_1 = require("../types");
const NETWORK_CALLEE_NAMES = new Set(['fetch', 'axios', 'XMLHttpRequest']);
const HTTP_MODULES = new Set([
    'http',
    'https',
    'http2',
    'node-fetch',
    'axios',
    'got',
    'undici',
    'node:http',
    'node:https',
    'node:http2',
]);
const FORBIDDEN_MODULE_PREFIXES = ['luxon', 'openai', '@openai/', 'langchain', '@langchain/'];
const EACH_ITEM_DISALLOWED_INPUT_METHODS = new Set(['first', 'last', 'all', 'itemMatching']);
function parseJs(code) {
    const opts = {
        ecmaVersion: 'latest',
        locations: true,
        allowReturnOutsideFunction: true,
    };
    try {
        return acorn.parse(code, { ...opts, sourceType: 'script' });
    }
    catch {
        try {
            return acorn.parse(code, { ...opts, sourceType: 'module' });
        }
        catch {
            return undefined;
        }
    }
}
function stringLiteralArg(node) {
    if (!node)
        return undefined;
    if (node.type === 'Literal' && typeof node.value === 'string')
        return node.value;
    return undefined;
}
function isRequireCall(call) {
    return call.callee.type === 'Identifier' && call.callee.name === 'require';
}
function moduleSpecifierFromCall(call) {
    if (isRequireCall(call)) {
        return stringLiteralArg(call.arguments[0]);
    }
    return undefined;
}
function moduleIsForbidden(specifier) {
    return FORBIDDEN_MODULE_PREFIXES.some((prefix) => specifier === prefix || specifier.startsWith(prefix));
}
function hasNestedTemplateLiteral(ast) {
    let found = false;
    (0, ast_walk_1.walkAst)(ast, (node) => {
        if (found || node.type !== 'TemplateLiteral')
            return;
        for (const expr of node.expressions) {
            let nested = false;
            (0, ast_walk_1.walkAst)(expr, (inner) => {
                if (inner.type === 'TemplateLiteral')
                    nested = true;
            });
            if (nested) {
                found = true;
                return;
            }
        }
    });
    return found;
}
function lintJsCode(jsCode, options = {}) {
    if (jsCode.length === 0)
        return [];
    const ast = parseJs(jsCode);
    if (!ast)
        return [];
    const issues = [];
    const namePrefix = options.nodeName ? `'${options.nodeName}' ` : '';
    let sawNetwork = false;
    let sawForbiddenImport = false;
    let disallowedInputMethod;
    (0, ast_walk_1.walkAst)(ast, (node) => {
        if (node.type === 'ImportDeclaration') {
            const source = node.source.type === 'Literal' ? String(node.source.value) : '';
            if (moduleIsForbidden(source))
                sawForbiddenImport = true;
            if (HTTP_MODULES.has(source))
                sawNetwork = true;
            return;
        }
        if (node.type === 'ImportExpression') {
            const mod = stringLiteralArg(node.source);
            if (mod) {
                if (HTTP_MODULES.has(mod))
                    sawNetwork = true;
                if (moduleIsForbidden(mod))
                    sawForbiddenImport = true;
            }
            return;
        }
        if (node.type === 'CallExpression') {
            const call = node;
            if (call.callee.type === 'Identifier' && NETWORK_CALLEE_NAMES.has(call.callee.name)) {
                sawNetwork = true;
            }
            if (call.callee.type === 'MemberExpression' &&
                !call.callee.computed &&
                call.callee.property.type === 'Identifier' &&
                EACH_ITEM_DISALLOWED_INPUT_METHODS.has(call.callee.property.name) &&
                call.callee.object.type === 'Identifier' &&
                call.callee.object.name === '$input' &&
                disallowedInputMethod === undefined) {
                disallowedInputMethod = call.callee.property.name;
            }
            const required = moduleSpecifierFromCall(call);
            if (required) {
                if (HTTP_MODULES.has(required))
                    sawNetwork = true;
                if (moduleIsForbidden(required))
                    sawForbiddenImport = true;
            }
        }
        if (node.type === 'NewExpression' &&
            node.callee.type === 'Identifier' &&
            node.callee.name === 'XMLHttpRequest') {
            sawNetwork = true;
        }
    });
    if (sawNetwork) {
        issues.push((0, types_1.lintIssue)({
            code: 'CODE_NODE_NETWORK_CALL',
            message: `${namePrefix}Code node calls fetch/axios/XMLHttpRequest or requires an HTTP module. ` +
                'Code nodes have no network access at runtime — make the HTTP/API call with an HTTP Request node ' +
                'and transform its output in the Code node instead.',
            lintTarget: 'jsCode',
            nodeName: options.nodeName,
            parameterPath: 'jsCode',
        }));
    }
    if (sawForbiddenImport) {
        issues.push((0, types_1.lintIssue)({
            code: 'CODE_NODE_FORBIDDEN_IMPORT',
            message: `${namePrefix}Code node imports a module unavailable in the sandbox (luxon, openai, langchain, …). ` +
                'Use JavaScript `Date`/`Intl`, `$now`/`$today`, existing workflow data, or dedicated AI nodes instead.',
            lintTarget: 'jsCode',
            nodeName: options.nodeName,
            parameterPath: 'jsCode',
        }));
    }
    if (options.mode === 'runOnceForEachItem' && disallowedInputMethod !== undefined) {
        issues.push((0, types_1.lintIssue)({
            code: 'CODE_MODE_API_MISUSE',
            message: `${namePrefix}uses mode: 'runOnceForEachItem' but calls $input.${disallowedInputMethod}(). ` +
                `$input.${disallowedInputMethod}() is only available in runOnceForAllItems (the default). ` +
                'Switch mode to runOnceForAllItems, or use $input.item / $json for per-item work.',
            lintTarget: 'jsCode',
            nodeName: options.nodeName,
            parameterPath: 'jsCode',
        }));
    }
    if (hasNestedTemplateLiteral(ast)) {
        issues.push((0, types_1.lintIssue)({
            code: 'CODE_NESTED_TEMPLATE_LITERAL',
            message: `${namePrefix}Code node uses nested template literals, which often break after save. ` +
                'Build multi-line strings with arrays joined by a runtime separator, e.g. ' +
                '`const LF = String.fromCharCode(10); return lines.join(LF);`.',
            lintTarget: 'jsCode',
            nodeName: options.nodeName,
            parameterPath: 'jsCode',
        }));
    }
    return issues;
}
function hasNestedTemplateLiterals(jsCode) {
    const ast = parseJs(jsCode);
    return ast ? hasNestedTemplateLiteral(ast) : false;
}
//# sourceMappingURL=js.js.map