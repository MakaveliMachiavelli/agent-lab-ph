import type { Node } from 'estree';
import type { SourceLintIssue } from './types';
export declare function walkAst(node: Node, visit: (n: Node, parent: Node | undefined) => void, options?: {
    parent?: Node;
    skipChildren?: (n: Node, parent: Node | undefined) => boolean;
}): void;
export declare function dedupeSourceLintIssues(issues: SourceLintIssue[]): SourceLintIssue[];
