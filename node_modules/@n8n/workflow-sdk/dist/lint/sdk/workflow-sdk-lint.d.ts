import type { Program } from 'estree';
import { type SourceLintIssue } from '../types';
export interface AsConstMatch {
    line: number;
    column: number;
}
export declare function prepareSourceForLint(source: string): {
    code: string;
    asConstMatches: AsConstMatch[];
};
export declare function lintWorkflowSdkAst(ast: Program, asConstMatches?: AsConstMatch[]): SourceLintIssue[];
export declare function lintWorkflowSdkSource(source: string): SourceLintIssue[];
