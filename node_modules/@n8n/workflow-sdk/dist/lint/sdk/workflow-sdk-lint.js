"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.prepareSourceForLint = prepareSourceForLint;
exports.lintWorkflowSdkAst = lintWorkflowSdkAst;
exports.lintWorkflowSdkSource = lintWorkflowSdkSource;
const ast_interpreter_1 = require("../../ast-interpreter");
const ast_walk_1 = require("../ast-walk");
const extract_snippets_1 = require("../code-node/extract-snippets");
const types_1 = require("../types");
const NATIVE_ARRAY_METHODS = new Set([
    'map',
    'join',
    'filter',
    'reduce',
    'forEach',
    'flatMap',
    'find',
    'some',
    'every',
]);
const SDK_FLUENT_METHODS = new Set([
    'to',
    'add',
    'onTrue',
    'onFalse',
    'onCase',
    'onDone',
    'onEachBatch',
    'onError',
    'input',
    'output',
    'settings',
    'update',
    'then',
    'group',
]);
function prepareSourceForLint(source) {
    let code = source;
    const blankSameLines = (match) => '\n'.repeat((match.match(/\n/g) ?? []).length);
    const spaces = (match) => ' '.repeat(match.length);
    code = code.replace(/^\s*import\s[\s\S]*?from\s+['"][^'"]+['"];?\s*$/gm, blankSameLines);
    code = code.replace(/^\s*import\s+type\s[\s\S]*?;?\s*$/gm, blankSameLines);
    code = code.replace(/\b((?:const|let|var)\s+[A-Za-z_$][\w$]*)\s*:\s*[A-Za-z_$][\w$.|<>[\]\s,&?]*(?=\s*=)/g, '$1');
    code = code.replace(/([,(]\s*[A-Za-z_$][\w$]*)\s*:\s*[A-Za-z_$][\w$.|<>[\]\s,&?]*(?=\s*[,)=])/g, '$1');
    const asConstMatches = [];
    const asConstPattern = /\bas\s+const\b/g;
    const lines = code.split(/\r?\n/);
    for (let i = 0; i < lines.length; i++) {
        asConstPattern.lastIndex = 0;
        let match;
        while ((match = asConstPattern.exec(lines[i] ?? '')) !== null) {
            asConstMatches.push({ line: i + 1, column: match.index });
        }
    }
    code = code.replace(/\bas\s+const\b/g, spaces);
    code = code.replace(/\bas\s+[A-Za-z_$][\w$.<>,\s|&[\]?]*/g, spaces);
    code = code.replace(/\bsatisfies\s+[A-Za-z_$][\w$.|<>[\]\s,&?]*/g, spaces);
    return { code, asConstMatches };
}
function locationOf(node) {
    if (!node.loc)
        return {};
    return { line: node.loc.start.line, column: node.loc.start.column + 1 };
}
function isPlaceholderCall(node) {
    return node.callee.type === 'Identifier' && node.callee.name === 'placeholder';
}
function isExprCall(node) {
    return node.callee.type === 'Identifier' && node.callee.name === 'expr';
}
function directReceiverName(member) {
    if (member.object.type === 'Identifier') {
        return member.object.name;
    }
    return undefined;
}
function stringContentRanges(ast) {
    const ranges = [];
    (0, ast_walk_1.walkAst)(ast, (node) => {
        const isString = node.type === 'TemplateLiteral' ||
            (node.type === 'Literal' && typeof node.value === 'string');
        if (!isString || !node.loc)
            return;
        ranges.push({
            startLine: node.loc.start.line,
            startColumn: node.loc.start.column,
            endLine: node.loc.end.line,
            endColumn: node.loc.end.column,
        });
    });
    return ranges;
}
function rangeContains(range, line, column) {
    if (line < range.startLine || line > range.endLine)
        return false;
    if (line === range.startLine && column < range.startColumn)
        return false;
    if (line === range.endLine && column >= range.endColumn)
        return false;
    return true;
}
function lintWorkflowSdkAst(ast, asConstMatches = []) {
    const issues = [];
    const stringRanges = stringContentRanges(ast);
    for (const match of asConstMatches) {
        if (stringRanges.some((range) => rangeContains(range, match.line, match.column)))
            continue;
        issues.push((0, types_1.lintIssue)({
            code: 'SDK_AS_CONST',
            message: '`as const` is TypeScript-only and the workflow parser cannot interpret it. Remove the assertion.',
            line: match.line,
            column: match.column + 1,
            lintTarget: 'sdk',
        }));
    }
    const exportIndex = ast.body.findIndex((stmt) => stmt.type === 'ExportDefaultDeclaration');
    if (exportIndex >= 0) {
        for (let i = exportIndex + 1; i < ast.body.length; i++) {
            const stmt = ast.body[i];
            if (!stmt || stmt.type === 'EmptyStatement')
                continue;
            issues.push((0, types_1.lintIssue)({
                code: 'SDK_CODE_AFTER_EXPORT_DEFAULT',
                message: 'Statement after `export default workflow(...)` never reaches the builder — nodes/wiring here are dropped. ' +
                    'Compose all `.to()` / `.onTrue()` / `.onFalse()` / `.onCase()` inside the export default chain.',
                ...locationOf(stmt),
                lintTarget: 'sdk',
            }));
        }
    }
    const branchCounts = new Map();
    (0, ast_walk_1.walkAst)(ast, (node, parent) => {
        if (node.type === 'ImportDeclaration')
            return;
        const forbidden = ast_interpreter_1.FORBIDDEN_NODE_TYPES[node.type];
        if (forbidden) {
            issues.push((0, types_1.lintIssue)({
                code: 'SDK_FORBIDDEN_CONSTRUCT',
                message: forbidden,
                ...locationOf(node),
                lintTarget: 'sdk',
            }));
        }
        if (node.type === 'Identifier' && ast_interpreter_1.DANGEROUS_GLOBALS.has(node.name)) {
            const isPropertyName = parent?.type === 'MemberExpression' && parent.property === node && !parent.computed;
            const isObjectKey = parent?.type === 'Property' && parent.key === node;
            const isSafeMethodObject = parent?.type === 'MemberExpression' &&
                parent.object === node &&
                !parent.computed &&
                parent.property.type === 'Identifier' &&
                (0, ast_interpreter_1.getSafeJSONMethod)(node.name, parent.property.name) !== undefined;
            if (!isPropertyName && !isObjectKey && !isSafeMethodObject) {
                issues.push((0, types_1.lintIssue)({
                    code: 'SDK_FORBIDDEN_CONSTRUCT',
                    message: `Global '${node.name}' is unavailable in SDK builder code. Move runtime logic to a Code node or expr().`,
                    ...locationOf(node),
                    lintTarget: 'sdk',
                }));
            }
        }
        if (node.type !== 'CallExpression')
            return;
        const call = node;
        if (call.callee.type === 'Identifier' && call.callee.name === 'sticky') {
            issues.push((0, types_1.lintIssue)({
                code: 'SDK_UNSOLICITED_STICKY',
                message: 'Do not add sticky() / stickyNote nodes unless the user explicitly asked for canvas notes. ' +
                    'Put explanations in the chat reply instead.',
                ...locationOf(call),
                lintTarget: 'sdk',
            }));
        }
        if (call.callee.type === 'MemberExpression' &&
            !call.callee.computed &&
            call.callee.property.type === 'Identifier') {
            const method = call.callee.property.name;
            if (method === 'onTrue' || method === 'onFalse') {
                const receiver = directReceiverName(call.callee);
                if (receiver) {
                    const key = `${receiver}.${method}`;
                    const prev = branchCounts.get(key);
                    if (prev) {
                        prev.count += 1;
                    }
                    else {
                        branchCounts.set(key, { count: 1, ...locationOf(call) });
                    }
                }
            }
            if (!SDK_FLUENT_METHODS.has(method) && NATIVE_ARRAY_METHODS.has(method)) {
                issues.push((0, types_1.lintIssue)({
                    code: 'SDK_FORBIDDEN_CONSTRUCT',
                    message: `'.${method}()' is not available on SDK builder objects. Build strings with template ` +
                        'literals, or do transforms in a Code node / expr().',
                    ...locationOf(call),
                    lintTarget: 'sdk',
                }));
            }
        }
        if (isExprCall(call)) {
            for (const arg of call.arguments) {
                if (arg.type === 'SpreadElement')
                    continue;
                if (arg.type === 'CallExpression' && isPlaceholderCall(arg)) {
                    issues.push((0, types_1.lintIssue)({
                        code: 'SDK_PLACEHOLDER_WRAPPED',
                        message: "Do not wrap placeholder() in expr(). Use placeholder('hint') directly as the parameter value.",
                        ...locationOf(call),
                        lintTarget: 'sdk',
                    }));
                }
                if (arg.type === 'TemplateLiteral') {
                    for (const expr of arg.expressions) {
                        if (expr.type === 'CallExpression' && isPlaceholderCall(expr)) {
                            issues.push((0, types_1.lintIssue)({
                                code: 'SDK_PLACEHOLDER_WRAPPED',
                                message: 'Do not embed placeholder() inside expr()/template strings. Use placeholder() as the direct parameter value.',
                                ...locationOf(call),
                                lintTarget: 'sdk',
                            }));
                        }
                    }
                }
                if (arg.type === 'ArrayExpression') {
                    for (const el of arg.elements) {
                        if (el && el.type === 'CallExpression' && isPlaceholderCall(el)) {
                            issues.push((0, types_1.lintIssue)({
                                code: 'SDK_PLACEHOLDER_WRAPPED',
                                message: 'Do not wrap placeholder() in an array unless the node definition expects an array and placeholder is a direct element value with no expr() wrapper.',
                                ...locationOf(call),
                                lintTarget: 'sdk',
                            }));
                        }
                    }
                }
            }
        }
    }, { skipChildren: extract_snippets_1.isEmbeddedCodePropertyValue });
    for (const [key, info] of branchCounts) {
        if (info.count < 2)
            continue;
        const [, method] = key.split('.');
        issues.push((0, types_1.lintIssue)({
            code: 'SDK_REPEATED_BRANCH_WIRING',
            message: `Repeated \`.${method}()\` (${info.count} times) — each call overwrites the previous target. ` +
                'Wire once on the workflow builder chain.',
            line: info.line,
            column: info.column,
            lintTarget: 'sdk',
        }));
    }
    return (0, ast_walk_1.dedupeSourceLintIssues)(issues);
}
function lintWorkflowSdkSource(source) {
    const { code, asConstMatches } = prepareSourceForLint(source);
    let ast;
    try {
        ast = (0, ast_interpreter_1.parseSDKCode)(code);
    }
    catch {
        return (0, ast_walk_1.dedupeSourceLintIssues)(asConstMatches.map((match) => (0, types_1.lintIssue)({
            code: 'SDK_AS_CONST',
            message: '`as const` is TypeScript-only and the workflow parser cannot interpret it. Remove the assertion.',
            line: match.line,
            column: match.column + 1,
            lintTarget: 'sdk',
        })));
    }
    return lintWorkflowSdkAst(ast, asConstMatches);
}
//# sourceMappingURL=workflow-sdk-lint.js.map