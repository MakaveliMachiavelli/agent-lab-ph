"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.lintWorkflowSource = lintWorkflowSource;
const ast_interpreter_1 = require("../ast-interpreter");
const ast_walk_1 = require("./ast-walk");
const extract_snippets_1 = require("./code-node/extract-snippets");
const js_1 = require("./code-node/js");
const python_1 = require("./code-node/python");
const workflow_sdk_lint_1 = require("./sdk/workflow-sdk-lint");
const types_1 = require("./types");
function lintWorkflowSource(source) {
    const { code, asConstMatches } = (0, workflow_sdk_lint_1.prepareSourceForLint)(source);
    let ast;
    try {
        ast = (0, ast_interpreter_1.parseSDKCode)(code);
    }
    catch {
        return (0, ast_walk_1.dedupeSourceLintIssues)(asConstMatches.map((match) => (0, types_1.lintIssue)({
            code: 'SDK_AS_CONST',
            message: '`as const` is TypeScript-only and the workflow parser cannot interpret it. Remove the assertion.',
            line: match.line,
            column: match.column + 1,
            lintTarget: 'sdk',
        })));
    }
    const sdkIssues = (0, workflow_sdk_lint_1.lintWorkflowSdkAst)(ast, asConstMatches);
    const parents = (0, extract_snippets_1.buildParentMap)(ast);
    const snippets = (0, extract_snippets_1.extractEmbeddedCodeSnippets)(ast, code, parents);
    const embeddedIssues = [];
    for (const snippet of snippets) {
        const base = { line: snippet.line, column: snippet.column };
        if (snippet.parameter === 'jsCode') {
            embeddedIssues.push(...(0, js_1.lintJsCode)(snippet.code, { mode: snippet.mode }).map((issue) => ({
                ...issue,
                ...base,
            })));
        }
        else {
            embeddedIssues.push(...(0, python_1.lintPythonCode)(snippet.code).map((issue) => ({
                ...issue,
                ...base,
            })));
        }
    }
    return (0, ast_walk_1.dedupeSourceLintIssues)([...sdkIssues, ...embeddedIssues]);
}
//# sourceMappingURL=lint-workflow-source.js.map