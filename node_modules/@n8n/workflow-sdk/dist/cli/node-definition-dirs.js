"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.NODE_DEFINITION_DIRS_ENV_VAR = void 0;
exports.resolveNodeDefinitionDirs = resolveNodeDefinitionDirs;
const constants_1 = require("@n8n/constants");
const node_fs_1 = __importDefault(require("node:fs"));
const node_module_1 = require("node:module");
const node_path_1 = __importDefault(require("node:path"));
exports.NODE_DEFINITION_DIRS_ENV_VAR = 'N8N_NODE_DEFINITION_DIRS';
function looksLikeNodeDefinitionDir(dir) {
    return node_fs_1.default.existsSync(node_path_1.default.join(dir, 'nodes'));
}
function splitEnvValue(value) {
    return value
        .split(node_path_1.default.delimiter)
        .map((entry) => entry.trim())
        .filter(Boolean);
}
function keepExisting(candidates) {
    const dirs = [];
    for (const candidate of candidates) {
        const resolved = node_path_1.default.resolve(candidate);
        if (!dirs.includes(resolved) && looksLikeNodeDefinitionDir(resolved)) {
            dirs.push(resolved);
        }
    }
    return dirs;
}
function resolveFromPackages(startDirs) {
    const dirs = [];
    for (const startDir of startDirs) {
        const requireFrom = (0, node_module_1.createRequire)(node_path_1.default.join(startDir, 'noop.js'));
        for (const packageId of constants_1.BUILTIN_NODES_PACKAGES) {
            let packageJsonPath;
            try {
                packageJsonPath = requireFrom.resolve(`${packageId}/package.json`);
            }
            catch {
                continue;
            }
            const candidate = node_path_1.default.join(node_path_1.default.dirname(packageJsonPath), 'dist', 'node-definitions');
            if (!dirs.includes(candidate) && looksLikeNodeDefinitionDir(candidate)) {
                dirs.push(candidate);
            }
        }
    }
    return dirs;
}
function resolveNodeDefinitionDirs(options) {
    const { explicit = [], workflowDir, cwd = process.cwd() } = options;
    if (explicit.length > 0) {
        return keepExisting(explicit);
    }
    const envValue = process.env[exports.NODE_DEFINITION_DIRS_ENV_VAR];
    if (envValue) {
        return keepExisting(splitEnvValue(envValue));
    }
    return resolveFromPackages([workflowDir, cwd]);
}
//# sourceMappingURL=node-definition-dirs.js.map