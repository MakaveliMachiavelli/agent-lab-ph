"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parsePinDataResponse = parsePinDataResponse;
exports.repairStructuredOutput = repairStructuredOutput;
const ai_root_shapes_1 = require("./ai-root-shapes");
const context_1 = require("./context");
function parsePinDataResponse(responseText, expectedNodes) {
    let cleaned = responseText.trim();
    if (cleaned.startsWith('```')) {
        cleaned = cleaned.replace(/^```(?:json)?\n?/, '').replace(/\n?```$/, '');
    }
    let parsed;
    try {
        parsed = JSON.parse(cleaned);
    }
    catch {
        return {};
    }
    const pinData = {};
    for (const nodeName of expectedNodes) {
        const nodeData = parsed[nodeName];
        if (!Array.isArray(nodeData))
            continue;
        pinData[nodeName] = nodeData
            .map((item) => {
            if (typeof item === 'object' && item !== null && 'json' in item) {
                return item;
            }
            return { json: item ?? {} };
        })
            .filter((item) => !isEmptyFixtureItem(item));
    }
    return pinData;
}
function isEmptyFixtureItem(item) {
    const json = item.json;
    if (typeof json !== 'object' || json === null)
        return false;
    if (Object.keys(json).length > 0)
        return false;
    return !Object.keys(item).some((key) => key !== 'json' && key !== 'pairedItem');
}
function tryParseJsonContainer(text) {
    const trimmed = text.trim();
    if (!trimmed.startsWith('{') && !trimmed.startsWith('['))
        return undefined;
    try {
        const parsed = JSON.parse(trimmed);
        if (typeof parsed === 'object' && parsed !== null) {
            return parsed;
        }
    }
    catch {
    }
    return undefined;
}
function repairStructuredOutput(pinData, workflow, schemaContexts) {
    const schemaByName = new Map((schemaContexts ?? []).map((ctx) => [ctx.nodeName, ctx.schema]));
    const repaired = { ...pinData };
    const targets = new Set((0, context_1.findOutputParserTargets)(workflow).keys());
    const extractorNames = new Set();
    for (const node of workflow.nodes) {
        if (node.name && node.type === context_1.INFORMATION_EXTRACTOR_NODE_TYPE) {
            targets.add(node.name);
            extractorNames.add(node.name);
        }
    }
    for (const nodeName of targets) {
        const items = repaired[nodeName];
        if (!items)
            continue;
        const envelopeKey = (0, ai_root_shapes_1.findEnvelopeKey)(schemaByName.get(nodeName)) ??
            (extractorNames.has(nodeName) ? 'output' : undefined);
        if (!envelopeKey)
            continue;
        repaired[nodeName] = items.map((item) => {
            const json = item.json;
            if (typeof json !== 'object' || json === null || Array.isArray(json))
                return item;
            const record = json;
            const value = record[envelopeKey];
            if (typeof value === 'string') {
                const parsed = tryParseJsonContainer(value);
                if (parsed !== undefined) {
                    return { ...item, json: { ...record, [envelopeKey]: parsed } };
                }
                return item;
            }
            if (!(envelopeKey in record)) {
                return { ...item, json: { [envelopeKey]: record } };
            }
            return item;
        });
    }
    return repaired;
}
//# sourceMappingURL=parse.js.map