"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveMainOutputCount = resolveMainOutputCount;
function resolveMainOutputCount(nodeTypesProvider, nodeType, version) {
    try {
        const nodeTypeObj = nodeTypesProvider.getByNameAndVersion(nodeType, version);
        if (!nodeTypeObj?.description?.outputs)
            return undefined;
        const outputs = nodeTypeObj.description.outputs;
        if (typeof outputs === 'string')
            return undefined;
        return outputs.filter((output) => {
            if (typeof output === 'string') {
                return output === 'main';
            }
            return output.type === 'main';
        }).length;
    }
    catch {
        return undefined;
    }
}
//# sourceMappingURL=resolve-main-output-count.js.map