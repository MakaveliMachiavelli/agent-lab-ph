"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SDK_INLINE_CONSTRAINTS = exports.SAFE_STRING_METHOD_NAMES = exports.SAFE_JSON_METHOD_NAMES = exports.FORBIDDEN_NODE_TYPES = exports.DANGEROUS_GLOBALS = exports.BUILDER_BLOCKED_GLOBALS = exports.ALLOWED_METHODS = exports.SDK_METHODS = exports.AUTO_RENAMEABLE_SDK_FUNCTIONS = exports.ALLOWED_SDK_FUNCTIONS = void 0;
exports.isAutoRenameableSDKFunction = isAutoRenameableSDKFunction;
exports.validateNodeType = validateNodeType;
exports.validateIdentifier = validateIdentifier;
exports.validateCallExpression = validateCallExpression;
exports.validateMemberExpression = validateMemberExpression;
exports.isAllowedSDKFunction = isAllowedSDKFunction;
exports.isAllowedMethod = isAllowedMethod;
exports.allowedMethodNames = allowedMethodNames;
exports.getSafeJSONMethod = getSafeJSONMethod;
exports.getSafeStringMethod = getSafeStringMethod;
const errors_1 = require("./errors");
exports.ALLOWED_SDK_FUNCTIONS = new Set([
    'workflow',
    'node',
    'trigger',
    'sticky',
    'placeholder',
    'newCredential',
    'ifElse',
    'switchCase',
    'merge',
    'splitInBatches',
    'nextBatch',
    'languageModel',
    'memory',
    'tool',
    'outputParser',
    'embedding',
    'embeddings',
    'vectorStore',
    'retriever',
    'documentLoader',
    'textSplitter',
    'reranker',
    'fromAi',
    'nodeJson',
]);
exports.AUTO_RENAMEABLE_SDK_FUNCTIONS = new Set([
    'languageModel',
    'memory',
    'tool',
    'outputParser',
    'embedding',
    'embeddings',
    'vectorStore',
    'retriever',
    'documentLoader',
    'textSplitter',
    'reranker',
]);
function isAutoRenameableSDKFunction(name) {
    return exports.AUTO_RENAMEABLE_SDK_FUNCTIONS.has(name);
}
exports.SDK_METHODS = [
    { name: 'add', group: 'workflow', public: true },
    { name: 'to', group: 'workflow', public: true },
    { name: 'group', group: 'workflow', public: true },
    { name: 'input', group: 'node', public: true },
    { name: 'output', group: 'node', public: true },
    { name: 'onError', group: 'node', public: true },
    { name: 'onTrue', group: 'control-flow', public: true },
    { name: 'onFalse', group: 'control-flow', public: true },
    { name: 'onCase', group: 'control-flow', public: true },
    { name: 'onEachBatch', group: 'control-flow', public: true },
    { name: 'onDone', group: 'control-flow', public: true },
    { name: 'connect', group: 'connection', public: true },
    { name: 'toJSON', group: 'internal', public: false },
    { name: 'validate', group: 'internal', public: false },
];
exports.ALLOWED_METHODS = new Set(exports.SDK_METHODS.map((m) => m.name));
exports.BUILDER_BLOCKED_GLOBALS = [
    { name: 'eval' },
    { name: 'Function' },
    { name: 'require' },
    { name: 'import' },
    { name: 'process' },
    { name: 'global' },
    { name: 'globalThis' },
    { name: 'window' },
    { name: 'document' },
    { name: 'setTimeout' },
    { name: 'setInterval' },
    { name: 'setImmediate' },
    { name: 'clearTimeout' },
    { name: 'clearInterval' },
    { name: 'clearImmediate' },
    { name: '__dirname' },
    { name: '__filename' },
    { name: 'module' },
    { name: 'exports' },
    { name: 'Buffer' },
    { name: 'Reflect' },
    { name: 'Proxy' },
    { name: 'Object', alternative: 'build object literals directly, or transform in a Code node' },
    { name: 'Array', alternative: 'build array literals directly, or transform in a Code node' },
    { name: 'String' },
    { name: 'Number', alternative: 'use numeric literals, or convert in a Code node / expression' },
    { name: 'Boolean' },
    { name: 'Symbol' },
    { name: 'BigInt' },
    {
        name: 'JSON',
        alternative: 'only JSON.stringify is available; parse at runtime in a Code node',
    },
    { name: 'Math', alternative: 'compute at runtime in a Code node or an n8n expression' },
    { name: 'Date', alternative: 'use the $now / $today helpers inside expr()' },
    { name: 'RegExp', alternative: 'match at runtime in a Code node or an n8n expression' },
    { name: 'Promise', alternative: 'builder code is synchronous; no async/await or promises' },
    { name: 'WeakRef' },
    { name: 'WeakMap' },
    { name: 'WeakSet' },
    { name: 'Map', alternative: 'use object literals, or build collections in a Code node' },
    { name: 'Set', alternative: 'use array literals, or build collections in a Code node' },
    { name: 'ArrayBuffer' },
    { name: 'SharedArrayBuffer' },
    { name: 'DataView' },
    { name: 'Atomics' },
    { name: 'WebAssembly' },
    { name: 'fetch' },
    { name: 'XMLHttpRequest' },
    { name: 'queueMicrotask' },
    { name: 'structuredClone' },
    { name: 'Error' },
    { name: 'TypeError' },
    { name: 'RangeError' },
    { name: 'console' },
];
exports.DANGEROUS_GLOBALS = new Set(exports.BUILDER_BLOCKED_GLOBALS.map((g) => g.name));
const ALLOWED_NODE_TYPES = new Set([
    'Program',
    'VariableDeclaration',
    'VariableDeclarator',
    'ExpressionStatement',
    'ExportDefaultDeclaration',
    'CallExpression',
    'MemberExpression',
    'ObjectExpression',
    'ArrayExpression',
    'Identifier',
    'Literal',
    'TemplateLiteral',
    'TemplateElement',
    'SpreadElement',
    'Property',
    'AssignmentExpression',
    'UnaryExpression',
    'BinaryExpression',
    'LogicalExpression',
    'ConditionalExpression',
]);
exports.FORBIDDEN_NODE_TYPES = {
    ArrowFunctionExpression: 'Arrow functions are not allowed. Use fromAi() directly instead of ($) => $.fromAi()',
    FunctionExpression: 'Function expressions are not allowed in SDK code',
    FunctionDeclaration: 'Function declarations are not allowed in SDK code',
    ClassDeclaration: 'Class declarations are not allowed in SDK code',
    ClassExpression: 'Class expressions are not allowed in SDK code',
    ForStatement: 'For loops are not allowed in SDK code',
    ForInStatement: 'For-in loops are not allowed in SDK code',
    ForOfStatement: 'For-of loops are not allowed in SDK code',
    WhileStatement: 'While loops are not allowed in SDK code',
    DoWhileStatement: 'Do-while loops are not allowed in SDK code',
    TryStatement: 'Try-catch is not allowed in SDK code',
    ThrowStatement: 'Throw statements are not allowed in SDK code',
    WithStatement: 'With statements are not allowed in SDK code',
    UpdateExpression: 'Update expressions (++, --) are not allowed in SDK code',
    NewExpression: 'new expressions are not allowed. Use SDK factory functions instead.',
    ImportDeclaration: 'Import declarations are not allowed in SDK code',
    ImportExpression: 'Dynamic imports are not allowed in SDK code',
    ExportNamedDeclaration: 'Named exports are not allowed. Use export default only.',
    ExportAllDeclaration: 'Re-exports are not allowed in SDK code',
    AwaitExpression: 'Await expressions are not allowed in SDK code',
    YieldExpression: 'Yield expressions are not allowed in SDK code',
};
function validateNodeType(node, sourceCode) {
    if (exports.FORBIDDEN_NODE_TYPES[node.type]) {
        throw new errors_1.UnsupportedNodeError(`${node.type}: ${exports.FORBIDDEN_NODE_TYPES[node.type]}`, node.loc ?? undefined, sourceCode);
    }
    if (!ALLOWED_NODE_TYPES.has(node.type)) {
        throw new errors_1.UnsupportedNodeError(node.type, node.loc ?? undefined, sourceCode);
    }
}
function validateIdentifier(name, _allowedVariables, node, sourceCode) {
    if (exports.DANGEROUS_GLOBALS.has(name)) {
        throw new errors_1.SecurityError(name, node.loc ?? undefined, sourceCode);
    }
}
function validateCallExpression(node, sourceCode) {
    if (node.callee.type === 'Identifier') {
        const name = node.callee.name;
        if (name === 'eval') {
            throw new errors_1.SecurityError('eval()', node.loc ?? undefined, sourceCode);
        }
        if (name === 'Function') {
            throw new errors_1.SecurityError('Function()', node.loc ?? undefined, sourceCode);
        }
        if (name === 'require') {
            throw new errors_1.SecurityError('require()', node.loc ?? undefined, sourceCode);
        }
    }
    if (node.callee.type === 'MemberExpression') {
        const memberExpr = node.callee;
        if (memberExpr.property.type === 'Identifier' && memberExpr.property.name === 'constructor') {
            throw new errors_1.SecurityError('constructor access', node.loc ?? undefined, sourceCode);
        }
    }
}
function validateMemberExpression(node, sourceCode) {
    if (node.computed) {
        if (node.property.type !== 'Literal') {
            throw new errors_1.SecurityError('computed-member-access', node.loc ?? undefined, sourceCode, 'Dynamic property access is not allowed. Use static property names.');
        }
    }
    const propName = node.property.type === 'Identifier'
        ? node.property.name
        : node.property.type === 'Literal' && typeof node.property.value === 'string'
            ? node.property.value
            : undefined;
    if (propName !== undefined &&
        (propName === '__proto__' || propName === 'prototype' || propName === 'constructor')) {
        throw new errors_1.SecurityError(propName, node.loc ?? undefined, sourceCode);
    }
}
function isAllowedSDKFunction(name) {
    return exports.ALLOWED_SDK_FUNCTIONS.has(name);
}
function isAllowedMethod(name) {
    return exports.ALLOWED_METHODS.has(name);
}
function allowedMethodNames() {
    return exports.SDK_METHODS.filter((m) => m.public).map((m) => m.name);
}
const SAFE_JSON_METHODS = {
    stringify: (...args) => JSON.stringify(args[0], args[1], args[2]),
};
function getSafeJSONMethod(objectName, methodName) {
    if (objectName !== 'JSON')
        return undefined;
    return SAFE_JSON_METHODS[methodName];
}
const SAFE_STRING_METHODS = {
    repeat: (str, count) => str.repeat(count),
    trim: (str) => str.trim(),
};
function getSafeStringMethod(value, methodName) {
    if (typeof value !== 'string')
        return undefined;
    const factory = SAFE_STRING_METHODS[methodName];
    if (!factory)
        return undefined;
    return (...args) => factory(value, ...args);
}
exports.SAFE_JSON_METHOD_NAMES = Object.keys(SAFE_JSON_METHODS);
exports.SAFE_STRING_METHOD_NAMES = Object.keys(SAFE_STRING_METHODS);
exports.SDK_INLINE_CONSTRAINTS = [
    'Use `const` only — `let` and `var` are not allowed.',
    'No destructuring in declarations — declare each value with its own `const`.',
    'No variable reassignment — only property assignment (`obj.prop = value`) is allowed.',
    "No computed/dynamic member access (`obj[expr]`); use static names. Literal keys like `obj['a']` or `arr[0]` are fine.",
];
//# sourceMappingURL=validators.js.map