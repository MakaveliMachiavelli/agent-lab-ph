import { z } from 'zod/v4';
export declare const PLATFORM_TELEMETRY: {
    readonly USER_IS_PART_OF_EXPERIMENT: {
        readonly name: "User is part of experiment";
        readonly description: "User was assigned a variant of a running experiment, reported when feature flags resolve in the editor.";
        readonly properties: z.ZodObject<{
            name: z.ZodString;
            variant: z.ZodUnion<readonly [z.ZodString, z.ZodBoolean]>;
        }, z.core.$strip>;
    } & import("../define").TelemetryEventInput & {
        readonly getValidationError: (properties: unknown) => string | null;
    };
    readonly TASK_RUNNER_DISCONNECTED: {
        readonly name: "Task runner disconnected";
        readonly description: "The broker tore down a task runner connection because the runner was unhealthy: it failed a heartbeat check, or was reported unresponsive after it stopped acknowledging or offering tasks. Normal shutdowns are not reported.";
        readonly properties: z.ZodObject<{
            reason: z.ZodEnum<{
                "failed-heartbeat-check": "failed-heartbeat-check";
                "runner-unresponsive": "runner-unresponsive";
            }>;
            mode: z.ZodEnum<{
                external: "external";
                internal: "internal";
            }>;
        }, z.core.$strip>;
    } & import("../define").TelemetryEventInput & {
        readonly getValidationError: (properties: unknown) => string | null;
    };
};
