import { z } from 'zod/v4';
export declare const CREDENTIALS_TELEMETRY: {
    readonly USER_PROBED_CREDENTIAL: {
        readonly name: "User probed credential";
        readonly description: "A stored credential was auth-probed against its own persisted test URL (POST /credentials/:id/probe) — the test path for generic credential types that declare no test, currently Simplified Custom Auth. Fires once per probe, including retries. The outcome is the probe's three-state verdict; joins to 'User created credentials' on credential_id.";
        readonly properties: z.ZodObject<{
            user_id: z.ZodString;
            credential_id: z.ZodString;
            outcome: z.ZodEnum<{
                accepted: "accepted";
                rejected: "rejected";
                unverified: "unverified";
            }>;
        }, z.core.$strip>;
    } & import("../define").TelemetryEventInput & {
        readonly getValidationError: (properties: unknown) => string | null;
    };
};
