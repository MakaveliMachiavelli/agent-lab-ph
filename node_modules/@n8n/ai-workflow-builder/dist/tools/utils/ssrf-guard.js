"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CrossHostRedirectError = void 0;
exports.createPassthroughSsrfGuard = createPassthroughSsrfGuard;
exports.isSsrfBlockedError = isSsrfBlockedError;
exports.findCrossHostRedirectError = findCrossHostRedirectError;
const ensure_error_1 = require("@n8n/utils/errors/ensure-error");
const result_1 = require("@n8n/utils/result");
const node_dns_1 = __importDefault(require("node:dns"));
class CrossHostRedirectError extends Error {
    finalUrl;
    constructor(finalUrl) {
        super(`Redirect to a different host: ${finalUrl}`);
        this.finalUrl = finalUrl;
        this.name = 'CrossHostRedirectError';
    }
}
exports.CrossHostRedirectError = CrossHostRedirectError;
function createPassthroughSsrfGuard() {
    return {
        validateUrl: async () => (0, result_1.createResultOk)(undefined),
        validateRedirectSync: () => { },
        createSecureLookup: () => node_dns_1.default.lookup,
    };
}
function isSsrfBlockedError(error) {
    let current = (0, ensure_error_1.ensureError)(error);
    for (let depth = 0; depth < 4 && current; depth++) {
        if (current.name === 'SsrfBlockedIpError')
            return true;
        current = current.cause ? (0, ensure_error_1.ensureError)(current.cause) : undefined;
    }
    return false;
}
function findCrossHostRedirectError(error) {
    let current = (0, ensure_error_1.ensureError)(error);
    for (let depth = 0; depth < 4 && current; depth++) {
        if (current instanceof CrossHostRedirectError)
            return current;
        current = current.cause ? (0, ensure_error_1.ensureError)(current.cause) : undefined;
    }
    return undefined;
}
//# sourceMappingURL=ssrf-guard.js.map