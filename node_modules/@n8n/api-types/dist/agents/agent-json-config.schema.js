"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RunnableAgentJsonConfigSchema = exports.AgentJsonConfigSchema = exports.AgentJsonConfigBaseSchema = exports.NodeToolJsonConfigSchema = exports.WorkflowToolJsonConfigSchema = exports.AgentVectorStoreConfigSchema = exports.VECTOR_STORE_NAME_REGEX = exports.VECTOR_STORE_USE_WHEN_MAX_LENGTH = exports.AGENT_VECTOR_STORE_CREDENTIAL_TYPES = exports.AGENT_VECTOR_STORE_PROVIDERS = exports.McpServerConfigSchema = exports.McpAuthenticationSchemaTypes = exports.NodeConfigSchema = exports.DraftAgentModelSchema = exports.SUB_AGENT_TASK_DIFFICULTIES = exports.SUB_AGENT_USE_WHEN_MAX_LENGTH = exports.DEFAULT_AGENT_PERSONALISATION = exports.PromptCachingConfigSchema = exports.AgentModelSchema = exports.MANAGED_CREDENTIAL_TOKEN = exports.CUSTOM_TOOL_ID_REGEX = void 0;
exports.tryParseConfigJson = tryParseConfigJson;
exports.findVectorStoreToolNameCollisions = findVectorStoreToolNameCollisions;
exports.formatZodErrors = formatZodErrors;
exports.formatAgentConfigZodError = formatAgentConfigZodError;
const zod_1 = require("zod");
const agent_config_lifecycle_1 = require("./agent-config-lifecycle");
const agent_integration_schema_1 = require("./agent-integration.schema");
const model_providers_1 = require("./model-providers");
const reasoning_1 = require("./reasoning");
exports.CUSTOM_TOOL_ID_REGEX = /^[A-Za-z0-9_]+$/;
const sub_agent_schema_1 = require("./sub-agent.schema");
exports.MANAGED_CREDENTIAL_TOKEN = 'managed';
exports.AgentModelSchema = zod_1.z.string().min(1).regex(model_providers_1.AGENT_MODEL_STRING_REGEX, 'Model must be "provider/model-name" format (e.g. "anthropic/claude-sonnet-4-5" or "openrouter/amazon/nova-micro-v1")');
const CredentialIdSchema = zod_1.z.string().trim();
const EpisodicMemoryCredentialSchema = zod_1.z.union([
    zod_1.z.literal(exports.MANAGED_CREDENTIAL_TOKEN),
    CredentialIdSchema,
]);
const MemoryWorkerModelSchema = zod_1.z.object({
    model: exports.AgentModelSchema,
    credential: CredentialIdSchema,
});
const ObservationalMemoryConfigSchema = zod_1.z.object({
    enabled: zod_1.z.boolean().optional(),
    observerModel: MemoryWorkerModelSchema.optional(),
    reflectorModel: MemoryWorkerModelSchema.optional(),
    observerThresholdTokens: zod_1.z.number().int().min(1).optional(),
    reflectorThresholdTokens: zod_1.z.number().int().min(1).optional(),
    renderTokenBudget: zod_1.z.number().int().min(1).optional(),
    observationLogTailLimit: zod_1.z.number().int().min(1).optional(),
    lockTtlMs: zod_1.z.number().int().min(0).optional(),
});
const EpisodicMemoryConfigSchema = zod_1.z.discriminatedUnion('enabled', [
    zod_1.z.object({
        enabled: zod_1.z.literal(false),
    }),
    zod_1.z.object({
        enabled: zod_1.z.literal(true),
        credential: EpisodicMemoryCredentialSchema,
        extractorModel: MemoryWorkerModelSchema.optional(),
        reflectorModel: MemoryWorkerModelSchema.optional(),
        topK: zod_1.z.number().int().min(1).max(100).optional(),
        maxEntriesPerRun: zod_1.z.number().int().min(1).max(50).optional(),
    }),
]);
const MemoryConfigSchema = zod_1.z.object({
    enabled: zod_1.z.boolean(),
    storage: zod_1.z.enum(['n8n']),
    observationalMemory: ObservationalMemoryConfigSchema.optional(),
    episodicMemory: EpisodicMemoryConfigSchema.optional(),
});
exports.PromptCachingConfigSchema = zod_1.z.object({
    enabled: zod_1.z.boolean(),
    anthropic: zod_1.z.object({ ttl: zod_1.z.enum(['5m', '1h']).optional() }).optional(),
});
const WebSearchConfigSchema = zod_1.z.object({
    enabled: zod_1.z.boolean(),
    provider: zod_1.z.enum(['auto', 'native', 'brave', 'searxng']).optional(),
    credential: zod_1.z.string().optional(),
});
const HexColorSchema = zod_1.z
    .string()
    .regex(/^#[0-9A-Fa-f]{6}$/, 'Color must be a 6-digit hex value (e.g. #FF5500)');
exports.DEFAULT_AGENT_PERSONALISATION = {
    icon: 'bot',
    gradient: {
        from: '#FF1500',
        to: '#FF6900',
        angle: 135,
        fromStop: 0,
        toStop: 100,
    },
};
const AgentPersonalisationGradientSchema = zod_1.z
    .object({
    from: HexColorSchema,
    to: HexColorSchema,
    angle: zod_1.z.number().int().min(0).max(359).default(exports.DEFAULT_AGENT_PERSONALISATION.gradient.angle),
    fromStop: zod_1.z
        .number()
        .int()
        .min(0)
        .max(45)
        .default(exports.DEFAULT_AGENT_PERSONALISATION.gradient.fromStop),
    toStop: zod_1.z
        .number()
        .int()
        .min(55)
        .max(100)
        .default(exports.DEFAULT_AGENT_PERSONALISATION.gradient.toStop),
})
    .strict();
const AgentPersonalisationConfigSchema = zod_1.z
    .object({
    icon: zod_1.z.string().trim().min(1).max(64),
    gradient: AgentPersonalisationGradientSchema.default(() => ({
        ...exports.DEFAULT_AGENT_PERSONALISATION.gradient,
    })),
})
    .strict();
exports.SUB_AGENT_USE_WHEN_MAX_LENGTH = 512;
const SubAgentConfigSchema = zod_1.z
    .object({
    agentId: zod_1.z.string().trim().min(1),
    useWhen: zod_1.z.string().trim().max(exports.SUB_AGENT_USE_WHEN_MAX_LENGTH).optional(),
})
    .strict();
exports.SUB_AGENT_TASK_DIFFICULTIES = ['low', 'medium', 'high'];
const SubAgentTaskDifficultySchema = zod_1.z.enum(exports.SUB_AGENT_TASK_DIFFICULTIES);
const SubAgentDifficultyModelConfigSchema = zod_1.z
    .object({
    model: exports.AgentModelSchema,
    credential: zod_1.z.string().trim(),
})
    .strict();
const SubAgentsConfigSchema = zod_1.z
    .object({
    maxChildren: zod_1.z
        .number()
        .int()
        .min(sub_agent_schema_1.SUB_AGENT_MAX_CHILDREN_MIN)
        .max(sub_agent_schema_1.SUB_AGENT_MAX_CHILDREN_MAX)
        .optional()
        .describe(`Maximum number of child sub-agent runs this parent agent may run in parallel. Defaults to ${sub_agent_schema_1.SUB_AGENT_MAX_CHILDREN_DEFAULT} when unset.`),
    agents: zod_1.z.array(SubAgentConfigSchema).optional(),
    modelsByDifficulty: zod_1.z
        .object({
        low: SubAgentDifficultyModelConfigSchema.optional(),
        medium: SubAgentDifficultyModelConfigSchema.optional(),
        high: SubAgentDifficultyModelConfigSchema.optional(),
    })
        .strict()
        .optional()
        .describe('Optional inline sub-agent model mappings by task difficulty. Missing mappings fall back to the parent agent model.'),
})
    .strict();
const NodeToolCredentialSchema = zod_1.z.union([
    zod_1.z.object({ id: zod_1.z.string(), name: zod_1.z.string() }),
    zod_1.z.object({ id: zod_1.z.null(), name: zod_1.z.string(), __aiGatewayManaged: zod_1.z.literal(true) }),
]);
exports.DraftAgentModelSchema = zod_1.z.union([zod_1.z.literal(''), exports.AgentModelSchema]);
exports.NodeConfigSchema = zod_1.z.object({
    nodeType: zod_1.z.string().min(1),
    nodeTypeVersion: zod_1.z.number(),
    nodeParameters: zod_1.z.record(zod_1.z.unknown()).optional().default({}),
    credentials: zod_1.z.record(NodeToolCredentialSchema).optional(),
});
const AgentJsonSkillConfigSchema = zod_1.z.object({
    type: zod_1.z.literal('skill'),
    id: zod_1.z
        .string()
        .min(1)
        .regex(/^[A-Za-z0-9_-]+$/, 'Skill id can only contain letters, numbers, hyphens, and underscores'),
});
const AgentJsonTaskConfigSchema = zod_1.z.object({
    type: zod_1.z.literal('task'),
    id: zod_1.z
        .string()
        .min(1)
        .regex(/^[A-Za-z0-9_-]+$/, 'Task id can only contain letters, numbers, hyphens, and underscores'),
    enabled: zod_1.z.boolean(),
});
exports.McpAuthenticationSchemaTypes = zod_1.z.enum([
    'none',
    'bearerAuth',
    'headerAuth',
    'multipleHeadersAuth',
    'mcpOAuth2Api',
]);
exports.McpServerConfigSchema = zod_1.z
    .object({
    name: zod_1.z
        .string()
        .min(1)
        .max(64)
        .refine((name) => name.trim().length > 0, 'MCP server name cannot be blank')
        .describe('Unique display name. The SDK normalizes it when building model-facing tool names'),
    description: zod_1.z.string().max(512).optional().describe('Human-readable server description'),
    url: zod_1.z.string().describe('MCP server endpoint URL. Empty string means setup is incomplete'),
    transport: zod_1.z
        .enum(['sse', 'streamableHttp'])
        .default('streamableHttp')
        .describe('Transport protocol'),
    authentication: zod_1.z
        .union([exports.McpAuthenticationSchemaTypes, zod_1.z.string().endsWith('McpOAuth2Api')])
        .default('none')
        .describe('Auth method. Named variants or any string ending in McpOAuth2Api for registry credential types'),
    credential: zod_1.z
        .string()
        .optional()
        .describe('Credential id from ask_credential. Required when authentication is not "none"'),
    metadata: zod_1.z
        .object({
        nodeTypeName: zod_1.z
            .string()
            .optional()
            .describe('Source node type for registry servers (e.g. @n8n/mcp-registry.github). Enables correct UI form'),
    })
        .optional()
        .describe('Server-generated metadata. Do not set this manually; only copy it from an MCP discovery result when present'),
    toolFilter: zod_1.z
        .discriminatedUnion('mode', [
        zod_1.z
            .object({
            mode: zod_1.z.literal('allow'),
            tools: zod_1.z.array(zod_1.z.string().min(1)).default([]),
        })
            .strict(),
        zod_1.z
            .object({
            mode: zod_1.z.literal('exclude'),
            tools: zod_1.z.array(zod_1.z.string().min(1)).default([]),
        })
            .strict(),
    ])
        .optional()
        .describe('Restricts which tools are surfaced. Tools matched by original un-prefixed name'),
    approval: zod_1.z
        .discriminatedUnion('mode', [
        zod_1.z.object({ mode: zod_1.z.literal('global') }).strict(),
        zod_1.z
            .object({
            mode: zod_1.z.literal('selected'),
            tools: zod_1.z.array(zod_1.z.string().min(1)).min(1),
        })
            .strict(),
    ])
        .optional()
        .describe('Human-in-the-loop approval. Absent = no approval required'),
    connectionTimeoutMs: zod_1.z
        .number()
        .int()
        .min(1)
        .max(120_000)
        .optional()
        .describe('Connection timeout in milliseconds'),
})
    .strict();
exports.AGENT_VECTOR_STORE_PROVIDERS = ['pinecone', 'supabase', 'qdrant', 'postgres'];
exports.AGENT_VECTOR_STORE_CREDENTIAL_TYPES = {
    pinecone: 'pineconeApi',
    supabase: 'supabaseApi',
    qdrant: 'qdrantApi',
    postgres: 'postgres',
};
exports.VECTOR_STORE_USE_WHEN_MAX_LENGTH = 512;
exports.VECTOR_STORE_NAME_REGEX = /^[a-zA-Z0-9_-]+$/;
const VectorStoreEmbeddingSchema = zod_1.z
    .object({
    model: exports.AgentModelSchema,
    credential: CredentialIdSchema,
})
    .strict();
const VectorStoreBaseShape = {
    name: zod_1.z
        .string()
        .min(1)
        .max(64)
        .regex(exports.VECTOR_STORE_NAME_REGEX, 'Vector store name can only contain letters, numbers, hyphens, and underscores')
        .describe('Unique connection name, also used as the SDK tool-name suffix: search_<name>'),
    credential: CredentialIdSchema,
    useWhen: zod_1.z.string().trim().min(1).max(exports.VECTOR_STORE_USE_WHEN_MAX_LENGTH),
    embedding: VectorStoreEmbeddingSchema,
};
exports.AgentVectorStoreConfigSchema = zod_1.z.discriminatedUnion('provider', [
    zod_1.z
        .object({
        provider: zod_1.z.literal('pinecone'),
        ...VectorStoreBaseShape,
        indexName: zod_1.z.string().min(1),
        namespace: zod_1.z.string().optional(),
    })
        .strict(),
    zod_1.z
        .object({
        provider: zod_1.z.literal('qdrant'),
        ...VectorStoreBaseShape,
        collectionName: zod_1.z.string().min(1),
    })
        .strict(),
    zod_1.z
        .object({
        provider: zod_1.z.literal('supabase'),
        ...VectorStoreBaseShape,
        tableName: zod_1.z.string().min(1),
        queryName: zod_1.z.string().optional(),
    })
        .strict(),
    zod_1.z
        .object({
        provider: zod_1.z.literal('postgres'),
        ...VectorStoreBaseShape,
        tableName: zod_1.z.string().min(1),
    })
        .strict(),
]);
const CustomToolJsonConfigSchema = zod_1.z.object({
    type: zod_1.z.literal('custom'),
    id: zod_1.z
        .string()
        .min(1)
        .regex(exports.CUSTOM_TOOL_ID_REGEX, 'Custom tool id can only contain letters, numbers, and underscores'),
    requireApproval: zod_1.z.boolean().optional(),
});
exports.WorkflowToolJsonConfigSchema = zod_1.z
    .object({
    type: zod_1.z.literal('workflow'),
    workflowId: zod_1.z.string().min(1).optional().describe("The workflow's stable ID."),
    workflow: zod_1.z.string().min(1).describe("The workflow's display name and legacy lookup key."),
    name: zod_1.z.string().optional(),
    description: zod_1.z.string().optional(),
    requireApproval: zod_1.z.boolean().optional(),
    allOutputs: zod_1.z
        .boolean()
        .optional()
        .describe('Whether to return all node outputs instead of just the last node'),
})
    .strict();
exports.NodeToolJsonConfigSchema = zod_1.z
    .object({
    type: zod_1.z.literal('node'),
    name: zod_1.z.string().min(1),
    description: zod_1.z.string().optional(),
    inputSchema: zod_1.z.never().optional(),
    node: exports.NodeConfigSchema,
    requireApproval: zod_1.z.boolean().optional(),
})
    .strict();
const AgentJsonToolConfigSchema = zod_1.z.discriminatedUnion('type', [
    CustomToolJsonConfigSchema,
    exports.WorkflowToolJsonConfigSchema,
    exports.NodeToolJsonConfigSchema,
]);
exports.AgentJsonConfigBaseSchema = zod_1.z.object({
    name: zod_1.z.string().min(1).max(128),
    model: exports.DraftAgentModelSchema,
    credential: zod_1.z.string().optional(),
    instructions: zod_1.z.string(),
    personalisation: AgentPersonalisationConfigSchema.optional(),
    memory: MemoryConfigSchema.optional(),
    subAgents: SubAgentsConfigSchema.optional(),
    tools: zod_1.z
        .array(AgentJsonToolConfigSchema)
        .superRefine((tools, ctx) => {
        const customIds = tools.filter((t) => t.type === 'custom').map((t) => t.id);
        const seen = new Set();
        for (const id of customIds) {
            if (seen.has(id)) {
                ctx.addIssue({
                    code: zod_1.z.ZodIssueCode.custom,
                    message: `Duplicate custom tool id: "${id}"`,
                });
            }
            seen.add(id);
        }
    })
        .optional(),
    skills: zod_1.z
        .array(AgentJsonSkillConfigSchema)
        .superRefine((skills, ctx) => {
        const seen = new Set();
        for (const skill of skills) {
            if (seen.has(skill.id)) {
                ctx.addIssue({
                    code: zod_1.z.ZodIssueCode.custom,
                    message: `Duplicate skill id: "${skill.id}"`,
                });
            }
            seen.add(skill.id);
        }
    })
        .optional(),
    tasks: zod_1.z.array(AgentJsonTaskConfigSchema).optional(),
    providerTools: zod_1.z.record(zod_1.z.record(zod_1.z.unknown())).optional(),
    integrations: zod_1.z.array(agent_integration_schema_1.AgentIntegrationConfigSchema).optional(),
    mcpServers: zod_1.z
        .array(exports.McpServerConfigSchema)
        .max(20)
        .refine((servers) => new Set(servers.map((s) => s.name)).size === servers.length, {
        message: 'MCP server names must be unique within an agent',
    })
        .optional(),
    vectorStores: zod_1.z
        .array(exports.AgentVectorStoreConfigSchema)
        .max(20)
        .refine((stores) => new Set(stores.map((s) => s.name.replace(/-/g, '_'))).size === stores.length, { message: 'Vector store names must be unique within an agent' })
        .optional(),
    config: zod_1.z
        .object({
        reasoning: zod_1.z.enum(reasoning_1.AGENT_REASONING_LEVELS).optional(),
        promptCaching: exports.PromptCachingConfigSchema.optional(),
        webSearch: WebSearchConfigSchema.optional(),
        toolCallConcurrency: zod_1.z.number().int().min(1).max(100).optional(),
        maxIterations: zod_1.z
            .number()
            .int()
            .min(1)
            .max(200)
            .optional()
            .describe('Maximum number of agent loop iterations per run. Do not set unless the user explicitly asks.'),
    })
        .optional(),
});
exports.AgentJsonConfigSchema = exports.AgentJsonConfigBaseSchema.superRefine((config, ctx) => {
    if (config.credential?.trim() && (0, agent_config_lifecycle_1.isDraftAgentConfig)(config)) {
        ctx.addIssue({
            code: zod_1.z.ZodIssueCode.custom,
            path: ['credential'],
            message: 'A credential requires a model to be set',
        });
    }
});
exports.RunnableAgentJsonConfigSchema = exports.AgentJsonConfigBaseSchema.extend({
    model: exports.AgentModelSchema,
    credential: zod_1.z.string().refine((value) => value.trim().length > 0, {
        message: 'Credential is required',
    }),
});
function tryParseConfigJson(raw) {
    try {
        return { ok: true, data: JSON.parse(raw) };
    }
    catch (e) {
        const msg = e instanceof SyntaxError ? e.message : String(e);
        return { ok: false, errors: [{ path: '(root)', message: `JSON parse error: ${msg}` }] };
    }
}
function findVectorStoreToolNameCollisions(config) {
    if (!config.vectorStores?.length)
        return [];
    const toolNames = new Set((config.tools ?? []).map((tool) => {
        switch (tool.type) {
            case 'custom':
                return tool.id;
            case 'workflow':
                return tool.name ?? tool.workflow;
            case 'node':
                return tool.name;
        }
    }));
    return config.vectorStores
        .map((store) => `search_${store.name.replace(/-/g, '_')}`)
        .filter((toolName) => toolNames.has(toolName));
}
function formatZodErrors(error) {
    return error.issues.map((issue) => ({
        path: issue.path.join('.') || '(root)',
        message: issue.message,
        expected: 'expected' in issue ? String(issue.expected) : undefined,
        received: 'received' in issue ? String(issue.received) : undefined,
    }));
}
function formatAgentConfigZodError(error) {
    return formatZodErrors(error)
        .map((issue) => `${issue.path}: ${issue.message}`)
        .join('; ');
}
//# sourceMappingURL=agent-json-config.schema.js.map