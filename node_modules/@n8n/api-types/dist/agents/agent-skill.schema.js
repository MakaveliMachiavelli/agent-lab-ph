"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.agentSkillSchema = exports.agentSkillShape = exports.AGENT_SKILL_REFERENCES_TOTAL_MAX_BYTES = exports.AGENT_SKILL_REFERENCE_CONTENT_MAX_BYTES = exports.AGENT_SKILL_REFERENCE_MAX_COUNT = exports.AGENT_SKILL_INSTRUCTIONS_MAX_LENGTH = void 0;
const zod_1 = require("zod");
exports.AGENT_SKILL_INSTRUCTIONS_MAX_LENGTH = 65_536;
exports.AGENT_SKILL_REFERENCE_MAX_COUNT = 20;
exports.AGENT_SKILL_REFERENCE_CONTENT_MAX_BYTES = 65_536;
exports.AGENT_SKILL_REFERENCES_TOTAL_MAX_BYTES = 262_144;
const agentSkillStringArraySchema = zod_1.z.array(zod_1.z.string().trim().min(1));
const utf8ByteLength = (value) => new TextEncoder().encode(value).byteLength;
const agentSkillReferenceSchema = zod_1.z
    .object({
    path: zod_1.z
        .string()
        .min(1)
        .max(512)
        .refine((path) => {
        const normalized = path.replaceAll('\\', '/');
        const segments = normalized.split('/');
        return (path === normalized &&
            normalized.startsWith('references/') &&
            (normalized.endsWith('.md') || normalized.endsWith('.markdown')) &&
            segments.every((segment) => segment !== '' && segment !== '.' && segment !== '..'));
    }, 'Reference path must be a markdown file under references/'),
    content: zod_1.z.string().min(1),
})
    .strict()
    .superRefine((reference, ctx) => {
    if (utf8ByteLength(reference.content) > exports.AGENT_SKILL_REFERENCE_CONTENT_MAX_BYTES) {
        ctx.addIssue({
            code: zod_1.z.ZodIssueCode.custom,
            message: `Reference content must be ${exports.AGENT_SKILL_REFERENCE_CONTENT_MAX_BYTES} bytes or fewer`,
            path: ['content'],
        });
    }
});
const agentSkillReferencesSchema = zod_1.z
    .array(agentSkillReferenceSchema)
    .max(exports.AGENT_SKILL_REFERENCE_MAX_COUNT)
    .superRefine((references, ctx) => {
    const paths = new Set();
    let totalBytes = 0;
    for (const [index, reference] of references.entries()) {
        totalBytes += utf8ByteLength(reference.content);
        if (paths.has(reference.path)) {
            ctx.addIssue({
                code: zod_1.z.ZodIssueCode.custom,
                message: `Duplicate reference path "${reference.path}"`,
                path: [index, 'path'],
            });
        }
        paths.add(reference.path);
    }
    if (totalBytes > exports.AGENT_SKILL_REFERENCES_TOTAL_MAX_BYTES) {
        ctx.addIssue({
            code: zod_1.z.ZodIssueCode.custom,
            message: `Reference content must total ${exports.AGENT_SKILL_REFERENCES_TOTAL_MAX_BYTES} bytes or fewer`,
        });
    }
});
exports.agentSkillShape = {
    name: zod_1.z.string().min(1).max(128),
    description: zod_1.z.string().min(1).max(512),
    instructions: zod_1.z
        .string()
        .min(1)
        .refine((value) => utf8ByteLength(value) <= exports.AGENT_SKILL_INSTRUCTIONS_MAX_LENGTH, {
        message: `Instructions must be ${exports.AGENT_SKILL_INSTRUCTIONS_MAX_LENGTH} bytes or fewer`,
    }),
    allowedTools: agentSkillStringArraySchema.optional(),
    references: agentSkillReferencesSchema.optional(),
    scripts: zod_1.z.never().optional(),
    templates: zod_1.z.never().optional(),
    assets: zod_1.z.never().optional(),
    examples: zod_1.z.never().optional(),
    other: zod_1.z.never().optional(),
};
exports.agentSkillSchema = zod_1.z.object(exports.agentSkillShape).strict();
//# sourceMappingURL=agent-skill.schema.js.map