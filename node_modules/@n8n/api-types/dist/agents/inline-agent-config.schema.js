"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RunnableInlineAgentConfigSchema = exports.InlineAgentConfigSchema = exports.InlineAgentJsonConfigSchema = void 0;
const zod_1 = require("zod");
const agent_json_config_schema_1 = require("./agent-json-config.schema");
const agent_skill_schema_1 = require("./agent-skill.schema");
const InlineAgentToolConfigSchema = zod_1.z.discriminatedUnion('type', [
    agent_json_config_schema_1.WorkflowToolJsonConfigSchema.omit({ requireApproval: true }).strict(),
    agent_json_config_schema_1.NodeToolJsonConfigSchema.omit({ requireApproval: true }).strict(),
]);
const InlineSkillIdSchema = zod_1.z
    .string()
    .min(1)
    .max(64)
    .regex(/^[A-Za-z0-9_-]+$/)
    .refine((id) => !['__proto__', 'constructor', 'prototype'].includes(id), {
    message: 'Reserved skill id',
});
const InlineAgentSkillBodiesSchema = zod_1.z.record(InlineSkillIdSchema, agent_skill_schema_1.agentSkillSchema);
exports.InlineAgentJsonConfigSchema = agent_json_config_schema_1.AgentJsonConfigBaseSchema.pick({
    name: true,
    model: true,
    credential: true,
    instructions: true,
    skills: true,
})
    .extend({
    tools: zod_1.z.array(InlineAgentToolConfigSchema).optional(),
    mcpServers: agent_json_config_schema_1.AgentJsonConfigBaseSchema.shape.mcpServers.refine((servers) => (servers ?? []).every((server) => server.approval === undefined), { message: 'MCP tool approval is not available for inline agents' }),
})
    .strict();
function assertSkillRefIntegrity(value, ctx) {
    const bodies = value.skills ?? {};
    const idsByName = new Map();
    for (const [index, ref] of (value.config.skills ?? []).entries()) {
        if (!Object.hasOwn(bodies, ref.id)) {
            ctx.addIssue({
                code: zod_1.z.ZodIssueCode.custom,
                message: `Skill "${ref.id}" is referenced in config.skills but has no body in skills`,
                path: ['config', 'skills', index, 'id'],
            });
            continue;
        }
        const name = bodies[ref.id].name.trim();
        const existingId = idsByName.get(name.toLowerCase());
        if (existingId !== undefined) {
            ctx.addIssue({
                code: zod_1.z.ZodIssueCode.custom,
                message: `Skill "${ref.id}" has the same name as skill "${existingId}" ("${name}")`,
                path: ['skills', ref.id, 'name'],
            });
            continue;
        }
        idsByName.set(name.toLowerCase(), ref.id);
    }
}
exports.InlineAgentConfigSchema = zod_1.z
    .object({
    config: exports.InlineAgentJsonConfigSchema,
    skills: InlineAgentSkillBodiesSchema.optional(),
})
    .strict()
    .superRefine(assertSkillRefIntegrity);
exports.RunnableInlineAgentConfigSchema = zod_1.z
    .object({
    config: exports.InlineAgentJsonConfigSchema.extend({
        model: agent_json_config_schema_1.AgentModelSchema,
        credential: zod_1.z.string().refine((value) => value.trim().length > 0, {
            message: 'Credential is required',
        }),
    }),
    skills: InlineAgentSkillBodiesSchema.optional(),
})
    .strict()
    .superRefine(assertSkillRefIntegrity);
//# sourceMappingURL=inline-agent-config.schema.js.map