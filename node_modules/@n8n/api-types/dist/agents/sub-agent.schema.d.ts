import type { RunnableAgentJsonConfig } from './agent-json-config.schema';
export declare const SUB_AGENT_MAX_CHILDREN_MIN = 1;
export declare const SUB_AGENT_MAX_CHILDREN_MAX = 20;
export declare const SUB_AGENT_MAX_CHILDREN_DEFAULT = 10;
export type SubAgentSource = {
    agentId: string;
    versionId?: string;
};
export interface ResolvedSubAgentSource {
    config: RunnableAgentJsonConfig;
    sourceId: string;
    versionId?: string;
}
export interface SubAgentRunPolicy {
    maxChildren?: number;
}
export interface SubAgentSpawnRequest {
    goal: string;
    context?: string;
    expectedOutput?: string;
    source: SubAgentSource;
    executionMode?: 'foreground' | 'background';
    policy?: SubAgentRunPolicy;
    parentThreadId?: string;
    parentResourceId?: string;
    taskPath: string;
}
