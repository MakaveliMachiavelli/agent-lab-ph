export declare function isDraftAgentConfig(config: {
    model?: string;
} | null | undefined): boolean;
export declare function isDraftIntegration(integration: {
    credentialId: string;
}): boolean;
