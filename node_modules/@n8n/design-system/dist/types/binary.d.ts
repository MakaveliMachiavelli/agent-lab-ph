declare const BINARY_METADATA_KEYS: readonly ["data", "mimeType", "fileType", "fileName", "directory", "fileExtension", "fileSize", "id", "bytes"];
export type BinaryMetadata = {
    [K in (typeof BINARY_METADATA_KEYS)[number] as K extends 'mimeType' | 'id' | 'data' ? K : never]: K extends 'mimeType' | 'id' | 'data' ? string : never;
} & {
    [K in Exclude<(typeof BINARY_METADATA_KEYS)[number], 'mimeType' | 'id' | 'data'>]?: K extends 'bytes' ? number : string;
};
export declare const isBinary: (obj: unknown) => obj is BinaryMetadata;
export {};
