import { ClassValue } from 'clsx';
import { IconSize } from './icon';
import { IconName } from '../components/N8nIcon/icons';
declare const BUTTON_VARIANT: readonly ["solid", "subtle", "ghost", "outline", "destructive", "success"];
export type ButtonVariant = (typeof BUTTON_VARIANT)[number];
export type LegacyButtonVariant = 'highlight' | 'highlight-fill';
declare const BUTTON_SIZE: readonly ["mini", "xmini", "small", "medium", "large", "xlarge", "xsmall"];
export type ButtonSize = (typeof BUTTON_SIZE)[number];
export interface ButtonProps {
    variant?: ButtonVariant | LegacyButtonVariant;
    size?: ButtonSize;
    href?: string;
    loading?: boolean;
    iconOnly?: boolean;
    disabled?: boolean;
    class?: ClassValue;
    icon?: IconName;
    iconSize?: IconSize;
    label?: string;
}
export interface IconButtonProps extends ButtonProps {
    icon: IconName;
}
export type IN8nButton = {
    attrs: ButtonProps & {
        'data-test-id'?: string;
    };
    listeners?: Record<string, (event: Event) => void>;
};
export {};
