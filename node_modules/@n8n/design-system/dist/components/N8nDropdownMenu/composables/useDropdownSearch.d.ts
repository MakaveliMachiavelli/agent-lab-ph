import { MaybeRefOrGetter } from 'vue';
import { DropdownMenuItemProps } from '../DropdownMenu.types';
type SearchField = string | null | undefined;
interface UseDropdownSearchOptions<T, D> {
    flatList?: boolean;
    searchFields?: (item: DropdownMenuItemProps<T, D>) => SearchField[];
    isSearchable?: (item: DropdownMenuItemProps<T, D>) => boolean;
    includeChildrenWhenParentMatches?: boolean;
    mapResult?: (item: DropdownMenuItemProps<T, D>, path: Array<DropdownMenuItemProps<T, D>>) => DropdownMenuItemProps<T, D>;
}
export declare function useDropdownSearch<T = string, D = never>(items: MaybeRefOrGetter<Array<DropdownMenuItemProps<T, D>>>, options?: UseDropdownSearchOptions<T, D>): {
    search: import('vue').Ref<string, string>;
    filteredItems: import('vue').ComputedRef<DropdownMenuItemProps<T, D>[]>;
    handleSearch: (query: string) => void;
};
export {};
