import { ItemWithKey } from '../../types';
export interface RecycleScrollerProps<Key extends string = string, Item extends ItemWithKey<Key> = ItemWithKey<Key>> {
    itemSize: number;
    items: Item[];
    itemKey: Key;
    offset?: number;
}
