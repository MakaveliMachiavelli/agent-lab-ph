import { ButtonSize, IconSize } from '../../types';
export type ColumnHeader = {
    key: string;
    label: string;
    visible: boolean;
    disabled: false;
} | {
    key: string;
    disabled: true;
};
export interface TableHeaderControlsButtonProps<ColumnType extends ColumnHeader = ColumnHeader> {
    columns: ColumnType[];
    buttonSize?: ButtonSize;
    iconSize?: IconSize;
}
