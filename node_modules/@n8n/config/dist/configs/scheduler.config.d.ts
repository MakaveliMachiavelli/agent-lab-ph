export declare class SchedulerConfig {
    enabled: boolean;
    materializationWindowSeconds: number;
    materializationIntervalSeconds: number;
    materializationTimeoutSeconds: number;
    executorIntervalSeconds: number;
    executorTimeoutSeconds: number;
    claimBatchSize: number;
    reaperIntervalSeconds: number;
    reaperBatchSize: number;
    reaperTimeoutSeconds: number;
    leaseDurationSeconds: number;
    retentionSeconds: number;
    failedRetentionSeconds: number;
    retentionIntervalSeconds: number;
    retentionTimeoutSeconds: number;
    maxConcurrentPasses: number;
    jitterRatio: number;
    minIntervalSeconds: number;
    triggerNodeMode: 'legacy' | 'new';
    enabledForPollTriggers: boolean;
    allowSkipDurableScheduler: boolean;
    maxAttempts: number;
    misfireGraceSeconds: number;
}
