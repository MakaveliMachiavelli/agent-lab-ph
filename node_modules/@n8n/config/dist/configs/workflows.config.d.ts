import { z } from 'zod';
declare const callerPolicySchema: z.ZodEnum<["any", "none", "workflowsFromAList", "workflowsFromSameOwner"]>;
type CallerPolicy = z.infer<typeof callerPolicySchema>;
export declare class WorkflowsConfig {
    defaultName: string;
    callerPolicyDefaultOption: CallerPolicy;
    activationBatchSize: number;
    indexingBatchSize: number;
    useWorkflowPublicationService: boolean;
    publicationOutboxPollIntervalMs: number;
    publicationOutboxLeaseSeconds: number;
    workflowPublicationConcurrency: number;
    publicationOutboxCompletedRetentionHours: number;
    publicationOutboxFailedRetentionHours: number;
    publicationOutboxCleanupIntervalSeconds: number;
    publicationOutboxCleanupBatchSize: number;
    publicationReconcileIntervalSeconds: number;
    autosaveDisabled: boolean;
}
export {};
