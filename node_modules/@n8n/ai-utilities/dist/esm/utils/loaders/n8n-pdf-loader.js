"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.N8nPdfLoader = void 0;
const buffer_1 = require("@langchain/classic/document_loaders/fs/buffer");
const documents_1 = require("@langchain/core/documents");
const n8n_workflow_1 = require("n8n-workflow");
class N8nPdfLoader extends buffer_1.BufferLoader {
    constructor(filePathOrBlob, { splitPages = true } = {}) {
        super(filePathOrBlob);
        this.splitPages = splitPages;
    }
    async parse(raw, metadata) {
        if (typeof Reflect.get(globalThis, 'DOMMatrix') === 'undefined') {
            const { default: DOMMatrix } = await import('@thednp/dommatrix');
            Reflect.set(globalThis, 'DOMMatrix', DOMMatrix);
        }
        const { PDFParse } = await import('pdf-parse');
        const parser = new PDFParse({ data: raw });
        try {
            const result = await parser.getText({ pageJoiner: '' });
            const info = await parser.getInfo().catch((error) => {
                n8n_workflow_1.LoggerProxy.debug('N8nPdfLoader: getInfo() failed; continuing without pdf.info metadata', {
                    error,
                });
                return null;
            });
            const pdfMeta = {
                info: info?.info,
                metadata: info?.metadata,
                totalPages: result.total,
            };
            const documents = result.pages.map((page) => new documents_1.Document({
                pageContent: page.text,
                metadata: {
                    ...metadata,
                    pdf: pdfMeta,
                    loc: { pageNumber: page.num },
                },
            }));
            if (this.splitPages)
                return documents;
            if (documents.length === 0)
                return [];
            return [
                new documents_1.Document({
                    pageContent: documents.map((doc) => doc.pageContent).join('\n\n'),
                    metadata: {
                        ...metadata,
                        pdf: pdfMeta,
                    },
                }),
            ];
        }
        finally {
            await parser.destroy().catch(() => undefined);
        }
    }
}
exports.N8nPdfLoader = N8nPdfLoader;
//# sourceMappingURL=n8n-pdf-loader.js.map