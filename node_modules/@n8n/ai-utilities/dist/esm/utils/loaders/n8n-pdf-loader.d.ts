import { BufferLoader } from '@langchain/classic/document_loaders/fs/buffer';
import { Document } from '@langchain/core/documents';
export interface N8nPdfLoaderOptions {
    splitPages?: boolean;
}
export declare class N8nPdfLoader extends BufferLoader {
    private readonly splitPages;
    constructor(filePathOrBlob: string | Blob, { splitPages }?: N8nPdfLoaderOptions);
    protected parse(raw: Buffer, metadata: Record<string, unknown>): Promise<Document[]>;
}
