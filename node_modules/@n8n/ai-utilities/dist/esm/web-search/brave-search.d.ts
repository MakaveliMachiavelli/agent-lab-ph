import type { WebSearchOptions, WebSearchResponse } from './types';
export interface BraveSearchOptions extends WebSearchOptions {
    proxyConfig?: {
        apiUrl: string;
        getAuthHeaders: () => Promise<Record<string, string>>;
    };
}
export declare function braveSearch(apiKey: string, query: string, options: BraveSearchOptions): Promise<WebSearchResponse>;
