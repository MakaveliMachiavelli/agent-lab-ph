"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.collectFromAiParameterReferences = collectFromAiParameterReferences;
exports.hasMatchingFromAiParameterReference = hasMatchingFromAiParameterReference;
const fromai_helpers_1 = require("../utils/fromai-helpers");
function isRecord(value) {
    return typeof value === 'object' && value !== null && !Array.isArray(value);
}
function escapeJsonPointerPart(part) {
    return part.replaceAll('~', '~0').replaceAll('/', '~1');
}
function containsFromAiCall(value) {
    const parameters = { value };
    try {
        return (0, fromai_helpers_1.extractFromAIParameters)(parameters).length > 0;
    }
    catch {
        return false;
    }
}
function collectFromAiParameterReferences(value, pathParts = [], jsonPointerParts = pathParts) {
    if (typeof value === 'string') {
        if (!containsFromAiCall(value) || pathParts.length === 0)
            return [];
        return [
            {
                parameterPath: pathParts.join('.'),
                jsonPointer: jsonPointerParts.map(escapeJsonPointerPart).join('/'),
                value,
            },
        ];
    }
    if (Array.isArray(value)) {
        return value.flatMap((item, index) => collectFromAiParameterReferences(item, pathParts, [...jsonPointerParts, String(index)]));
    }
    if (!isRecord(value))
        return [];
    return Object.entries(value).flatMap(([key, nested]) => collectFromAiParameterReferences(nested, [...pathParts, key], [...jsonPointerParts, key]));
}
function hasMatchingFromAiParameterReference(references, candidate) {
    return references.some((reference) => reference.jsonPointer === candidate.jsonPointer && reference.value === candidate.value);
}
//# sourceMappingURL=from-ai-node-parameters.js.map