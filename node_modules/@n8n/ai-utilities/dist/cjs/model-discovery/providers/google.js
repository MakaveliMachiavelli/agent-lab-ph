"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.listGoogleModels = void 0;
const request_1 = require("../request");
const listGoogleModels = async (options) => {
    const base = (0, request_1.baseUrl)(options, 'https://generativelanguage.googleapis.com');
    const data = (await (0, request_1.getJson)(`${base}/v1beta/models`, { 'x-goog-api-key': options.apiKey }, options, 'google'));
    return (data.models ?? [])
        .filter((model) => typeof model.name === 'string' &&
        !model.name.includes('embedding') &&
        !model.name.includes('imagen'))
        .map((model) => ({ id: model.name, name: model.name }))
        .sort(request_1.byName);
};
exports.listGoogleModels = listGoogleModels;
//# sourceMappingURL=google.js.map