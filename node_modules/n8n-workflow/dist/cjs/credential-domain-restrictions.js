"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DOMAIN_RESTRICTION_FIELDS = void 0;
exports.injectDomainRestrictionFields = injectDomainRestrictionFields;
exports.isDomainAllowed = isDomainAllowed;
exports.assertUrlAllowed = assertUrlAllowed;
exports.getCredentialAllowedDomains = getCredentialAllowedDomains;
exports.assertCredentialAllowsUrl = assertCredentialAllowsUrl;
const errors_1 = require("./errors");
const node_operation_error_1 = require("./errors/node-operation.error");
const DEFAULT_SURFACE = 'HTTP Request';
exports.DOMAIN_RESTRICTION_FIELDS = [
    {
        displayName: 'Allowed HTTP Request Domains',
        name: 'allowedHttpRequestDomains',
        type: 'options',
        options: [
            {
                name: 'All',
                value: 'all',
                description: 'Allow all requests when used in the HTTP Request or GraphQL node',
            },
            {
                name: 'Specific Domains',
                value: 'domains',
                description: 'Restrict requests to specific domains',
            },
            {
                name: 'None',
                value: 'none',
                description: 'Block all requests when used in the HTTP Request or GraphQL node',
            },
        ],
        default: 'all',
        description: 'Control which domains this credential can be used with in HTTP Request or GraphQL nodes',
    },
    {
        displayName: 'Allowed Domains',
        name: 'allowedDomains',
        type: 'string',
        default: '',
        placeholder: 'example.com, *.subdomain.com',
        description: 'Comma-separated list of allowed domains (supports wildcards with *)',
        displayOptions: {
            show: {
                allowedHttpRequestDomains: ['domains'],
            },
        },
    },
];
function isRestrictionMode(value) {
    return value === 'all' || value === 'domains' || value === 'none';
}
function readMode(credentialData) {
    const value = credentialData.allowedHttpRequestDomains;
    return isRestrictionMode(value) ? value : undefined;
}
function readAllowedDomainsField(credentialData) {
    const value = credentialData.allowedDomains;
    if (typeof value !== 'string')
        return undefined;
    const trimmed = value.trim();
    return trimmed.length > 0 ? trimmed : undefined;
}
function shouldInjectDomainRestrictionFields(credentialType) {
    return (credentialType.authenticate !== undefined ||
        credentialType.genericAuth === true ||
        (Array.isArray(credentialType.extends) &&
            (credentialType.extends.includes('oAuth2Api') ||
                credentialType.extends.includes('oAuth1Api') ||
                credentialType.extends.includes('googleOAuth2Api'))));
}
/** Idempotent — safe to call repeatedly. */
function injectDomainRestrictionFields(credentialType) {
    const properties = Array.isArray(credentialType.properties) ? credentialType.properties : [];
    if (!shouldInjectDomainRestrictionFields(credentialType)) {
        return properties;
    }
    if (properties.some((prop) => prop.name === 'allowedHttpRequestDomains')) {
        return properties;
    }
    return [...properties, ...exports.DOMAIN_RESTRICTION_FIELDS];
}
/** `*.example.com` matches subdomains only, never the bare domain. */
function isDomainAllowed(options) {
    const { url: urlString, allowedDomains } = options;
    if (!allowedDomains || allowedDomains.trim() === '') {
        return true;
    }
    let url;
    try {
        url = new URL(urlString);
    }
    catch {
        return false;
    }
    const hostname = url.hostname.toLowerCase().replace(/\.$/, '');
    if (!hostname)
        return false;
    const allowedDomainsList = allowedDomains
        .split(',')
        .map((domain) => domain.trim().toLowerCase().replace(/\.$/, ''))
        .filter(Boolean);
    for (const allowedDomain of allowedDomainsList) {
        if (allowedDomain.startsWith('*.')) {
            const domainSuffix = allowedDomain.substring(2);
            if (!domainSuffix)
                continue;
            if (hostname.endsWith('.' + domainSuffix))
                return true;
        }
        else if (hostname === allowedDomain) {
            return true;
        }
    }
    return false;
}
/** Throws `UserError` when `node` is omitted, so callers without an `INode` (axios helper) get a wrappable error. */
function assertUrlAllowed(options) {
    const { url, allowedDomains, node } = options;
    if (!allowedDomains)
        return;
    if (isDomainAllowed({ url, allowedDomains }))
        return;
    const message = `Domain not allowed: This credential is restricted from accessing ${url}. Only the following domains are allowed: ${allowedDomains}`;
    throw node ? new node_operation_error_1.NodeOperationError(node, message) : new errors_1.UserError(message);
}
/** Returns the allowlist for forwarding to per-hop redirect checks; `undefined` means allow-all. */
function getCredentialAllowedDomains(options) {
    const { node, credentialData, surface = DEFAULT_SURFACE } = options;
    const mode = readMode(credentialData);
    if (mode === 'none') {
        throw new node_operation_error_1.NodeOperationError(node, `This credential is configured to prevent use within an ${surface} node`);
    }
    if (mode === 'domains') {
        const allowedDomains = readAllowedDomainsField(credentialData);
        if (!allowedDomains) {
            throw new node_operation_error_1.NodeOperationError(node, 'No allowed domains specified. Configure allowed domains or change restriction setting.');
        }
        return allowedDomains;
    }
    return undefined;
}
/**
 * Passing `pinnedUrl` switches `'none'` mode from "block all" to "allow only
 * the pinned URL" — for surfaces like OpenAI where the credential pins a base
 * URL but the user may override it. A falsy `pinnedUrl` makes `'none'` a no-op.
 */
function assertCredentialAllowsUrl(options) {
    const { node, credentialData, url, surface = DEFAULT_SURFACE } = options;
    const usePinnedUrlSemantic = 'pinnedUrl' in options;
    const mode = readMode(credentialData);
    if (mode === 'none') {
        if (!usePinnedUrlSemantic) {
            throw new node_operation_error_1.NodeOperationError(node, `This credential is configured to prevent use within an ${surface} node`);
        }
        if (options.pinnedUrl && url !== options.pinnedUrl) {
            throw new node_operation_error_1.NodeOperationError(node, `Domain not allowed: This credential is restricted from accessing ${url}.`);
        }
        return undefined;
    }
    if (mode === 'domains') {
        const allowedDomains = readAllowedDomainsField(credentialData);
        if (!allowedDomains) {
            throw new node_operation_error_1.NodeOperationError(node, 'No allowed domains specified. Configure allowed domains or change restriction setting.');
        }
        assertUrlAllowed({ url, allowedDomains, node });
        return allowedDomains;
    }
    return undefined;
}
//# sourceMappingURL=credential-domain-restrictions.js.map