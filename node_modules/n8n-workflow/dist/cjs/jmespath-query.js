"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.JmespathQueryError = void 0;
exports.evaluateJmespathQuery = evaluateJmespathQuery;
const jmespath = __importStar(require("jmespath"));
const utils_1 = require("./utils");
/** Thrown when a query is rejected by the property-name security guard. */
class JmespathQueryError extends Error {
    name = 'JmespathQueryError';
}
exports.JmespathQueryError = JmespathQueryError;
/**
 * Evaluate a JMESPath query against arbitrary JSON data, applying the same
 * property-name security guard n8n uses for the `$jmespath()` expression
 * helper. Throws `JmespathQueryError` for guarded queries and rethrows
 * jmespath's own parser errors for invalid syntax.
 */
function evaluateJmespathQuery(data, query) {
    if (query.includes('\\') || (0, utils_1.containsUnsafeObjectPropertyToken)(query)) {
        throw new JmespathQueryError('Cannot access this property in a jmespath query due to security concerns');
    }
    // `null` is not a plain object: skip the spread and let jmespath handle it.
    if (data !== null && typeof data === 'object' && !Array.isArray(data)) {
        // Spread strips any proxy wrappers, mirroring workflow-data-proxy.
        return jmespath.search({ ...data }, query);
    }
    return jmespath.search(data, query);
}
//# sourceMappingURL=jmespath-query.js.map