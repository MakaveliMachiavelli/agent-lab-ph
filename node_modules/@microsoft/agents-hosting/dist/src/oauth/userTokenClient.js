"use strict";
// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
Object.defineProperty(exports, "__esModule", { value: true });
exports.UserTokenClient = void 0;
const httpClient_1 = require("../httpClient");
const agents_activity_1 = require("@microsoft/agents-activity");
const agents_telemetry_1 = require("@microsoft/agents-telemetry");
const activityWireCompat_1 = require("../activityWireCompat");
const getProductInfo_1 = require("../getProductInfo");
const headerPropagation_1 = require("../headerPropagation");
const customUserTokenAPI_1 = require("./customUserTokenAPI");
const agents_telemetry_2 = require("@microsoft/agents-telemetry");
const observability_1 = require("../observability");
const logger = (0, agents_telemetry_1.debug)('agents:user-token-client');
function formatHttpErrorMessage(error) {
    var _a;
    const responseData = (_a = error.response) === null || _a === void 0 ? void 0 : _a.data;
    if (responseData === undefined) {
        return error.message;
    }
    try {
        const serializedResponseData = JSON.stringify(responseData);
        return serializedResponseData === undefined ? error.message : `${error.message}: ${serializedResponseData}`;
    }
    catch {
        return error.message;
    }
}
/**
 * Client for managing user tokens.
 */
class UserTokenClient {
    constructor(param) {
        this.msAppId = '';
        if (typeof param === 'string') {
            const baseURL = (0, customUserTokenAPI_1.getTokenServiceEndpoint)();
            this.client = new httpClient_1.HttpClient({
                baseURL,
                headers: {
                    Accept: 'application/json',
                    'User-Agent': (0, getProductInfo_1.getProductInfo)(),
                }
            });
        }
        else {
            this.client = param;
        }
    }
    /**
     * Creates a new instance of UserTokenClient with authentication.
     * @param baseURL - The base URL for the API.
     * @param authConfig - The authentication configuration.
     * @param authProvider - The authentication provider.
     * @param scope - The scope for the authentication token.
     * @param headers - Optional headers to propagate in the request.
     * @returns A new instance of ConnectorClient.
     */
    static async createClientWithScope(baseURL, authProvider, scope, headers) {
        const headerPropagation = headers !== null && headers !== void 0 ? headers : new headerPropagation_1.HeaderPropagation({});
        (0, getProductInfo_1.applyUserAgentHeader)(headerPropagation);
        headerPropagation.override({
            Accept: 'application/json',
            'Content-Type': 'application/json',
        });
        const clientHeaders = { ...headerPropagation.outgoing };
        const token = await (authProvider === null || authProvider === void 0 ? void 0 : authProvider.getAccessToken(scope));
        if (token && token.length > 1) {
            clientHeaders.Authorization = `Bearer ${token}`;
        }
        const httpClient = new httpClient_1.HttpClient({
            baseURL,
            headers: clientHeaders,
        });
        return new UserTokenClient(httpClient);
    }
    /**
     * Gets the user token.
     * @param connectionName The connection name.
     * @param channelIdComposite The channel ID.
     * @param userId The user ID.
     * @param code The optional code.
     * @returns A promise that resolves to the user token.
     */
    async getUserToken(connectionName, channelIdComposite, userId, code) {
        return (0, agents_telemetry_2.trace)(observability_1.UserTokenClientTraceDefinitions.getUserToken, async ({ record }) => {
            var _a;
            const [channelId] = agents_activity_1.Activity.parseChannelId(channelIdComposite);
            const params = { connectionName, channelId, userId, code };
            try {
                const response = await this.executeRequest('get', '/api/usertoken/GetToken', undefined, { params });
                record({ connectionName, channelId, userId, httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
                if (response === null || response === void 0 ? void 0 : response.data) {
                    return response.data;
                }
                return { token: undefined };
            }
            catch (error) {
                if (error instanceof httpClient_1.HttpError && error.status === 404) {
                    record({ connectionName, channelId, userId, httpStatusCode: '404' });
                    return { token: undefined };
                }
                throw error;
            }
        });
    }
    /**
     * Signs the user out.
     * @param userId The user ID.
     * @param connectionName The connection name.
     * @param channelIdComposite The channel ID.
     * @returns A promise that resolves when the sign-out operation is complete.
     */
    async signOut(userId, connectionName, channelIdComposite) {
        return (0, agents_telemetry_2.trace)(observability_1.UserTokenClientTraceDefinitions.signOut, async ({ record }) => {
            var _a;
            const [channelId] = agents_activity_1.Activity.parseChannelId(channelIdComposite);
            const params = { userId, connectionName, channelId };
            const response = await this.executeRequest('delete', '/api/usertoken/SignOut', undefined, { params });
            record({ userId, connectionName, channelId, httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            if (response.status !== 200) {
                throw new Error('Failed to sign out');
            }
        });
    }
    /**
     * Gets the sign-in resource.
     * @param msAppId The application ID.
     * @param connectionName The connection name.
     * @param conversation The conversation reference.
     * @param relatesTo Optional. The related conversation reference.
     * @returns A promise that resolves to the signing resource.
     */
    async getSignInResource(msAppId, connectionName, conversation, relatesTo) {
        return (0, agents_telemetry_2.trace)(observability_1.UserTokenClientTraceDefinitions.getSignInResource, async ({ record }) => {
            var _a;
            const tokenExchangeState = {
                connectionName,
                conversation,
                relatesTo,
                msAppId
            };
            const tokenExchangeStateNormalized = (0, activityWireCompat_1.normalizeTokenExchangeState)(tokenExchangeState);
            const state = Buffer.from(JSON.stringify(tokenExchangeStateNormalized)).toString('base64');
            const params = { state };
            const response = await this.executeRequest('get', '/api/botsignin/GetSignInResource', undefined, { params });
            record({ connectionName, httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            return response.data;
        });
    }
    /**
     * Exchanges the token.
     * @param userId The user ID.
     * @param connectionName The connection name.
     * @param channelIdComposite The channel ID.
     * @param tokenExchangeRequest The token exchange request.
     * @returns A promise that resolves to the exchanged token.
     */
    async exchangeTokenAsync(userId, connectionName, channelIdComposite, tokenExchangeRequest) {
        return (0, agents_telemetry_2.trace)(observability_1.UserTokenClientTraceDefinitions.exchangeToken, async ({ record }) => {
            var _a;
            const [channelId] = agents_activity_1.Activity.parseChannelId(channelIdComposite);
            const params = { userId, connectionName, channelId };
            const response = await this.executeRequest('post', '/api/usertoken/exchange', tokenExchangeRequest, { params });
            record({ userId, connectionName, channelId, httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            if (response === null || response === void 0 ? void 0 : response.data) {
                return response.data;
            }
            else {
                return { token: undefined };
            }
        });
    }
    /**
     * Gets the token or sign-in resource.
     * @param userId The user ID.
     * @param connectionName The connection name.
     * @param channelIdComposite The channel ID.
     * @param conversation The conversation reference.
     * @param relatesTo The related conversation reference.
     * @param code The code.
     * @param finalRedirect The final redirect URL.
     * @param fwdUrl The forward URL.
     * @returns A promise that resolves to the token or sign-in resource response.
     */
    async getTokenOrSignInResource(userId, connectionName, channelIdComposite, conversation, relatesTo, code, finalRedirect = '', fwdUrl = '') {
        return (0, agents_telemetry_2.trace)(observability_1.UserTokenClientTraceDefinitions.getTokenOrSignInResource, async ({ record }) => {
            var _a;
            const [channelId] = agents_activity_1.Activity.parseChannelId(channelIdComposite);
            const state = Buffer.from(JSON.stringify({ conversation, relatesTo, connectionName, msAppId: this.msAppId })).toString('base64');
            const params = { userId, connectionName, channelId, state, code, finalRedirect, fwdUrl };
            const response = await this.executeRequest('get', '/api/usertoken/GetTokenOrSignInResource', undefined, { params });
            record({ userId, connectionName, channelId, httpStatusCode: (_a = response === null || response === void 0 ? void 0 : response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            return response.data;
        });
    }
    /**
     * Gets the token status.
     * @param userId The user ID.
     * @param channelIdComposite The channel ID.
     * @param include The optional include parameter.
     * @returns A promise that resolves to the token status.
     */
    async getTokenStatus(userId, channelIdComposite, include = null) {
        return (0, agents_telemetry_2.trace)(observability_1.UserTokenClientTraceDefinitions.getTokenStatus, async ({ record }) => {
            var _a;
            const [channelId] = agents_activity_1.Activity.parseChannelId(channelIdComposite);
            const params = { userId, channelId, include };
            const response = await this.executeRequest('get', '/api/usertoken/GetTokenStatus', undefined, { params });
            record({ userId, channelId, httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            return response.data;
        });
    }
    /**
     * Gets the AAD tokens.
     * @param userId The user ID.
     * @param connectionName The connection name.
     * @param channelIdComposite The channel ID.
     * @param resourceUrls The resource URLs.
     * @returns A promise that resolves to the AAD tokens.
     */
    async getAadTokens(userId, connectionName, channelIdComposite, resourceUrls) {
        return (0, agents_telemetry_2.trace)(observability_1.UserTokenClientTraceDefinitions.getAadTokens, async ({ record }) => {
            var _a;
            const [channelId] = agents_activity_1.Activity.parseChannelId(channelIdComposite);
            const params = { userId, connectionName, channelId };
            const response = await this.executeRequest('post', '/api/usertoken/GetAadTokens', resourceUrls, { params });
            record({ userId, connectionName, channelId, httpStatusCode: (_a = response.status) === null || _a === void 0 ? void 0 : _a.toString() });
            return response.data;
        });
    }
    updateAuthToken(token) {
        this.client.setHeader('Authorization', `Bearer ${token}`);
    }
    async executeRequest(method, url, data, options) {
        var _a, _b, _c, _d, _e, _f;
        const { params } = options !== null && options !== void 0 ? options : {};
        const { Authorization, authorization, ...headersToLog } = this.client.defaultHeaders;
        logger.debug('Request: ', {
            host: this.client.baseURL,
            url,
            data,
            method,
            params,
            headers: headersToLog
        });
        try {
            const response = await this.client.request({ method, url, data, params });
            const { token: _token, ...redactedData } = ((_b = (_a = response.config) === null || _a === void 0 ? void 0 : _a.data) !== null && _b !== void 0 ? _b : {});
            logger.debug('Response: ', {
                status: response.status,
                statusText: response.statusText,
                host: this.client.baseURL,
                url: (_c = response.config) === null || _c === void 0 ? void 0 : _c.url,
                data: redactedData,
                method: (_d = response.config) === null || _d === void 0 ? void 0 : _d.method,
            });
            return response;
        }
        catch (error) {
            if (error instanceof httpClient_1.HttpError) {
                const message = formatHttpErrorMessage(error);
                const errorDetails = {
                    host: this.client.baseURL,
                    url: error.config.url,
                    method: error.config.method,
                    data: error.config.data,
                    message,
                    headers: (_e = error.response) === null || _e === void 0 ? void 0 : _e.headers,
                    stack: error.stack,
                };
                logger.debug('Response error: ', errorDetails);
                Object.assign(error, {
                    host: this.client.baseURL,
                    url: error.config.url,
                    method: error.config.method,
                    data: error.config.data,
                    headers: (_f = error.response) === null || _f === void 0 ? void 0 : _f.headers,
                    message,
                });
            }
            throw error;
        }
    }
}
exports.UserTokenClient = UserTokenClient;
//# sourceMappingURL=userTokenClient.js.map