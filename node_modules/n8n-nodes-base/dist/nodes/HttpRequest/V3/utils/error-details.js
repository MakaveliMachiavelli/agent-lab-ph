"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createErrorDetails = createErrorDetails;
const to_json_value_1 = require("@n8n/utils/json/to-json-value");
const n8n_workflow_1 = require("n8n-workflow");
/**
 * Looks for body in the error object in the following order:
 * 1. error.response.data
 * 2. error.response.body
 * 3. error.error - body is here, when legacy http handler is used
 * 4. error.body
 *
 */
function getResponseBody(error) {
    const response = error.response;
    if (response !== null && typeof response === 'object' && !Array.isArray(response)) {
        const responseData = response;
        if (Object.hasOwn(responseData, 'data')) {
            return responseData.data;
        }
        if (Object.hasOwn(responseData, 'body')) {
            return responseData.body;
        }
    }
    if (Object.hasOwn(error, 'error')) {
        return error.error;
    }
    if (Object.hasOwn(error, 'body')) {
        return error.body;
    }
    return undefined;
}
function parseErrorResponseBody(body) {
    const jsonSafeBody = (0, to_json_value_1.toJsonValue)(body);
    if (typeof jsonSafeBody !== 'string') {
        return jsonSafeBody;
    }
    try {
        return (0, to_json_value_1.toJsonValue)(JSON.parse(jsonSafeBody));
    }
    catch {
        return jsonSafeBody;
    }
}
function createErrorDetails(node, reason, itemIndex) {
    try {
        const responseBody = parseErrorResponseBody(getResponseBody(reason));
        const error = new n8n_workflow_1.NodeApiError(node, reason, { itemIndex });
        return {
            message: error.message,
            ...(error.description && { description: error.description }),
            httpCode: error.httpCode,
            body: responseBody,
            context: error.context,
        };
    }
    catch (error) {
        n8n_workflow_1.LoggerProxy.warn('Failed to parse error response body', { error });
        return null;
    }
}
//# sourceMappingURL=error-details.js.map