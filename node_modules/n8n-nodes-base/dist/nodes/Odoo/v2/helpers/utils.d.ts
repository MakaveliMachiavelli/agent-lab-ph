import type { INodeProperties } from 'n8n-workflow';
export declare function recordRLC(displayName: string, name: string, searchListMethod: string, description: string, dependsOn?: string[]): INodeProperties;
export declare const mapFilterOperators: Record<string, string>;
export interface IOdooFilterItem {
    fieldName: string;
    operator: string;
    value: string;
}
export interface IOdooFilters {
    filter: IOdooFilterItem[];
}
export declare function odooGetDBName(databaseName: string | undefined, url: string): string;
/**
 * Converts the filter fixedCollection value into an Odoo domain array.
 * e.g. [['email', '=', 'foo@bar.com'], ['name', 'like', 'foo']]
 *
 * For `in` / `not in` operators Odoo requires a list value, not a scalar.
 * Users enter comma-separated values which are split and coerced here.
 */
export declare function buildDomain(filters: IOdooFilters | undefined): unknown[][];
//# sourceMappingURL=utils.d.ts.map