"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.description = void 0;
exports.execute = execute;
const utils_1 = require("../../helpers/utils");
const transport_1 = require("../../transport");
const utilities_1 = require("../../../../../utils/utilities");
const properties = [
    (0, utils_1.recordRLC)('Activity', 'activityId', 'searchActivities', 'Activity to delete'),
];
const displayOptions = {
    show: { resource: ['activity'], operation: ['delete'] },
};
exports.description = (0, utilities_1.updateDisplayOptions)(displayOptions, properties);
async function execute(items) {
    const returnData = [];
    for (let i = 0; i < items.length; i++) {
        try {
            const activityId = Number(this.getNodeParameter('activityId', i, undefined, {
                extractValue: true,
            }));
            await transport_1.odooApiRequest.call(this, 'mail.activity', 'unlink', {
                ids: [activityId],
            });
            const executionData = this.helpers.constructExecutionMetaData(this.helpers.returnJsonArray({ id: activityId, deleted: true }), { itemData: { item: i } });
            returnData.push(...executionData);
        }
        catch (error) {
            if (this.continueOnFail()) {
                const executionData = this.helpers.constructExecutionMetaData(this.helpers.returnJsonArray({ error: error.message }), { itemData: { item: i } });
                returnData.push(...executionData);
                continue;
            }
            throw error;
        }
    }
    return returnData;
}
//# sourceMappingURL=delete.operation.js.map