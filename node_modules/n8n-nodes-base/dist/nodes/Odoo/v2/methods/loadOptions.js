"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getContactFields = getContactFields;
exports.getOpportunityFields = getOpportunityFields;
exports.getActivityFields = getActivityFields;
const change_case_1 = require("change-case");
const transport_1 = require("../transport");
async function getContactFields() {
    return await getModelFields.call(this, 'res.partner');
}
async function getOpportunityFields() {
    return await getModelFields.call(this, 'crm.lead');
}
async function getActivityFields() {
    return await getModelFields.call(this, 'mail.activity');
}
// ─── Internal helper ──────────────────────────────────────────────────────────
async function getModelFields(model) {
    const response = (await transport_1.odooApiRequest.call(this, model, 'fields_get', {
        attributes: ['string', 'type', 'required'],
    }));
    return Object.entries(response)
        .map(([key, field]) => {
        let displayName = field.string;
        try {
            displayName = (0, change_case_1.capitalCase)(field.string);
        }
        catch { }
        return {
            name: displayName,
            value: key,
            // nodelinter-ignore-next-line
            description: `name: ${key}, type: ${field.type}, required: ${field.required}`,
        };
    })
        .sort((a, b) => a.name.localeCompare(b.name));
}
//# sourceMappingURL=loadOptions.js.map