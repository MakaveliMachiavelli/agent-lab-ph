"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.searchModels = searchModels;
exports.searchCustomRecords = searchCustomRecords;
exports.searchModelRecords = searchModelRecords;
exports.searchContacts = searchContacts;
exports.searchOpportunities = searchOpportunities;
exports.searchActivities = searchActivities;
exports.searchActivityTypes = searchActivityTypes;
exports.searchUsers = searchUsers;
const transport_1 = require("../transport");
async function searchModels(filter) {
    const domain = filter ? [['name', 'ilike', filter]] : [];
    const response = (await transport_1.odooApiRequest.call(this, 'ir.model', 'search_read', {
        domain,
        fields: ['name', 'model'],
        limit: 60,
        offset: 0,
    }));
    return {
        results: response
            .sort((a, b) => a.name.localeCompare(b.name))
            .map((m) => ({
            name: m.name,
            value: m.model,
            description: m.model,
        })),
    };
}
async function searchCustomRecords(filter) {
    const model = this.getNodeParameter('customResource', undefined, {
        extractValue: true,
    });
    if (!model)
        return { results: [] };
    const domain = filter ? [['display_name', 'ilike', filter]] : [];
    const response = (await transport_1.odooApiRequest.call(this, model, 'search_read', {
        domain,
        fields: ['id', 'display_name'],
        limit: 60,
        offset: 0,
    }));
    return {
        results: response.map((r) => ({
            name: r.display_name,
            value: r.id,
        })),
    };
}
async function searchModelRecords(filter) {
    const model = this.getNodeParameter('res_model', undefined, {
        extractValue: true,
    });
    if (!model)
        return { results: [] };
    const domain = filter ? [['display_name', 'ilike', filter]] : [];
    const response = (await transport_1.odooApiRequest.call(this, model, 'search_read', {
        domain,
        fields: ['id', 'display_name'],
        limit: 60,
        offset: 0,
    }));
    return {
        results: response.map((r) => ({
            name: r.display_name,
            value: r.id,
        })),
    };
}
async function searchContacts(filter) {
    const domain = filter ? [['name', 'ilike', filter]] : [];
    const response = (await transport_1.odooApiRequest.call(this, 'res.partner', 'search_read', {
        domain,
        fields: ['id', 'name'],
        limit: 60,
        offset: 0,
    }));
    return {
        results: response.map((r) => ({
            name: r.name,
            value: r.id,
        })),
    };
}
async function searchOpportunities(filter) {
    const domain = filter ? [['name', 'ilike', filter]] : [];
    const response = (await transport_1.odooApiRequest.call(this, 'crm.lead', 'search_read', {
        domain,
        fields: ['id', 'name'],
        limit: 60,
        offset: 0,
    }));
    return {
        results: response.map((r) => ({
            name: r.name,
            value: r.id,
        })),
    };
}
async function searchActivities(filter) {
    const domain = filter ? [['summary', 'ilike', filter]] : [];
    const response = (await transport_1.odooApiRequest.call(this, 'mail.activity', 'search_read', {
        domain,
        fields: ['id', 'summary', 'res_name'],
        limit: 60,
        offset: 0,
    }));
    return {
        results: response.map((r) => ({
            name: r.summary || `Activity #${r.id}`,
            value: r.id,
            description: r.res_name,
        })),
    };
}
async function searchActivityTypes(filter) {
    const domain = filter ? [['name', 'ilike', filter]] : [];
    const response = (await transport_1.odooApiRequest.call(this, 'mail.activity.type', 'search_read', {
        domain,
        fields: ['id', 'name'],
        limit: 60,
        offset: 0,
    }));
    return {
        results: response
            .sort((a, b) => a.name.localeCompare(b.name))
            .map((t) => ({
            name: t.name,
            value: t.id,
        })),
    };
}
async function searchUsers(filter) {
    const domain = [['active', '=', true]];
    if (filter)
        domain.push(['name', 'ilike', filter]);
    const response = (await transport_1.odooApiRequest.call(this, 'res.users', 'search_read', {
        domain,
        fields: ['id', 'name'],
        limit: 60,
        offset: 0,
    }));
    return {
        results: response
            .sort((a, b) => a.name.localeCompare(b.name))
            .map((u) => ({
            name: u.name,
            value: u.id,
        })),
    };
}
//# sourceMappingURL=listSearch.js.map