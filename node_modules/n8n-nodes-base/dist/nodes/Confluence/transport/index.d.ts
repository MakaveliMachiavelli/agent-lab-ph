import type { IDataObject, IExecuteFunctions, IHttpRequestMethods, ILoadOptionsFunctions } from 'n8n-workflow';
export declare const CONFLUENCE_CREDENTIAL_NAME = "confluenceCloudOAuth2Api";
export interface AccessibleResource {
    id: string;
    url: string;
    name?: string;
}
/**
 * Pure hostname matcher, exported so ENT-128's credential Test can reuse the exact
 * matching this node routes with. Case-insensitive, never an exact-string comparison
 * (the trap in Jira's `getCloudId`).
 */
export declare function matchSiteByHostname(resources: AccessibleResource[], hostname: string): AccessibleResource | undefined;
/** Test-only escape hatch for the module-level cache. */
export declare function clearCloudIdCache(): void;
/** Reduce any pasted site-URL form (scheme optional, `/wiki` or deeper paths, any casing) to its lowercased hostname. */
export declare function normalizeSiteUrl(siteUrl: string): string;
export declare function getConfluenceCloudId(this: IExecuteFunctions | ILoadOptionsFunctions, siteUrl: string): Promise<string>;
export declare function confluenceApiRequest(this: IExecuteFunctions | ILoadOptionsFunctions, method: IHttpRequestMethods, endpoint: string, body?: IDataObject, qs?: IDataObject): Promise<IDataObject>;
//# sourceMappingURL=index.d.ts.map