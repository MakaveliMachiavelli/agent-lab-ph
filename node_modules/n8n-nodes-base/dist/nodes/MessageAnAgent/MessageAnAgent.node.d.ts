import type { INodeTypeBaseDescription } from 'n8n-workflow';
import { VersionedNodeType } from 'n8n-workflow';
/**
 * Identity shared by every version. The per-version descriptions add `version`,
 * the `agentId` property, and (v1 only) the `listAgents` search method.
 */
export declare const baseDescription: INodeTypeBaseDescription;
export declare class MessageAnAgent extends VersionedNodeType {
    constructor();
}
//# sourceMappingURL=MessageAnAgent.node.d.ts.map