import type { JSONSchema7 } from 'json-schema';
import type { IExecuteFunctions, INodeExecutionData, INodeProperties, INodeTypeDescription } from 'n8n-workflow';
/** Description fields that are identical across versions. */
export declare const sharedVersionDescription: Pick<INodeTypeDescription, 'hidden' | 'defaults' | 'codex' | 'usableAsTool' | 'inputs' | 'outputs'>;
/** The prompt input, identical across versions; rendered after the version-specific `agentId` picker. */
export declare const messageProperty: INodeProperties;
export declare const commonProperties: INodeProperties[];
/**
 * Read and parse the per-item structured-output JSON Schema. Returns `undefined`
 * when the toggle is off; throws a user-facing error when the toggle is on but
 * the schema is empty or not valid JSON. On typeVersion >= 3, supports inferring
 * the schema from a JSON example (`schemaType: fromJson`).
 */
export declare function getStructuredOutputSchema(ctx: IExecuteFunctions, itemIndex: number): JSONSchema7 | undefined;
/** Shared execution for every version. */
export declare function execute(this: IExecuteFunctions): Promise<INodeExecutionData[][]>;
//# sourceMappingURL=shared.d.ts.map