"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getWorkspaces = getWorkspaces;
exports.getBases = getBases;
exports.getTables = getTables;
exports.getViews = getViews;
exports.getFields = getFields;
exports.getTriggerFields = getTriggerFields;
exports.getDownloadFields = getDownloadFields;
exports.getDownloadFieldsId = getDownloadFieldsId;
exports.getLinkFieldsId = getLinkFieldsId;
exports.getRelatedTableFields = getRelatedTableFields;
const n8n_workflow_1 = require("n8n-workflow");
const helpers_1 = require("../helpers");
const columns_fetcher_1 = require("../helpers/columns-fetcher");
const transport_1 = require("../transport");
async function getBaseUrl() {
    const authenticationMethod = this.getNodeParameter('authentication', 0);
    const credentials = await this.getCredentials(authenticationMethod);
    if (credentials === undefined) {
        throw new n8n_workflow_1.NodeOperationError(this.getNode(), 'No credentials got returned!');
    }
    return credentials.host;
}
async function getWorkspaces(filter) {
    try {
        const baseUrl = await getBaseUrl.call(this);
        const constructUrl = (workspaceId) => {
            return `${baseUrl}/#/${workspaceId && workspaceId !== 'none' ? workspaceId : 'nc'}`;
        };
        const requestMethod = 'GET';
        // no v3 api yet for workspaces list
        const endpoint = '/api/v2/meta/workspaces';
        const responseData = await transport_1.apiRequest.call(this, requestMethod, endpoint, {}, {});
        const results = responseData.list.map((i) => ({
            name: i.title,
            value: i.id,
            url: constructUrl(i.id),
        }));
        return {
            results: filter && filter !== '' ? results.filter((flt) => flt.name.includes(filter)) : results,
        };
    }
    catch (e) {
        return { results: [{ name: 'No Workspace', value: 'none' }] };
    }
}
async function getBases(filter) {
    const configWorkspaceId = this.getNodeParameter('workspaceId', 0, {
        extractValue: true,
    });
    const workspaceId = !configWorkspaceId || configWorkspaceId === 'none' ? 'nc' : configWorkspaceId;
    const baseUrl = await getBaseUrl.call(this);
    const constructUrl = (baseId) => {
        return `${baseUrl}/#/${workspaceId}/${baseId}`;
    };
    try {
        const requestMethod = 'GET';
        const endpoint = `/api/v3/meta/workspaces/${workspaceId}/bases`;
        const responseData = await transport_1.apiRequest.call(this, requestMethod, endpoint, {}, {});
        let results = responseData.list.map((i) => ({
            name: i.title,
            value: i.id,
            url: constructUrl(i.id),
        }));
        if (filter && filter !== '') {
            results = results.filter((flt) => flt.name.includes(filter));
        }
        return { results };
    }
    catch (e) {
        throw new n8n_workflow_1.NodeOperationError(this.getNode(), new Error('Error while fetching bases!', {
            cause: e,
        }), {
            level: 'warning',
        });
    }
}
// This only supports using the Base ID
async function getTables(filter) {
    const baseId = this.getNodeParameter('projectId', 0, {
        extractValue: true,
    });
    if (baseId) {
        const baseUrl = await getBaseUrl.call(this);
        const workspaceId = this.getNodeParameter('workspaceId', 0, {
            extractValue: true,
        });
        const constructUrl = (tableId) => {
            return `${baseUrl}/#/${workspaceId && workspaceId !== 'none' ? workspaceId : 'nc'}/${baseId}/${tableId}`;
        };
        try {
            const requestMethod = 'GET';
            const endpoint = `/api/v3/meta/bases/${baseId}/tables`;
            const responseData = await transport_1.apiRequest.call(this, requestMethod, endpoint, {}, {});
            const results = responseData.list.map((i) => ({
                name: i.title,
                value: i.id,
                url: constructUrl(i.id),
            }));
            return {
                results: filter && filter !== '' ? results.filter((flt) => flt.name.includes(filter)) : results,
            };
        }
        catch (e) {
            throw (0, helpers_1.parseToApiNodeOperationError)({
                error: e,
                errorLevel: 'warning',
                subject: 'Error while fetching tables:',
                node: this.getNode(),
            });
        }
    }
    else {
        throw new n8n_workflow_1.NodeOperationError(this.getNode(), 'No base selected!', {
            level: 'warning',
        });
    }
}
async function getViews(filter) {
    const baseId = this.getNodeParameter('projectId', 0, {
        extractValue: true,
    });
    const tableId = this.getNodeParameter('table', 0, {
        extractValue: true,
    });
    const baseUrl = await getBaseUrl.call(this);
    const workspaceId = this.getNodeParameter('workspaceId', 0, {
        extractValue: true,
    });
    const constructUrl = (viewId) => {
        return `${baseUrl}/#/${workspaceId && workspaceId !== 'none' ? workspaceId : 'nc'}/${baseId}/${tableId}/${viewId}`;
    };
    if (tableId) {
        try {
            const requestMethod = 'GET';
            const endpoint = `/api/v3/meta/bases/${baseId}/tables/${tableId}`;
            const responseData = await transport_1.apiRequest.call(this, requestMethod, endpoint, {}, {});
            const results = responseData.views.map((i) => {
                return {
                    name: i.title,
                    value: i.id,
                    url: constructUrl(i.id),
                };
            });
            return {
                results: filter && filter !== ''
                    ? results.filter((flt) => flt.name.includes(filter))
                    : results,
            };
        }
        catch (e) {
            throw (0, helpers_1.parseToApiNodeOperationError)({
                error: e,
                errorLevel: 'warning',
                subject: 'Error while fetching views:',
                node: this.getNode(),
            });
        }
    }
    else {
        throw new n8n_workflow_1.NodeOperationError(this.getNode(), 'No table selected!', {
            level: 'warning',
        });
    }
}
async function getFields(filter) {
    const baseId = this.getNodeParameter('projectId', 0, {
        extractValue: true,
    });
    const tableId = this.getNodeParameter('table', 0, {
        extractValue: true,
    });
    if (tableId) {
        try {
            const requestMethod = 'GET';
            const endpoint = `/api/v3/meta/bases/${baseId}/tables/${tableId}`;
            const responseData = await transport_1.apiRequest.call(this, requestMethod, endpoint, {}, {});
            const results = responseData.fields.map((i) => {
                return {
                    name: i.title,
                    value: i.id,
                };
            });
            return {
                results: filter && filter !== ''
                    ? results.filter((flt) => flt.name.includes(filter))
                    : results,
            };
        }
        catch (e) {
            throw (0, helpers_1.parseToApiNodeOperationError)({
                error: e,
                errorLevel: 'warning',
                subject: 'Error while fetching fields:',
                node: this.getNode(),
            });
        }
    }
    else {
        throw new n8n_workflow_1.NodeOperationError(this.getNode(), 'No table selected!', {
            level: 'warning',
        });
    }
}
// This only supports using the table ID
async function getTriggerFields(filter) {
    const baseId = this.getNodeParameter('projectId', 0, {
        extractValue: true,
    });
    const tableId = this.getNodeParameter('table', 0, {
        extractValue: true,
    });
    if (tableId) {
        try {
            const fetcher = new columns_fetcher_1.ColumnsFetcher(this);
            const responseData = await fetcher.fetch({
                baseId,
                tableId,
            });
            const results = responseData
                .filter((field) => {
                return ['CreatedTime', 'LastModifiedTime'].includes(field.type);
            })
                .map((i) => ({
                name: i.title,
                value: i.title,
            }));
            return {
                results: filter && filter !== ''
                    ? results.filter((flt) => flt.name.includes(filter))
                    : results,
            };
        }
        catch (e) {
            throw (0, helpers_1.parseToApiNodeOperationError)({
                error: e,
                errorLevel: 'warning',
                subject: 'Error while fetching fields:',
                node: this.getNode(),
            });
        }
    }
    else {
        throw new n8n_workflow_1.NodeOperationError(this.getNode(), 'No table selected!', {
            level: 'warning',
        });
    }
}
async function getDownloadFields(filter) {
    const { results } = await getDownloadFieldsId.call(this, filter);
    return {
        results: results.map((field) => ({ name: field.name, value: field.name })),
    };
}
async function getDownloadFieldsId(filter) {
    try {
        const fetcher = new columns_fetcher_1.ColumnsFetcher(this);
        const fields = await fetcher.fetchFromDefinedParam();
        return {
            results: fields
                .filter((field) => field.type === 'Attachment' && (!filter || field.title.includes(filter)))
                .map((field) => {
                return {
                    name: field.title,
                    value: field.id,
                };
            }),
        };
    }
    catch (e) {
        throw (0, helpers_1.parseToApiNodeOperationError)({
            error: e,
            errorLevel: 'warning',
            subject: 'Error while fetching fields:',
            node: this.getNode(),
        });
    }
}
async function getLinkFieldsId(filter) {
    try {
        const fetcher = new columns_fetcher_1.ColumnsFetcher(this);
        const fields = await fetcher.fetchFromDefinedParam();
        return {
            results: fields
                .filter((field) => ['Link', 'LinkToAnotherRecord'].includes(field.type) &&
                (!filter || field.title.includes(filter)))
                .map((field) => {
                return {
                    name: field.title,
                    value: field.id,
                };
            }),
        };
    }
    catch (e) {
        throw (0, helpers_1.parseToApiNodeOperationError)({
            error: e,
            errorLevel: 'warning',
            subject: 'Error while fetching fields:',
            node: this.getNode(),
        });
    }
}
async function getRelatedTableFields() {
    const baseId = this.getNodeParameter('projectId', 0, {
        extractValue: true,
    });
    const linkFieldName = this.getNodeParameter('linkFieldName', 0, {
        extractValue: true,
    });
    const tableId = this.getNodeParameter('table', 0, {
        extractValue: true,
    });
    if (linkFieldName) {
        try {
            const requestMethod = 'GET';
            let endpoint = `/api/v3/meta/bases/${baseId}/tables/${tableId}/fields/${linkFieldName}`;
            let responseData = await transport_1.apiRequest.call(this, requestMethod, endpoint, {}, {});
            const relatedTableId = responseData.options?.related_table_id;
            if (!relatedTableId) {
                return { results: [] };
            }
            endpoint = `/api/v3/meta/bases/${baseId}/tables/${relatedTableId}`;
            responseData = await transport_1.apiRequest.call(this, requestMethod, endpoint, {}, {});
            return {
                results: responseData.fields.map((i) => {
                    return {
                        name: i.title,
                        value: i.id,
                    };
                }),
            };
        }
        catch (e) {
            throw (0, helpers_1.parseToApiNodeOperationError)({
                error: e,
                errorLevel: 'warning',
                subject: 'Error while fetching fields:',
                node: this.getNode(),
            });
        }
    }
    else {
        throw new n8n_workflow_1.NodeOperationError(this.getNode(), 'No link field selected!', {
            level: 'warning',
        });
    }
}
//# sourceMappingURL=listSearch.js.map