"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.NotionTrigger = void 0;
const moment_timezone_1 = __importDefault(require("moment-timezone"));
const n8n_workflow_1 = require("n8n-workflow");
const constants_1 = require("./shared/constants");
const GenericFunctions_1 = require("./shared/GenericFunctions");
const methods_1 = require("./shared/methods");
const methods_2 = require("./v3/methods");
const transport_1 = require("./v3/transport");
function createQueryDatabaseRequest(ctx) {
    const nodeVersion = ctx.getNode().typeVersion;
    if (nodeVersion >= 1.1) {
        const dataSourceId = ctx.getNodeParameter('dataSourceId', '', {
            extractValue: true,
        });
        return async (body) => (await transport_1.notionApiRequestV3.call(ctx, 'POST', `/data_sources/${dataSourceId}/query`, body));
    }
    const databaseId = ctx.getNodeParameter('databaseId', '', { extractValue: true });
    return async (body) => (await GenericFunctions_1.notionApiRequest.call(ctx, 'POST', `/databases/${databaseId}/query`, body, {}, '', {
        headers: {
            'Notion-Version': '2022-02-22',
        },
    }));
}
class NotionTrigger {
    description = {
        displayName: 'Notion Trigger',
        name: 'notionTrigger',
        icon: { light: 'file:notion.svg', dark: 'file:notion.dark.svg' },
        group: ['trigger'],
        version: [1, 1.1],
        defaultVersion: 1.1,
        description: 'Starts the workflow when Notion events occur',
        subtitle: '={{$parameter["event"]}}',
        defaults: {
            name: 'Notion Trigger',
        },
        credentials: [
            {
                name: 'notionApi',
                required: true,
                displayOptions: {
                    show: {
                        authentication: ['apiKey'],
                    },
                },
            },
            {
                name: 'notionOAuth2Api',
                required: true,
                displayOptions: {
                    show: {
                        authentication: ['oAuth2'],
                    },
                },
            },
        ],
        polling: true,
        inputs: [],
        outputs: [n8n_workflow_1.NodeConnectionTypes.Main],
        properties: [
            {
                displayName: 'Authentication',
                name: 'authentication',
                type: 'options',
                options: [
                    {
                        name: 'API Key',
                        value: 'apiKey',
                    },
                    {
                        name: 'OAuth2',
                        value: 'oAuth2',
                    },
                ],
                default: 'apiKey',
            },
            {
                displayName: 'Event',
                name: 'event',
                type: 'options',
                options: [
                    {
                        name: 'Page Added to Database',
                        value: 'pageAddedToDatabase',
                    },
                    {
                        name: 'Page Updated in Database',
                        value: 'pagedUpdatedInDatabase',
                    },
                ],
                required: true,
                default: 'pageAddedToDatabase',
            },
            {
                displayName: 'In Notion, make sure to <a href="https://www.notion.com/help/add-and-manage-connections-with-the-api" target="_blank">add your connection</a> to the pages you want to access.',
                name: 'notionNotice',
                type: 'notice',
                default: '',
            },
            {
                displayName: 'Database',
                name: 'databaseId',
                type: 'resourceLocator',
                default: { mode: 'list', value: '' },
                required: true,
                modes: [
                    {
                        displayName: 'Database',
                        name: 'list',
                        type: 'list',
                        placeholder: 'Select a Database...',
                        typeOptions: {
                            searchListMethod: 'getDatabases',
                            searchable: true,
                        },
                    },
                    {
                        displayName: 'Link',
                        name: 'url',
                        type: 'string',
                        placeholder: 'https://www.notion.com/0fe2f7de558b471eab07e9d871cdf4a9?v=f2d424ba0c404733a3f500c78c881610',
                        validation: [
                            {
                                type: 'regex',
                                properties: {
                                    regex: constants_1.databaseUrlValidationRegexp,
                                    errorMessage: 'Not a valid Notion Database URL',
                                },
                            },
                        ],
                        extractValue: {
                            type: 'regex',
                            regex: constants_1.databaseUrlExtractionRegexp,
                        },
                    },
                    {
                        displayName: 'ID',
                        name: 'id',
                        type: 'string',
                        placeholder: 'ab1545b247fb49fa92d6f4b49f4d8116',
                        validation: [
                            {
                                type: 'regex',
                                properties: {
                                    regex: constants_1.idValidationRegexp,
                                    errorMessage: 'Not a valid Notion Database ID',
                                },
                            },
                        ],
                        extractValue: {
                            type: 'regex',
                            regex: constants_1.idExtractionRegexp,
                        },
                        url: '=https://www.notion.com/{{$value.replace(/-/g, "")}}',
                    },
                ],
                displayOptions: {
                    show: {
                        '@version': [1],
                        event: ['pageAddedToDatabase', 'pagedUpdatedInDatabase'],
                    },
                },
                description: 'The Notion Database to operate on',
            },
            {
                displayName: 'Data Source',
                name: 'dataSourceId',
                type: 'resourceLocator',
                default: { mode: 'list', value: '' },
                required: true,
                modes: [
                    {
                        displayName: 'Data Source',
                        name: 'list',
                        type: 'list',
                        placeholder: 'Select a Data Source...',
                        typeOptions: {
                            searchListMethod: 'getDataSources',
                            searchable: true,
                        },
                    },
                    {
                        displayName: 'ID',
                        name: 'id',
                        type: 'string',
                        placeholder: 'ab1545b247fb49fa92d6f4b49f4d8116',
                        validation: [
                            {
                                type: 'regex',
                                properties: {
                                    regex: constants_1.idValidationRegexp,
                                    errorMessage: 'Not a valid Notion Data Source ID',
                                },
                            },
                        ],
                        extractValue: {
                            type: 'regex',
                            regex: constants_1.idExtractionRegexp,
                        },
                    },
                ],
                displayOptions: {
                    show: {
                        '@version': [{ _cnd: { gte: 1.1 } }],
                        event: ['pageAddedToDatabase', 'pagedUpdatedInDatabase'],
                    },
                },
                description: 'The Notion Data Source to operate on',
            },
            {
                displayName: 'Simplify',
                name: 'simple',
                type: 'boolean',
                displayOptions: {
                    show: {
                        event: ['pageAddedToDatabase', 'pagedUpdatedInDatabase'],
                    },
                },
                default: true,
                description: 'Whether to return a simplified version of the response instead of the raw data',
            },
        ],
    };
    methods = {
        listSearch: {
            getDatabases: methods_1.listSearch.getDatabases,
            getDataSources: methods_2.listSearch.getDataSources,
        },
    };
    async poll() {
        const webhookData = this.getWorkflowStaticData('node');
        const event = this.getNodeParameter('event');
        const simple = this.getNodeParameter('simple');
        const queryDatabase = createQueryDatabaseRequest(this);
        const lastTimeChecked = webhookData.lastTimeChecked
            ? (0, moment_timezone_1.default)(webhookData.lastTimeChecked)
            : (0, moment_timezone_1.default)().set({ second: 0, millisecond: 0 }); // Notion timestamp accuracy is only down to the minute
        // update lastTimeChecked to now
        webhookData.lastTimeChecked = (0, moment_timezone_1.default)().set({ second: 0, millisecond: 0 }).utc().format();
        // because Notion timestamp accuracy is only down to the minute some duplicates can be fetch
        const possibleDuplicates = webhookData.possibleDuplicates ?? [];
        const sortProperty = event === 'pageAddedToDatabase' ? 'created_time' : 'last_edited_time';
        const body = {
            page_size: 1,
            sorts: [
                {
                    timestamp: sortProperty,
                    direction: 'descending',
                },
            ],
            ...(this.getMode() !== 'manual' && {
                filter: {
                    timestamp: sortProperty,
                    [sortProperty]: {
                        on_or_after: lastTimeChecked.utc().format(),
                    },
                },
            }),
        };
        let records = [];
        let hasMore = true;
        //get last record
        let { results: data } = await queryDatabase(body);
        if (this.getMode() === 'manual') {
            if (simple) {
                data = (0, GenericFunctions_1.simplifyObjects)(data, false, 1);
            }
            if (Array.isArray(data) && data.length) {
                return [this.helpers.returnJsonArray(data)];
            }
        }
        // if something changed after the last check
        if (Array.isArray(data) && data.length && Object.keys(data[0]).length !== 0) {
            do {
                body.page_size = 10;
                const { results, has_more, next_cursor } = await queryDatabase(body);
                records.push(...results);
                hasMore = has_more ?? false;
                if (next_cursor !== undefined && next_cursor !== null) {
                    body.start_cursor = next_cursor;
                }
                // Only stop when we reach records strictly before last recorded time to be sure we catch records from the same minute
            } while (records.length > 0 &&
                !(0, moment_timezone_1.default)(records[records.length - 1][sortProperty]).isBefore(lastTimeChecked) &&
                hasMore);
            // Filter out already processed left over records:
            // with a time strictly before the last record processed
            // or from the same minute not present in the list of processed records
            records = records.filter((record) => !possibleDuplicates.includes(record.id));
            // Save the time of the most recent record processed
            if (records[0]) {
                const latestTimestamp = (0, moment_timezone_1.default)(records[0][sortProperty]);
                // Save record ids with the same timestamp as the latest processed records
                webhookData.possibleDuplicates = records
                    .filter((record) => (0, moment_timezone_1.default)(record[sortProperty]).isSame(latestTimestamp))
                    .map((record) => record.id);
            }
            else {
                webhookData.possibleDuplicates = undefined;
            }
            if (simple) {
                records = (0, GenericFunctions_1.simplifyObjects)(records, false, 1);
            }
            if (Array.isArray(records) && records.length) {
                return [this.helpers.returnJsonArray(records)];
            }
        }
        return null;
    }
}
exports.NotionTrigger = NotionTrigger;
//# sourceMappingURL=NotionTrigger.node.js.map