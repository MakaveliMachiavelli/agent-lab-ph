"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.description = void 0;
exports.append = append;
exports.getMarkdown = getMarkdown;
exports.getAll = getAll;
const utils_1 = require("../../helpers/utils");
const transport_1 = require("../../transport");
const common_descriptions_1 = require("../common.descriptions");
async function fetchNestedBlocks(blocks, responseData = [], limit) {
    for (const block of blocks) {
        responseData.push(block);
        const blockId = block.id;
        if (limit && responseData.length >= limit) {
            return responseData.slice(0, limit);
        }
        if (typeof blockId !== 'string' ||
            block.type === 'child_page' ||
            block.type === 'unsupported' ||
            !block.has_children) {
            continue;
        }
        const children = await transport_1.notionApiRequestAllItemsV3.call(this, 'results', 'GET', `/blocks/${blockId}/children`);
        const nestedBlocks = children.map((entry) => ({
            object: entry.object,
            parent_id: blockId,
            ...entry,
        }));
        await fetchNestedBlocks.call(this, nestedBlocks, responseData, limit);
        if (limit && responseData.length >= limit) {
            return responseData.slice(0, limit);
        }
    }
    return responseData;
}
exports.description = [
    {
        displayName: 'Operation',
        name: 'operation',
        type: 'options',
        noDataExpression: true,
        displayOptions: { show: { resource: ['block'] } },
        options: [
            {
                name: 'Append After',
                value: 'append',
                description: 'Append a block',
                action: 'Append a block',
            },
            {
                name: 'Get Markdown',
                value: 'getMarkdown',
                description: 'Get block markdown',
                action: 'Get block markdown',
            },
            {
                name: 'Get Many',
                value: 'getAll',
                description: 'Get many child blocks',
                action: 'Get many child blocks',
            },
        ],
        default: 'append',
    },
    {
        ...common_descriptions_1.blockId,
        displayName: 'Parent Block',
        displayOptions: { show: { resource: ['block'], operation: ['append'] } },
        description: 'The Notion block to append blocks to',
    },
    {
        displayName: 'Insert After Block',
        name: 'afterBlockId',
        type: 'string',
        default: '',
        displayOptions: { show: { resource: ['block'], operation: ['append'] } },
        description: 'ID of the block after which to insert the new blocks. Leave empty to append at the end.',
    },
    (0, common_descriptions_1.blockBuilder)('block', 'append'),
    {
        ...common_descriptions_1.blockId,
        displayOptions: { show: { resource: ['block'], operation: ['getAll'] } },
        description: 'The Notion block to get children from',
    },
    {
        ...common_descriptions_1.blockId,
        displayOptions: { show: { resource: ['block'], operation: ['getMarkdown'] } },
        description: 'The Notion block to get markdown from',
    },
    {
        displayName: 'Include Transcript',
        name: 'includeTranscript',
        type: 'boolean',
        default: false,
        displayOptions: { show: { resource: ['block'], operation: ['getMarkdown'] } },
    },
    ...(0, common_descriptions_1.returnAllOrLimit)('block', 'getAll'),
    {
        displayName: 'Fetch Nested Blocks',
        name: 'fetchNestedBlocks',
        type: 'boolean',
        default: false,
        displayOptions: { show: { resource: ['block'], operation: ['getAll'] } },
    },
    {
        displayName: 'Simplify Output',
        name: 'simplifyOutput',
        type: 'boolean',
        default: true,
        displayOptions: { show: { resource: ['block'], operation: ['getAll'] } },
    },
];
async function append(items) {
    const returnData = [];
    for (let i = 0; i < items.length; i++) {
        try {
            const blockIdValue = utils_1.extractBlockId.call(this, i);
            const rawBlockValues = this.getNodeParameter('blockUi.blockValues', i, []);
            const blockValues = Array.isArray(rawBlockValues) ? rawBlockValues.filter(transport_1.isDataObject) : [];
            const blockValuesData = (0, utils_1.normalizeBlockValues)(blockValues);
            const afterBlockId = this.getNodeParameter('afterBlockId', i, '');
            const body = { children: (0, utils_1.formatBlocks)(blockValuesData) };
            if (afterBlockId) {
                body.position = {
                    type: 'after_block',
                    after_block: { id: afterBlockId },
                };
            }
            const response = await transport_1.notionApiRequestV3.call(this, 'PATCH', `/blocks/${blockIdValue}/children`, body);
            const executionData = this.helpers.constructExecutionMetaData(this.helpers.returnJsonArray(response), { itemData: { item: i } });
            returnData.push.apply(returnData, executionData);
        }
        catch (error) {
            utils_1.handleOperationError.call(this, returnData, error, i);
        }
    }
    return returnData;
}
async function getMarkdown(items) {
    const returnData = [];
    for (let i = 0; i < items.length; i++) {
        try {
            const blockIdValue = utils_1.extractBlockId.call(this, i);
            const includeTranscript = this.getNodeParameter('includeTranscript', i);
            // uses page endpoint, but it supports block ids too
            const response = await transport_1.notionApiRequestV3.call(this, 'GET', `/pages/${blockIdValue}/markdown`, {}, includeTranscript ? { include_transcript: true } : {});
            const executionData = this.helpers.constructExecutionMetaData(this.helpers.returnJsonArray(response), { itemData: { item: i } });
            returnData.push.apply(returnData, executionData);
        }
        catch (error) {
            utils_1.handleOperationError.call(this, returnData, error, i);
        }
    }
    return returnData;
}
async function getAll(items) {
    const returnData = [];
    for (let i = 0; i < items.length; i++) {
        try {
            const blockIdValue = utils_1.extractBlockId.call(this, i);
            const returnAll = this.getNodeParameter('returnAll', i);
            const limit = returnAll ? undefined : this.getNodeParameter('limit', i);
            let response = await transport_1.notionApiRequestAllItemsV3.call(this, 'results', 'GET', `/blocks/${blockIdValue}/children`, {}, limit ? { page_size: Math.min(limit, 100), limit } : {});
            const fetchNestedBlocksOption = this.getNodeParameter('fetchNestedBlocks', i);
            if (fetchNestedBlocksOption) {
                response = await fetchNestedBlocks.call(this, response, [], limit);
            }
            const simplifyOutput = this.getNodeParameter('simplifyOutput', i);
            if (simplifyOutput) {
                response = (0, utils_1.simplifyBlocksOutput)(response, blockIdValue);
            }
            const executionData = this.helpers.constructExecutionMetaData(this.helpers.returnJsonArray(response), { itemData: { item: i } });
            returnData.push.apply(returnData, executionData);
        }
        catch (error) {
            utils_1.handleOperationError.call(this, returnData, error, i);
        }
    }
    return returnData;
}
//# sourceMappingURL=Block.resource.js.map