"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.description = void 0;
exports.get = get;
const utils_1 = require("../../helpers/utils");
const transport_1 = require("../../transport");
const common_descriptions_1 = require("../common.descriptions");
exports.description = [
    {
        displayName: 'Operation',
        name: 'operation',
        type: 'options',
        noDataExpression: true,
        displayOptions: { show: { resource: ['database'] } },
        options: [
            {
                name: 'Get',
                value: 'get',
                description: 'Get database metadata and its data sources',
                action: 'Get a database',
            },
        ],
        default: 'get',
    },
    {
        ...common_descriptions_1.databaseLocator,
        displayName: 'Database',
        name: 'databaseId',
        displayOptions: { show: { resource: ['database'], operation: ['get'] } },
        description: 'The Notion database to retrieve. Use Data Source operations to search, query, or create database pages.',
    },
    {
        displayName: 'Simplify',
        name: 'simple',
        type: 'boolean',
        default: true,
        displayOptions: { show: { resource: ['database'], operation: ['get'] } },
        description: 'Whether to return a simplified version of the response instead of the raw data',
    },
];
async function get(items) {
    const returnData = [];
    for (let i = 0; i < items.length; i++) {
        try {
            const databaseId = this.getNodeParameter('databaseId', i, '', {
                extractValue: true,
            });
            let response = await transport_1.notionApiRequestV3.call(this, 'GET', `/databases/${databaseId}`);
            if (this.getNodeParameter('simple', i)) {
                response = (0, utils_1.simplifyObjects)(response, false, 3);
            }
            const executionData = this.helpers.constructExecutionMetaData(this.helpers.returnJsonArray(response), { itemData: { item: i } });
            returnData.push.apply(returnData, executionData);
        }
        catch (error) {
            utils_1.handleOperationError.call(this, returnData, error, i);
        }
    }
    return returnData;
}
//# sourceMappingURL=Database.resource.js.map