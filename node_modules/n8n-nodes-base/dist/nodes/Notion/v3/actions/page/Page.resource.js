"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.description = void 0;
exports.archive = archive;
exports.create = create;
exports.getMarkdown = getMarkdown;
exports.search = search;
exports.updateMarkdown = updateMarkdown;
const utils_1 = require("../../helpers/utils");
const transport_1 = require("../../transport");
const common_descriptions_1 = require("../common.descriptions");
exports.description = [
    {
        displayName: 'Operation',
        name: 'operation',
        type: 'options',
        noDataExpression: true,
        displayOptions: { show: { resource: ['page'] } },
        options: [
            {
                name: 'Archive',
                value: 'archive',
                description: 'Archive a page',
                action: 'Archive a page',
            },
            { name: 'Create', value: 'create', description: 'Create a page', action: 'Create a page' },
            {
                name: 'Get Markdown',
                value: 'getMarkdown',
                description: 'Get page markdown',
                action: 'Get page markdown',
            },
            { name: 'Search', value: 'search', description: 'Search pages', action: 'Search a page' },
            {
                name: 'Update Markdown',
                value: 'updateMarkdown',
                description: 'Update page markdown',
                action: 'Update page markdown',
            },
        ],
        default: 'create',
    },
    {
        ...common_descriptions_1.pageLocator,
        displayName: 'Parent Page',
        displayOptions: { show: { resource: ['page'], operation: ['create'] } },
    },
    {
        ...common_descriptions_1.pageLocator,
        displayOptions: {
            show: { resource: ['page'], operation: ['archive', 'getMarkdown', 'updateMarkdown'] },
        },
    },
    {
        displayName: 'Title',
        name: 'title',
        type: 'string',
        default: '',
        required: true,
        displayOptions: { show: { resource: ['page'], operation: ['create'] } },
    },
    {
        displayName: 'Content Type',
        name: 'contentType',
        type: 'options',
        options: [
            { name: 'Block Builder', value: 'blockUi' },
            { name: 'JSON Blocks', value: 'json' },
            { name: 'Markdown', value: 'markdown' },
        ],
        default: 'blockUi',
        displayOptions: { show: { resource: ['page'], operation: ['create'] } },
    },
    {
        displayName: 'Blocks (JSON)',
        name: 'blocksJson',
        type: 'string',
        typeOptions: { rows: 8 },
        default: '',
        displayOptions: { show: { resource: ['page'], operation: ['create'], contentType: ['json'] } },
    },
    (0, common_descriptions_1.blockBuilder)('page', 'create', { contentType: ['blockUi'] }),
    {
        displayName: 'Markdown',
        name: 'markdown',
        type: 'string',
        typeOptions: { rows: 8 },
        default: '',
        displayOptions: {
            show: { resource: ['page'], operation: ['create'], contentType: ['markdown'] },
        },
    },
    (0, common_descriptions_1.iconOptions)('page', ['create']),
    {
        displayName: 'Include Transcript',
        name: 'includeTranscript',
        type: 'boolean',
        default: false,
        displayOptions: { show: { resource: ['page'], operation: ['getMarkdown'] } },
    },
    {
        displayName: 'Update Type',
        name: 'markdownUpdateType',
        type: 'options',
        options: [
            { name: 'Replace Content', value: 'replace_content' },
            { name: 'Update Content', value: 'update_content' },
        ],
        default: 'replace_content',
        displayOptions: { show: { resource: ['page'], operation: ['updateMarkdown'] } },
    },
    {
        displayName: 'Markdown',
        name: 'markdown',
        type: 'string',
        typeOptions: { rows: 8 },
        default: '',
        displayOptions: {
            show: {
                resource: ['page'],
                operation: ['updateMarkdown'],
                markdownUpdateType: ['replace_content'],
            },
        },
    },
    {
        displayName: 'Content Updates',
        name: 'contentUpdates',
        type: 'fixedCollection',
        typeOptions: {
            multipleValues: true,
        },
        default: {},
        placeholder: 'Add Update',
        displayOptions: {
            show: {
                resource: ['page'],
                operation: ['updateMarkdown'],
                markdownUpdateType: ['update_content'],
            },
        },
        options: [
            {
                name: 'updates',
                displayName: 'Update',
                values: [
                    {
                        displayName: 'Old String',
                        name: 'oldString',
                        type: 'string',
                        default: '',
                        description: 'Existing markdown content to find',
                    },
                    {
                        displayName: 'New String',
                        name: 'newString',
                        type: 'string',
                        default: '',
                        description: 'Replacement markdown content',
                    },
                    {
                        displayName: 'Replace All Matches',
                        name: 'replaceAllMatches',
                        type: 'boolean',
                        default: false,
                        description: 'Whether to replace all matches when old string appears multiple times',
                    },
                ],
            },
        ],
    },
    {
        displayName: 'Search Text',
        name: 'text',
        type: 'string',
        default: '',
        displayOptions: { show: { resource: ['page'], operation: ['search'] } },
    },
    ...(0, common_descriptions_1.returnAllOrLimit)('page', 'search'),
    (0, common_descriptions_1.searchOptions)('page', 'search'),
    {
        displayName: 'Simplify',
        name: 'simple',
        type: 'boolean',
        default: true,
        displayOptions: { show: { resource: ['page'], operation: ['archive', 'create', 'search'] } },
        description: 'Whether to return a simplified version of the response instead of the raw data',
    },
];
async function archive(items) {
    const returnData = [];
    for (let i = 0; i < items.length; i++) {
        try {
            let response = await transport_1.notionApiRequestV3.call(this, 'PATCH', `/pages/${utils_1.getPageId.call(this, i)}`, { in_trash: true });
            if (this.getNodeParameter('simple', i))
                response = (0, utils_1.simplifyObjects)(response, false, 3);
            const executionData = this.helpers.constructExecutionMetaData(this.helpers.returnJsonArray(response), { itemData: { item: i } });
            returnData.push.apply(returnData, executionData);
        }
        catch (error) {
            utils_1.handleOperationError.call(this, returnData, error, i);
        }
    }
    return returnData;
}
async function create(items) {
    const returnData = [];
    for (let i = 0; i < items.length; i++) {
        try {
            const body = {
                parent: { page_id: utils_1.getPageId.call(this, i) },
                properties: (0, utils_1.formatTitle)(this.getNodeParameter('title', i)),
                ...utils_1.getPageCreateContent.call(this, i),
            };
            const icon = utils_1.getIconFromOptions.call(this, i);
            if (icon)
                body.icon = icon;
            let response = await transport_1.notionApiRequestV3.call(this, 'POST', '/pages', body);
            if (this.getNodeParameter('simple', i))
                response = (0, utils_1.simplifyObjects)(response, false, 3);
            const executionData = this.helpers.constructExecutionMetaData(this.helpers.returnJsonArray(response), { itemData: { item: i } });
            returnData.push.apply(returnData, executionData);
        }
        catch (error) {
            utils_1.handleOperationError.call(this, returnData, error, i);
        }
    }
    return returnData;
}
async function getMarkdown(items) {
    const returnData = [];
    for (let i = 0; i < items.length; i++) {
        try {
            const includeTranscript = this.getNodeParameter('includeTranscript', i);
            const response = await transport_1.notionApiRequestV3.call(this, 'GET', `/pages/${utils_1.getPageId.call(this, i)}/markdown`, {}, includeTranscript ? { include_transcript: true } : {});
            const executionData = this.helpers.constructExecutionMetaData(this.helpers.returnJsonArray(response), { itemData: { item: i } });
            returnData.push.apply(returnData, executionData);
        }
        catch (error) {
            utils_1.handleOperationError.call(this, returnData, error, i);
        }
    }
    return returnData;
}
async function search(items) {
    const returnData = [];
    for (let i = 0; i < items.length; i++) {
        try {
            const body = { filter: { property: 'object', value: 'page' } };
            const text = this.getNodeParameter('text', i);
            if (text)
                body.query = text;
            const sort = utils_1.getSearchSort.call(this, i);
            if (sort)
                body.sort = sort;
            const returnAll = this.getNodeParameter('returnAll', i);
            const limit = returnAll ? undefined : this.getNodeParameter('limit', i);
            if (limit)
                body.page_size = Math.min(limit, 100);
            let response = await transport_1.notionApiRequestAllItemsV3.call(this, 'results', 'POST', '/search', body, limit ? { limit } : {});
            if (this.getNodeParameter('simple', i))
                response = (0, utils_1.simplifyObjects)(response, false, 3);
            const executionData = this.helpers.constructExecutionMetaData(this.helpers.returnJsonArray(response), { itemData: { item: i } });
            returnData.push.apply(returnData, executionData);
        }
        catch (error) {
            utils_1.handleOperationError.call(this, returnData, error, i);
        }
    }
    return returnData;
}
async function updateMarkdown(items) {
    const returnData = [];
    for (let i = 0; i < items.length; i++) {
        try {
            const response = await transport_1.notionApiRequestV3.call(this, 'PATCH', `/pages/${utils_1.getPageId.call(this, i)}/markdown`, utils_1.getMarkdownUpdateBody.call(this, i));
            const executionData = this.helpers.constructExecutionMetaData(this.helpers.returnJsonArray(response), { itemData: { item: i } });
            returnData.push.apply(returnData, executionData);
        }
        catch (error) {
            utils_1.handleOperationError.call(this, returnData, error, i);
        }
    }
    return returnData;
}
//# sourceMappingURL=Page.resource.js.map