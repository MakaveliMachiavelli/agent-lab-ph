"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.jsonParse = exports.validateJSON = exports.simplifyObjects = exports.simplifyBlocksOutput = exports.prepareNotionError = exports.mapSorting = exports.mapProperties = exports.formatTitle = exports.extractDatabaseMentionRLC = void 0;
exports.splitPropertyKey = splitPropertyKey;
exports.normalizePropertyValues = normalizePropertyValues;
exports.normalizeBlockValues = normalizeBlockValues;
exports.formatBlocks = formatBlocks;
exports.parseJsonParameter = parseJsonParameter;
exports.getPageId = getPageId;
exports.extractBlockId = extractBlockId;
exports.getJsonBlocks = getJsonBlocks;
exports.getPageCreateContent = getPageCreateContent;
exports.getIconFromOptions = getIconFromOptions;
exports.getSearchSort = getSearchSort;
exports.getMarkdownUpdateBody = getMarkdownUpdateBody;
exports.flattenDataSources = flattenDataSources;
exports.handleOperationError = handleOperationError;
const n8n_workflow_1 = require("n8n-workflow");
Object.defineProperty(exports, "jsonParse", { enumerable: true, get: function () { return n8n_workflow_1.jsonParse; } });
const constants_1 = require("../../shared/constants");
const GenericFunctions_1 = require("../../shared/GenericFunctions");
Object.defineProperty(exports, "extractDatabaseMentionRLC", { enumerable: true, get: function () { return GenericFunctions_1.extractDatabaseMentionRLC; } });
Object.defineProperty(exports, "formatTitle", { enumerable: true, get: function () { return GenericFunctions_1.formatTitle; } });
Object.defineProperty(exports, "mapProperties", { enumerable: true, get: function () { return GenericFunctions_1.mapProperties; } });
Object.defineProperty(exports, "mapSorting", { enumerable: true, get: function () { return GenericFunctions_1.mapSorting; } });
Object.defineProperty(exports, "prepareNotionError", { enumerable: true, get: function () { return GenericFunctions_1.prepareNotionError; } });
Object.defineProperty(exports, "simplifyBlocksOutput", { enumerable: true, get: function () { return GenericFunctions_1.simplifyBlocksOutput; } });
Object.defineProperty(exports, "simplifyObjects", { enumerable: true, get: function () { return GenericFunctions_1.simplifyObjects; } });
Object.defineProperty(exports, "validateJSON", { enumerable: true, get: function () { return GenericFunctions_1.validateJSON; } });
const transport_1 = require("../transport");
function splitPropertyKey(key) {
    const delimiterIndex = key.lastIndexOf('|');
    if (delimiterIndex === -1)
        return { name: key, type: '' };
    return {
        name: key.slice(0, delimiterIndex),
        type: key.slice(delimiterIndex + 1),
    };
}
function normalizePropertyValue(value) {
    const normalizedValue = { ...value };
    if (typeof normalizedValue.textContent === 'string') {
        normalizedValue.richText = false;
    }
    const isDateProperty = normalizedValue.type === 'date' ||
        (typeof normalizedValue.key === 'string' && normalizedValue.key.endsWith('|date'));
    if (isDateProperty) {
        normalizedValue.range = normalizedValue.range ?? false;
        normalizedValue.includeTime = normalizedValue.includeTime ?? true;
        normalizedValue.timezone = normalizedValue.timezone ?? 'default';
        if (!normalizedValue.range) {
            delete normalizedValue.dateStart;
            delete normalizedValue.dateEnd;
        }
        else {
            delete normalizedValue.date;
        }
    }
    return normalizedValue;
}
function normalizePropertyValues(values) {
    return values.map(normalizePropertyValue);
}
function normalizeBlockValues(values) {
    return values.map((value) => {
        if (typeof value.textContent !== 'string')
            return value;
        return {
            ...value,
            richText: false,
        };
    });
}
function getRichTextAnnotations(entry) {
    return (0, transport_1.isDataObject)(entry.annotationUi) ? { annotations: entry.annotationUi } : {};
}
function getTextLink(entry) {
    if (entry.isLink === true && typeof entry.textLink === 'string' && entry.textLink !== '') {
        return { link: { url: entry.textLink } };
    }
    return {};
}
function getMentionId(value) {
    if (typeof value === 'string')
        return value;
    if ((0, transport_1.isDataObject)(value) && typeof value.value === 'string')
        return value.value;
    return undefined;
}
function formatMention(entry) {
    const mentionType = entry.mentionType;
    if (typeof mentionType !== 'string')
        return undefined;
    if (mentionType === 'date') {
        return {
            type: 'mention',
            mention: {
                type: 'date',
                date: entry.range === true
                    ? { start: entry.dateStart, end: entry.dateEnd }
                    : { start: entry.date, end: null },
            },
            ...getRichTextAnnotations(entry),
        };
    }
    const mentionId = getMentionId(entry[mentionType]);
    if (!mentionId)
        return undefined;
    return {
        type: 'mention',
        mention: {
            type: mentionType,
            [mentionType]: { id: mentionId },
        },
        ...getRichTextAnnotations(entry),
    };
}
const parseText = (value) => {
    if (typeof value === 'string')
        return value;
    if (typeof value === 'number' || typeof value === 'boolean')
        return String(value);
    throw new Error(`Text value must be a string, number, or boolean. Received: ${typeof value}`);
};
function formatRichText(values) {
    if (!Array.isArray(values))
        return [];
    const results = [];
    for (const value of values) {
        if (!(0, transport_1.isDataObject)(value))
            continue;
        const entry = value;
        if (entry.textType === 'text') {
            results.push({
                type: 'text',
                text: {
                    content: parseText(entry.text),
                    ...getTextLink(entry),
                },
                ...getRichTextAnnotations(entry),
            });
        }
        else if (entry.textType === 'mention') {
            const mention = formatMention(entry);
            if (mention)
                results.push(mention);
        }
        else if (entry.textType === 'equation') {
            results.push({
                type: 'equation',
                equation: {
                    expression: parseText(entry.expression),
                },
                ...getRichTextAnnotations(entry),
            });
        }
    }
    return results;
}
function formatBlocks(blockValues) {
    const results = [];
    for (const block of blockValues) {
        const blockType = block.type;
        if (typeof blockType !== 'string')
            continue;
        const blockBody = {};
        const prepareText = () => {
            // rich text uses `text` parameter
            if (block.richText === true && (0, transport_1.isDataObject)(block.text)) {
                blockBody.rich_text = formatRichText(block.text.text);
            }
            else {
                // regular text uses `textContent`
                const textContent = parseText(block.textContent);
                blockBody.rich_text = (0, GenericFunctions_1.formatText)(textContent).text;
            }
        };
        switch (blockType) {
            case 'to_do':
                blockBody.checked = block.checked;
                prepareText();
                break;
            case 'child_page':
                blockBody.title = block.title;
                break;
            case 'image':
                blockBody.type = 'external';
                blockBody.external = { url: block.url };
                break;
            // other block types such as paragraph, heading, etc. don't need special handling
            default:
                prepareText();
                break;
        }
        results.push({
            object: 'block',
            type: blockType,
            [blockType]: blockBody,
        });
    }
    return results;
}
function parseJsonParameter(value, itemIndex) {
    try {
        return JSON.parse(value);
    }
    catch {
        throw new n8n_workflow_1.NodeOperationError(this.getNode(), 'JSON value must be valid JSON', { itemIndex });
    }
}
function getUrlSearchParam(value, parameterName) {
    try {
        return new URL(value).searchParams.get(parameterName) ?? '';
    }
    catch {
        return '';
    }
}
function getUrlHash(value) {
    try {
        return new URL(value).hash.slice(1);
    }
    catch {
        return '';
    }
}
function getPageId(itemIndex) {
    const page = this.getNodeParameter('pageId', itemIndex, {});
    let pageId = '';
    if (page.value && typeof page.value === 'string') {
        if (page.mode === 'id') {
            pageId = page.value;
        }
        else if (page.value.includes('p=')) {
            pageId = getUrlSearchParam(page.value, 'p');
        }
        else {
            pageId = page.value.match(constants_1.databasePageUrlExtractionRegexp)?.[1] ?? '';
        }
    }
    if (!pageId) {
        throw new n8n_workflow_1.NodeOperationError(this.getNode(), `Could not extract page ID from URL: ${page.value}`);
    }
    return pageId;
}
function extractBlockId(itemIndex) {
    const blockIdRLCData = this.getNodeParameter('blockId', itemIndex, {});
    if (blockIdRLCData.mode === 'id') {
        return blockIdRLCData.value;
    }
    const blockUrl = blockIdRLCData.value;
    const hashId = getUrlHash(blockUrl);
    if (hashId) {
        return (0, GenericFunctions_1.extractPageId)(hashId);
    }
    const pageRegex = new RegExp(constants_1.blockUrlExtractionRegexp);
    const pageMatch = blockUrl.match(pageRegex);
    if (pageMatch !== null) {
        return (0, GenericFunctions_1.extractPageId)(pageMatch[1]);
    }
    throw new n8n_workflow_1.NodeOperationError(this.getNode(), 'Invalid URL, could not find block ID or page ID', {
        itemIndex,
    });
}
function getJsonBlocks(itemIndex) {
    const blocksJson = this.getNodeParameter('blocksJson', itemIndex, '');
    if (!blocksJson)
        return [];
    const parsed = parseJsonParameter.call(this, blocksJson, itemIndex);
    if (!Array.isArray(parsed)) {
        throw new n8n_workflow_1.NodeOperationError(this.getNode(), 'Blocks (JSON) must be an array of Notion block objects', { itemIndex });
    }
    return parsed.filter(transport_1.isDataObject);
}
function getPageCreateContent(itemIndex) {
    const contentType = this.getNodeParameter('contentType', itemIndex, 'blockUi');
    if (contentType === 'json') {
        return { children: getJsonBlocks.call(this, itemIndex) };
    }
    if (contentType === 'markdown') {
        return { markdown: this.getNodeParameter('markdown', itemIndex, '') };
    }
    const blockValues = normalizeBlockValues(this.getNodeParameter('blockUi.blockValues', itemIndex, []));
    (0, GenericFunctions_1.extractDatabaseMentionRLC)(blockValues);
    return { children: formatBlocks(blockValues) };
}
function getIconFromOptions(itemIndex) {
    const options = this.getNodeParameter('options', itemIndex, {});
    const icon = options.icon;
    if (typeof icon !== 'string' || icon === '')
        return undefined;
    let isUrl = false;
    try {
        const url = new URL(icon);
        isUrl = url.protocol === 'http:' || url.protocol === 'https:';
    }
    catch {
        isUrl = false;
    }
    if (isUrl) {
        return { type: 'external', external: { url: icon } };
    }
    return { type: 'emoji', emoji: icon };
}
function getSearchSort(itemIndex) {
    const sort = this.getNodeParameter('options.sort.sortValue', itemIndex, {});
    const sortValue = Array.isArray(sort) ? sort[0] : sort;
    if (!(0, transport_1.isDataObject)(sortValue))
        return undefined;
    const { direction, timestamp } = sortValue;
    if (typeof direction !== 'string' || typeof timestamp !== 'string')
        return undefined;
    return { direction, timestamp };
}
function getMarkdownUpdateBody(itemIndex) {
    const type = this.getNodeParameter('markdownUpdateType', itemIndex);
    if (type === 'replace_content') {
        return {
            type,
            replace_content: {
                new_str: this.getNodeParameter('markdown', itemIndex),
            },
        };
    }
    const updates = (this.getNodeParameter('contentUpdates.updates', itemIndex, []) ??
        []).filter(transport_1.isDataObject);
    return {
        type,
        update_content: {
            content_updates: updates.map((update) => ({
                old_str: update.oldString,
                new_str: update.newString,
                ...(update.replaceAllMatches ? { replace_all_matches: true } : {}),
            })),
        },
    };
}
function flattenDataSources(searchResults) {
    const dataSources = [];
    for (const result of searchResults) {
        if (result.object === 'data_source') {
            dataSources.push(result);
            continue;
        }
        if (Array.isArray(result.data_sources)) {
            for (const dataSource of result.data_sources) {
                if ((0, transport_1.isDataObject)(dataSource)) {
                    dataSources.push({
                        ...dataSource,
                        database_id: result.id,
                        database_url: result.url,
                    });
                }
            }
        }
    }
    return dataSources;
}
function handleOperationError(returnData, error, itemIndex) {
    const normalizedError = error instanceof Error ? error : new Error(String(error));
    const preparedError = (0, GenericFunctions_1.prepareNotionError)(this.getNode(), normalizedError, itemIndex);
    if (this.continueOnFail()) {
        returnData.push({
            json: { error: preparedError.message },
            pairedItem: { item: itemIndex },
        });
        return;
    }
    throw preparedError;
}
//# sourceMappingURL=utils.js.map