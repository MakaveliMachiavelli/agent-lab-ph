"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createKafkaProducer = createKafkaProducer;
const client_1 = require("./client");
async function createKafkaProducer(credentials, options) {
    const kafka = await (0, client_1.createKafkaClient)(credentials);
    // acks and timeout are locked in at construction: the library's KafkaJS
    // compatibility layer ignores them when passed to sendBatch.
    return kafka.producer({
        kafkaJS: {
            acks: options.acks,
            timeout: options.timeout,
            allowAutoTopicCreation: true,
            ...(options.compression && { compression: options.compression }),
        },
    });
}
//# sourceMappingURL=producer.js.map