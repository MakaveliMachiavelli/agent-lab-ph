"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.description = void 0;
exports.execute = execute;
const descriptions_1 = require("../../../../../../utils/descriptions");
const utilities_1 = require("../../../../../../utils/utilities");
const descriptions_2 = require("../../descriptions");
const transport_1 = require("../../transport");
const helpers_1 = require("./helpers");
const properties = [
    {
        displayName: 'Tasks For',
        name: 'tasksFor',
        default: 'member',
        required: true,
        type: 'options',
        description: 'Whether to retrieve the tasks for a user or for a plan',
        options: [
            {
                name: 'Group Member',
                value: 'member',
                description: 'Tasks assigned to group member',
            },
            {
                name: 'Plan',
                value: 'plan',
                description: 'Tasks in group plan',
            },
        ],
        // App-only Graph has no `/me`, so "Group Member" mode (which reads /v1.0/me)
        // has no app-only form — hidden under the Service Principal credential, which
        // is forced to plan mode in execute.
        displayOptions: {
            hide: {
                ...transport_1.SP_HIDE,
            },
        },
    },
    {
        // The group picker (member mode, and the dependency for the OAuth2 plan picker)
        // is part of the member-mode flow that has no app-only form — hidden under the
        // Service Principal credential, which is forced to plan mode in execute.
        ...descriptions_2.groupRLC,
        displayOptions: {
            hide: {
                ...transport_1.SP_HIDE,
            },
        },
    },
    {
        ...descriptions_2.planRLC,
        displayOptions: {
            show: {
                tasksFor: ['plan'],
            },
            hide: {
                ...transport_1.SP_HIDE,
            },
        },
    },
    // Plan picker shown under the Service Principal credential (which has no `tasksFor`
    // selector). By-ID — plan mode is forced in execute and the value routes through
    // `buildTeamsPath`. (See `byIdUnderSp` for why list mode is dropped under SP.)
    (0, helpers_1.byIdUnderSp)(descriptions_2.planRLC),
    ...descriptions_1.returnAllOrLimit,
];
const displayOptions = {
    show: {
        resource: ['task'],
        operation: ['getAll'],
    },
};
exports.description = (0, utilities_1.updateDisplayOptions)(displayOptions, properties);
async function execute(i) {
    // App-only Graph has no `/me`, so "Group Member" mode is unavailable; force plan
    // mode (and never read the hidden `tasksFor`) under the Service Principal credential.
    const isServicePrincipal = transport_1.getTeamsCredentialType.call(this) === transport_1.SERVICE_PRINCIPAL_AUTH;
    const tasksFor = isServicePrincipal ? 'plan' : this.getNodeParameter('tasksFor', i);
    const returnAll = this.getNodeParameter('returnAll', i);
    if (tasksFor === 'member') {
        //https://docs.microsoft.com/en-us/graph/api/planneruser-list-tasks?view=graph-rest-1.0&tabs=http
        const memberId = (await transport_1.microsoftApiRequest.call(this, 'GET', '/v1.0/me'))
            .id;
        if (returnAll) {
            return await transport_1.microsoftApiRequestAllItems.call(this, 'value', 'GET', `/v1.0/users/${memberId}/planner/tasks`);
        }
        else {
            const limit = this.getNodeParameter('limit', i);
            // Planner endpoints don't support OData query params like $top;
            // the limit still stops pagination early
            return await transport_1.microsoftApiRequestAllItems.call(this, 'value', 'GET', `/v1.0/users/${memberId}/planner/tasks`, {}, {}, limit);
        }
    }
    else {
        //https://docs.microsoft.com/en-us/graph/api/plannerplan-list-tasks?view=graph-rest-1.0&tabs=http
        const planId = this.getNodeParameter('planId', i, '', { extractValue: true });
        const endpoint = transport_1.buildTeamsPath.call(this, ['/v1.0/planner/plans/', { id: planId }, '/tasks']);
        if (returnAll) {
            return await transport_1.microsoftApiRequestAllItems.call(this, 'value', 'GET', endpoint);
        }
        else {
            const limit = this.getNodeParameter('limit', i);
            return await transport_1.microsoftApiRequestAllItems.call(this, 'value', 'GET', endpoint, {}, {}, limit);
        }
    }
}
//# sourceMappingURL=getAll.operation.js.map