"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.description = void 0;
exports.execute = execute;
const luxon_1 = require("luxon");
const utilities_1 = require("../../../../../../utils/utilities");
const descriptions_1 = require("../../descriptions");
const transport_1 = require("../../transport");
const helpers_1 = require("./helpers");
const properties = [
    {
        displayName: 'Task ID',
        name: 'taskId',
        required: true,
        type: 'string',
        default: '',
        placeholder: 'e.g. h3ufgLvXPkSRzYm-zO5cY5gANtBQ',
        description: 'The ID of the task to update',
    },
    {
        displayName: 'Update Fields',
        name: 'updateFields',
        type: 'collection',
        default: {},
        placeholder: 'Add Field',
        options: [
            // OAuth2 assignee picker (list, scoped by the group) — hidden under SP, which
            // uses the By-ID copy below (the group picker it depends on is hidden under SP).
            {
                ...descriptions_1.memberRLC,
                displayName: 'Assigned To',
                name: 'assignedTo',
                description: 'Who the task should be assigned to',
                hint: "Select 'Team' from options first",
                required: false,
                typeOptions: {
                    loadOptionsDependsOn: ['updateFields.groupId.value'],
                },
                displayOptions: { hide: { ...transport_1.SP_HIDE } },
            },
            // SP assignee picker: By-ID (Planner assignment is app-only-capable with a user ID).
            (0, helpers_1.byIdUnderSp)(descriptions_1.memberRLC, {
                displayName: 'Assigned To',
                name: 'assignedTo',
                description: 'Who the task should be assigned to',
                required: false,
            }),
            // OAuth2 bucket picker (list, depends on plan) — hidden under SP.
            {
                ...descriptions_1.bucketRLC,
                required: false,
                typeOptions: {
                    loadOptionsDependsOn: ['updateFields.planId.value'],
                },
                displayOptions: { hide: { ...transport_1.SP_HIDE } },
            },
            // SP bucket picker: By-ID.
            (0, helpers_1.byIdUnderSp)(descriptions_1.bucketRLC, { required: false }),
            {
                displayName: 'Due Date Time',
                name: 'dueDateTime',
                type: 'string',
                validateType: 'dateTime',
                default: '',
                description: 'Date and time at which the task is due. The Timestamp type represents date and time information using ISO 8601 format and is always in UTC time.',
            },
            // Group picker is OAuth2-only (used to scope the plan/bucket lists); hidden
            // under SP, which uses By-ID plan/bucket.
            {
                ...descriptions_1.groupRLC,
                required: false,
                typeOptions: {
                    loadOptionsDependsOn: ['/groupSource'],
                },
                displayOptions: { hide: { ...transport_1.SP_HIDE } },
            },
            {
                displayName: 'Percent Complete',
                name: 'percentComplete',
                type: 'number',
                typeOptions: {
                    minValue: 0,
                    maxValue: 100,
                },
                default: 0,
                placeholder: 'e.g. 75',
                description: 'Percentage of task completion. When set to 100, the task is considered completed.',
            },
            // OAuth2 plan picker (list, depends on group) — hidden under SP.
            {
                ...descriptions_1.planRLC,
                required: false,
                hint: "Select 'Team' from options first",
                typeOptions: {
                    loadOptionsDependsOn: ['updateFields.groupId.value'],
                },
                displayOptions: { hide: { ...transport_1.SP_HIDE } },
            },
            // SP plan picker: By-ID.
            (0, helpers_1.byIdUnderSp)(descriptions_1.planRLC, { required: false }),
            {
                displayName: 'Title',
                name: 'title',
                type: 'string',
                default: '',
                placeholder: 'e.g. my task',
                description: 'Title of the task',
            },
        ],
    },
];
const displayOptions = {
    show: {
        resource: ['task'],
        operation: ['update'],
    },
};
exports.description = (0, utilities_1.updateDisplayOptions)(displayOptions, properties);
async function execute(i) {
    //https://docs.microsoft.com/en-us/graph/api/plannertask-update?view=graph-rest-1.0&tabs=http
    const taskId = this.getNodeParameter('taskId', i, '', { extractValue: true });
    const updateFields = this.getNodeParameter('updateFields', i);
    for (const key of Object.keys(updateFields)) {
        if (key === 'groupId') {
            // tasks are assigned to a plan and bucket, group is used for filtering
            delete updateFields.groupId;
            continue;
        }
        if (key === 'assignedTo') {
            const assignedTo = this.getNodeParameter('updateFields.assignedTo', i, '', {
                extractValue: true,
            });
            updateFields.assignments = {
                [assignedTo]: {
                    '@odata.type': 'microsoft.graph.plannerAssignment',
                    orderHint: ' !',
                },
            };
            delete updateFields.assignedTo;
            continue;
        }
        if (['bucketId', 'planId'].includes(key)) {
            updateFields[key] = this.getNodeParameter(`updateFields.${key}`, i, '', {
                extractValue: true,
            });
        }
        if (key === 'dueDateTime' && updateFields.dueDateTime instanceof luxon_1.DateTime) {
            updateFields.dueDateTime = updateFields.dueDateTime.toISO();
        }
    }
    // `planId`/`bucketId` are written into the PATCH body; shape-validate them under SP
    // (defense-in-depth, same as task:create).
    transport_1.validateTaskBodyIdsUnderSp.call(this, {
        planId: updateFields.planId,
        bucketId: updateFields.bucketId,
    });
    const body = {};
    Object.assign(body, updateFields);
    const taskPath = transport_1.buildTeamsPath.call(this, ['/v1.0/planner/tasks/', { id: taskId }]);
    const task = await transport_1.microsoftApiRequest.call(this, 'GET', taskPath);
    await transport_1.microsoftApiRequest.call(this, 'PATCH', taskPath, body, {}, undefined, {
        'If-Match': task['@odata.etag'],
    });
    return { success: true };
}
//# sourceMappingURL=update.operation.js.map