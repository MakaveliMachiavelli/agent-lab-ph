"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.description = void 0;
exports.execute = execute;
const descriptions_1 = require("../../../../../../utils/descriptions");
const utilities_1 = require("../../../../../../utils/utilities");
const descriptions_2 = require("../../descriptions");
const transport_1 = require("../../transport");
const sharedGuard_1 = require("./sharedGuard");
const properties = [descriptions_2.chatRLC, ...descriptions_1.returnAllOrLimit];
const displayOptions = {
    show: {
        resource: ['chatMessage'],
        operation: ['getAll'],
    },
    hide: {
        ...transport_1.SP_HIDE,
    },
};
exports.description = (0, utilities_1.updateDisplayOptions)(displayOptions, properties);
async function execute(i) {
    // https://docs.microsoft.com/en-us/graph/api/chat-list-messages?view=graph-rest-1.0&tabs=http
    // App-only Graph cannot read chats; fail before any request.
    sharedGuard_1.throwIfChatUnsupported.call(this);
    const chatId = this.getNodeParameter('chatId', i, '', { extractValue: true });
    const returnAll = this.getNodeParameter('returnAll', i);
    if (returnAll) {
        return await transport_1.microsoftApiRequestAllItems.call(this, 'value', 'GET', transport_1.buildTeamsPath.call(this, ['/v1.0/chats/', { id: chatId }, '/messages']));
    }
    else {
        const limit = this.getNodeParameter('limit', i);
        return await transport_1.microsoftApiRequestAllItems.call(this, 'value', 'GET', transport_1.buildTeamsPath.call(this, ['/v1.0/chats/', { id: chatId }, '/messages']), {}, { $top: limit }, limit);
    }
}
//# sourceMappingURL=getAll.operation.js.map