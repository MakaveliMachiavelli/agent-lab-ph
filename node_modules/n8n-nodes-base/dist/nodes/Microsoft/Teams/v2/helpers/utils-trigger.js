"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateClientState = generateClientState;
exports.fetchAllTeams = fetchAllTeams;
exports.fetchAllChannels = fetchAllChannels;
exports.createSubscription = createSubscription;
exports.verifyWebhook = verifyWebhook;
exports.getResourcePath = getResourcePath;
const crypto_1 = require("crypto");
const n8n_workflow_1 = require("n8n-workflow");
const webhook_signature_verification_1 = require("../../../../../utils/webhook-signature-verification");
const transport_1 = require("../transport");
function generateClientState() {
    return (0, crypto_1.randomBytes)(32).toString('hex');
}
async function fetchAllTeams() {
    const { value: teams } = (await transport_1.microsoftApiRequest.call(this, 'GET', transport_1.joinedTeamsEndpoint.call(this)));
    return teams;
}
async function fetchAllChannels(teamId) {
    // Route through buildTeamsPath so `teamId` is validated before interpolation
    // (watch-all is also guarded in getResourcePath).
    const { value: channels } = (await transport_1.microsoftApiRequest.call(this, 'GET', transport_1.buildTeamsPath.call(this, ['/v1.0/teams/', { id: teamId }, '/channels'])));
    return channels;
}
async function createSubscription(webhookUrl, resourcePath, clientState) {
    const expirationTime = new Date(Date.now() + 4318 * 60 * 1000).toISOString();
    const body = {
        changeType: 'created',
        notificationUrl: webhookUrl,
        resource: resourcePath,
        expirationDateTime: expirationTime,
        latestSupportedTlsVersion: 'v1_2',
        lifecycleNotificationUrl: webhookUrl,
    };
    if (clientState) {
        body.clientState = clientState;
    }
    const response = (await transport_1.microsoftApiRequest.call(this, 'POST', '/v1.0/subscriptions', body));
    return response;
}
function verifyWebhook() {
    const req = this.getRequestObject();
    const webhookData = this.getWorkflowStaticData('node');
    const expectedSecret = webhookData.webhookSecret;
    const body = req.body;
    const notifications = body?.value;
    if (!Array.isArray(notifications) || notifications.length === 0) {
        return false;
    }
    return notifications.every((notification) => {
        const actualClientState = typeof notification === 'object' && notification !== null
            ? notification.clientState
            : undefined;
        return (0, webhook_signature_verification_1.verifySignature)({
            getExpectedSignature: () => typeof expectedSecret === 'string' && expectedSecret.length > 0 ? expectedSecret : null,
            skipIfNoExpectedSignature: true,
            getActualSignature: () => (typeof actualClientState === 'string' ? actualClientState : null),
        });
    });
}
async function getResourcePath(event) {
    // Path-interpolated ids are validated + interpolated via buildTeamsPath under
    // both credential types. A By-URL channel/chat id arrives percent-encoded, so it
    // is decoded first (guarded helper) so validation runs on the decoded value.
    // App-only Graph has no signed-in user: chat events and watch-all are disabled
    // under SP (UI-hidden + the runtime guards below, since fetchAllTeams/
    // fetchAllChannels fan out an org-wide-token request).
    const isServicePrincipal = transport_1.getTeamsCredentialType.call(this) === transport_1.SERVICE_PRINCIPAL_AUTH;
    switch (event) {
        case 'newChat': {
            if (isServicePrincipal)
                throwChatTriggerUnsupported.call(this);
            return '/me/chats';
        }
        case 'newChatMessage': {
            if (isServicePrincipal)
                throwChatTriggerUnsupported.call(this);
            const watchAllChats = this.getNodeParameter('watchAllChats', false);
            if (watchAllChats) {
                return '/me/chats/getAllMessages';
            }
            else {
                const chatId = this.getNodeParameter('chatId', undefined, { extractValue: true });
                return transport_1.buildTeamsPath.call(this, [
                    '/chats/',
                    { id: decodeSelectedId.call(this, chatId) },
                    '/messages',
                ]);
            }
        }
        case 'newChannel': {
            // No `extractValue` for booleans: it triggers a property-descriptor lookup that
            // throws "Could not find property" when the toggle is hidden under SP. The 2-arg
            // form returns the `false` fallback cleanly when hidden.
            const watchAllTeams = this.getNodeParameter('watchAllTeams', false);
            if (isServicePrincipal && watchAllTeams)
                throwWatchAllUnsupported.call(this);
            if (watchAllTeams) {
                const teams = await fetchAllTeams.call(this);
                return teams.map((team) => `/teams/${team.id}/channels`);
            }
            else {
                const teamId = this.getNodeParameter('teamId', undefined, { extractValue: true });
                return transport_1.buildTeamsPath.call(this, ['/teams/', { id: teamId }, '/channels']);
            }
        }
        case 'newChannelMessage': {
            // No `extractValue` for booleans: it triggers a property-descriptor lookup that
            // throws "Could not find property" when the toggle is hidden under SP. The 2-arg
            // form returns the `false` fallback cleanly when hidden.
            const watchAllTeams = this.getNodeParameter('watchAllTeams', false);
            if (isServicePrincipal && watchAllTeams)
                throwWatchAllUnsupported.call(this);
            if (watchAllTeams) {
                const teams = await fetchAllTeams.call(this);
                const teamChannels = await Promise.all(teams.map(async (team) => {
                    const channels = await fetchAllChannels.call(this, team.id);
                    return channels.map((channel) => `/teams/${team.id}/channels/${channel.id}/messages`);
                }));
                return teamChannels.flat();
            }
            else {
                const teamId = this.getNodeParameter('teamId', undefined, { extractValue: true });
                const watchAllChannels = this.getNodeParameter('watchAllChannels', false);
                if (isServicePrincipal && watchAllChannels)
                    throwWatchAllUnsupported.call(this);
                if (watchAllChannels) {
                    const channels = await fetchAllChannels.call(this, teamId);
                    return channels.map((channel) => `/teams/${teamId}/channels/${channel.id}/messages`);
                }
                else {
                    const channelId = this.getNodeParameter('channelId', undefined, {
                        extractValue: true,
                    });
                    return transport_1.buildTeamsPath.call(this, [
                        '/teams/',
                        { id: teamId },
                        '/channels/',
                        { id: decodeSelectedId.call(this, channelId) },
                        '/messages',
                    ]);
                }
            }
        }
        case 'newTeamMember': {
            // No `extractValue` for booleans: it triggers a property-descriptor lookup that
            // throws "Could not find property" when the toggle is hidden under SP. The 2-arg
            // form returns the `false` fallback cleanly when hidden.
            const watchAllTeams = this.getNodeParameter('watchAllTeams', false);
            if (isServicePrincipal && watchAllTeams)
                throwWatchAllUnsupported.call(this);
            if (watchAllTeams) {
                const teams = await fetchAllTeams.call(this);
                return teams.map((team) => `/teams/${team.id}/members`);
            }
            else {
                const teamId = this.getNodeParameter('teamId', undefined, { extractValue: true });
                return transport_1.buildTeamsPath.call(this, ['/teams/', { id: teamId }, '/members']);
            }
        }
        default: {
            throw new n8n_workflow_1.NodeOperationError(this.getNode(), {
                message: `Invalid event: ${event}`,
                description: `The selected event "${event}" is not recognized.`,
            });
        }
    }
}
/**
 * Chat triggers subscribe to `/me/chats[...]`, which has no app-only equivalent.
 * Throws a static `NodeOperationError` before composing any `/me` path under the
 * Service Principal credential.
 */
function throwChatTriggerUnsupported() {
    throw new n8n_workflow_1.NodeOperationError(this.getNode(), 'Chat triggers are not available with the Service Principal credential', {
        description: 'App-only Microsoft Graph cannot subscribe to the chats of a signed-in user. Use an OAuth2 credential for chat triggers.',
    });
}
/**
 * Watch-all fans out one subscription per team/channel via fetchAllTeams/
 * fetchAllChannels under the org-wide app token, which the plan disables under SP
 * (option b). The UI hides the toggles; this guard backs that at runtime for
 * hand-edited workflows, throwing a static error before any fan-out request.
 */
function throwWatchAllUnsupported() {
    throw new n8n_workflow_1.NodeOperationError(this.getNode(), 'Watching all teams/channels is not available with the Service Principal credential', {
        description: 'Select a specific team and channel. App-only fan-out across all teams/channels is disabled for the Service Principal credential.',
    });
}
/**
 * Decodes a By-URL id (percent-encoded, e.g. `19%3A...%40thread.tacv2`) before it is
 * validated and interpolated. A malformed `%` sequence makes `decodeURIComponent`
 * throw a raw `URIError`; convert it to a static `NodeOperationError`.
 */
function decodeSelectedId(id) {
    try {
        // A missing selection arrives as a non-string — keep it empty (not the
        // string "undefined") so validation raises the required-ID error
        return decodeURIComponent(String(id ?? ''));
    }
    catch {
        throw new n8n_workflow_1.NodeOperationError(this.getNode(), 'The ID is not valid', {
            description: 'The ID could not be decoded. Re-select it from the list or by URL.',
        });
    }
}
//# sourceMappingURL=utils-trigger.js.map