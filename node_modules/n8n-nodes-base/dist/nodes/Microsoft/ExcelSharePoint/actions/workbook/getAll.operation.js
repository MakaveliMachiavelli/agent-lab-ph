"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.description = void 0;
exports.execute = execute;
const utilities_1 = require("../../../../../utils/utilities");
const common_descriptions_1 = require("../../descriptions/common.descriptions");
const utils_1 = require("../../helpers/utils");
const workbookSearch_1 = require("../../helpers/workbookSearch");
const properties = [
    common_descriptions_1.siteRLC,
    common_descriptions_1.libraryRLC,
    {
        displayName: 'Filter',
        name: 'filter',
        type: 'string',
        default: '',
        placeholder: 'e.g. budget',
        description: 'Text to search the library for. Leave empty to list every workbook.',
    },
    ...common_descriptions_1.returnAllAndLimit,
    {
        displayName: 'Options',
        name: 'options',
        type: 'collection',
        placeholder: 'Add option',
        default: {},
        options: [
            {
                displayName: 'Fields',
                name: 'fields',
                type: 'string',
                default: '',
                description: 'Fields to include in the response. Separate multiple fields with a comma.',
            },
        ],
    },
];
const displayOptions = {
    show: {
        resource: ['workbook'],
        operation: ['getAll'],
    },
};
exports.description = (0, utilities_1.updateDisplayOptions)(displayOptions, properties);
async function execute(items) {
    const siteIdCache = new Map();
    return await utils_1.runPerItem.call(this, items, async (i) => {
        const filterText = this.getNodeParameter('filter', i, '');
        const options = this.getNodeParameter('options', i, {});
        const qs = {};
        if (options.fields) {
            // name and file must survive a narrowed $select — the workbook trim reads them
            qs.$select = [
                ...new Set([...options.fields.split(',').map((field) => field.trim()), 'name', 'file']),
            ].join(',');
        }
        const siteId = await utils_1.resolveSiteId.call(this, i, siteIdCache);
        const driveId = (0, utils_1.validatePathSegment)(this.getNode(), 'Library', String(this.getNodeParameter('library', i).value ?? ''));
        const endpoint = (0, workbookSearch_1.workbookSearchEndpoint)(siteId, driveId, filterText);
        // Filter per page inside fetchCollection so a limited listing keeps paging
        // until it has `limit` workbooks, not just whatever the first page holds
        return await (utils_1.fetchCollection).call(this, i, endpoint, qs, (page) => page.filter(workbookSearch_1.isWorkbookFile));
    });
}
//# sourceMappingURL=getAll.operation.js.map