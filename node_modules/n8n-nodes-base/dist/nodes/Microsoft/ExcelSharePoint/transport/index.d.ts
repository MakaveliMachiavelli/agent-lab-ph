import type { IDataObject, IHttpRequestMethods } from 'n8n-workflow';
import { type ExcelSharePointCredentialType } from '../helpers/constants';
import type { AuthContext } from '../helpers/interfaces';
export declare function getExcelSharePointCredentialType(this: AuthContext): ExcelSharePointCredentialType;
export declare function microsoftApiRequest<T extends IDataObject = IDataObject>(this: AuthContext, method: IHttpRequestMethods, resource: string, body?: IDataObject, qs?: IDataObject, uri?: string, headers?: IDataObject): Promise<T>;
/**
 * Accumulates every page of a Graph collection for "Return All". Only the first
 * request carries `qs`; every following page is fetched from `@odata.nextLink`
 * verbatim, exactly as Graph returned it, never rebuilt with extra params.
 */
export declare function microsoftApiRequestAllItems<T extends IDataObject = IDataObject>(this: AuthContext, endpoint: string, qs?: IDataObject): Promise<T[]>;
//# sourceMappingURL=index.d.ts.map