"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.WORKBOOK_EXTENSIONS = void 0;
exports.isWorkbookFile = isWorkbookFile;
exports.workbookSearchEndpoint = workbookSearchEndpoint;
exports.WORKBOOK_EXTENSIONS = ['.xlsx', '.xlsm'];
function isWorkbookFile(item) {
    const name = String(item.name ?? '').toLowerCase();
    return (item.file !== undefined && exports.WORKBOOK_EXTENSIONS.some((extension) => name.endsWith(extension)));
}
function workbookSearchEndpoint(siteId, driveId, text) {
    const trimmed = text?.trim() ?? '';
    const q = trimmed === '' ? exports.WORKBOOK_EXTENSIONS.join(' OR ') : trimmed;
    return `/v1.0/sites/${encodeURIComponent(siteId)}/drives/${encodeURIComponent(driveId)}/root/search(q='${encodeURIComponent(q.replace(/'/g, "''"))}')`;
}
//# sourceMappingURL=workbookSearch.js.map