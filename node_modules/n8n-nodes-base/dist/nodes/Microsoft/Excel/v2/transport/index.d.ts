import type { IDataObject, IExecuteFunctions, IHttpRequestMethods, ILoadOptionsFunctions, INode } from 'n8n-workflow';
export type ExcelCredentialType = 'microsoftExcelOAuth2Api' | 'microsoftOAuth2Api' | 'microsoftEntraServicePrincipalApi';
/** Configured credential type; defaults to the Excel credential so legacy nodes keep working. */
export declare function getExcelCredentialType(this: IExecuteFunctions | ILoadOptionsFunctions): ExcelCredentialType;
/** Validates a resource-target id before it is encoded into a Graph path; static messages never echo the id. */
export declare function validateResourceTargetId(target: string, id: string, node: INode): void;
/** Builds the `/drive`-free Graph root (`/users/{id}` or `/drives/{id}`); validates the id before encoding. */
export declare function getServicePrincipalResourceRoot(target: string, rawId: string, node: INode): string;
/** Appends `/drive` to a user root; `/drives/{id}` is already a drive and used as-is. */
export declare function driveEndpoint(root: string): string;
/**
 * App-only Graph scope root (`/users/{id}` or `/drives/{id}`), or `undefined` for OAuth2 (`/me`).
 * The target RLC accepts expressions and is resolved per item: execute call sites pass the loop's
 * item index; load-options call sites pass a literal 0 — there the 2nd getNodeParameter arg is the
 * fallback (not an itemIndex), which the `|| 'user'` and `''` fallbacks cover, so an unpersisted
 * target coalesces to the "target ID required" error.
 */
export declare function resolveScopeRoot(this: IExecuteFunctions | ILoadOptionsFunctions, itemIndex: number): string | undefined;
export declare function microsoftApiRequest(this: IExecuteFunctions | ILoadOptionsFunctions, method: IHttpRequestMethods, resource: string, body: any | undefined, qs: IDataObject | undefined, uri: string | undefined, headers: IDataObject | undefined, itemIndex: number): Promise<any>;
export declare function microsoftApiRequestAllItems(this: IExecuteFunctions | ILoadOptionsFunctions, propertyName: string, method: IHttpRequestMethods, endpoint: string, body: any | undefined, query: IDataObject | undefined, itemIndex: number): Promise<any>;
export declare function microsoftApiRequestAllItemsSkip(this: IExecuteFunctions | ILoadOptionsFunctions, propertyName: string, method: IHttpRequestMethods, endpoint: string, body: any | undefined, query: IDataObject | undefined, itemIndex: number): Promise<any>;
//# sourceMappingURL=index.d.ts.map