import type { IHttpRequestMethods, IDataObject, IExecuteFunctions, IExecuteSingleFunctions, ILoadOptionsFunctions, INodeExecutionData, IPollFunctions } from 'n8n-workflow';
export type OutlookCredentialType = 'microsoftOutlookOAuth2Api' | 'microsoftOAuth2Api' | 'microsoftEntraServicePrincipalApi';
/**
 * Resolves which credential type the node is configured to use. Defaults to the
 * node-specific `microsoftOutlookOAuth2Api` so existing workflows (and nodes
 * without the `authentication` selector) keep working unchanged, while allowing
 * the generic `microsoftOAuth2Api` (Graph) credential or the app-only
 * `microsoftEntraServicePrincipalApi` credential to be selected.
 */
export declare function getOutlookCredentialType(this: IExecuteFunctions | IExecuteSingleFunctions | ILoadOptionsFunctions | IPollFunctions): OutlookCredentialType;
/**
 * Resolves the mailbox the request should target. Returns `undefined` for OAuth2
 * (which uses `/me` or the credential's shared mailbox). For the Service Principal
 * credential, app-only Graph has no `/me`, so the node's `mailbox` parameter is
 * read, validated, and returned as the bare (un-encoded) UPN/ID.
 *
 * The mailbox accepts expressions and is resolved per item: execute call sites pass
 * the loop's item index; poll/loadOptions call sites pass a literal 0 — in those
 * contexts `getNodeParameter`'s 2nd arg is a fallback, not an index. Encoding happens
 * once at the URL-build site in `microsoftApiRequest`.
 */
export declare function resolveMailbox(this: IExecuteFunctions | IExecuteSingleFunctions | ILoadOptionsFunctions | IPollFunctions, credentialType: OutlookCredentialType, itemIndex: number): string | undefined;
export declare function microsoftApiRequest(this: IExecuteFunctions | IExecuteSingleFunctions | ILoadOptionsFunctions | IPollFunctions, method: IHttpRequestMethods, resource: string, itemIndex: number, body?: IDataObject, qs?: IDataObject, uri?: string, headers?: IDataObject, option?: IDataObject): Promise<any>;
export declare function microsoftApiRequestAllItems(this: IExecuteFunctions | ILoadOptionsFunctions | IPollFunctions, propertyName: string, method: IHttpRequestMethods, endpoint: string, itemIndex: number, body?: IDataObject, query?: IDataObject, headers?: IDataObject): Promise<IDataObject[]>;
export declare function downloadAttachments(this: IExecuteFunctions | IPollFunctions, messages: IDataObject[] | IDataObject, prefix: string, itemIndex: number): Promise<INodeExecutionData[]>;
export declare function getMimeContent(this: IExecuteFunctions, messageId: string, binaryPropertyName: string, itemIndex: number, outputFileName?: string): Promise<IDataObject>;
export declare function getSubfolders(this: IExecuteFunctions | ILoadOptionsFunctions, folders: IDataObject[], itemIndex: number, addPathToDisplayName?: boolean): Promise<IDataObject[]>;
//# sourceMappingURL=index.d.ts.map