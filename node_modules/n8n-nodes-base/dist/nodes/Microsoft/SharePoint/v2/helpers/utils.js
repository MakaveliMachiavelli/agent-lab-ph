"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MAX_SIMPLE_UPLOAD_BYTES = exports.SHAREPOINT_ILLEGAL_FILE_NAME_CHARS = exports.HYPERLINK_WRITE_HEADERS = exports.NON_INDEXED_QUERY_HEADERS = exports.odataFieldEqualsClause = exports.ITEM_SIMPLIFY_EXPAND = exports.ITEM_SIMPLIFY_SELECT = exports.LIST_SIMPLIFY_SELECT = void 0;
exports.simplifyItem = simplifyItem;
exports.assertPathSegment = assertPathSegment;
exports.nonIndexedFilterThresholdError = nonIndexedFilterThresholdError;
exports.addUniqueConstraintHint = addUniqueConstraintHint;
exports.validateSharePointFileName = validateSharePointFileName;
exports.getUploadBufferWithinCap = getUploadBufferWithinCap;
const n8n_workflow_1 = require("n8n-workflow");
/** v1's Simplify $select list — the exact trimmed fields v2 keeps returning; Get Many reuses it. */
exports.LIST_SIMPLIFY_SELECT = 'id,name,displayName,description,createdDateTime,lastModifiedDateTime,webUrl';
/** v1's item Simplify request shape — the trimmed top-level fields v2 keeps returning. */
exports.ITEM_SIMPLIFY_SELECT = 'id,createdDateTime,lastModifiedDateTime,webUrl';
// Not v1's `fields(select=Title)`: through this request stack Graph only accepts
// the nested option $-prefixed, else it rejects the whole query as unparseable.
exports.ITEM_SIMPLIFY_EXPAND = 'fields($select=Title)';
/** Match v1's simplifyItemPostReceive exactly: only these annotation keys are stripped. */
function simplifyItem(item) {
    delete item['@odata.context'];
    delete item['@odata.etag'];
    delete item['fields@odata.navigationLink'];
    delete item.fields?.['@odata.etag'];
    return item;
}
/**
 * Validates and trims a value used verbatim as a URL path segment. Rejects
 * empty, `.` and `..`, which would address a different resource than intended.
 */
function assertPathSegment(node, value, paramName) {
    const trimmed = value.trim();
    if (trimmed === '') {
        throw new n8n_workflow_1.NodeOperationError(node, `The '${paramName}' parameter is empty`, {
            description: `Set the ${paramName.toLowerCase()} and try again.`,
        });
    }
    if (trimmed === '.' || trimmed === '..') {
        throw new n8n_workflow_1.NodeOperationError(node, `The '${paramName}' value '${trimmed}' is not valid`, {
            description: `Set a specific ${paramName.toLowerCase()} and try again.`,
        });
    }
    return trimmed;
}
/**
 * Builds a `fields/{column} eq '{value}'` OData $filter clause. The value is
 * always compared as a quoted string literal, where the only special character
 * is the single quote, escaped by doubling; URL encoding is the transport's job.
 */
const odataFieldEqualsClause = (column, value) => `fields/${column} eq '${String(value ?? '').replaceAll("'", "''")}'`;
exports.odataFieldEqualsClause = odataFieldEqualsClause;
/** SharePoint refuses to filter on non-indexed columns without this opt-in. */
exports.NON_INDEXED_QUERY_HEADERS = {
    Prefer: 'HonorNonIndexedQueriesWarningMayFailRandomly',
};
/** Graph rejects hyperlink field writes without this (see list/columns.ts). */
exports.HYPERLINK_WRITE_HEADERS = { Prefer: 'apiversion=2.1' };
/**
 * Translates Graph's list-view-threshold failure (a non-indexed $filter on a
 * >5,000-item list) into an error naming the filtered column(s); the run must
 * never return a silent partial result. Returns `undefined` for other errors.
 */
function nonIndexedFilterThresholdError(node, error, filter) {
    if (filter === '' || !(error instanceof n8n_workflow_1.NodeApiError) || !/threshold/i.test(error.message)) {
        return undefined;
    }
    const columns = [...filter.matchAll(/fields\/(\w+)/g)].map((match) => match[1]);
    const subject = columns.length > 0 ? `column(s) '${columns.join("', '")}'` : 'this filter';
    return new n8n_workflow_1.NodeOperationError(node, `SharePoint could not finish filtering on ${subject}: large lists can only be filtered on indexed columns`, {
        description: 'Add an index to the column in SharePoint (List settings → Indexed columns) or filter on an indexed column, then try again.',
    });
}
/** Graph's unique-constraint rejection names no column; point the user at the mapper. */
function addUniqueConstraintHint(error) {
    if (error instanceof n8n_workflow_1.NodeApiError && error.message.includes('unique constraints')) {
        error.description = "Double-check the value(s) in 'Columns' and try again";
    }
}
/** Characters SharePoint forbids in file names; Graph rejects them with a misleading 400. */
exports.SHAREPOINT_ILLEGAL_FILE_NAME_CHARS = ['"', '*', ':', '<', '>', '?', '/', '\\', '|'];
/** Graph's cap for a single-request `PUT …:/content` upload; larger files need an upload session. */
exports.MAX_SIMPLE_UPLOAD_BYTES = 250 * 1024 * 1024;
function validateSharePointFileName(node, fileName, itemIndex) {
    if (fileName === undefined || fileName.trim() === '') {
        throw new n8n_workflow_1.NodeOperationError(node, 'File name must be set!', { itemIndex });
    }
    const illegalChars = exports.SHAREPOINT_ILLEGAL_FILE_NAME_CHARS.filter((char) => fileName.includes(char));
    if (illegalChars.length > 0) {
        throw new n8n_workflow_1.NodeOperationError(node, `The file name "${fileName}" contains characters that SharePoint doesn't allow: ${illegalChars.join(' ')}`, {
            itemIndex,
            description: `SharePoint file names can't contain any of these characters: ${exports.SHAREPOINT_ILLEGAL_FILE_NAME_CHARS.join(' ')}. Remove them from the file name and try again.` +
                (illegalChars.includes(':')
                    ? " If you're inserting a timestamp, use a colon-free format such as {{ $now.toFormat('yyyy-MM-dd_HH-mm-ss') }}."
                    : ''),
        });
    }
}
/**
 * Asserts the input binary exists, enforces the simple-upload size cap, and
 * returns the payload with its content type. Out-of-process binary data is
 * size-checked from its metadata — oversized files are rejected without ever
 * being read.
 */
async function getUploadBufferWithinCap(ctx, itemIndex, binaryPropertyName) {
    const binaryData = ctx.helpers.assertBinaryData(itemIndex, binaryPropertyName);
    let body;
    let fileSize;
    if (binaryData.id) {
        ({ fileSize } = await ctx.helpers.getBinaryMetadata(binaryData.id));
    }
    else {
        body = Buffer.from(binaryData.data, n8n_workflow_1.BINARY_ENCODING);
        fileSize = body.byteLength;
    }
    if (fileSize > exports.MAX_SIMPLE_UPLOAD_BYTES) {
        // Ceil so a just-over-the-cap file never reads as "250 MB is larger than 250 MB"
        const sizeMb = Math.ceil(fileSize / (1024 * 1024));
        throw new n8n_workflow_1.NodeOperationError(ctx.getNode(), `The file is ${sizeMb} MB, which is larger than the 250 MB limit for SharePoint uploads`, {
            itemIndex,
            description: 'Files over 250 MB need to be uploaded in pieces, which this operation does not support yet.',
        });
    }
    body ??= await ctx.helpers.getBinaryDataBuffer(itemIndex, binaryPropertyName);
    return { body, contentType: binaryData.mimeType ?? 'application/octet-stream' };
}
//# sourceMappingURL=utils.js.map