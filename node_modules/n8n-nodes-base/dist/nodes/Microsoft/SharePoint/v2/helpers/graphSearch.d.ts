import type { IDataObject, ILoadOptionsFunctions, INodeListSearchResult } from 'n8n-workflow';
export declare const SEARCH_PAGE_LIMIT = 10;
export type CollectionSearchOptions<T> = {
    /** First-page resource path; ignored once `paginationToken` is set. */
    endpoint: string;
    qs: IDataObject;
    toResult: (item: T) => {
        name: string;
        value: string;
    } | null;
    filter?: string;
    paginationToken?: string;
};
/**
 * Pages through a Graph collection, mapping each entry via `toResult` (return
 * `null` to drop it) and filtering on the produced label — Graph can't
 * substring-filter these collections server-side.
 */
export declare function searchGraphCollection<T>(this: ILoadOptionsFunctions, options: CollectionSearchOptions<T>): Promise<INodeListSearchResult>;
type DriveItem = {
    id?: string;
    name?: string;
    folder?: unknown;
    file?: unknown;
};
/** Drive-items wrapper over `searchGraphCollection`, labelling each by file name. */
export declare function searchDriveItems(this: ILoadOptionsFunctions, options: {
    /** First-page resource path; ignored once `paginationToken` is set. */
    endpoint: string;
    qs: IDataObject;
    keep: (item: DriveItem) => boolean;
    filter?: string;
    paginationToken?: string;
}): Promise<INodeListSearchResult>;
export {};
//# sourceMappingURL=graphSearch.d.ts.map