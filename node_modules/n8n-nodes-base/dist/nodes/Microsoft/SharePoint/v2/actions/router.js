"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.router = router;
const n8n_workflow_1 = require("n8n-workflow");
const file = __importStar(require("./file"));
const item = __importStar(require("./item"));
const list = __importStar(require("./list"));
async function router() {
    const items = this.getInputData();
    const returnData = [];
    let responseData;
    const resource = this.getNodeParameter('resource', 0);
    const operation = this.getNodeParameter('operation', 0);
    const sharePointTypeData = {
        resource,
        operation,
    };
    // Operations run once per item, so the site ID cache is hoisted here to
    // span the whole run — each distinct site URL then costs one Graph lookup.
    const siteIdCache = new Map();
    for (let i = 0; i < items.length; i++) {
        try {
            switch (sharePointTypeData.resource) {
                case 'file':
                    if (!(sharePointTypeData.operation in file)) {
                        throw new n8n_workflow_1.NodeOperationError(this.getNode(), `The operation "${operation}" is not supported!`);
                    }
                    responseData = await file[sharePointTypeData.operation].execute.call(this, i, siteIdCache);
                    break;
                case 'list':
                    if (!(sharePointTypeData.operation in list)) {
                        throw new n8n_workflow_1.NodeOperationError(this.getNode(), `The operation "${operation}" is not supported!`);
                    }
                    // get returns a single object, getAll returns an array — both operations
                    // declare the wider Promise<IDataObject | IDataObject[]> return type so
                    // TS can resolve .execute.call across either one without a local wrapper.
                    responseData = await list[sharePointTypeData.operation].execute.call(this, i, siteIdCache);
                    break;
                case 'item':
                    if (!(sharePointTypeData.operation in item)) {
                        throw new n8n_workflow_1.NodeOperationError(this.getNode(), `The operation "${operation}" is not supported!`);
                    }
                    responseData = await item[sharePointTypeData.operation].execute.call(this, i, siteIdCache);
                    break;
                default:
                    throw new n8n_workflow_1.NodeOperationError(this.getNode(), `The resource "${resource}" is not supported!`);
            }
            // A binary-carrying result is already a full execution item —
            // returnJsonArray would nest it under `json` — so it only gets the
            // pairedItem stamp. Graph replies never carry a top-level `binary` key.
            const asItems = typeof responseData === 'object' && responseData !== null && 'binary' in responseData
                ? [responseData]
                : this.helpers.returnJsonArray(responseData);
            const executionData = this.helpers.constructExecutionMetaData(asItems, {
                itemData: { item: i },
            });
            returnData.push(...executionData);
        }
        catch (error) {
            if (this.continueOnFail()) {
                const executionErrorData = this.helpers.constructExecutionMetaData(this.helpers.returnJsonArray({ error: error.message }), { itemData: { item: i } });
                returnData.push(...executionErrorData);
                continue;
            }
            throw error;
        }
    }
    return [returnData];
}
//# sourceMappingURL=router.js.map