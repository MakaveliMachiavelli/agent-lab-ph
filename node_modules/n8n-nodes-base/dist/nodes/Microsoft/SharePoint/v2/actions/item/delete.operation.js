"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.description = void 0;
exports.execute = execute;
const utilities_1 = require("../../../../../../utils/utilities");
const utils_1 = require("../../helpers/utils");
const item_1 = require("../../item");
const list_1 = require("../../list");
const site_1 = require("../../site");
const transport_1 = require("../../transport");
const properties = [
    {
        ...site_1.siteRLC,
        description: 'Select the site the item is in',
    },
    {
        ...list_1.listRLC,
        description: 'Select the list the item is in',
        displayOptions: {
            hide: {
                ...list_1.untilSiteSelected,
            },
        },
    },
    {
        ...item_1.itemRLC,
        description: 'Select the item you want to delete',
        displayOptions: {
            hide: {
                ...list_1.untilSiteSelected,
                ...item_1.untilListSelected,
            },
        },
    },
];
const displayOptions = {
    show: {
        resource: ['item'],
        operation: ['delete'],
    },
};
exports.description = (0, utilities_1.updateDisplayOptions)(displayOptions, properties);
async function execute(i, siteIdCache) {
    // https://learn.microsoft.com/en-us/graph/api/listitem-delete
    const siteId = await site_1.resolveSiteId.call(this, i, siteIdCache);
    const list = (0, utils_1.assertPathSegment)(this.getNode(), String(this.getNodeParameter('list', i, '', { extractValue: true })), 'List');
    const item = (0, utils_1.assertPathSegment)(this.getNode(), String(this.getNodeParameter('item', i, '', { extractValue: true })), 'Item');
    // 204 has no body — report the deletion ourselves, like v1.
    await transport_1.microsoftApiRequest.call(this, 'DELETE', `/v1.0/sites/${encodeURIComponent(siteId)}/lists/${encodeURIComponent(list)}/items/${encodeURIComponent(item)}`);
    return { deleted: true };
}
//# sourceMappingURL=delete.operation.js.map