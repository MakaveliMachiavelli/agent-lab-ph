"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.description = void 0;
exports.execute = execute;
const utilities_1 = require("../../../../../../utils/utilities");
const utils_1 = require("../../helpers/utils");
const item_1 = require("../../item");
const list_1 = require("../../list");
const columns_1 = require("../../list/columns");
const site_1 = require("../../site");
const transport_1 = require("../../transport");
const properties = [
    {
        ...site_1.siteRLC,
        description: 'Select the site the list belongs to',
    },
    {
        ...list_1.listRLC,
        description: 'Select the list to create an item in',
        displayOptions: {
            hide: {
                ...list_1.untilSiteSelected,
            },
        },
    },
    (0, columns_1.itemColumns)('add'),
];
const displayOptions = {
    show: {
        resource: ['item'],
        operation: ['create'],
    },
};
exports.description = (0, utilities_1.updateDisplayOptions)(displayOptions, properties);
async function execute(i, siteIdCache) {
    // https://learn.microsoft.com/en-us/graph/api/listitem-create
    const siteId = await site_1.resolveSiteId.call(this, i, siteIdCache);
    const listIdOrTitle = (0, utils_1.assertPathSegment)(this.getNode(), String(this.getNodeParameter('list', i, '', { extractValue: true })), 'List');
    const schema = this.getNodeParameter('columns.schema', i, []);
    const value = item_1.resolveItemMapperValues.call(this, i);
    const { fields, hasHyperlink } = (0, item_1.buildItemFieldsPayload)(value, schema);
    try {
        return await transport_1.microsoftApiRequest.call(this, 'POST', `/v1.0/sites/${encodeURIComponent(siteId)}/lists/${encodeURIComponent(listIdOrTitle)}/items`, { fields }, {}, undefined, hasHyperlink ? utils_1.HYPERLINK_WRITE_HEADERS : {});
    }
    catch (error) {
        (0, utils_1.addUniqueConstraintHint)(error);
        throw error;
    }
}
//# sourceMappingURL=create.operation.js.map