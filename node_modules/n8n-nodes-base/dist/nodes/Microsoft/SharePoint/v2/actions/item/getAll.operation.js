"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.description = void 0;
exports.execute = execute;
const descriptions_1 = require("../../../../../../utils/descriptions");
const utilities_1 = require("../../../../../../utils/utilities");
const utils_1 = require("../../helpers/utils");
const list_1 = require("../../list");
const site_1 = require("../../site");
const transport_1 = require("../../transport");
const properties = [
    {
        ...site_1.siteRLC,
        description: 'Select the site the list belongs to',
    },
    {
        ...list_1.listRLC,
        description: 'Select the list you want to search for items in',
        displayOptions: {
            hide: {
                ...list_1.untilSiteSelected,
            },
        },
    },
    {
        displayName: 'Filter by Formula',
        name: 'filter',
        type: 'string',
        default: '',
        description: 'The formula will be evaluated for each record. <a href="https://learn.microsoft.com/en-us/graph/filter-query-parameter">More info</a>.',
        hint: 'If empty, all the items will be returned',
        placeholder: "e.g. fields/Title eq 'item1'",
    },
    ...descriptions_1.returnAllOrLimit,
    {
        displayName: 'Options',
        name: 'options',
        type: 'collection',
        default: {},
        placeholder: 'Add option',
        options: [
            {
                displayName: 'Fields',
                name: 'fields',
                type: 'multiOptions',
                default: [],
                description: 'The fields you want to include in the output',
                displayOptions: {
                    hide: {
                        '/simplify': [true],
                    },
                },
                options: [
                    { name: 'Content Type', value: 'contentType' },
                    { name: 'Created At', value: 'createdDateTime' },
                    { name: 'Created By', value: 'createdBy' },
                    { name: 'Fields', value: 'fields' },
                    { name: 'ID', value: 'id' },
                    { name: 'Last Modified At', value: 'lastModifiedDateTime' },
                    { name: 'Last Modified By', value: 'lastModifiedBy' },
                    { name: 'Parent Reference', value: 'parentReference' },
                    { name: 'Web URL', value: 'webUrl' },
                ],
            },
        ],
    },
    {
        displayName: 'Simplify',
        name: 'simplify',
        type: 'boolean',
        default: true,
        description: 'Whether to return a simplified version of the response instead of the raw data',
    },
];
const displayOptions = {
    show: {
        resource: ['item'],
        operation: ['getAll'],
    },
};
exports.description = (0, utilities_1.updateDisplayOptions)(displayOptions, properties);
async function execute(i, siteIdCache) {
    // https://learn.microsoft.com/en-us/graph/api/listitem-list
    const siteId = await site_1.resolveSiteId.call(this, i, siteIdCache);
    const listIdOrTitle = (0, utils_1.assertPathSegment)(this.getNode(), String(this.getNodeParameter('list', i, '', { extractValue: true })), 'List');
    const filter = this.getNodeParameter('filter', i, '').trim();
    const returnAll = this.getNodeParameter('returnAll', i);
    const simplify = this.getNodeParameter('simplify', i);
    const fields = this.getNodeParameter('options.fields', i, []);
    const qs = {};
    if (filter !== '') {
        qs.$filter = filter;
    }
    if (simplify) {
        // v1 parity: the trim happens server-side, the annotations client-side
        qs.$select = utils_1.ITEM_SIMPLIFY_SELECT;
        qs.$expand = utils_1.ITEM_SIMPLIFY_EXPAND;
    }
    else if (fields.length > 0) {
        qs.$select = fields.join(',');
        if (fields.includes('fields')) {
            qs.$expand = 'fields';
        }
    }
    let limit;
    if (!returnAll) {
        limit = this.getNodeParameter('limit', i);
        qs.$top = limit; // hint only — Graph may still return fewer per page
    }
    let items;
    try {
        items = await transport_1.microsoftApiRequestAllItems.call(this, 'value', 'GET', `/v1.0/sites/${encodeURIComponent(siteId)}/lists/${encodeURIComponent(listIdOrTitle)}/items`, {}, qs, limit, filter !== '' ? utils_1.NON_INDEXED_QUERY_HEADERS : {});
    }
    catch (error) {
        throw (0, utils_1.nonIndexedFilterThresholdError)(this.getNode(), error, filter) ?? error;
    }
    return simplify ? items.map(utils_1.simplifyItem) : items;
}
//# sourceMappingURL=getAll.operation.js.map