/**
 * MongoDB Node - Version 1.1
 * Discriminator: resource=document, operation=insert
 */


interface Credentials {
  mongoDb: CredentialReference;
}

/** Insert documents */
export type MongoDbV11DocumentInsertParams = {
  resource: 'document';
  operation: 'insert';
/**
 * MongoDB Collection
 */
    collection: string | Expression<string>;
/**
 * Comma-separated list of the fields to be included into the new document
 */
    fields?: string | Expression<string>;
/**
 * Options
 * @default {}
 */
    options?: {
    /** Comma-separated list of fields that will be parsed as Mongo Date type
     */
    dateFields?: string | Expression<string>;
    /** Whether to use dot notation to access date fields
     * @default false
     */
    useDotNotation?: boolean | Expression<boolean>;
  };
};

export type MongoDbV11DocumentInsertNode = {
  type: 'n8n-nodes-base.mongoDb';
  version: 1.1;
  config: NodeConfig<MongoDbV11DocumentInsertParams> & { credentials?: Credentials };
};