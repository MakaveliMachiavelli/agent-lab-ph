/**
 * GitHub - PullRequest Resource - Zod Schema Factory
 * Exports a factory that unions all operation schemas for this resource.
 *
 * Schema helpers (z, expressionSchema, etc.) are passed as parameters
 * by the schema-validator, not imported from external files.
 *
 * @generated - CommonJS JavaScript for runtime loading
 */

const getCloseSchema = require('./operation_close.schema');
const getCreateSchema = require('./operation_create.schema');
const getCreateCommentSchema = require('./operation_create_comment.schema');
const getEditCommentSchema = require('./operation_edit_comment.schema');
const getGetSchema = require('./operation_get.schema');
const getGetDiffSchema = require('./operation_get_diff.schema');
const getGetPatchSchema = require('./operation_get_patch.schema');
const getMergeSchema = require('./operation_merge.schema');
const getReopenSchema = require('./operation_reopen.schema');
const getUpdateSchema = require('./operation_update.schema');

module.exports = function getSchema(helpers) {
  const { parameters, z } = helpers;
  // Apply operation default if not set
  const effectiveParams = parameters.operation === undefined ? { ...parameters, operation: 'getRepositories' } : parameters;
  return z.union([
    getCloseSchema({ ...helpers, parameters: effectiveParams }),
    getCreateSchema({ ...helpers, parameters: effectiveParams }),
    getCreateCommentSchema({ ...helpers, parameters: effectiveParams }),
    getEditCommentSchema({ ...helpers, parameters: effectiveParams }),
    getGetSchema({ ...helpers, parameters: effectiveParams }),
    getGetDiffSchema({ ...helpers, parameters: effectiveParams }),
    getGetPatchSchema({ ...helpers, parameters: effectiveParams }),
    getMergeSchema({ ...helpers, parameters: effectiveParams }),
    getReopenSchema({ ...helpers, parameters: effectiveParams }),
    getUpdateSchema({ ...helpers, parameters: effectiveParams }),
  ]);
};