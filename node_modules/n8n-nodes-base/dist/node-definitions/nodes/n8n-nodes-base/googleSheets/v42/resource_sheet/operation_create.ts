/**
 * Google Sheets Node - Version 4.2
 * Discriminator: resource=sheet, operation=create
 */


interface Credentials {
  googleApi: CredentialReference;
  googleSheetsOAuth2Api: CredentialReference;
}

/** Create a new sheet */
export type GoogleSheetsV42SheetCreateParams = {
  resource: 'sheet';
  operation: 'create';
  authentication?: 'serviceAccount' | 'oAuth2' | Expression<string>;
/**
 * Document
 * @builderHint Default to mode: 'list', which is easiest for users to set up: it gives them the From-list picker at setup. Use mode: 'id' only when the user supplied a concrete spreadsheet ID; otherwise use mode: 'list' with an empty value and a cachedResultName placeholder (e.g. the sheet name from the prompt). Never invent or fabricate a spreadsheet ID. Resource locator value must be `{ __rl: true, mode, value }`, not a plain string or `expr()` wrapper.
 * @searchListMethod spreadSheetsSearch
 * @default {"mode":"list","value":""}
 */
    documentId?: { __rl: true; mode: 'list' | 'url' | 'id'; value: string; cachedResultName?: string };
/**
 * The name of the sheet
 * @default n8n-sheet
 */
    title?: string | Expression<string>;
/**
 * Options
 * @default {}
 */
    options?: {
    /** Whether the sheet is hidden in the UI, false if it's visible
     * @default false
     */
    hidden?: boolean | Expression<boolean>;
    /** Whether the sheet is an RTL sheet instead of an LTR sheet
     * @default false
     */
    rightToLeft?: boolean | Expression<boolean>;
    /** The ID of the sheet. Must be non-negative. This field cannot be changed once set.
     * @default 0
     */
    sheetId?: number | Expression<number>;
    /** The index of the sheet within the spreadsheet
     * @default 0
     */
    index?: number | Expression<number>;
    /** The color of the tab in the UI
     * @default 0aa55c
     */
    tabColor?: string | Expression<string>;
  };
};

export type GoogleSheetsV42SheetCreateNode = {
  type: 'n8n-nodes-base.googleSheets';
  version: 4.2;
  config: NodeConfig<GoogleSheetsV42SheetCreateParams> & { credentials?: Credentials };
};