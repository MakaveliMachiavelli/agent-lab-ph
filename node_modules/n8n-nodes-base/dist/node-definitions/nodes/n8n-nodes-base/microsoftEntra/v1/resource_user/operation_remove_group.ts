/**
 * Microsoft Entra ID Node - Version 1
 * Discriminator: resource=user, operation=removeGroup
 */


interface Credentials {
  microsoftEntraOAuth2Api: CredentialReference;
}

/** Remove user from group */
export type MicrosoftEntraV1UserRemoveGroupParams = {
  resource: 'user';
  operation: 'removeGroup';
/**
 * Group
 * @searchListMethod getGroups
 * @default {"mode":"list","value":""}
 */
    group?: { __rl: true; mode: 'list' | 'id'; value: string; cachedResultName?: string };
/**
 * User to Remove
 * @searchListMethod getUsers
 * @default {"mode":"list","value":""}
 */
    user?: { __rl: true; mode: 'list' | 'id'; value: string; cachedResultName?: string };
  requestOptions?: {
    /** Batching
     * @default {"batch":{}}
     */
    batching?: {
        /** Batching
     */
    batch?: {
      /** Input will be split in batches to throttle requests. -1 for disabled. 0 will be treated as 1.
       * @default 50
       */
      batchSize?: number | Expression<number>;
      /** Time (in milliseconds) between each batch of requests. 0 for disabled.
       * @default 1000
       */
      batchInterval?: number | Expression<number>;
    };
  };
    /** Whether to accept the response even if SSL certificate validation is not possible
     * @default false
     */
    allowUnauthorizedCerts?: boolean;
    /** HTTP proxy to use. If authentication is required it can be defined as follow: http://username:password@myproxy:3128
     */
    proxy?: string | Expression<string>;
    /** Time in ms to wait for the server to send response headers (and start the response body) before aborting the request
     * @default 10000
     */
    timeout?: number | Expression<number>;
  };
};

export type MicrosoftEntraV1UserRemoveGroupNode = {
  type: 'n8n-nodes-base.microsoftEntra';
  version: 1;
  config: NodeConfig<MicrosoftEntraV1UserRemoveGroupParams> & { credentials?: Credentials };
};