/**
 * Microsoft OneDrive Node - Version 1
 * Discriminator: resource=file, operation=copy
 */


interface Credentials {
  microsoftOneDriveOAuth2Api: CredentialReference;
  microsoftOAuth2Api: CredentialReference;
  microsoftEntraServicePrincipalApi: CredentialReference;
}

/** Copy a file */
export type MicrosoftOneDriveV1FileCopyParams = {
  resource: 'file';
  operation: 'copy';
  authentication?: 'microsoftOneDriveOAuth2Api' | 'microsoftOAuth2Api' | 'microsoftEntraServicePrincipalApi';
/**
 * Which drive the Service Principal should act on (app-only has no personal drive)
 * @displayOptions.show { authentication: ["microsoftEntraServicePrincipalApi"] }
 * @default user
 */
    resourceTarget?: 'user' | 'drive';
/**
 * The user whose OneDrive the Service Principal should act on. Evaluated per input item, so an expression can target a different user for each item.
 * @displayOptions.show { authentication: ["microsoftEntraServicePrincipalApi"], resourceTarget: ["user"] }
 * @default {"mode":"id","value":""}
 */
    userTarget?: { __rl: true; mode: 'id'; value: string; cachedResultName?: string };
/**
 * The drive the Service Principal should act on. Evaluated per input item, so an expression can target a different drive for each item.
 * @displayOptions.show { authentication: ["microsoftEntraServicePrincipalApi"], resourceTarget: ["drive"] }
 * @default {"mode":"id","value":""}
 */
    driveTarget?: { __rl: true; mode: 'id'; value: string; cachedResultName?: string };
/**
 * File ID
 */
    fileId?: string | Expression<string>;
/**
 * Additional Fields
 * @default {}
 */
    additionalFields?: {
    /** The new name for the copy. If this isn't provided, the same name will be used as the original.
     */
    name?: string | Expression<string>;
  };
/**
 * Reference to the parent item the copy will be created in &lt;a href="https://docs.microsoft.com/en-us/onedrive/developer/rest-api/resources/itemreference?view=odsp-graph-online"&gt; Details &lt;/a&gt;
 * @default {}
 */
    parentReference?: {
    /** Identifier of the drive instance that contains the item
     */
    driveId?: string | Expression<string>;
    /** Identifies the type of drive
     */
    driveType?: string | Expression<string>;
    /** Identifier of the item in the drive
     */
    id?: string | Expression<string>;
    /** Identifier of the list
     */
    listId?: string | Expression<string>;
    /** The name of the item being referenced
     */
    name?: string | Expression<string>;
    /** Path that can be used to navigate to the item
     */
    path?: string | Expression<string>;
    /** Identifier for a shared resource that can be accessed via the Shares API
     */
    shareId?: string | Expression<string>;
    /** Identifier of the site
     */
    siteId?: string | Expression<string>;
  };
};

export type MicrosoftOneDriveV1FileCopyOutput = {
  location?: string;
};

export type MicrosoftOneDriveV1FileCopyNode = {
  type: 'n8n-nodes-base.microsoftOneDrive';
  version: 1;
  config: NodeConfig<MicrosoftOneDriveV1FileCopyParams> & { credentials?: Credentials };
  output?: Items<MicrosoftOneDriveV1FileCopyOutput>;
};