/**
 * Pipedrive Node - Version 2
 * Discriminator: resource=organization, operation=getAll
 */


interface Credentials {
  pipedriveApi: CredentialReference;
  pipedriveOAuth2Api: CredentialReference;
}

/** Get many activities */
export type PipedriveV2OrganizationGetAllParams = {
  resource: 'organization';
  operation: 'getAll';
  authentication?: 'apiToken' | 'oAuth2' | Expression<string>;
/**
 * Whether to return all results or only up to a given limit
 * @default false
 */
    returnAll?: boolean | Expression<boolean>;
/**
 * Max number of results to return
 * @displayOptions.show { returnAll: [false] }
 * @default 100
 */
    limit?: number | Expression<number>;
/**
 * Whether to show raw Pipedrive field keys and option IDs instead of human-readable names in output
 * @default false
 */
    rawCustomFieldOutput?: boolean | Expression<boolean>;
/**
 * Filters
 * @default {}
 */
    filters?: {
    /** Filter organizations whose name starts with this character (single character)
     */
    first_char?: string | Expression<string>;
    /** Predefined filter to apply to the organizations to retrieve. Choose from the list, or specify an ID using an &lt;a href="https://docs.n8n.io/code/expressions/"&gt;expression&lt;/a&gt;.
     * @loadOptionsMethod getFilters
     */
    filter_id?: string | Expression<string>;
  };
};

export type PipedriveV2OrganizationGetAllNode = {
  type: 'n8n-nodes-base.pipedrive';
  version: 2;
  config: NodeConfig<PipedriveV2OrganizationGetAllParams> & { credentials?: Credentials };
};