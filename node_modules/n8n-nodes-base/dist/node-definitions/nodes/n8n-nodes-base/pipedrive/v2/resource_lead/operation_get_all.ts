/**
 * Pipedrive Node - Version 2
 * Discriminator: resource=lead, operation=getAll
 */


interface Credentials {
  pipedriveApi: CredentialReference;
  pipedriveOAuth2Api: CredentialReference;
}

/** Get many activities */
export type PipedriveV2LeadGetAllParams = {
  resource: 'lead';
  operation: 'getAll';
  authentication?: 'apiToken' | 'oAuth2' | Expression<string>;
/**
 * Whether to return all results or only up to a given limit
 * @default false
 */
    returnAll?: boolean | Expression<boolean>;
/**
 * Max number of results to return
 * @displayOptions.show { returnAll: [false] }
 * @default 100
 */
    limit?: number | Expression<number>;
/**
 * Filters
 * @default {}
 */
    filters?: {
    /** Archived Status
     * @default all
     */
    archived_status?: 'all' | 'archived' | 'not_archived' | Expression<string>;
    /** Filter leads by organization ID
     * @default 0
     */
    organization_id?: number | Expression<number>;
    /** Filter leads by owner. Choose from the list, or specify an ID using an &lt;a href="https://docs.n8n.io/code/expressions/"&gt;expression&lt;/a&gt;.
     * @loadOptionsMethod getUserIds
     */
    owner_id?: string | Expression<string>;
    /** Filter leads by person ID
     * @default 0
     */
    person_id?: number | Expression<number>;
  };
};

export type PipedriveV2LeadGetAllNode = {
  type: 'n8n-nodes-base.pipedrive';
  version: 2;
  config: NodeConfig<PipedriveV2LeadGetAllParams> & { credentials?: Credentials };
};