/**
 * Pipedrive Node - Version 2
 * Discriminator: resource=note, operation=getAll
 */


interface Credentials {
  pipedriveApi: CredentialReference;
  pipedriveOAuth2Api: CredentialReference;
}

/** Get many activities */
export type PipedriveV2NoteGetAllParams = {
  resource: 'note';
  operation: 'getAll';
  authentication?: 'apiToken' | 'oAuth2' | Expression<string>;
/**
 * Whether to return all results or only up to a given limit
 * @default false
 */
    returnAll?: boolean | Expression<boolean>;
/**
 * Max number of results to return
 * @displayOptions.show { returnAll: [false] }
 * @default 100
 */
    limit?: number | Expression<number>;
/**
 * Filters
 * @default {}
 */
    filters?: {
    /** Filter notes by deal ID
     * @default 0
     */
    deal_id?: number | Expression<number>;
    /** The end date for filtering notes in YYYY-MM-DD format
     */
    end_date?: string | Expression<string>;
    /** Filter notes by lead ID
     */
    lead_id?: string | Expression<string>;
    /** Filter notes by organization. Choose from the list, or specify an ID using an &lt;a href="https://docs.n8n.io/code/expressions/"&gt;expression&lt;/a&gt;.
     * @loadOptionsMethod getOrganizationIds
     */
    org_id?: string | Expression<string>;
    /** Filter notes by person ID
     * @default 0
     */
    person_id?: number | Expression<number>;
    /** Whether to filter by notes pinned to a deal
     * @default false
     */
    pinned_to_deal_flag?: boolean | Expression<boolean>;
    /** Whether to filter by notes pinned to an organization
     * @default false
     */
    pinned_to_organization_flag?: boolean | Expression<boolean>;
    /** Whether to filter by notes pinned to a person
     * @default false
     */
    pinned_to_person_flag?: boolean | Expression<boolean>;
    /** The field to sort by
     * @default id
     */
    sort?: 'id' | 'update_time' | 'content' | Expression<string>;
    /** The start date for filtering notes in YYYY-MM-DD format
     */
    start_date?: string | Expression<string>;
  };
};

export type PipedriveV2NoteGetAllNode = {
  type: 'n8n-nodes-base.pipedrive';
  version: 2;
  config: NodeConfig<PipedriveV2NoteGetAllParams> & { credentials?: Credentials };
};