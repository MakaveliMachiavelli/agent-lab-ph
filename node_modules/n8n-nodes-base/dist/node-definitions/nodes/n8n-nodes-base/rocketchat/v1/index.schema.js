/**
 * RocketChat Node - Version 1 - Zod Schema Factory
 * Exports a factory that unions all discriminator schemas.
 *
 * Schema helpers (z, expressionSchema, etc.) are passed as parameters
 * by the schema-validator, not imported from external files.
 *
 * @generated - CommonJS JavaScript for runtime loading
 */

const getChatSchema = require('./resource_chat/index.schema');
const getSubscriptionsSchema = require('./resource_subscriptions/index.schema');
const getDmSchema = require('./resource_dm/index.schema');

module.exports = function getSchema(helpers) {
  const { parameters, z } = helpers;
  // Apply discriminator default if not set
  const effectiveParams = parameters.resource === undefined ? { ...parameters, resource: 'chat' } : parameters;
  return z.union([
    getChatSchema({ ...helpers, parameters: effectiveParams }),
    getSubscriptionsSchema({ ...helpers, parameters: effectiveParams }),
    getDmSchema({ ...helpers, parameters: effectiveParams }),
  ]);
};