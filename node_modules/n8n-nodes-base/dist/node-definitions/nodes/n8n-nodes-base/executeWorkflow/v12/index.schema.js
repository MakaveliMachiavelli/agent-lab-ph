/**
 * Execute Sub-workflow Node - Version 1.2 - Zod Schema Factory
 * Exports a factory that unions all discriminator schemas.
 *
 * Schema helpers (z, expressionSchema, etc.) are passed as parameters
 * by the schema-validator, not imported from external files.
 *
 * @generated - CommonJS JavaScript for runtime loading
 */

const getEachSchema = require('./mode_each.schema');
const getOnceSchema = require('./mode_once.schema');

module.exports = function getSchema(helpers) {
  const { parameters, z } = helpers;
  // Apply discriminator default if not set
  const effectiveParams = parameters.mode === undefined ? { ...parameters, mode: 'once' } : parameters;
  return z.union([
    getEachSchema({ ...helpers, parameters: effectiveParams }),
    getOnceSchema({ ...helpers, parameters: effectiveParams }),
  ]);
};