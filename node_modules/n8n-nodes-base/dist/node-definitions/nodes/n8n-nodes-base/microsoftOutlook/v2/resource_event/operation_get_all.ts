/**
 * Microsoft Outlook Node - Version 2
 * Discriminator: resource=event, operation=getAll
 */


interface Credentials {
  microsoftOutlookOAuth2Api: CredentialReference;
  microsoftOAuth2Api: CredentialReference;
  microsoftEntraServicePrincipalApi: CredentialReference;
}

/** List and search calendars */
export type MicrosoftOutlookV2EventGetAllParams = {
  resource: 'event';
  operation: 'getAll';
  authentication?: 'microsoftOutlookOAuth2Api' | 'microsoftOAuth2Api' | 'microsoftEntraServicePrincipalApi';
/**
 * The mailbox the Service Principal should act on. In the action node this is evaluated per input item (an expression can target a different mailbox per item); the trigger resolves it once.
 * @displayOptions.show { authentication: ["microsoftEntraServicePrincipalApi"] }
 * @default {"mode":"id","value":""}
 */
    mailbox?: { __rl: true; mode: 'id'; value: string; cachedResultName?: string };
/**
 * From All Calendars
 * @default true
 */
    fromAllCalendars?: boolean | Expression<boolean>;
/**
 * Calendar
 * @searchListMethod searchCalendars
 * @displayOptions.show { fromAllCalendars: [false] }
 * @default {"mode":"list","value":""}
 */
    calendarId?: { __rl: true; mode: 'list' | 'id'; value: string; cachedResultName?: string };
/**
 * Whether to expand recurring events into individual instances within the specified date range. When disabled, recurring events are returned as a single series master event.
 * @default false
 */
    includeRecurringInstances?: boolean | Expression<boolean>;
/**
 * Start of the date range to retrieve event instances for
 * @displayOptions.show { includeRecurringInstances: [true] }
 */
    startDateTime: string | Expression<string>;
/**
 * End of the date range to retrieve event instances for
 * @displayOptions.show { includeRecurringInstances: [true] }
 */
    endDateTime: string | Expression<string>;
/**
 * Whether to return all results or only up to a given limit
 * @default false
 */
    returnAll?: boolean | Expression<boolean>;
/**
 * Max number of results to return
 * @displayOptions.show { returnAll: [false] }
 * @default 100
 */
    limit?: number | Expression<number>;
/**
 * Output
 * @default simple
 */
    output?: 'simple' | 'raw' | 'fields' | Expression<string>;
/**
 * The fields to add to the output
 * @displayOptions.show { output: ["fields"] }
 * @default []
 */
    fields?: Array<'allowNewTimeProposals' | 'attendees' | 'body' | 'bodyPreview' | 'categories' | 'changeKey' | 'createdDateTime' | 'end' | 'hasAttachments' | 'hideAttendees' | 'iCalUId' | 'importance' | 'isAllDay' | 'isCancelled' | 'isDraft' | 'isOnlineMeeting' | 'isOrganizer' | 'isReminderOn' | 'lastModifiedDateTime' | 'location' | 'locations' | 'onlineMeeting' | 'onlineMeetingProvider' | 'onlineMeetingUrl' | 'organizer' | 'originalEndTimeZone' | 'originalStartTimeZone' | 'recurrence' | 'reminderMinutesBeforeStart' | 'responseRequested' | 'responseStatus' | 'sensitivity' | 'seriesMasterId' | 'showAs' | 'start' | 'subject' | 'transactionId' | 'type' | 'webLink'>;
/**
 * Filters
 * @displayOptions.show { includeRecurringInstances: [false] }
 * @default {}
 */
    filters?: {
    /** Filter Query
     * @hint Search query to filter events. &lt;a href="https://learn.microsoft.com/en-us/graph/filter-query-parameter"&gt;More info&lt;/a&gt;.
     */
    custom?: string | Expression<string>;
  };
};

export type MicrosoftOutlookV2EventGetAllOutput = {
  '@odata.etag'?: string;
  attendees?: Array<{
    emailAddress?: {
      address?: string;
      name?: string;
    };
    status?: {
      response?: string;
      time?: string;
    };
    type?: string;
  }>;
  bodyPreview?: string;
  end?: {
    dateTime?: string;
    timeZone?: string;
  };
  id?: string;
  organizer?: {
    emailAddress?: {
      address?: string;
      name?: string;
    };
  };
  start?: {
    dateTime?: string;
    timeZone?: string;
  };
  subject?: string;
  webLink?: string;
};

export type MicrosoftOutlookV2EventGetAllNode = {
  type: 'n8n-nodes-base.microsoftOutlook';
  version: 2;
  config: NodeConfig<MicrosoftOutlookV2EventGetAllParams> & { credentials?: Credentials };
  output?: Items<MicrosoftOutlookV2EventGetAllOutput>;
};