/**
 * Slack Node - Version 2.5
 * Discriminator: resource=channel, operation=kick
 */


interface Credentials {
  slackApi: CredentialReference;
  slackOAuth2Api: CredentialReference;
}

/** Removes a user from a channel */
export type SlackV25ChannelKickParams = {
  resource: 'channel';
  operation: 'kick';
  authentication?: 'accessToken' | 'oAuth2' | Expression<string>;
/**
 * The Slack channel to kick the user from
 * @searchListMethod getChannels
 * @default {"mode":"list","value":""}
 */
    channelId?: { __rl: true; mode: 'list' | 'id' | 'url'; value: string; cachedResultName?: string };
/**
 * Choose from the list, or specify an ID using an &lt;a href="https://docs.n8n.io/code/expressions/"&gt;expression&lt;/a&gt;
 * @loadOptionsMethod getUsers
 */
    userId?: string | Expression<string>;
};

export type SlackV25ChannelKickNode = {
  type: 'n8n-nodes-base.slack';
  version: 2.5;
  config: NodeConfig<SlackV25ChannelKickParams> & { credentials?: Credentials };
};