/**
 * Microsoft Excel (OneDrive) Node - Version 2.2
 * Discriminator: resource=table, operation=addTable
 */


interface Credentials {
  microsoftExcelOAuth2Api: CredentialReference;
  microsoftOAuth2Api: CredentialReference;
  microsoftEntraServicePrincipalApi: CredentialReference;
}

/** Represents an Excel table */
export type MicrosoftExcelV22TableAddTableParams = {
  resource: 'table';
  operation: 'addTable';
  authentication?: 'microsoftExcelOAuth2Api' | 'microsoftOAuth2Api' | 'microsoftEntraServicePrincipalApi';
/**
 * Which drive the Service Principal should act on (app-only has no personal drive)
 * @displayOptions.show { authentication: ["microsoftEntraServicePrincipalApi"] }
 * @default user
 */
    resourceTarget?: 'user' | 'drive';
/**
 * The user whose drive the Service Principal should act on. Evaluated per input item — an expression can target a different user for each item. Operations that write all rows in one request (append, update, upsert) use the first item's target.
 * @displayOptions.show { authentication: ["microsoftEntraServicePrincipalApi"], resourceTarget: ["user"] }
 * @default {"mode":"id","value":""}
 */
    userTarget?: { __rl: true; mode: 'id'; value: string; cachedResultName?: string };
/**
 * The drive the Service Principal should act on. Evaluated per input item — an expression can target a different drive for each item. Operations that write all rows in one request (append, update, upsert) use the first item's target.
 * @displayOptions.show { authentication: ["microsoftEntraServicePrincipalApi"], resourceTarget: ["drive"] }
 * @default {"mode":"id","value":""}
 */
    driveTarget?: { __rl: true; mode: 'id'; value: string; cachedResultName?: string };
/**
 * Workbook
 * @searchListMethod searchWorkbooks
 * @default {"mode":"list","value":""}
 */
    workbook?: { __rl: true; mode: 'list' | 'id'; value: string; cachedResultName?: string };
/**
 * Sheet
 * @searchListMethod getWorksheetsList
 * @default {"mode":"list","value":""}
 */
    worksheet?: { __rl: true; mode: 'list' | 'id'; value: string; cachedResultName?: string };
/**
 * Select Range
 * @default auto
 */
    selectRange?: 'auto' | 'manual' | Expression<string>;
/**
 * The range of cells that will be converted to a table
 * @displayOptions.show { selectRange: ["manual"] }
 */
    range?: string | Expression<string>;
/**
 * Whether the range has column labels. When this property set to false Excel will automatically generate header shifting the data down by one row.
 * @default true
 */
    hasHeaders?: boolean | Expression<boolean>;
};

export type MicrosoftExcelV22TableAddTableNode = {
  type: 'n8n-nodes-base.microsoftExcel';
  version: 2.2;
  config: NodeConfig<MicrosoftExcelV22TableAddTableParams> & { credentials?: Credentials };
};