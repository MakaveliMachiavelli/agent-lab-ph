/**
 * Notion Node - Version 3
 * Discriminator: resource=databasePage, operation=getAll
 */


interface Credentials {
  notionApi: CredentialReference;
  notionOAuth2Api: CredentialReference;
}

/** Get many child blocks */
export type NotionV3DatabasePageGetAllParams = {
  resource: 'databasePage';
  operation: 'getAll';
  authentication?: 'apiKey' | 'oAuth2' | Expression<string>;
/**
 * The Notion data source to operate on
 * @searchListMethod getDataSources
 * @default {"mode":"list","value":""}
 */
    dataSourceId?: { __rl: true; mode: 'list' | 'id'; value: string; cachedResultName?: string };
/**
 * Whether to return all results or only up to a given limit
 * @default false
 */
    returnAll?: boolean | Expression<boolean>;
/**
 * Max number of results to return
 * @displayOptions.show { returnAll: [false] }
 * @default 50
 */
    limit?: number | Expression<number>;
/**
 * Filter
 * @default none
 */
    filterType?: 'none' | 'manual' | 'json' | Expression<string>;
/**
 * Must Match
 * @displayOptions.show { filterType: ["manual"] }
 * @default anyFilter
 */
    matchType?: 'anyFilter' | 'allFilters' | Expression<string>;
/**
 * Filters
 * @displayOptions.show { filterType: ["manual"] }
 * @default {}
 */
    filters?: {
        /** Conditions
     */
    conditions?: Array<{
      /** The name of the property to filter by. Choose from the list, or specify an ID using an &lt;a href="https://docs.n8n.io/code/expressions/"&gt;expression&lt;/a&gt;.
       * @loadOptionsMethod getFilterProperties
       */
      key?: string | Expression<string>;
      /** Type
       * @default ={{$parameter["&key"].split("|").pop()}}
       */
      type?: unknown;
      /** The condition to filter by
       * @displayOptions.show { type: ["checkbox"] }
       */
      condition?: 'equals' | 'does_not_equal' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["created_by"] }
       */
      condition?: 'contains' | 'does_not_contain' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["created_time"] }
       */
      condition?: 'equals' | 'before' | 'after' | 'on_or_before' | 'on_or_after' | 'is_empty' | 'is_not_empty' | 'next_month' | 'next_week' | 'next_year' | 'past_month' | 'past_week' | 'past_year' | 'this_week' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["date"] }
       */
      condition?: 'equals' | 'before' | 'after' | 'on_or_before' | 'on_or_after' | 'is_empty' | 'is_not_empty' | 'next_month' | 'next_week' | 'next_year' | 'past_month' | 'past_week' | 'past_year' | 'this_week' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["email"] }
       */
      condition?: 'equals' | 'does_not_equal' | 'contains' | 'does_not_contain' | 'starts_with' | 'ends_with' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["files"] }
       */
      condition?: 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["last_edited_by"] }
       */
      condition?: 'contains' | 'does_not_contain' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["last_edited_time"] }
       */
      condition?: 'equals' | 'before' | 'after' | 'on_or_before' | 'on_or_after' | 'is_empty' | 'is_not_empty' | 'next_month' | 'next_week' | 'next_year' | 'past_month' | 'past_week' | 'past_year' | 'this_week' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["multi_select"] }
       */
      condition?: 'contains' | 'does_not_contain' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["number"] }
       */
      condition?: 'equals' | 'does_not_equal' | 'greater_than' | 'less_than' | 'greater_than_or_equal_to' | 'less_than_or_equal_to' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["people"] }
       */
      condition?: 'contains' | 'does_not_contain' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["phone_number"] }
       */
      condition?: 'equals' | 'does_not_equal' | 'contains' | 'does_not_contain' | 'starts_with' | 'ends_with' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["relation"] }
       */
      condition?: 'contains' | 'does_not_contain' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["rich_text"] }
       */
      condition?: 'equals' | 'does_not_equal' | 'contains' | 'does_not_contain' | 'starts_with' | 'ends_with' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["select"] }
       */
      condition?: 'equals' | 'does_not_equal' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["status"] }
       */
      condition?: 'equals' | 'does_not_equal' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["title"] }
       */
      condition?: 'equals' | 'does_not_equal' | 'contains' | 'does_not_contain' | 'starts_with' | 'ends_with' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["unique_id"] }
       */
      condition?: 'equals' | 'does_not_equal' | 'greater_than' | 'less_than' | 'greater_than_or_equal_to' | 'less_than_or_equal_to' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["url"] }
       */
      condition?: 'equals' | 'does_not_equal' | 'contains' | 'does_not_contain' | 'starts_with' | 'ends_with' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["verification"] }
       */
      condition?: 'status' | Expression<string>;
      /** Formula Return Type
       * @displayOptions.show { type: ["formula"] }
       * @default string
       */
      returnType?: 'checkbox' | 'date' | 'number' | 'string' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["formula"], returnType: ["checkbox"] }
       */
      condition?: 'equals' | 'does_not_equal' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["formula"], returnType: ["date"] }
       */
      condition?: 'equals' | 'before' | 'after' | 'on_or_before' | 'on_or_after' | 'is_empty' | 'is_not_empty' | 'next_month' | 'next_week' | 'next_year' | 'past_month' | 'past_week' | 'past_year' | 'this_week' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["formula"], returnType: ["number"] }
       */
      condition?: 'equals' | 'does_not_equal' | 'greater_than' | 'less_than' | 'greater_than_or_equal_to' | 'less_than_or_equal_to' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** The condition to filter by
       * @displayOptions.show { type: ["formula"], returnType: ["string"] }
       */
      condition?: 'equals' | 'does_not_equal' | 'contains' | 'does_not_contain' | 'starts_with' | 'ends_with' | 'is_empty' | 'is_not_empty' | Expression<string>;
      /** Provide the value for the Notion rollup filter object, for example {"number":{"greater_than":10}} or {"any":{"rich_text":{"contains":"Task"}}}.
       * @displayOptions.show { type: ["rollup"] }
       * @default {}
       */
      rollupJson?: IDataObject | string | Expression<string>;
      /** Text
       * @displayOptions.show { type: ["email", "phone_number", "rich_text", "title", "url"] }
       * @displayOptions.hide { condition: ["is_empty", "is_not_empty"] }
       */
      richTextValue?: string | Expression<string>;
      /** Text
       * @displayOptions.show { type: ["formula"], returnType: ["string"] }
       * @displayOptions.hide { condition: ["is_empty", "is_not_empty"] }
       */
      richTextValue?: string | Expression<string>;
      /** Number
       * @displayOptions.show { type: ["number", "unique_id"] }
       * @displayOptions.hide { condition: ["is_empty", "is_not_empty"] }
       * @default 0
       */
      numberValue?: number | Expression<number>;
      /** Number
       * @displayOptions.show { type: ["formula"], returnType: ["number"] }
       * @displayOptions.hide { condition: ["is_empty", "is_not_empty"] }
       * @default 0
       */
      numberValue?: number | Expression<number>;
      /** Checked
       * @displayOptions.show { type: ["checkbox"] }
       * @default false
       */
      checkboxValue?: boolean | Expression<boolean>;
      /** Checked
       * @displayOptions.show { type: ["formula"], returnType: ["checkbox"] }
       * @default false
       */
      checkboxValue?: boolean | Expression<boolean>;
      /** ISO 8601 date or a Notion relative date value like today, tomorrow, yesterday, one_week_ago, or one_month_from_now
       * @displayOptions.show { type: ["created_time", "date", "last_edited_time"] }
       * @displayOptions.hide { condition: ["is_empty", "is_not_empty", "next_month", "next_week", "next_year", "past_month", "past_week", "past_year", "this_week"] }
       */
      dateValue?: string | Expression<string>;
      /** ISO 8601 date or a Notion relative date value like today, tomorrow, yesterday, one_week_ago, or one_month_from_now
       * @displayOptions.show { type: ["formula"], returnType: ["date"] }
       * @displayOptions.hide { condition: ["is_empty", "is_not_empty", "next_month", "next_week", "next_year", "past_month", "past_week", "past_year", "this_week"] }
       */
      dateValue?: string | Expression<string>;
      /** Option name. For select, status, and multi-select filters that support multiple values, separate names with commas.
       * @displayOptions.show { type: ["multi_select", "select", "status"] }
       * @displayOptions.hide { condition: ["is_empty", "is_not_empty"] }
       */
      optionValue?: string | Expression<string>;
      /** User ID or Me
       * @displayOptions.show { type: ["created_by", "last_edited_by", "people"] }
       * @displayOptions.hide { condition: ["is_empty", "is_not_empty"] }
       */
      peopleValue?: string | Expression<string>;
      /** Relation Page ID
       * @displayOptions.show { type: ["relation"] }
       * @displayOptions.hide { condition: ["is_empty", "is_not_empty"] }
       */
      relationValue?: string | Expression<string>;
      /** Verification Status
       * @displayOptions.show { type: ["verification"] }
       * @default verified
       */
      verificationStatus?: 'verified' | 'expired' | 'none' | Expression<string>;
    }>;
  };
/**
 * Filters (JSON)
 * @displayOptions.show { filterType: ["json"] }
 * @default {}
 */
    filterJson?: IDataObject | string | Expression<string>;
/**
 * Whether to return a simplified version of the response instead of the raw data
 * @default true
 */
    simple?: boolean | Expression<boolean>;
/**
 * Options
 * @default {}
 */
    options?: {
    /** Sort
     * @default {}
     */
    sort?: {
        /** Sort
     */
    sortValue?: Array<{
      /** Whether or not to use the record's timestamp to sort the response
       * @default false
       */
      timestamp?: boolean | Expression<boolean>;
      /** The name of the property to filter by. Choose from the list, or specify an ID using an &lt;a href="https://docs.n8n.io/code/expressions/"&gt;expression&lt;/a&gt;.
       * @loadOptionsMethod getFilterProperties
       * @displayOptions.show { timestamp: [false] }
       */
      key?: string | Expression<string>;
      /** The name of the property to filter by
       * @displayOptions.show { timestamp: [true] }
       */
      key?: 'created_time' | 'last_edited_time' | Expression<string>;
      /** Type
       * @displayOptions.show { timestamp: [true] }
       * @default ={{$parameter["&key"].split("|").pop()}}
       */
      type?: unknown;
      /** The direction to sort
       */
      direction?: 'ascending' | 'descending' | Expression<string>;
    }>;
  };
    /** Whether to download a file if a page's property contains it
     * @default false
     */
    downloadFiles?: boolean | Expression<boolean>;
  };
};

export type NotionV3DatabasePageGetAllOutput = {
  id?: string;
  name?: string;
  url?: string;
};

export type NotionV3DatabasePageGetAllNode = {
  type: 'n8n-nodes-base.notion';
  version: 3;
  config: NodeConfig<NotionV3DatabasePageGetAllParams> & { credentials?: Credentials };
  output?: Items<NotionV3DatabasePageGetAllOutput>;
};