/**
 * Salesforce Node - Version 1.1
 * Discriminator: resource=contact, operation=delete
 */


interface Credentials {
  salesforceOAuth2Api: CredentialReference;
  salesforceJwtApi: CredentialReference;
}

/** Represents a contact, which is an individual associated with an account */
export type SalesforceV11ContactDeleteParams = {
  resource: 'contact';
  operation: 'delete';
/**
 * OAuth Authorization Flow
 * @default oAuth2
 */
    authentication?: 'oAuth2' | 'jwt' | Expression<string>;
/**
 * ID of contact that needs to be fetched
 */
    contactId: string | Expression<string>;
};

export type SalesforceV11ContactDeleteOutput = {
  success?: boolean;
};

export type SalesforceV11ContactDeleteNode = {
  type: 'n8n-nodes-base.salesforce';
  version: 1.1;
  config: NodeConfig<SalesforceV11ContactDeleteParams> & { credentials?: Credentials };
  output?: Items<SalesforceV11ContactDeleteOutput>;
};