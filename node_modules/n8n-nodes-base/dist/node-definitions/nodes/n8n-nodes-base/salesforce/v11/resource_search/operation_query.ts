/**
 * Salesforce Node - Version 1.1
 * Discriminator: resource=search, operation=query
 */


interface Credentials {
  salesforceOAuth2Api: CredentialReference;
  salesforceJwtApi: CredentialReference;
}

/** Search records */
export type SalesforceV11SearchQueryParams = {
  resource: 'search';
  operation: 'query';
/**
 * OAuth Authorization Flow
 * @default oAuth2
 */
    authentication?: 'oAuth2' | 'jwt' | Expression<string>;
/**
 * A SOQL query. An example query parameter string might look like: “SELECT+Name+FROM+MyObject”. If the SOQL query string is invalid, a MALFORMED_QUERY response is returned.
 */
    query: string | Expression<string>;
};

export type SalesforceV11SearchQueryOutput = {
  attributes?: {
    type?: string;
    url?: string;
  };
  Id?: string;
};

export type SalesforceV11SearchQueryNode = {
  type: 'n8n-nodes-base.salesforce';
  version: 1.1;
  config: NodeConfig<SalesforceV11SearchQueryParams> & { credentials?: Credentials };
  output?: Items<SalesforceV11SearchQueryOutput>;
};