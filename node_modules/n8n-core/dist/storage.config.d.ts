import { Logger } from '@n8n/backend-common';
import { z } from 'zod';
import { InstanceSettings } from './instance-settings';
export declare const EXECUTION_DATA_STORAGE_MODES: readonly ['database', 'filesystem', 's3', 'azure'];
declare const modeSchema: z.ZodEnum<["database", "filesystem", "s3", "azure"]>;
export declare class StorageConfig {
    mode: z.infer<typeof modeSchema>;
    get modeTag(): 'db' | 'fs' | 's3' | 'az';
    storagePath: string;
    private readonly instanceSettings;
    private readonly logger;
    constructor(instanceSettings: InstanceSettings, logger: Logger);
    sanitize(): void;
    private migrateStorageDir;
    private isIgnorableRenameError;
}
export {};
