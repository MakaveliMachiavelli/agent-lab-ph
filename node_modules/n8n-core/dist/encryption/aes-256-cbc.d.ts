import { CipherWrapper } from './interface';
export declare class CipherAes256CBC implements CipherWrapper {
    encrypt(data: string, key: string): string;
    decrypt(data: string, key: string): string;
    private getKeyAndIv;
}
