import type { TracesSamplerSamplingContext, TransactionEvent } from '@sentry/core';
export declare const DEFAULT_SLOW_SPAN_THRESHOLD_MS = 1000;
export declare function shouldIgnoreIncomingRequest(urlPath: string): boolean;
export declare function shouldIgnoreOutgoingRequest(url: string): boolean;
export declare function buildBeforeSendTransaction(slowSpanThresholdMs: number, webhook?: {
    endpoint: string;
    sampleRate: number;
}): (event: TransactionEvent) => TransactionEvent | null;
export declare function buildTracesSampler(baseRate: number): (samplingContext: TracesSamplerSamplingContext) => number;
