import type Sentry from '@sentry/node';
import { type Span, type StartSpanOpts, type Tracer } from './tracing';
export declare class SentryTracing implements Tracer {
    private readonly sentry;
    constructor(sentry: Pick<typeof Sentry, 'startSpan' | 'startNewTrace'>);
    startSpan<T>(options: StartSpanOpts, spanCb: (span: Span) => Promise<T>): Promise<T>;
    startNewTraceSpan<T>(options: StartSpanOpts, spanCb: (span: Span) => Promise<T>): Promise<T>;
}
