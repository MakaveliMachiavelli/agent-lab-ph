"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SLACK_HITL_WEBHOOK_SUFFIX = exports.TELEGRAM_HITL_WEBHOOK_SUFFIX = exports.HITL_CALLBACK_PREFIX = void 0;
exports.markSlackInteractionRequest = markSlackInteractionRequest;
exports.isSlackInteractionRequest = isSlackInteractionRequest;
exports.buildHitlCallbackReference = buildHitlCallbackReference;
exports.parseHitlCallbackReference = parseHitlCallbackReference;
exports.verifyHitlCallbackReference = verifyHitlCallbackReference;
const crypto_1 = require("crypto");
const n8n_workflow_1 = require("n8n-workflow");
exports.HITL_CALLBACK_PREFIX = 'nhitl1|';
exports.TELEGRAM_HITL_WEBHOOK_SUFFIX = '-telegram';
exports.SLACK_HITL_WEBHOOK_SUFFIX = '-slack';
const SLACK_INTERACTION_REQUEST = Symbol('n8nSlackInteractionRequest');
function markSlackInteractionRequest(req) {
    Object.defineProperty(req, SLACK_INTERACTION_REQUEST, {
        value: true,
        enumerable: false,
        configurable: true,
    });
}
function isSlackInteractionRequest(req) {
    return req[SLACK_INTERACTION_REQUEST] === true;
}
function buildHmacInput(executionId, decision) {
    return `n8n-hitl-callback:v1:${executionId}:${decision}`;
}
function computeHmac(executionId, decision, secret) {
    return (0, crypto_1.createHmac)('sha256', secret)
        .update(buildHmacInput(executionId, decision))
        .digest('hex')
        .slice(0, 32);
}
function buildHitlCallbackReference(executionId, decision, secret) {
    const hmac = computeHmac(executionId, decision, secret);
    const reference = `${exports.HITL_CALLBACK_PREFIX}${executionId}|${decision}|${hmac}`;
    if (reference.length > 64) {
        throw new n8n_workflow_1.UnexpectedError(`HITL callback reference exceeds the 64-byte platform limit (${reference.length} bytes) for execution "${executionId}"`);
    }
    return reference;
}
function parseHitlCallbackReference(data) {
    if (!data.startsWith(exports.HITL_CALLBACK_PREFIX))
        return null;
    const [executionId, decision, hmac] = data.slice(exports.HITL_CALLBACK_PREFIX.length).split('|');
    if (!executionId || !hmac || (decision !== 'a' && decision !== 'd'))
        return null;
    return { executionId, decision, hmac };
}
function verifyHitlCallbackReference(parsed, secret) {
    const expected = computeHmac(parsed.executionId, parsed.decision, secret);
    const expectedBuffer = Buffer.from(expected);
    const providedBuffer = Buffer.from(parsed.hmac);
    return (expectedBuffer.byteLength === providedBuffer.byteLength &&
        (0, crypto_1.timingSafeEqual)(expectedBuffer, providedBuffer));
}
//# sourceMappingURL=hitl-callback-reference.js.map