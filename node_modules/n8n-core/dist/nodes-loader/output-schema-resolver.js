"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.OUTPUT_PARSER_SCHEMA_VARIANT = void 0;
exports.resolveOutputSchemaPath = resolveOutputSchemaPath;
exports.loadOutputSchema = loadOutputSchema;
const backend_common_1 = require("@n8n/backend-common");
const node_fs_1 = require("node:fs");
const node_path_1 = __importDefault(require("node:path"));
exports.OUTPUT_PARSER_SCHEMA_VARIANT = 'with-parser';
function padVersion(version) {
    return String(version).split('.').concat(['0', '0']).slice(0, 3).join('.');
}
const VERSION_DIR_PATTERN = /^v\d+(\.\d+)*$/;
function parseVersionDir(name) {
    return name.slice(1).split('.').map(Number);
}
function compareVersionTuplesDesc(a, b) {
    for (let i = 0; i < Math.max(a.length, b.length); i++) {
        const diff = (b[i] ?? 0) - (a[i] ?? 0);
        if (diff !== 0)
            return diff;
    }
    return 0;
}
function orderFallbackCandidates(dirNames, target) {
    const dirs = dirNames.map((name) => ({ name, tuple: parseVersionDir(name) }));
    const byDesc = (a, b) => compareVersionTuplesDesc(a.tuple, b.tuple);
    const byAsc = (a, b) => compareVersionTuplesDesc(b.tuple, a.tuple);
    const sameMajor = dirs.filter((d) => d.tuple[0] === target[0]);
    return [
        ...sameMajor.filter((d) => compareVersionTuplesDesc(d.tuple, target) >= 0).sort(byDesc),
        ...sameMajor.filter((d) => compareVersionTuplesDesc(d.tuple, target) < 0).sort(byAsc),
        ...dirs.filter((d) => d.tuple[0] < target[0]).sort(byDesc),
        ...dirs.filter((d) => d.tuple[0] > target[0]).sort(byAsc),
    ].map((d) => d.name);
}
function resolveOutputSchemaPath(ref) {
    const { nodeDir, version, resource, operation, versionFallback, variant } = ref;
    const schemaBaseDir = node_path_1.default.join(nodeDir, '__schema__');
    const exactVersionDir = `v${padVersion(version)}`;
    const buildPath = (versionDir, filename) => {
        const parts = [versionDir, resource, operation, filename].filter((part) => typeof part === 'string' && part.length > 0);
        const filePath = node_path_1.default.resolve(schemaBaseDir, parts.join('/') + '.json');
        return (0, backend_common_1.isContainedWithin)(schemaBaseDir, filePath) ? filePath : undefined;
    };
    if (!versionFallback)
        return buildPath(exactVersionDir);
    let available = [];
    try {
        available = orderFallbackCandidates((0, node_fs_1.readdirSync)(schemaBaseDir).filter((dir) => VERSION_DIR_PATTERN.test(dir)), parseVersionDir(exactVersionDir));
    }
    catch {
        return undefined;
    }
    const hasDiscriminators = Boolean(resource) || Boolean(operation);
    for (const versionDir of [...new Set([exactVersionDir, ...available])]) {
        const filePath = buildPath(versionDir);
        if (filePath && (0, node_fs_1.existsSync)(filePath))
            return filePath;
        if (!hasDiscriminators) {
            if (variant) {
                const variantPath = buildPath(versionDir, `output.${variant}`);
                if (variantPath && (0, node_fs_1.existsSync)(variantPath))
                    return variantPath;
            }
            const outputPath = buildPath(versionDir, 'output');
            if (outputPath && (0, node_fs_1.existsSync)(outputPath))
                return outputPath;
        }
    }
    return undefined;
}
function loadOutputSchema(ref) {
    const filePath = resolveOutputSchemaPath(ref);
    if (!filePath)
        return undefined;
    try {
        const parsed = JSON.parse((0, node_fs_1.readFileSync)(filePath, 'utf-8'));
        if (typeof parsed === 'object' && parsed !== null && !Array.isArray(parsed)) {
            return parsed;
        }
    }
    catch { }
    return undefined;
}
//# sourceMappingURL=output-schema-resolver.js.map