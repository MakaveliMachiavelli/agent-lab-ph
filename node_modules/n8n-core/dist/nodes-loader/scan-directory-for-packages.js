"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.scanDirectoryForPackages = scanDirectoryForPackages;
const backend_common_1 = require("@n8n/backend-common");
const di_1 = require("@n8n/di");
const fast_glob_1 = __importDefault(require("fast-glob"));
const path_1 = __importDefault(require("path"));
const lazy_package_directory_loader_1 = require("./lazy-package-directory-loader");
async function scanDirectoryForPackages(nodeModulesDir, options = {}) {
    const globOptions = {
        cwd: nodeModulesDir,
        onlyDirectories: true,
        deep: 1,
    };
    const installedPackagePaths = [
        ...(await (0, fast_glob_1.default)('n8n-nodes-*', globOptions)),
        ...(await (0, fast_glob_1.default)('@*/n8n-nodes-*', { ...globOptions, deep: 2 })),
    ];
    const logger = di_1.Container.get(backend_common_1.Logger);
    const loaders = [];
    for (const packagePath of installedPackagePaths) {
        try {
            loaders.push(new lazy_package_directory_loader_1.LazyPackageDirectoryLoader(path_1.default.join(nodeModulesDir, packagePath), options.excludeNodes, options.includeNodes));
        }
        catch (error) {
            logger.warn(`Skipping package directory "${packagePath}": failed to load package metadata. The package may be partially installed or corrupted.`, { error });
        }
    }
    return loaders;
}
//# sourceMappingURL=scan-directory-for-packages.js.map