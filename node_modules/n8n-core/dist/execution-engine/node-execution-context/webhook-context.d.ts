import type { Request, Response } from 'express';
import type { AINodeConnectionType, CloseFunction, CredentialCheckResult, ICredentialDataDecryptedObject, IDataObject, INode, IRunExecutionData, IUser, IWebhookData, IWebhookFunctions, IWorkflowExecuteAdditionalData, N8nOAuth2ValidationResult, WebhookType, Workflow, WorkflowExecuteMode, N8nOAuth2FlowResult } from 'n8n-workflow';
import { NodeExecutionContext } from './node-execution-context';
export declare class WebhookContext extends NodeExecutionContext implements IWebhookFunctions {
    private readonly webhookData;
    private readonly closeFunctions;
    readonly helpers: IWebhookFunctions['helpers'];
    readonly nodeHelpers: IWebhookFunctions['nodeHelpers'];
    constructor(workflow: Workflow, node: INode, additionalData: IWorkflowExecuteAdditionalData, mode: WorkflowExecuteMode, webhookData: IWebhookData, closeFunctions: CloseFunction[], runExecutionData: IRunExecutionData | null);
    getCredentials<T extends object = ICredentialDataDecryptedObject>(type: string): Promise<T>;
    getBodyData(): IDataObject;
    getHeaderData(): import("http").IncomingHttpHeaders;
    getParamsData(): object;
    getQueryData(): object;
    getRequestObject(): Request;
    getResponseObject(): Response;
    private assertHttpRequest;
    getNodeWebhookUrl(name: WebhookType): string | undefined;
    getWebhookResourceUrl(name: WebhookType): string | undefined;
    getWebhookName(): WebhookType;
    logHitlResponse(payload: {
        approved: boolean;
        authorized: boolean;
    }): void;
    validateCookieAuth(cookieValue: string): Promise<IUser>;
    beginN8nOAuth2Flow(resourceUrl: string, metadata?: Record<string, string>): Promise<string>;
    completeN8nOAuth2Flow(code: string, state: string): Promise<N8nOAuth2FlowResult>;
    validateN8nOAuth2Token(token: string, resourceUrl: string): Promise<N8nOAuth2ValidationResult>;
    establishTriggerIdentity(token: string, resource: string): Promise<void>;
    checkTriggerCredentialStatus(): Promise<CredentialCheckResult | undefined>;
    getInputConnectionData(connectionType: AINodeConnectionType, itemIndex: number): Promise<unknown>;
}
