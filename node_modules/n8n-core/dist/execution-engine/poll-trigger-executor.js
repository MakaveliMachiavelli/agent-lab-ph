"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PollTriggerExecutor = void 0;
const backend_common_1 = require("@n8n/backend-common");
const di_1 = require("@n8n/di");
const observability_1 = require("../observability");
const triggers_and_pollers_1 = require("./triggers-and-pollers");
let PollTriggerExecutor = class PollTriggerExecutor {
    constructor(logger, triggersAndPollers, tracing) {
        this.logger = logger;
        this.triggersAndPollers = triggersAndPollers;
        this.tracing = tracing;
        this.logger = logger.scoped('poll-trigger');
    }
    create(workflow, node, pollFunctions, isCurrent) {
        return async (testingTrigger = false) => {
            const startTraced = testingTrigger
                ? this.tracing.startSpan.bind(this.tracing)
                : this.tracing.startNewTraceSpan.bind(this.tracing);
            return await startTraced({
                name: 'Workflow Trigger Poll',
                op: 'trigger.poll',
                attributes: {
                    ...this.tracing.pickWorkflowAttributes(workflow),
                    ...this.tracing.pickNodeAttributes(node),
                },
            }, async (span) => {
                this.logger.debug(`Poll trigger initiated for workflow "${workflow.name}"`, {
                    workflowName: workflow.name,
                    workflowId: workflow.id,
                });
                const ownsIsolate = !testingTrigger;
                if (!testingTrigger && !isCurrent()) {
                    this.logger.debug(`Skipping poll for superseded workflow "${workflow.name}"`, {
                        workflowId: workflow.id,
                    });
                    span.setStatus({ code: 1 });
                    return;
                }
                try {
                    if (ownsIsolate)
                        await workflow.expression.acquireIsolate();
                    const pollResponse = await this.triggersAndPollers.runPollFunction(workflow, node, pollFunctions);
                    if (!testingTrigger && !isCurrent()) {
                        this.logger.debug(`Discarding in-flight poll result for superseded workflow "${workflow.name}"`, { workflowId: workflow.id });
                        span.setStatus({ code: 1 });
                        return;
                    }
                    if (pollResponse !== null) {
                        pollFunctions.__emit(pollResponse);
                    }
                    span.setStatus({ code: 1 });
                }
                catch (error) {
                    if (testingTrigger) {
                        span.setStatus({ code: 2 });
                        throw error;
                    }
                    if (!isCurrent()) {
                        this.logger.debug(`Ignoring in-flight poll error for superseded workflow "${workflow.name}"`, { workflowId: workflow.id });
                        span.setStatus({ code: 1 });
                        return;
                    }
                    span.setStatus({ code: 2 });
                    pollFunctions.__emitError(error);
                }
                finally {
                    if (ownsIsolate)
                        await workflow.expression.releaseIsolate();
                }
            });
        };
    }
};
exports.PollTriggerExecutor = PollTriggerExecutor;
exports.PollTriggerExecutor = PollTriggerExecutor = __decorate([
    (0, di_1.Service)(),
    __metadata("design:paramtypes", [backend_common_1.Logger, triggers_and_pollers_1.TriggersAndPollers, observability_1.Tracing])
], PollTriggerExecutor);
//# sourceMappingURL=poll-trigger-executor.js.map