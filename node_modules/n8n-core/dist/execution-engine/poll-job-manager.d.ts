import type { INode, TriggerTime } from 'n8n-workflow';
export declare abstract class PollJobManager {
    abstract register(workflowId: string, node: INode, pollTimes: TriggerTime[], timezone: string): Promise<{
        inserted: boolean;
    }>;
    abstract remove(workflowId: string, nodeId: string): Promise<void>;
}
