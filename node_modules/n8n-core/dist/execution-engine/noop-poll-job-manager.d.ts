import type { INode, TriggerTime } from 'n8n-workflow';
import { PollJobManager } from './poll-job-manager';
export declare class NoOpPollJobManager extends PollJobManager {
    register(_workflowId: string, _node: INode, _pollTimes: TriggerTime[], _timezone: string): Promise<{
        inserted: boolean;
    }>;
    remove(_workflowId: string, _nodeId: string): Promise<void>;
}
