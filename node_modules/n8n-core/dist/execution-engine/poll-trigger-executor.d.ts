import { Logger } from '@n8n/backend-common';
import type { INode, IPollFunctions, Workflow } from 'n8n-workflow';
import { Tracing } from '../observability';
import { TriggersAndPollers } from './triggers-and-pollers';
export type PollTriggerExecuteFn = (testingTrigger?: boolean) => Promise<void>;
export declare class PollTriggerExecutor {
    private readonly logger;
    private readonly triggersAndPollers;
    private readonly tracing;
    constructor(logger: Logger, triggersAndPollers: TriggersAndPollers, tracing: Tracing);
    create(workflow: Workflow, node: INode, pollFunctions: IPollFunctions, isCurrent: () => boolean): PollTriggerExecuteFn;
}
