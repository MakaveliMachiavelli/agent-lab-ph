import { Logger } from '@n8n/backend-common';
import { ICredentialContext, IExecuteData, IExecutionContext, INodeExecutionData, PlaintextExecutionContext, Workflow } from 'n8n-workflow';
import { Cipher } from '../encryption';
import { ExecutionContextHookRegistry } from './execution-context-hook-registry.service';
export declare class ExecutionContextService {
    private readonly logger;
    private readonly executionContextHookRegistry;
    private readonly cipher;
    constructor(logger: Logger, executionContextHookRegistry: ExecutionContextHookRegistry, cipher: Cipher);
    decryptCredentialContext(encrypted: string): Promise<ICredentialContext>;
    decryptExecutionContext(context: IExecutionContext): Promise<PlaintextExecutionContext>;
    buildManualExecutionCredentials(n8nAuthCookie: string): Promise<string>;
    buildRequestBoundCredentialContext(n8nAuthCookie: string, { method, endpoint, browserId }: {
        method: string;
        endpoint: string;
        browserId?: string;
    }): ICredentialContext;
    buildRequestBoundCredentials(n8nAuthCookie: string, request: {
        method: string;
        endpoint: string;
        browserId?: string;
    }): Promise<string>;
    buildTriggerIdentityCredentials(token: string, resource: string): Promise<string>;
    encryptExecutionContext(context: PlaintextExecutionContext): Promise<IExecutionContext>;
    mergeExecutionContexts(baseContext: PlaintextExecutionContext, contextToMerge: Partial<PlaintextExecutionContext>): PlaintextExecutionContext;
    augmentSubExecutionContext(workflow: Workflow, startItem: IExecuteData, contextToAugment: IExecutionContext): Promise<IExecutionContext>;
    augmentExecutionContextWithHooks(workflow: Workflow, startItem: IExecuteData, contextToAugment: IExecutionContext): Promise<{
        context: IExecutionContext;
        triggerItems: INodeExecutionData[] | null;
    }>;
}
